# Provenance

For every file under `src/core/`, `cmake/portable/` and `cmake/FetchTransferBound.cmake`: which
upstream file and commit it came from, or `origin: core-cpp` for code written here. This is the
mechanical record behind the Global Constraints' "Upstream sync discipline" —
[`tests/cmake/check-cmake-hygiene.cmake`](../../tests/cmake/check-cmake-hygiene.cmake)'s
`provenance` rule refuses a file in scope without a row, and a row naming a file that no longer
exists.

An import or port task (A4–A7, B1–B12) appends or bumps rows in the same commit as the import.
Task B12b reads this table mechanically, before v0.1.0, to catch up every row whose upstream has
moved since it was synced. A consumer migration's delta check (`.agent/guides/`) reads it the same
way. `NOTICE` and `CHANGELOG.md` record the same commits at the granularity of a whole import; this
table is the per-file index into them.

**Do not sweep this table by hand.** [`scripts/check-upstream-drift.py`](../../scripts/check-upstream-drift.py)
does the reading and the `git log`: `python scripts/check-upstream-drift.py` fetches each upstream
once and reports, per row, the commits that touched it since its synced SHA, with `--repo` and
`--path-prefix` to ask about one module. Drift is reported and never fails; a row that is wrong
about its own upstream does fail, which is the distinction that lets a nightly run it. It is
registered as `core-cpp.upstream-drift` under `ctest -L hygiene`, where it skips on a machine
without the upstream checkouts rather than passing silently.

Because it is mechanical, **every row's upstream path must name exactly one file**: a pattern like
`Foo.{hpp,cpp}` cannot be read by `git log -- <path>`, so the row stops being checkable without
anything saying so. A file merged from several upstream files names its primary one here and the
rest in notes, as below.

**A `-` in the notes column does not mean "byte-identical to upstream."** Every import applies the
rewrites this repository's rules require — the namespace, the include paths, the header guard, the
SPDX line — and a row records what is left over. For the endo `src/tui/*` rows in particular,
Task A7 also applied its house-style conversions across the module: 110 C-style `for` loops became
`std::views::iota` and 21 became `while`, 87 `find(...) != npos` became `contains(...)`, 138
declarations gained `const`, 9 diagnostic-pragma blocks and 1 `NOLINT` were deleted, 12 private
constants were renamed out of the `k` prefix, 2 conditions were rewritten by De Morgan, a dead test
helper was deleted and one was added. 84 of the 141 files with an upstream counterpart differ for
one of those reasons, and 79 of them carry a bare `-` because that is all they carry.

**So a row is not a licence to re-sync by overwriting.** B12b and every consumer's delta check read
this table mechanically; taking a `-` for "replaceable with upstream" would revert all of the above
silently, including the `NOLINT` and the pragma blocks the rules forbid. Re-syncing a file means
merging the upstream delta into what is here, never replacing it.

Repos: [`contour-terminal/contour`](https://github.com/contour-terminal/contour),
[`contour-terminal/endo`](https://github.com/contour-terminal/endo),
[`LASTRADA-Software/fastcached`](https://github.com/LASTRADA-Software/fastcached). A file adapted
from more than one upstream file (a merge) names its primary upstream in the table and lists the
others in notes.

| core-cpp path | upstream repo | upstream path | synced SHA | notes |
|---|---|---|---|---|
| `cmake/FetchTransferBound.cmake` | LASTRADA-Software/fastcached | `cmake/FetchTransferBound.cmake` | `f6ec49f3446b8bc121eba82c64cde2de759e774a` | verbatim; re-synced together with `CompileCache.cmake` (`cmake/portable/README.md`) |
| `cmake/portable/CompileCache.cmake` | LASTRADA-Software/fastcached | `cmake/portable/CompileCache.cmake` | `f6ec49f3446b8bc121eba82c64cde2de759e774a` | verbatim |
| `cmake/portable/README.md` | origin: core-cpp | - | - | documents the two verbatim files above |
| `src/core/Assert.hpp` | contour-terminal/contour | `src/crispy/Assert.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | `Require`/`Guarantee`/`todo`/`unreachable`/`setFailHandler`; `fatal`/`SoftRequire` split out to `src/core/log/Assert.hpp` to close a base→log layering cycle |
| `src/core/Base64.hpp` | contour-terminal/contour | `src/crispy/Base64.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import fix (Task A9): `decodeLength()` stops at the table's 64 sentinel rather than at its size, so it measures the base64 prefix; `decode()`'s index lambda no longer copies the 256-byte table per call |
| `src/core/Base64_test.cpp` | contour-terminal/contour | `src/crispy/Base64_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import additions (Task A9): `decodeLength()`'s cases |
| `src/core/CMakeLists.txt` | origin: core-cpp | - | - | - |
| `src/core/Config.hpp.in` | origin: core-cpp | - | - | generates `core/Config.hpp` |
| `src/core/Deferred.hpp` | contour-terminal/contour | `src/crispy/Deferred.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | - |
| `src/core/Defines.hpp` | contour-terminal/contour | `src/crispy/Defines.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | `CRISPY_*` macros renamed `CORE_*` |
| `src/core/Environment.cpp` | contour-terminal/contour | `src/crispy/Environment.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import additions (Task A4): the process-environment writer (`setProcessEnvironmentVariable()`/`unsetProcessEnvironmentVariable()`), and on Windows `LiveEnvironment` reads a variable set to the empty string as set |
| `src/core/Environment.hpp` | contour-terminal/contour | `src/crispy/Environment.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import addition (Task A4): the process-environment writer |
| `src/core/Environment_test.cpp` | contour-terminal/contour | `src/crispy/Environment_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import additions (Task A4): the process-environment writer's cases |
| `src/core/Escape.hpp` | contour-terminal/contour | `src/crispy/Escape.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import fix (Task A9): 0x7E is printable, `escape()`'s `\"` is read back as a quote, and an octal escape is three digits whatever its leading one -- escape and unescape round-trip again; post-import addition (Task A9 fix round 2): every function in the header is `[[nodiscard]]` |
| `src/core/Escape_test.cpp` | origin: core-cpp | - | - | the upstream file has no test; written for the fix above |
| `src/core/FNV.hpp` | contour-terminal/contour | `src/crispy/FNV.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import fix (`48b261a`): the trivially-copyable overload no longer recurses forever for `T != unsigned char`; post-import fix (Task A9): the byte-wise overload takes only a type with unique object representations, and reads the bytes with `std::bit_cast`, so a hash is available in a constant expression |
| `src/core/FNV_test.cpp` | origin: core-cpp | - | - | written for the fix above; post-import additions (Task A9): the padding and constant-evaluation cases |
| `src/core/Flags.hpp` | contour-terminal/contour | `src/crispy/Flags.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import fix (Task A9): `operator&=` intersects, as `operator&` does, instead of clearing the named flag, and it also takes a `Flags` |
| `src/core/Flags_test.cpp` | origin: core-cpp | - | - | the upstream file has no test; written for the fix above |
| `src/core/Generator.hpp` | contour-terminal/endo | `src/platform/Generator.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `endo::Generator` renamed `core::Generator`; the fallback is always defined, as `core::detail::GeneratorFallback`; `<version>` is included first, so whether `Generator` is `std::generator` no longer depends on what a translation unit included before it; `handle_type` renamed `HandleType`; `ENDO_GENERATOR_FORCE_FALLBACK` renamed `CORE_GENERATOR_FORCE_FALLBACK`; moved from `core::coro` to `core::base` (Task A5b): `core::async::Generator` would read as an asynchronous, `co_await`-able stream, and this one is synchronous |
| `src/core/Generator_test.cpp` | contour-terminal/endo | `src/platform/Generator_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | every case runs over `Generator` and over the fallback; asserts the include-order-independent choice |
| `src/core/Overloaded.hpp` | contour-terminal/contour | `src/crispy/Utils.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | `crispy::Overloaded` (defined in `Utils.hpp`) and the ambient global `::Overloaded` merged into one `core::Overloaded` here |
| `src/core/Profiling.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Core/Profiling.hpp` | `ee71f868547712892b7d9a2ebff60d49c496e25c` | `FC_*` macros renamed `CORE_*` |
| `src/core/Profiling_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Core/Profiling_test.cpp` | `ee71f868547712892b7d9a2ebff60d49c496e25c` | - |
| `src/core/Ranges.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Core/Ranges.hpp` | `ee71f868547712892b7d9a2ebff60d49c496e25c` | `FastCache::Ranges` renamed `core::ranges`; `FC_RANGES_FORCE_FALLBACK` renamed `CORE_RANGES_FORCE_FALLBACK` |
| `src/core/Ranges_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Core/Ranges_test.cpp` | `ee71f868547712892b7d9a2ebff60d49c496e25c` | imported although the A3 brief did not list it: `Ranges.hpp`'s own documentation points to it as the proof that its fallbacks work |
| `src/core/Times.hpp` | contour-terminal/contour | `src/crispy/Times.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import fix (`6b1a4d7`): the postfix `operator++`/`operator--` return the prior position, not the new one; post-import fixes (Task A9 fix round 1): `Times2D::operator[]` answers the tuple its `value_type` declares, not the inner coordinate alone; `Times::size()` and `Times::operator[]`, which nothing had instantiated, spell out the conversions their arithmetic implies |
| `src/core/Times_test.cpp` | contour-terminal/contour | `src/crispy/Times_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import addition (Task A9 fix round 1): the `Times2D` subscript case; post-import addition (Task A9 fix round 2): the `Times::size()`/`operator[]` cases, which are what instantiates them |
| `src/core/UserInfo.cpp` | contour-terminal/contour | `src/crispy/UserInfo.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | - |
| `src/core/UserInfo.hpp` | contour-terminal/contour | `src/crispy/UserInfo.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | - |
| `src/core/Utils.cpp` | contour-terminal/contour | `src/crispy/Utils.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import fix (Task A9): Windows' `threadName()` frees the thread description on every path, and a failed conversion answers empty instead of throwing |
| `src/core/Utils.hpp` | contour-terminal/contour | `src/crispy/Utils.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | `Overloaded` split out to `Overloaded.hpp`; `views::enumerate` is now a function template; post-import fixes (Task A9): `splitKeyValuePairs()`'s last segment ends where the view does; `toLower()`/`toUpper()` widen through `unsigned char`; `readFileAsString()` takes the path itself, opens it binary and keeps only what it read; `eachElement()`'s end iterator no longer wraps onto its begin; post-import fixes (Task A9 fix round 1): `readFileAsString()` reports a missing file as empty rather than throwing; the case mapping sends a wide character to `towlower`/`towupper` instead of truncating it into the narrow functions' domain; `joinHumanReadableQuoted()`'s separator is a `std::string_view`, so its default is usable |
| `src/core/Utils_test.cpp` | contour-terminal/contour | `src/crispy/Utils_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import additions (Task A9): the bounds, case-mapping, file-reading, `eachElement()` and thread-name cases; post-import additions (Task A9 fix round 1): the missing-file and `joinHumanReadableQuoted()` cases |
| `src/core/async/AsTask.hpp` | origin: core-cpp | - | - | Task B6: the escape hatch from an awaitable back to a `Task`, for a caller that must STORE an operation. Nothing upstream has it, because neither lineage had both shapes at once: fastcached's socket operations are awaitables and never become tasks, contour's are tasks and never stop being them. The design spec names it (Part I §2 item 7) |
| `src/core/async/AsTask_test.cpp` | origin: core-cpp | - | - | Task B6. The load-bearing case is the LIFETIME one: it counts live awaitables rather than checking the value, because an implementation that bound a reference still reads the right value out of freed stack |
| `src/core/async/Awaitable_test.cpp` | origin: core-cpp | - | - | the `Awaiter` and `HasStopToken` concepts, asserted at compile time; also built over the `StopToken` fallback; Task B1: `CarriesUnownedRoot` over the module's promises and over the near misses |
| `src/core/async/Awaitable.hpp` | contour-terminal/contour | `src/coro/Awaitable.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespace; includes `StopToken.hpp` rather than `Cancellation.hpp`, which now includes this, so the concept every `await_suspend` reads a promise through sits below it; no unused `<utility>`; Task B1: the `CarriesUnownedRoot` concept, the second thing a templated `await_suspend` reads the awaiting promise through |
| `src/core/async/CMakeLists.txt` | origin: core-cpp | - | - | - |
| `src/core/async/Cancellation.hpp` | contour-terminal/contour | `src/coro/Cancellation.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | `OperationCancelled`, `ThisCoroStopToken`, `thisCoroStopToken()`; the `std::` aliases and their `#error` moved to `StopToken.hpp`; no `NOLINT`; the promise probe is the `HasStopToken` concept |
| `src/core/async/StopToken.hpp` | contour-terminal/contour | `src/coro/Cancellation.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | the `StopToken`/`StopSource`/`StopCallback` aliases of `std::` only, moved out of `Cancellation.hpp`; in place of contour's `#error` where `__cpp_lib_jthread` is missing, core-cpp's fallback (`detail::StopTokenFallback`, `StopSourceFallback`, `StopCallbackFallback`), always defined; the `constexpr` tag `NoStopState`; `<version>` included first; `CORE_ASYNC_FORCE_STOP_TOKEN_FALLBACK` |
| `src/core/async/StopTokenLinkSmoke.cpp` | origin: core-cpp | - | - | a program linking `core::async` alone, over the forced `StopToken` fallback: the link a consumer makes, which core-cpp's own test binaries hide |
| `src/core/async/StopToken_test.cpp` | origin: core-cpp | - | - | built twice: `core-cpp.async` and, with `CORE_ASYNC_FORCE_STOP_TOKEN_FALLBACK`, `core-cpp.async-fallback` |
| `src/core/async/Task.hpp` | contour-terminal/contour | `src/coro/Task.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespace; no `NOLINT`; both promise probes are the `HasStopToken` concept; Task B1 grafts fastcached's `Task.hpp` (`0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21`) onto it: the awaiter OWNS the frame it took from the rvalue `Task` (a `detail::UniqueCoroHandle`, not a raw handle), `release()`, `unownedRoot` in the promise and set at every `await_suspend`, and `detail::refuseEmptyTask()` in place of the `T {}` a null handle used to answer with -- which invented a value and required every `T` to be default-constructible. `DetachedTask` and `SyncRun` went to headers of their own (Ruling R66), and the ownership helpers to `ParkedWork.hpp` |
| `src/core/async/Task_test.cpp` | contour-terminal/contour | `src/coro/Task_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | no `NOLINT`; the deep-chain case is skipped under Emscripten without `-mtail-call`, and on GCC unless the build defines `CORE_ASYNC_SYMMETRIC_TRANSFER_IS_TAIL_CALL` (`-O2` and better), where symmetric transfer is not a tail call (core-cpp#15); `<stdexcept>` rather than Catch2 for `std::runtime_error`; also built over the `StopToken` fallback (`core-cpp.async-fallback`); Task B1: a frame sentinel, and cases for the owning awaiter, a non-default-constructible result, `release()`, the refusal of a task owning no frame, and `syncRun`/`syncRunWith` ported from fastcached's `Task_test.cpp` (`0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21`) |
| `src/core/async/AsyncQueue.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/AsyncQueue.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | camelBack; built over `core::async::IExecutor` rather than fastcached's `IReactor`, so the queue needs nothing of the layer above it; `AsyncQueuePush::accepted` is the `AsyncQueueAdmission` enum, because a `bool` result reporting success is a finding (`design-principles.md`); `pop()` is stop-aware -- it reads the awaiting promise's `StopToken` through `HasStopToken`, registers a `StopCallback` that hands the park back to the executor, and `await_resume()` answers data, then a close, then `OperationCancelled`; the callback is registered BEFORE the park is published and outside the queue's mutex, so an already-stopped token's inline run neither deadlocks nor posts a handle from inside `await_suspend` |
| `src/core/async/AsyncQueue_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/AsyncQueue_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | camelBack; `TestReactor`/`ManualClock` replaced by a file-local `QueuedExecutor`, since `core::async` has no loop; the #1041 cases (a detached consumer posted by `push` and by `close`, and the owned control) carried over; new cases for the stop-aware `pop`: a parked consumer cancelled, a flow already cancelled before it parks, data beating a cancellation and a close beating one; the cross-thread case is compiled only where a second thread exists and uses `std::thread`, not `std::jthread`; also built over the `StopToken` fallback |
| `src/core/async/DetachedTask.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/Task.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | a header of its own (Ruling R66), where upstream keeps it in `Task.hpp`; `stopToken()` added, answering with a never-stopped token, so a `Task` awaited from a detached flow takes the ordinary `HasStopToken` inheritance path |
| `src/core/async/IExecutor.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/IExecutor.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | camelBack; the `using IExecutor::submit;` rule is stated on the class (#1041) and asserted at compile time in `ParkedWork_test.cpp` |
| `src/core/async/Join.hpp` | origin: core-cpp | - | - | the runner, join state and awaiter that `whenAll` and `whenAny` are both written over, collapsed out of contour's two near-identical copies (Task B1). A policy supplies the one step that differs -- what a child finishing does to the shared state -- plus the token each child observes and the parent-cancellation bridge, if any. Public file, `core::async::detail` throughout, like `UniqueCoroHandle.hpp` |
| `src/core/async/ParkedWork.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/ParkedWork.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | camelBack; `Detail::UnownedRootOf` and `ParkedWorkFor` moved here from `Task.hpp` as `unownedRootOf` and `parkedWorkFor`, so the file that defines `ParkedWork::abandon` is the one that decides it, and `ResumeOn` and `AsyncQueue` need no `Task.hpp`; the promise probe is the `CarriesUnownedRoot` concept rather than `std::is_base_of_v<TaskPromiseBase, Promise>`, which is what lets the `whenAll` and `whenAny` runners carry the answer too; `Parked` gains `explicit operator bool` |
| `src/core/async/ParkedWork_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/ParkedWork_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | only the cases that need no event loop: upstream's reactor-driven teardown properties belong to Task B4. A file-local `QueuedExecutor` stands in for the loop, a frame sentinel counts destructions so no case rests on a sanitizer noticing, and the #1041 compile-time check is here with its negative control -- written over a non-virtual base, because GCC's `-Woverloaded-virtual` refuses the hiding shape on `IExecutor` outright. New: `unownedRoot` reaching a coroutine parked under a `whenAll` and under a `whenAny` runner; also built over the `StopToken` fallback |
| `src/core/async/ResumeOn.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/ResumeOn.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | camelBack; includes `ParkedWork.hpp` rather than `Task.hpp` |
| `src/core/async/SyncRun.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/Task.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | a header of its own (Ruling R66), where upstream keeps both in `Task.hpp`; `SyncRun` is `syncRun` and its `Retrieve` overload is `syncRunWith`, a name rather than an overload; each refuses a task owning no frame through `detail::refuseEmptyTask()` instead of resuming a null handle |
| `src/core/async/ThreadPoolExecutor.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/ThreadPoolExecutor.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | camelBack; merged with `src/FastCache/Async/ThreadPoolExecutor.cpp`, whose 92 lines are inlined into the header (Ruling R65): `core::async` is INTERFACE and depends on the standard library alone, and a compiled body would change what every consumer links and what the WebAssembly subset builds. The upstream column names the header alone because it names exactly one file, as the preamble requires -- a row keys on its core-cpp path, and there is one core-cpp file here, so the `.cpp` is a merge partner rather than a row of its own. `std::vector<std::jthread>` is `std::vector<std::thread>` joined explicitly, because `__cpp_lib_jthread` is exactly what `StopToken.hpp` has a fallback for; the single-threaded Emscripten `#error` added, and the header is in no `FILE_SET` there |
| `src/core/async/ThreadPoolExecutor_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/ThreadPoolExecutor_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | camelBack; each wait reports how far it got when it runs out; a `LatchRelease` guard declared after the pool releases the latch before the pool joins, so a failed assertion cannot turn a red into a hang; not compiled under single-threaded Emscripten; also built over the `StopToken` fallback |
| `src/core/async/UniqueCoroHandle.hpp` | contour-terminal/contour | `src/coro/UniqueCoroHandle.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespace only; Task B1: `release()`, which is what lets `Task::operator co_await` hand the frame to its awaiter |
| `src/core/async/WhenAll.hpp` | contour-terminal/contour | `src/coro/WhenAll.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | no `NOLINT`; the final awaiter's local `state` renamed `join` (`-Wshadow`); the variadic overload requires rvalues; `HasStopToken`; `<type_traits>`; Task B1: the runner promise carries `unownedRoot`, inherited from the awaiting coroutine like its token, so a coroutine parked under a `whenAll` can still state what an executor may free; Task B1 also collapsed the runner, the state and the awaiter into `Join.hpp`, shared with `WhenAny.hpp`: what is left here is the policy (every escape is the join's, a cancellation included), the child token and `await_resume`. The wrapper body's try/catch is gone -- what escaped a child is recorded once, in the runner promise |
| `src/core/async/WhenAll_test.cpp` | contour-terminal/contour | `src/coro/WhenAll_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespace; no `NOLINT`; the variadic overload's rvalue requirement asserted at compile time; also built over the `StopToken` fallback; Task B1: a cancelled child unwinds into a `whenAll` like any other escape -- the case that says which of the two combinators classifies what left a child's task, now that one runner serves both |
| `src/core/async/WhenAny.hpp` | contour-terminal/contour | `src/coro/WhenAny.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | no `NOLINT`; the final awaiter's local `state` renamed `race` (`-Wshadow`); Task A11: the race state is a `shared_ptr` that every call which can run foreign code holds for that call (a stop callback resuming its coroutine could free it mid-`request_stop()`), the result is `std::optional<std::size_t>` in place of the `detail::WhenAnyNoWinner` sentinel, only a child that COMPLETED latches the win (the runner promise classifies what escaped its task, in place of the body's swallow), the cancel bridge is a named functor in place of `StopCallback<std::function<void()>>`, the variadic overload requires rvalues, `HasStopToken`; Task B1: the runner promise carries `unownedRoot`, for `WhenAll`'s reason; Task B1 also collapsed the runner, the state and the awaiter into `Join.hpp`: what is left here is `WhenAnyState`, the cancel bridge, the policy and `await_resume` |
| `src/core/async/WhenAny_test.cpp` | contour-terminal/contour | `src/coro/WhenAny_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | no `NOLINT`; `ManualEvent::waiters` initialised to `nullptr` (`cppcoreguidelines-pro-type-member-init`); `failingRacer()` and `raceWithFailingWinner()` moved under `#ifndef _WIN32` with the one case that uses them (`-Wunused-function` on clang-cl); Task A11: the racers rethrow their `OperationCancelled` (a loser unwinds), `StopCallbackEvent` delivers cancellation from inside a stop callback, and the cases for a winner that beats a later cancellation and for the destroyed-mid-`request_stop()` race; also built over the `StopToken` fallback |
| `src/core/cli/App.cpp` | contour-terminal/contour | `src/crispy/App.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import fix (Task A5b): the local `namespace CLI = core::cli;` alias is `cli`, lowercase like every other namespace (`readability-identifier-naming.NamespaceCase`); post-import fixes (Task A9): `reparseParameters()` catches what `cli::parse()` throws, so its documented bool is true; `installLogging()` releases the previous output before creating its replacement; `screenWidth()` rejects a reported width of 0; `helpStyle()` and `customizeLogStoreOutput()` ask `core::log::isStdOutTerminal()` instead of each branching on the platform; `listDebugTags()` sorts a copy rather than the process-wide category registry |
| `src/core/cli/App.hpp` | contour-terminal/contour | `src/crispy/App.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | - |
| `src/core/cli/App_test.cpp` | origin: core-cpp | - | - | the upstream file has no test; written for the Task A9 fixes to `reparseParameters()` and `installLogging()` |
| `src/core/cli/CLI.cpp` | contour-terminal/contour | `src/crispy/CLI.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import fixes (Task A9): `wordWrapped()` no longer underflows its right margin, indexes `text[SIZE_MAX]` for a leading line feed, or returns an empty chunk; the options column accounts for the verbatim placeholder; the hyperlink scan widens through `unsigned char`; two duplicated assignments and one dead branch removed; post-import fix (Task A9 fix round 1): the wrapper advances its index by what a chunk consumed, not by what it emitted — trimming a space before a line feed made the two differ and the loop never ended |
| `src/core/cli/CLI.hpp` | contour-terminal/contour | `src/crispy/CLI.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import fix (Task A9): `parse()` documents what it throws as well as what it returns |
| `src/core/cli/CLI_test.cpp` | contour-terminal/contour | `src/crispy/CLI_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import additions (Task A9): the help-layout and parse-failure cases; post-import addition (Task A9 fix round 1): the space-before-a-line-feed cases |
| `src/core/cli/CMakeLists.txt` | origin: core-cpp | - | - | a TIMEOUT on the `cli` test (Task A9 fix round 3): a wrapping loop that stops making progress reads as a silence, not a failure |
| `src/core/log/Assert.hpp` | contour-terminal/contour | `src/crispy/Assert.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | `fatal()`/`SoftRequire()` moved here from crispy's `Assert.hpp` to close a base→log layering cycle; `Require`/`Guarantee`/`todo`/`unreachable`/`setFailHandler` stay in `src/core/Assert.hpp` |
| `src/core/log/CMakeLists.txt` | origin: core-cpp | - | - | - |
| `src/core/log/LogSink.cpp` | contour-terminal/contour | `src/crispy/LogSink.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import fix (Task A9): all three operating-system differences moved out of `#ifdef`s here into `posix/` and `windows/` implementations — the terminal query in fix round 1 (Ruling R52), where the Windows half answers the platform instead of returning `true` unconditionally, `processId()` in fix round 2 (Ruling R55) and `localTime()` in fix round 3 (Ruling R62); no `#ifdef` is left in the file |
| `src/core/log/LogSink.hpp` | contour-terminal/contour | `src/crispy/LogSink.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import addition (Task A9): `isStdOutTerminal()`/`isStdErrTerminal()`, the one place the platform is asked whether a standard stream is a terminal |
| `src/core/log/LogSink_test.cpp` | contour-terminal/contour | `src/crispy/LogSink_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | post-import fix (`fe62488`): reads the fixture files through a `contentsOf()` stringstream helper, not `std::istreambuf_iterator`; post-import additions (Task A9): the redirected-stream cases, and an RAII restore for the case that disables the error category |
| `src/core/log/LogStore.cpp` | contour-terminal/contour | `src/crispy/LogStore.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | `logstore::` renamed `core::log::` |
| `src/core/log/LogStore.hpp` | contour-terminal/contour | `src/crispy/LogStore.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | `logstore::` renamed `core::log::` |
| `src/core/log/detail/LocalTime.hpp` | origin: core-cpp | - | - | private (in SOURCES, not HEADERS): the one declaration the two `localTime()` implementations share (Task A9 fix round 3, Ruling R62) |
| `src/core/log/detail/ProcessId.hpp` | origin: core-cpp | - | - | private (in SOURCES, not HEADERS): the one declaration the two `processId()` implementations share (Task A9 fix round 2, Ruling R55) |
| `src/core/log/posix/LocalTime.cpp` | origin: core-cpp | - | - | the POSIX half of `localTime()` (`localtime_r`), split out of the last of `LogSink.cpp`'s `#ifdef`s; Emscripten compiles it too |
| `src/core/log/posix/ProcessId.cpp` | origin: core-cpp | - | - | the POSIX half of `processId()`, split out of one of `LogSink.cpp`'s `#ifdef`s; Emscripten compiles it too |
| `src/core/log/posix/TerminalQuery.cpp` | origin: core-cpp | - | - | the POSIX half of the terminal query, split out of `LogSink.cpp`'s `#ifdef` (Task A9 fix round 1, Ruling R52); Emscripten compiles it too |
| `src/core/log/windows/LocalTime.cpp` | origin: core-cpp | - | - | the Windows half of `localTime()` (`localtime_s`, whose arguments are the other way round) |
| `src/core/log/windows/ProcessId.cpp` | origin: core-cpp | - | - | the Windows half of `processId()` |
| `src/core/log/windows/TerminalQuery.cpp` | origin: core-cpp | - | - | the Windows half of the terminal query; contour's `LogSink.cpp` answered `true` unconditionally here |
| `src/core/net/AsyncBufferedReader.cpp` | contour-terminal/contour | `src/net/AsyncBufferedReader.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only Task B10: the refill reads into a member buffer and asserts `contract::requireReadBuffer` |
| `src/core/net/AsyncBufferedReader.hpp` | contour-terminal/contour | `src/net/AsyncBufferedReader.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only Task B10: the refill buffer is a member |
| `src/core/net/AsyncBufferedReader_test.cpp` | contour-terminal/contour | `src/net/AsyncBufferedReader_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; the four C-style `for` loops are range-`for`s over `std::views::iota` Task B10: the scripted double asserts `contract::requireReadBuffer` |
| `src/core/net/BackendParity_test.cpp` | contour-terminal/contour | `src/net/EventSourceParity_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | Task B3: `EventSourceParity_test` renamed, and its direct-registration cases rewritten over `ReadinessHandler` -- a case now asserts which callback ran and how often, because that is what a dispatching backend gives it. The end-to-end cases (the `[closehang]` family above all) are unchanged but for the matrix they run over. New here: Rule 1 asserted from a resumed flow's own frame on every backend, the batch withdrawal on a real kernel (fastcached#475), `setInterest` reporting the kernel's refusal under descriptor pressure (fastcached#1054, #1057), `wake()` from another thread, a dead handle not blinding the backend to a live one, and `makeBackend` answering null for a kind this platform does not build Task B8: `localPort` is `boundPort` Task B7b: every backend without a completion port refuses a `HandleKind::Completion` registration by name, the host-driven one included. |
| `src/core/net/CMakeLists.txt` | origin: core-cpp | - | - | replaces contour's `src/net/CMakeLists.txt`: the targets `core::net_types`, `core::net` and `core::net_tls`, and three test binaries; keeps its comment on the PUBLIC `Threads::Threads` Task B8: the dial, the connector and the two resolvers join `core::net`, with `DialPrimitives.cpp` per platform; five new test sources join the `net` binary. Task B9: the datagram, blocking-transport and in-memory-fake sources and their tests join `core::net` and the `net` binary, with `BlockingPrimitives.cpp`, `SocketErrors.cpp` and `UdpSocket.cpp` per platform |
| `src/core/net/AcceptedHalfClose_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/AcceptedHalfClose_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B6: `shutdownWrite` from the ACCEPTED side, which no other case here drives. Upstream's two assertions are kept verbatim in meaning -- the peer sees EOF, AND the shutting-down socket's own next write fails -- because an implementation that gets only the first right passes a half-written port. Upstream's reactor thread plus blocking connector becomes one sequential flow on one loop: a loopback dial completes out of the backlog before anything accepts, so no step waits on a sibling that may have bailed, and the accepted end provably has not closed when the peer reads -- a close would send a FIN of its own and pass for the half-close's. Portable on purpose: the defect it pins was one platform's Task B8: `localPort` is `boundPort` |
| `src/core/net/CancelRead_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/CancelRead_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B6. Upstream's three cases, re-driven over `testing::BackendMatrix` and a socket pair on one loop rather than a blocking connector and threads -- which makes the park arranged rather than waited for. Each asserts that something really parked, so it cannot degenerate into the synchronous arm and pass having tested nothing |
| `src/core/net/ClockRefresh_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/ReactorClockRefresh_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B4. Rewritten into one case per refresh: upstream asserted only that a cached clock advanced at all, which passes with the pre-wait refresh removed. Here step 3's refresh is asserted through the TIMEOUT the backend is handed and step 5's through the turn a deadline fires in, so neither can be deleted without a red. Over `ScriptedBackend` rather than a platform reactor, so the clock moves by exactly what the loop asked to wait |
| `src/core/net/DeadlineTimer.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/DeadlineTimer.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B5. Upstream's body is a detached coroutine that hops onto the reactor, sleeps in 50ms steps re-reading a disarm flag, and clears a shared handle on its one exit path; all of it is gone. `EventLoop::cancelTimer` retires the park by id, so the timer is four members and three functions: arm, disarm, and a static callback that marks the timer settled BEFORE the user's callback runs so the callback may destroy it |
| `src/core/net/DeadlineTimer.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/DeadlineTimer.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B5. `DefaultPollInterval` and the `pollInterval` parameter are gone, and so is `shared_ptr<State>`: upstream needed shared state because a late fire had to read live memory after the owner was destroyed, and there is no late fire to survive once a deadline can be taken back. `Disarm`/`IsSettled` become `disarm`/`settled`; the reactor reference becomes an `EventLoop&` |
| `src/core/net/DeadlineTimer_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/DeadlineTimer_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B5. Upstream's poll-interval cases are dropped -- they asserted how long a disarmed timer's frame lingered, which is now zero turns -- and one case replaces them: an armed timer bounds the turn's wait to its OWN deadline, which is the assertion upstream could not have written. The destroy-from-inside-its-own-callback case is kept and is now deterministic rather than interval-dependent |
| `src/core/net/Diagnostics.cpp` | contour-terminal/contour | `src/net/Diagnostics.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only |
| `src/core/net/Diagnostics.hpp` | contour-terminal/contour | `src/net/Diagnostics.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only |
| `src/core/net/EventLoop.cpp` | contour-terminal/contour | `src/net/EventLoop.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; `IClock`, `SystemPipe` and `createSystemPipe()` are `core::platform`'s; the two iterator `for` loops over `_fdToTokens` are a `std::ranges::find_if` and a range-`for` over a `std::ranges::subrange`; post-import addition (core-cpp): `pumpOnce()` calls `IClock::refresh()` before it computes a wait's timeout and after the wait, as the merged `IClock` asks of a loop | Task B3: the loop drives an `IoBackend`, so readiness arrives as a callback per park rather than as two token lists to route, and the post self-pipe is gone -- `post()` calls `IoBackend::wake()`, which every backend owns. `drainReadyQueue` asserts no backend dispatch is in flight, which is where Rule 1 is caught Task B4: the five-step turn, the six-step teardown and the park table; `IExecutor` is implemented here, so `submit`, `schedule` and `cancelPending` are the loop's; `pumpOnce` is `runOnce`, and what it resumes is drained in step 2 alone |
| `src/core/net/EventLoop.hpp` | contour-terminal/contour | `src/net/EventLoop.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; `IClock`, `defaultSteadyClock()`, `SteadyTimePoint`, `NativeHandle`, `InvalidHandle` and `SystemPipe` are `core::platform`'s; no `NOLINT`; the provenance pointer names contour's `src/coro/README.md`; the constructor's comment says when the loop refreshes the clock (core-cpp) | Task B3: `EventSource&` becomes `IoBackend&`, `FdToken` becomes the loop's own `ParkId` (Task B4 widens it), each park owns the `ReadinessHandler` the backend holds, and `parkedWaiterCount()` joins `pendingTimerCount()` as the leak assertion for the other kind of park Task B4: `EventLoop` is an `async::IExecutor`, `ParkId` and `ParkEntry` move to `detail/ParkTable.hpp`, `WaitFdAwaiter` becomes `WaitHandleAwaiter` and takes a `HandleKind`, and `IdlePolicy`, `EventLoopOptions`, `RunOnceResult`, `run`, `runOnce`, `runUntilIdle`, `stop`, `resumeSoon`, `registerPark`, `unregisterPark`, `wakeReasonOf`, `requestCancel`, `running`, `isOnWorkerThread` and `teardownIsSerialisedWithDispatch` arrive Task B7b: `completionPort()`, lending the backend's port (never the backend) to the socket factories that choose by it. |
| `src/core/net/EventLoop_test.cpp` | contour-terminal/contour | `src/net/EventLoop_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; `ManualClock`, `SystemPipe`, `createSystemPipe()`, `NativeHandle` and `InvalidHandle` are `core::platform`'s; a pipe read tests `ChannelResult::bytesRead()` where it compared a count; post-import addition (core-cpp): the case over a `CachedClock` that the loop refreshes before each timeout and after each wait | Task B3: over `ScriptedBackend` and `makeDefaultBackend()`; the registration cases are stated in handlers and `HandlerId`s, and the leak assertions read `EventLoop::parkedWaiterCount()` where they read the source's `attachedCount()` Task B4: the clock cases move to `ClockRefresh_test.cpp`, one per refresh; the turn's order and bound, guarantees G2 and G3, and `spawn`'s O(1) unlink are asserted here |
| `src/core/net/HostDrivenBackend.cpp` | origin: core-cpp | - | - | its body: the coalescing (several wakes before the host gets a turn are one pump), the clamp of a deadline already past to a zero delay, and the schedule cleared BEFORE a pump runs so the turn it drives can arm the next one |
| `src/core/net/HostDrivenBackend.hpp` | origin: core-cpp | - | - | Task B3: the IoBackend for a loop that is PUMPED rather than one that blocks. Portable, and tested on every platform over testing::ManualHostScheduler -- the browser is one of its hosts, not its definition |
| `src/core/net/HostDrivenBackend_test.cpp` | origin: core-cpp | - | - | Task B3: the host-driven backend over testing::ManualHostScheduler, on every platform including Emscripten -- the alternative leaves its behaviour observable only in a node run |
| `src/core/net/HostDrivenCanary.cpp` | origin: core-cpp | - | - | Task B4: `run()` and `blockOn()` on a host-driven loop are precondition violations, and an assertion cannot be asserted from inside a Catch case -- it aborts the binary. One process per mode, each judged by a marker it prints to stderr immediately before the forbidden call (`PASS_REGULAR_EXPRESSION`; `WILL_FAIL` was removed tree-wide by a48e727). Its SIGABRT handler is load-bearing: a raw signal defeats both regex properties. Task B13: its thread-affinity mode moved to `LoopAffinityCanary.cpp`, and `closedPark` joined it -- a host-driven backend that accepts readiness, which `armHostWake` asserts none does |
| `src/core/net/HostDrivenLoop_test.cpp` | origin: core-cpp | - | - | Task B4: a loop that does not own its thread, over `testing::ManualHostScheduler` on every platform. It asserts the DEADLINE the loop arms the host at rather than merely that it asked, and that a destroyed loop leaves the backend nothing to call -- the last step of the teardown, which nothing else reaches |
| `src/core/net/HttpServer.cpp` | contour-terminal/contour | `src/net/HttpServer.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only Task B10: `serve` awaits `handshakeIfNeeded` before reading |
| `src/core/net/HttpServer.hpp` | contour-terminal/contour | `src/net/HttpServer.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only Task B10: `serve` documents the handshake and core-cpp#35 |
| `src/core/net/HttpServer_test.cpp` | contour-terminal/contour | `src/net/HttpServer_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; the `for` over `find()` positions is a `while` Task B8: `localPort` is `boundPort` Task B10: a case for the handshake `serve` awaits |
| `src/core/net/ICompletionPort.hpp` | origin: core-cpp | - | - | Task B7a: the seam a completion-based backend lends its sockets, and the ONE place a handle is associated with a port -- which is what lets guarantee G4 be asserted rather than left to `CreateIoCompletionPort`, whose refusal of a second association is `ERROR_INVALID_PARAMETER` and so indistinguishable from six ordinary mistakes. fastcached had `IocpReactor::AttachHandle` and `NativeHandle()` on the reactor itself, so every consumer of the port also had the reactor; nothing upstream has the record or the refusal. Declared on every platform, not behind `#if defined(_WIN32)` as the design spec sketches it, because a public header that changes shape per platform is one a consumer's build can disagree with this one about (`.agent/rules/platform.md`) Task B7b: `beginOperation` and `withdrawOperation`, typed `void*` so the header stays portable -- the record that lets a port tell an owner's packet from its own. |
| `src/core/net/IHostScheduler.hpp` | origin: core-cpp | - | - | Task B3: the one thing a host event loop has to lend core-cpp's -- `callAfter(delay, fn, state)` and nothing else. The design spec names it (Part I §2); nothing upstream has it, because neither contour nor fastcached has a loop that does not own its thread |
| `src/core/net/IListener.hpp` | contour-terminal/contour | `src/net/IListener.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only Task B8: `localPort` is `boundPort` (the spec's rename map), and `AcceptResult` becomes an ALIAS of `SocketResult` rather than a second spelling of the same type -- accept and connect answer the same question |
| `src/core/net/ISocket.cpp` | origin: core-cpp | - | - | Task B6: the out-of-line defaults `ISocket`'s awaitable signatures need. `readWithFd` and `writeVectored` are coroutine-backed here, because a base class has nowhere frame-free to keep an inner operation -- every transport that can do better overrides them, and all of the ones here do |
| `src/core/net/ISocket.hpp` | contour-terminal/contour | `src/net/ISocket.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | Task B6 merged fastcached's `src/FastCache/Net/ISocket.hpp` (`0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21`) into it: every operation is now a frame-free `ResultAwaitable` rather than an `async::Task`, and `writeVectored`, `waitReadable`, `cancelRead`, `shutdownWrite` and `setReceiveDeadline` arrive from there with their reasoning. contour's `readWithFd` and the EOF latch on `isClosed` are kept |
| `src/core/net/ITlsContext.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/TlsWrap.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B11: `WrapTls` as `wrapTls`, over contour's `ITlsContext` (moved here from `src/net/Tls.hpp` at `6777ff05014f8ff163b071e8b0e942830119db80`) instead of a preprocessor switch; `certificateFingerprint` from upstream's `TlsContext` |
| `src/core/net/InterruptibleSleep.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/InterruptibleSleep.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B5. Upstream's `while` over `NextWakeStep` is gone: the wait parks once and a stop callback wakes it. New here is `TokenDelayAwaiter`, which watches TWO tokens -- the supplied one and the awaiting flow's own -- because `DelayAwaiter` reads its token out of the parking promise and this function is told which token to watch |
| `src/core/net/InterruptibleSleep.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/InterruptibleSleep.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B5. `WakeReason` is unchanged; `InterruptibleSleepUntil` becomes `interruptibleSleepUntil` over an `EventLoop*` and a `core::async::StopToken`. `wakeBound` loses its no-default rule and its meaning: the four-argument form is kept for one release and ignores it |
| `src/core/net/InterruptibleSleep_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/InterruptibleSleep_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B5. Rewritten: upstream's cases asserted that a cancel was noticed within one `wakeBound` of REAL time, so they could pass by time elapsing. These run with a `ManualClock` frozen, assert that ONE turn resolves the cancel, and assert the park table is empty afterwards. New here: the flow's own token unwinds while the supplied one reports, and the two coincide in the caller's favour |
| `src/core/net/IoAwaitable.cpp` | origin: core-cpp | - | - | Task B6 fix round 1: the one out-of-line hop that keeps `<core/net/EventLoop.hpp>` out of `<core/net/ISocket.hpp>`. `ResultAwaitable::onStop` needs exactly one member of the loop, and a header carrying a whole class definition for one call imposes it on every translation unit that touches a socket -- measured at 138718 preprocessed lines for `ISocket.hpp` before and 130777 after. `SocketContract.hpp` already forward-declares `EventLoop` for the same reason |
| `src/core/net/IoAwaitable.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/ISocket.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B6: upstream's `IoAwaitable` class, lifted out of `ISocket.hpp` into a header of its own and generalised to `ResultAwaitable<R>` so accept, handshake and fd-passing reads share it. Upstream's `SetSuspendCallback`/`CallbackState` pair becomes an arm/retire/owner triple taken at construction. **New here, and none of it exists upstream**: the RETIRE hook (upstream's awaitable is never cancelled out from under its owner), the stop token and the two cancellation rules of the design spec §2 item 5, `abandon()` for the destructor path, the pre-await move constructor `asTask` needs, and a coroutine-backed mode for decorators. `await_suspend` returns a handle rather than a bool, so a decorator starts its task by symmetric transfer instead of a nested resume |
| `src/core/net/IoAwaitable_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/IoAwaitable_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B6. Upstream's two synchronous-completion cases are kept in shape, including the 200000-iteration one that fails by crashing rather than by asserting; the six cancellation cases are new and pin the §2 item 5 rules upstream's awaitable has none of |
| `src/core/net/IoBackend.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/EpollReactor.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B3. `EpollFdHandler` is `ReadinessHandler` and `SelectEpollCallback` is `selectReadinessCallback`, both stated in a portable vocabulary (`Readiness`) rather than in epoll's bits, so the routing rule is one function every platform tests. Merged with contour's `src/net/EventSource.hpp` (6777ff05014f8ff163b071e8b0e942830119db80), from which it keeps the interest mask and the documented meaning of `Interest::None` (mute the handle without detaching it) and the factory trio of `src/net/DefaultEventSource.hpp`, renamed `preferredBackendKind`, `makeBackend` and `makeDefaultBackend`. New here: `setInterest` answers `std::expected<void, NetError>` so a kernel's refusal is reported (fastcached#1054, #1057), `wake()` is on the interface because the wakeup channel belongs to the backend, and `isHostDriven`/`armWakeAt` are the seam Task B3's second commit fills. Task B7a adds `ReadinessHandler::slot` -- the design spec declared it and Task B3 omitted it, because a public field with no reader has no defined meaning, so it arrives with its first writer -- and `completionPort()`, declared on every platform rather than behind the spec's `#if defined(_WIN32)` Task B7b adds `HandleKind::Completion`, with its first user: a park on an owner's overlapped operation, which is how a completion reaches the loop's turn instead of a callback inside the backend's walk. |
| `src/core/net/IoBackend_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/EpollReactor_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B3. Its five `SelectEpollCallback` cases, over `selectReadinessCallback`, and its batch-withdrawal case (fastcached#475) in the pure form `detail::ReadyBatch` allows -- no kernel, no reactor, no way to provoke an error needed. New here: the merge of a registration reported twice in one batch, the dispatch guard Rule 1 is asserted against, and `detail::toTimeoutMillis` |
| `src/core/net/IoResult.hpp` | contour-terminal/contour | `src/net/IoResult.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces; `NetErrorCode`, `toString()`, `NetError` and `makeNetError()` split out to `NetError.hpp`, which it includes; the `core::net_types` target |
| `src/core/net/LoopAffinityCanary.cpp` | origin: core-cpp | - | - | Task B13: fastcached's `reactor-teardown-gate` (`scripts/reactor-teardown-gate.cmake` at 0708dd54) in this tree's shape -- one process per loop-thread-only member of `EventLoop`, each calling it from a second thread while the loop runs, judged by the ASSERTION'S OWN TEXT naming that member rather than by a marker, so a death on the way there is not read as the guard firing |
| `src/core/net/LoopTeardown_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/ParkedWork_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B4. The six #1025 proofs, carried over the `BackendMatrix` and the `TestLoop` rather than over one platform reactor. Two of upstream's cases stayed where the type lives (`src/core/async/ParkedWork_test.cpp`), and one is new here: the READINESS park, which fastcached's reactors had and its test did not reach, and which is the only kind that also holds a registration the backend knows by address |
| `src/core/net/NetError.hpp` | contour-terminal/contour | `src/net/IoResult.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | `NetErrorCode`, `toString()`, `NetError` and `makeNetError()`, split out of `IoResult.hpp`, with a file comment of its own; the `core::net_types` target. Task B2 merged LASTRADA-Software/fastcached `src/FastCache/Net/NetError.hpp` (`0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21`) into it: `NetErrorCode` is the union of the two (fastcached's `AddressNotAvail`, `HostUnreach` and `PermissionDenied` join contour's, `BadFileHandle` is `BadHandle` and contour's `Other` is fastcached's `SystemError`), `IsDeadlineExpiry` is `isDeadlineExpiry` with its #824 reasoning rewritten against core-cpp's callers, and a `Last` enumerator states the count. Not taken: fastcached's `ToString()` (`NetError(code=6 …)`, a numeric code) and `ToStringView()` (the enumerator's name); `toString()` keeps contour's wording, and `<format>` stays out of this header |
| `src/core/net/NetError_test.cpp` | origin: core-cpp | - | - | the `core-cpp.net_types` test, which the Emscripten build runs too; the upstream file has no test |
| `src/core/net/PlatformLoop.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/PlatformReactor.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B4. Upstream is an `#if` ladder aliasing one of three concrete reactors; here there is one loop type and the choice is `makeDefaultBackend()`'s, so this is a class that OWNS the backend and is-a `EventLoop`. The private base is what gets the order right: a member would be initialised after every base, and `EventLoop` is one |
| `src/core/net/ReactorSocket_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/EpollSocket_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B6: upstream's `EpollSocket_test` and `KqueueSocket_test` (`src/FastCache/Net/KqueueSocket_test.cpp`, same SHA) merged into ONE suite over `testing::BackendMatrix`, which is the point of the task. Their listener `Adopt` cases stay behind for Task B8, which owns `adoptListener`. New here: the two cancellation rules at the socket, the receive deadline on the loop's own timer heap, and the dropped-operation case the read-slot canary found |
| `src/core/net/SleepUntil.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/SleepUntil.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B5. The aggregate `SleepUntil{&reactor, tp}` becomes a call returning the loop's own `DelayAwaiter`, which already carries the cancellation registration the aggregate would have had to grow; the nullable-reactor contract is kept as a nullable loop. `SleepFor` is `EventLoop::delay`. `NextWakeStep` becomes `nextWakeStep` and its doc is rewritten -- it is no longer the polling rule, because nothing here polls; it is kept as the arithmetic a consumer migrating a bounded wait of its own still needs |
| `src/core/net/SleepUntil_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/SleepUntil_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B5. The two inline resolutions are asserted through `await_suspend()` declining to park (v0.1.1; `await_ready()` until fastcached#1546) and through a flow resumed exactly once, rather than through a prompt return: a case that only observed promptness would pass on an implementation that parked and was resumed a turn later |
| `src/core/net/SocketContract.cpp` | origin: core-cpp | - | - | Task B6: the G5 teardown assertion, out of line so `SocketContract.hpp` stays free of `<core/net/EventLoop.hpp>`. Upstream spells it `Detail::AssertTeardownIsSerialisedWithDispatch`; here the loop answers the two facts and one function states the rule over them |
| `src/core/net/SocketContract.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/ReadSlot.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B6. Merged from three upstream places: `ClaimReadSlot` here, `ClaimWriteSlot` from `src/FastCache/Net/WriteSlot.hpp`, and `RequireReadBuffer` from `src/FastCache/Net/ISocket.hpp`'s `namespace Detail`. **Public rather than `detail`** (design spec §2 item 7): a transport outside this library is under the same rules, so it gets the same tripwires. The guards are templates over the slot pointer, because core-cpp's three read verbs resolve to different types through one slot. Upstream's `RetireParkedRead` is deliberately NOT carried over -- core-cpp's socket owns a park id and a deadline besides the awaitable, so retirement is `PosixSocket::takeRead` and there is no shared body left |
| `src/core/net/SocketContractCanary.cpp` | LASTRADA-Software/fastcached | `src/tests/EmptyReadBufferCanary.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B6. One program with three modes where upstream has three canary programs (`read-slot-guard-canary` and `write-slot-guard-canary` are its siblings); each drives a REAL socket into a guard and must die. SIGABRT becomes an exit code, as in `HostDrivenCanary.cpp`. Registered on MARKERS rather than on `WILL_FAIL`, which the upstream canaries use and which inverts ANY non-zero exit: each mode prints `<mode>: reached the guarded call` immediately before the forbidden operation and `SURVIVED` immediately after, so a process that died on its way to the call is not read as a guard that fired, and a mode that reached the WRONG guard is not read as having reached its own Task B7b: the write modes keep writing until one write stays pending, bounded at 256MiB and SKIPping with a reason beyond it, because a Windows non-blocking send takes 8MiB at once; all four modes run on Windows. |
| `src/core/net/SocketDecorator_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/SocketDecorator_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B6. Upstream drives its shared parking FAKES; this drives a real `PosixSocket` and a `SplitSocket` over it, because core-cpp has no `ParkingReadableSocket` and the rule belongs to the transports that ship. The `weak_ptr` observation, and the reason for it -- a late member access is otherwise an ASan report once in N runs -- are upstream's Task B10: a case for `SplitSocket::close()` destroyed by its first retirement |
| `src/core/net/Socket_test.cpp` | contour-terminal/contour | `src/net/Socket_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only Task B8: `localPort` is `boundPort` |
| `src/core/net/Sockets.hpp` | contour-terminal/contour | `src/net/Sockets.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only Task B8: `ListenOptions` and `listen(loop, ListenOptions)` (the spec's rename map for `*Listener::Bind`), `adoptListener`, and a `connect` overload taking an `IAsyncAddressResolver*` and `DialOptions`. The positional `listen` stays as a forwarder. `connect` no longer resolves on the caller's thread, which is the behaviour change its callers inherit; both `connect` overloads take the host as `std::string`, since a lazy coroutine's `string_view` parameter can dangle before the body runs |
| `src/core/net/SplitSocket.hpp` | contour-terminal/contour | `src/net/SplitSocket.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only Task B10: `close()` checks a liveness token between its two retirements |
| `src/core/net/Timers_test.cpp` | origin: core-cpp | - | - | Task B5: `addTimer`/`cancelTimer`, which have no upstream -- fastcached had no frameless timer, only `DeadlineTimer`'s coroutine. It holds the firing order ACROSS the two kinds of park, the cancellation window between step 5 queueing a due timer and step 2 running it, and the measurement of what lazy deadline-heap pruning costs (Task B4's report, concern 4) |
| `src/core/net/Tls.cpp` | contour-terminal/contour | `src/net/Tls.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; no `NOLINT`: contour's three `NOLINTNEXTLINE(readability-identifier-naming)` on `HandshakeGate`'s `await_*` hooks are gone, since `.clang-tidy` exempts the hook names; the `core::net_tls` target, which links OpenSSL PRIVATE Task B11 merged fastcached's `src/FastCache/Net/TlsSocket.cpp` and `TlsContext.cpp` (`0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21`) into it: `ERR_clear_error()` before every classified `SSL_*` call, the `SSL_peek` `waitReadable`, an eager `handshakeIfNeeded`, the P-256 generator with subjectAltNames, the certificate fingerprint; and wrote the `close_notify` half-close, one outbound flush at a time, chain serving and the random serial |
| `src/core/net/Tls.hpp` | contour-terminal/contour | `src/net/Tls.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; the `core::net_tls` target Task B11: `SelfSignedOptions` (fastcached's `CreateSelfSigned` parameters, without its hard-coded common name), `makeTlsServerContextFromFiles`, `certificateFingerprint`; `ITlsContext` moved to `ITlsContext.hpp` |
| `src/core/net/Tls_test.cpp` | contour-terminal/contour | `src/net/Tls_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; the two two-reactor cases make the client context before the server thread starts, so no `REQUIRE` can unwind past a joinable `std::thread` (`.agent/rules/testing.md`); the `core-cpp.net_tls` test Task B8: `localPort` is `boundPort` Task B11: `generateSelfSignedCertificate` takes `SelfSignedOptions` |
| `src/core/net/TlsContext_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/TlsContext_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B11: onto core-cpp's factories; certificates generated at run time rather than read from `testdata/tls/`, whose committed certificate expires; plus the common-name, chain, file-loading, `wrapTls` and `constantTimeEquals` cases |
| `src/core/net/TlsLifetime_test.cpp` | origin: core-cpp | - | - | Task B11 fix round 1: the handshake and flush gates under a destroyed socket, a waiter that drops it, a stop token and `cancelRead` |
| `src/core/net/TlsSocket_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/TlsSocket_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B11: the `WaitReadable` cases (#712) and the handshake over non-TLS input, over a real socket pair driven by the loop instead of hand-pumped pipes; plus the half-close, eager-handshake, stale-error-queue and single-flush cases |
| `src/core/net/WaitReadable_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/WaitReadable_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B6: the #677 count semantics, over every backend. Upstream's off-thread flag machinery is gone -- both ends live on one loop, so the ordering is arranged. Upstream's MEASURED abortive-close case is deliberately not carried over: it records platform behaviour rather than asserting a contract, and core-cpp has no `SO_LINGER` fixture |
| `src/core/net/WithTimeout.hpp` | contour-terminal/contour | `src/net/WithTimeout.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only |
| `src/core/net/WithTimeout_test.cpp` | origin: core-cpp | - | - | Task B10: `withTimeout` over a real socket read, and what a lost race leaves in the read slot and the reader |
| `src/core/net/WriteQueue.cpp` | contour-terminal/contour | `src/net/WriteQueue.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only |
| `src/core/net/WriteQueue.hpp` | contour-terminal/contour | `src/net/WriteQueue.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only |
| `src/core/net/WriteQueue_test.cpp` | contour-terminal/contour | `src/net/WriteQueue_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only Task B10: a case counting writes in flight over a parking double |
| `src/core/net/bsd/DefaultBackend.cpp` | contour-terminal/contour | `src/net/DefaultEventSource.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | its macOS/BSD arm: kqueue by preference, poll(2) beside it |
| `src/core/net/bsd/KqueueBackend.cpp` | contour-terminal/contour | `src/net/KqueueEventSource.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | Task B3 rewrote it behind `IoBackend`: readiness is DISPATCHED to the callbacks on the `ReadinessHandler` a caller registers, instead of being collected into a `WaitOutcome` of tokens for the caller to route. The `EV_RECEIPT` handling is unchanged and still load-bearing; what changed is where its verdict goes -- a refused filter is now a `NetError` carrying the kernel's errno rather than an invalid token (fastcached#1054) Task B7b: refuses `HandleKind::Completion` by name with `Unsupported`. |
| `src/core/net/bsd/KqueueBackend.hpp` | contour-terminal/contour | `src/net/KqueueEventSource.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | Task B3: `KqueueEventSource` renamed `KqueueBackend`. Task B3 rewrote it behind `IoBackend`: readiness is DISPATCHED to the callbacks on the `ReadinessHandler` a caller registers, instead of being collected into a `WaitOutcome` of tokens for the caller to route; `attach` here registers NOTHING with the kernel, and says so -- kqueue has no 'add with no filters' operation, so only `setInterest` can answer whether the kernel accepted the descriptor (fastcached#1057) |
| `src/core/net/detail/ParkId.hpp` | origin: core-cpp | - | - | Task B13: `ParkId` and its hash, moved out of `detail/ParkTable.hpp` so that `IoAwaitable.hpp` -- and so `ISocket.hpp` -- no longer includes the park table for one strong integer (core-cpp#43) |
| `src/core/net/detail/ParkTable.hpp` | origin: core-cpp | - | - | Task B4: `ParkId`, `ParkEntry` and the table. fastcached had no unified one -- a submission queue, a timer heap and each reactor's own descriptor map -- and cancellation therefore meant something slightly different in each. One table, one id space that never wraps, and the id IS the generation check: a cancel for a park that has gone resolves to nothing |
| `src/core/net/detail/PeerAddress.hpp` | contour-terminal/contour | `src/net/platform/PeerAddress.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces; moved from `platform/` to `detail/`, since a `core::net::platform` namespace would hide `core::platform` (Rulings R37 and R40); private, and it includes `<winsock2.h>` |
| `src/core/net/detail/ReadinessSlot.hpp` | origin: core-cpp | - | - | Task B7a: the backend-owned refcounted object a completion packet names, and the share of it `ReadinessHandler::slot` holds. The design spec declares the field (Part I §2); Task B3 left it out because a public field with no reader has no defined meaning, and this is its first writer. Nothing upstream has it: fastcached's completions pointed at an `IocpCompletion` embedded in the socket's `Impl`, which is the same idea one layer up and is why that `Impl` had to be a `shared_ptr` the operation kept alive (fastcached#465) |
| `src/core/net/detail/ReadyBatch.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/EpollReactor.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B3. `EpollReactor::_batch` and the withdrawal half of `EpollReactor::Detach`, lifted out of the two reactors that each had a copy so the four backends here share one (fastcached#475). New here: two reports of one registration in a batch merge into one entry, which is what makes 'at most one callback per registration per wait' hold on kqueue, whose kernel answers per (descriptor, filter); and `ReadinessDispatchGuard`, which publishes that a dispatch is in flight so `EventLoop::drainReadyQueue` can assert it is not |
| `src/core/net/detail/ScopeGuard.hpp` | contour-terminal/contour | `src/net/detail/ScopeGuard.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only |
| `src/core/net/detail/WaitChunking.hpp` | contour-terminal/contour | `src/net/WaitChunking.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only; moved to `detail/` (Ruling R40), and so private |
| `src/core/net/detail/WaitTimeout.hpp` | origin: core-cpp | - | - | Task B3: the millisecond conversion each backend's native wait wants, with contour's clamp of a sub-millisecond remainder to 1ms (`EventLoop::computeTimeoutMs`) kept -- truncating it to 0 turns a wait into a poll and spins the pump. Free of every platform header, so every OS tests it |
| `src/core/net/detail/WakeupChannel.hpp` | origin: core-cpp | - | - | Task B3: contour's `EventLoop` self-pipe, moved into the backend, because `IoBackend::wake()` is the one thread-safe member of the interface and every backend that blocks needs the same mechanism behind it. Shared rather than written four times: a lost wakeup is silent, and shows only as a shutdown that hangs |
| `src/core/net/detail/WorkerIdentity.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/ReactorWorkerIdentity.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B4. Verbatim but for the names and one addition: the claim NESTS, because a loop here is driven through `run()`, `runOnce()` and `blockOn()` and `run()` is a `runOnce()` in a loop -- a non-nesting claim would have the first inner turn release the outer one and leave `run()` reporting that nobody is driving |
| `src/core/net/detail/WouldBlock.hpp` | contour-terminal/contour | `src/net/detail/WouldBlock.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only |
| `src/core/net/emscripten/DefaultBackend.cpp` | origin: core-cpp | - | - | Task B3: the Emscripten arm of the four DefaultBackend.cpp files (Ruling R40). It is the only one with nothing to fall back to, and the only one whose host is process-wide -- there is one browser event loop, and `emscripten_async_call` reaches it from anywhere |
| `src/core/net/emscripten/EmscriptenHostScheduler.cpp` | origin: core-cpp | - | - | its body: `emscripten_async_call`, which is the browser's `setTimeout`, with the millisecond count clamped to what its `int` takes |
| `src/core/net/emscripten/EmscriptenHostScheduler.hpp` | origin: core-cpp | - | - | Task B3: the browser as a host. No `#ifdef __EMSCRIPTEN__` guards it -- SOURCES_EMSCRIPTEN selects the file (Ruling R42) -- and it is private: a program gets a host-driven backend from makeDefaultBackend() |
| `src/core/net/linux/DefaultBackend.cpp` | contour-terminal/contour | `src/net/DefaultEventSource.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | its Linux arm: epoll by preference, poll(2) beside it, and the silent fallback to poll when the epoll instance cannot be created |
| `src/core/net/linux/EpollBackend.cpp` | contour-terminal/contour | `src/net/EpollEventSource.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | Task B3 rewrote it behind `IoBackend`: readiness is DISPATCHED to the callbacks on the `ReadinessHandler` a caller registers, instead of being collected into a `WaitOutcome` of tokens for the caller to route. `attach` reaches the kernel no more: `Interest::None` keeps a registration OUT of the epoll set, because a descriptor sitting in it with `events == 0` still reports EPOLLHUP and EPOLLERR on every wait, which is what made a muted registration spin the pump. The private `dup()` for a genuine second registration of one descriptor is unchanged, and its failure is now reported with the kernel's errno instead of a bare refusal Task B7b: refuses `HandleKind::Completion` by name with `Unsupported`. |
| `src/core/net/linux/EpollBackend.hpp` | contour-terminal/contour | `src/net/EpollEventSource.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | Task B3: `EpollEventSource` renamed `EpollBackend`. Task B3 rewrote it behind `IoBackend`: readiness is DISPATCHED to the callbacks on the `ReadinessHandler` a caller registers, instead of being collected into a `WaitOutcome` of tokens for the caller to route; interest is no longer fixed at attach, so the registration record carries it and whether the descriptor is currently in the epoll set |
| `src/core/net/posix/AcceptLoop.cpp` | contour-terminal/contour | `src/net/posix/AcceptLoop.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; `PeerAddress.hpp` from `detail/`; no file-wide `#ifndef _WIN32` guard: `SOURCES_POSIX` selects the file (Ruling R42); it keeps the `#ifdef __linux__` that accepts with `accept4()` and the `#ifndef __linux__` that sets `O_NONBLOCK` and `FD_CLOEXEC` with `fcntl()` elsewhere |
| `src/core/net/posix/AcceptLoop.hpp` | contour-terminal/contour | `src/net/posix/AcceptLoop.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; no file-wide `#ifndef _WIN32` guard: `SOURCES_POSIX` selects the file (Ruling R42) |
| `src/core/net/posix/DefaultBackend.cpp` | contour-terminal/contour | `src/net/DefaultEventSource.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | Task B3: contour's file split four ways along its `#ifdef`s (Ruling R40), one per platform directory, with CMake compiling exactly one. This is the POSIX platform with no scalable readiness primitive of its own: poll(2), and nothing else |
| `src/core/net/posix/FdPassing_test.cpp` | contour-terminal/contour | `src/net/FdPassing_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; a coroutine lambda's parameter `loop` is `eventLoop`, which the local `loop` shadowed (GCC `-Wshadow`); `sendWithFds()` checks the header `CMSG_FIRSTHDR()` returns before writing through it (GCC `-Wnull-dereference` at `-O3`); it converts `CMSG_SPACE()` and `CMSG_LEN()` to the type of the field they are stored in, `socklen_t` on macOS (Clang `-Wshorten-64-to-32`); moved to `posix/` (Ruling R40: platform code lives in platform subdirectories); no file-wide `#ifndef _WIN32` guard: `SOURCES_POSIX` selects the file (Ruling R42); it keeps the `#ifndef __APPLE__` around the check that the kernel closed the write end |
| `src/core/net/posix/FdUtils.hpp` | contour-terminal/contour | `src/net/posix/FdUtils.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; never had a file-wide guard; it keeps the `#ifndef MSG_NOSIGNAL` fallback and the `#if defined(SOCK_NONBLOCK) && defined(SOCK_CLOEXEC)` branch, which choose within POSIX |
| `src/core/net/posix/PollBackend.cpp` | contour-terminal/contour | `src/net/PollEventSource.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | its POSIX half (Ruling R40). Task B3 rewrote it behind `IoBackend`: readiness is DISPATCHED to the callbacks on the `ReadinessHandler` a caller registers, instead of being collected into a `WaitOutcome` of tokens for the caller to route; the muting trick -- a negative descriptor in the `pollfd`, because `events == 0` does not stop the kernel reporting POLLHUP/POLLERR/POLLNVAL -- is kept verbatim, and POLLNVAL now reads as `Readiness::Failed` Task B7b: refuses `HandleKind::Completion` by name with `Unsupported`. |
| `src/core/net/posix/PollBackend.hpp` | contour-terminal/contour | `src/net/PollEventSource.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | Task B3: `PollEventSource` renamed `PollBackend`. Task B3 rewrote it behind `IoBackend`: readiness is DISPATCHED to the callbacks on the `ReadinessHandler` a caller registers, instead of being collected into a `WaitOutcome` of tokens for the caller to route. Its Windows half is a class of its own now (`windows/WfmoBackend.hpp`), so this one is POSIX-only and private: a program reaches it through `makeBackend(BackendKind::Poll)` |
| `src/core/net/posix/PosixListener.cpp` | contour-terminal/contour | `src/net/posix/PosixListener.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; the `addrinfo` walk is a `while` that steps to the next candidate first; no file-wide `#ifndef _WIN32` guard: `SOURCES_POSIX` selects the file (Ruling R42) Task B8: `adopt`, which asks the KERNEL for the port through `detail::portOfSockaddr` rather than taking one on trust -- the caller adopting a descriptor is exactly the caller that does not know which port it is |
| `src/core/net/posix/PosixListener.hpp` | contour-terminal/contour | `src/net/posix/PosixListener.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; no file-wide `#ifndef _WIN32` guard: `SOURCES_POSIX` selects the file (Ruling R42) Task B8: `localPort` is `boundPort`, and `adopt` joins `bind` for a descriptor this process did not create |
| `src/core/net/posix/PosixSocket.cpp` | contour-terminal/contour | `src/net/posix/PosixSocket.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | Task B6 rewrote it frame-free over fastcached's `src/FastCache/Net/EpollSocket.cpp` and `src/FastCache/Net/KqueueSocket.cpp` (`0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21`): the retry loop runs in a frameless readiness callback rather than in the awaiting coroutine. contour's `recvmsg` SCM_RIGHTS walk, its `ENOTSOCK`/`EIO` PTY handling, the stale-`errno` zero-write fix and the `SO_NOSIGPIPE` option are kept unchanged; the `MSG_PEEK` probe, the vectored cursor and the detach-then-complete ordering are fastcached's. No file-wide `#ifndef _WIN32` guard: `SOURCES_POSIX` selects the file (Ruling R42) |
| `src/core/net/posix/PosixSocket.hpp` | contour-terminal/contour | `src/net/posix/PosixSocket.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | Task B6 rewrote it as ONE socket replacing fastcached's `src/FastCache/Net/EpollSocket.hpp` and `src/FastCache/Net/KqueueSocket.hpp` (`0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21`), which differed from each other only in the reactor type they named. The per-direction operation structs are theirs; there is no `Impl` pimpl, because the multiplexer is `IoBackend`'s and nothing platform-shaped is left to hide. No file-wide `#ifndef _WIN32` guard: `SOURCES_POSIX` selects the file (Ruling R42) |
| `src/core/net/posix/SocketsPosix.cpp` | contour-terminal/contour | `src/net/posix/SocketsPosix.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; `ensureWinsockInitialized()` is `core::platform`'s; the `addrinfo` walk is a `while` that steps to the next candidate first; no file-wide `#ifndef _WIN32` guard: `SOURCES_POSIX` selects the file (Ruling R42) Task B8: `connect` is gone from here -- it is portable now, over `makeConnector`, in `Sockets.cpp` -- and `listen(loop, ListenOptions)` plus `adoptListener` arrive. `connectUnix` is untouched: an AF_UNIX dial resolves no name |
| `src/core/net/TlsCancelRead_test.cpp` | origin: core-cpp | - | - | Task B11 fix round 1: `cancelRead` during a handshake a read drives. POSIX-only until Task B13 gave `WindowsSocket` its `cancelRead`; it runs on every platform since |
| `src/core/net/posix/UnixListener.cpp` | contour-terminal/contour | `src/net/posix/UnixListener.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; no file-wide `#ifndef _WIN32` guard: `SOURCES_POSIX` selects the file (Ruling R42) |
| `src/core/net/posix/UnixListener.hpp` | contour-terminal/contour | `src/net/posix/UnixListener.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; no file-wide `#ifndef _WIN32` guard: `SOURCES_POSIX` selects the file (Ruling R42) |
| `src/core/net/posix/UnixSocket_test.cpp` | contour-terminal/contour | `src/net/UnixSocket_test.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; moved to `posix/` (Ruling R40: platform code lives in platform subdirectories); no file-wide `#ifndef _WIN32` guard: `SOURCES_POSIX` selects the file (Ruling R42) |
| `src/core/net/testing/BackendMatrix.hpp` | contour-terminal/contour | `src/net/testing/EventSourceBackends.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | Task B3: `AllBackends` renamed `BackendMatrix` and `Backend` renamed `BackendUnderTest`, over `BackendKind`; `Iocp` and `Wfmo` join the table, and `makeBackend` answers null for whichever of them this platform does not build |
| `src/core/net/testing/CoroTestSupport.hpp` | contour-terminal/contour | `src/net/testing/CoroTestSupport.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only |
| `src/core/net/testing/InMemoryTransport.hpp` | contour-terminal/contour | `src/net/testing/InMemoryTransport.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes only |
| `src/core/net/testing/ManualHostScheduler.hpp` | origin: core-cpp | - | - | Task B3: the host that does nothing until a case tells it to, so the exact delay the backend asked for is asserted rather than waited out |
| `src/core/net/testing/NullBackend.hpp` | origin: core-cpp | - | - | Task B3: the backend that reports nothing, for a loop driven entirely by `post`, `spawn` and timers. It never blocks, because the single-threaded WebAssembly subset has nothing to block with; Task B4's `testing::TestLoop` pairs it with `IdlePolicy::Return` for that reason |
| `src/core/net/testing/StrictTlsPeer.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/TlsSocket_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B11: upstream's test-local `TlsPeer`, made a public double of `core::net_tls`: a server role, a strict end-of-stream reading, and peer-certificate queries |
| `src/core/net/testing/StrictTlsPeer.hpp` | origin: core-cpp | - | - | Task B11: the interface of the double above, with its OpenSSL state behind an incomplete type |
| `src/core/net/testing/ScriptedBackend.hpp` | contour-terminal/contour | `src/net/testing/ScriptedEventSource.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | Task B3: `ScriptedEventSource` renamed `ScriptedBackend`, and a real backend rather than a stub -- it dispatches through the same `detail::ReadyBatch` the native ones do. Readiness is scripted against a `HandlerId` handed out in attach order, because a case cannot name a `ReadinessHandler` the loop created inside itself. The live-registration SET (not a count) that A12 added is kept, and so is the throw on an exhausted script; new here are `pushFailure`, `refuseNextAttach`/`refuseNextSetInterest`, and a `wake()` that makes the next wait return without consuming a scripted step |
| `src/core/net/testing/ScriptedBackend_test.cpp` | origin: core-cpp | - | - | Task B3 fix round 2 (F5): cases over the double itself, in the portable binary because the double is portable. A double that is not faithful about a rule every real backend keeps is worse than none -- a case written against it passes here and fails on four kernels, and its author reads the green as evidence |
| `src/core/net/testing/TestLoop.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/TestReactor.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B4. Upstream is a SECOND reactor, so every rule the platform reactors held had to be written twice and could differ; this is the real `EventLoop` over `NullBackend` with `IdlePolicy::Return`, so a case that passes here and fails on a `PlatformLoop` is a backend difference and nothing else. `Tick`/`Drain` become `tick`/`drain` over `runOnce`/`runUntilIdle`; the mutex, the sequence counter and `AbandonParkedWork` are the loop's |
| `src/core/net/testing/TestLoop_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/TestReactor_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B4. The thread-safety case asserts that nothing is LOST rather than the size of one container, because where cross-thread work waits between the producer's call and the loop's turn is the loop's business. New here: `cancelPending`'s ownership transfer, on a submission, a deadline park and a readiness park, over every backend -- 'cancelPending means the same on every backend' was a rule of the design that nothing checked |
| `src/core/net/testing/posix/InMemoryTransport.cpp` | contour-terminal/contour | `src/net/testing/InMemoryTransport.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | its POSIX half: contour's file split along its `#ifdef _WIN32` into `testing/posix/` and `testing/windows/` (Ruling R42); namespaces and includes; `ensureWinsockInitialized()` is `core::platform`'s |
| `src/core/net/testing/windows/InMemoryTransport.cpp` | contour-terminal/contour | `src/net/testing/InMemoryTransport.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | its Windows half: contour's file split along its `#ifdef _WIN32` into `testing/posix/` and `testing/windows/` (Ruling R42); namespaces and includes; `ensureWinsockInitialized()` is `core::platform`'s; `WindowsLoopback.hpp` from `windows/`; it includes `<array>` itself, which contour's branch took from `WindowsLoopback.hpp` Task B7b: the pair is `IocpSocket` over a completion port and `WindowsSocket` over WFMO, asked of the loop. |
| `src/core/net/windows/DefaultBackend.cpp` | contour-terminal/contour | `src/net/DefaultEventSource.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | its Windows arm. contour's file had no Windows branch beyond poll; this one names `Wfmo` as the platform's default and poll(2) as not built here, because Winsock's `WSAPoll` cannot wait on a console handle or an event. Task B7a adds IOCP beside it, reachable by name; the preferred kind stays `Wfmo` until Task B7b, because the sockets that issue overlapped operations on a port are B7b's Task B7b: the preferred kind is `Iocp`; `Wfmo` stays reachable by name for one release (core-cpp#6) and is the fallback when the port cannot be created. |
| `src/core/net/windows/IocpBackend.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/IocpReactor.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B7a. The port, its creation with one concurrent thread, `PostQueuedCompletionStatus` as the wake and the `GetQueuedCompletionStatusEx` batch loop are upstream's; everything else is new, because upstream's was a REACTOR (its own timer heap, its own submission queue, its own run loop) and this is the one blocking primitive an `EventLoop` drives. New here: the three readiness bridges a port has no concept of -- a thread-pool wait for a waitable HANDLE, a zero-byte `WSARecv` for socket readability (`src/FastCache/Net/IocpSocket.cpp:555-561` at the same SHA), `WSAEventSelect` plus the first bridge for writability -- the per-arm `Operation` node that is stood down rather than reused (fastcached#710, #884), and the refcounted `Slot` every `lpOverlapped` points into Task B7b: an OWNER's operation is routed rather than dropped -- recognised by the record `ICompletionPort::beginOperation` made, marked completed, reported to the `HandleKind::Completion` park naming it (level-triggered until the park is taken), and handed to its dequeue hook last; the teardown drain waits for these too. |
| `src/core/net/windows/IocpBackend.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Async/IocpReactor.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B7a. `IocpReactor` becomes an `IoBackend`: what survives is the one-port-one-thread rule (guarantee G1), the association rule (G4) and the completion-key discipline. `IocpStatus`, `IocpCompletion`, the timer heap and the `_posted` side table do not -- a backend here owns no work, so there is nothing to abandon, and the NTSTATUS conversion belongs to the socket that has the SOCKET to hand `WSAGetOverlappedResult`. `WaitBridge` is new and is injected rather than probed at the point of use, so the thread-pool fallback is exercised on a machine where the kernel path is available Task B7b: `beginOperation`/`withdrawOperation` and `issuedOperations()`. |
| `src/core/net/windows/IocpBackend_test.cpp` | origin: core-cpp | - | - | Task B7a: the properties only a completion-based backend has -- both waitable-handle bridges, `HandleKind::Socket` (which no other backend can be handed), and a completion arriving after its registration is freed. Deliberately NOT fastcached's `Async/IocpReactor_test.cpp`: its two cases ask whether a submission resumes on the reactor thread and whether a scheduled timer fires, which here are `EventLoop`'s and are covered backend-agnostically by `EventLoop_test.cpp` and `Timers_test.cpp`. Its `Net/IocpSocket_test.cpp` is 13 cases about `IocpSocket` and `IocpListener`, which arrive with Task B7b Task B7b: those 13 cases are `windows/IocpSocket_test.cpp` now. |
| `src/core/net/windows/IocpDial.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/IocpDial.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B7b: `detail::dialCompletion`, one candidate as an overlapped `ConnectEx`, and the connector's dial wherever the loop lends a completion port. Upstream's `ConnectOp`/`ConnectPark`/`SettleConnect` become `detail::CompletionWait`, shared with the accept; the bind to the family's wildcard, the `ConnectEx` fetch and `SO_UPDATE_CONNECT_CONTEXT` are upstream's (with `Net/IocpConnector.cpp` at the same SHA). The rule upstream stated and this keeps: the completion is the single writer of the outcome, so a deadline or a stop calls `CancelIoEx` and waits. What changed is WHY the wait is safe: upstream's op lived in the dialling frame and was safe only because the frame waited; here it lives on the heap and holds its own share until the port dequeues it, so a frame destroyed mid-dial is as safe as one that waited |
| `src/core/net/windows/IocpDial_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/IocpConnector_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B7b: rewritten rather than ported -- upstream's three cases are about `ParkedWork` ownership inside `IocpReactor`, which here is `EventLoop`'s. What is kept is the first one (a ConnectEx dial connects and moves bytes); added are the IOCP leg of the refused-connect case B8's hand-off asked for (the refusal observed as the completion of an operation the port held), the deadline that cancels and lets the completion report, and the three stop routes `ReadinessDial_test` has |
| `src/core/net/windows/IocpOperation.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/IocpStatus.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B7b: `completionError` is upstream's `Detail::WsaErrorOf` -- the closed-socket judgement, the `WSAGetOverlappedResult` conversion and the warning-severity-is-success rule, each kept with its reason. `IocpOperation` replaces upstream's `IocpCompletion` (`Async/IocpReactor.hpp`), and not by renaming: upstream's carried a dispatch pointer the reactor CALLED, which resumed the waiter from inside the dequeue; here the port only marks the operation completed and reports the `HandleKind::Completion` park naming it, so the loop resumes in turn step 2 (Rule 1). The dequeue hook that remains only lets go. `CompletionWait` is new, from upstream's `ConnectOp`/`ConnectPark` shape |
| `src/core/net/windows/IocpSocket.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/IocpSocket.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B7b, rewritten onto `ISocket`'s frame-free awaitables. Kept, each cited where it lives: the operation holds itself and never the socket (fastcached#465, `Op::inFlight`/`Op::self` collapsed into one self share per node), a node the kernel still owns is stood down rather than re-armed (#884), `cancelRead` settles a real read and retires a probe inline, the zero-byte receive plus `MSG_PEEK` (`:327-334`, `:555-561`), `SO_UPDATE_ACCEPT_CONTEXT` after `AcceptEx` (#1556) and `GetAcceptExSockaddrs` for the peer. New: a stop, a receive deadline or a dial deadline cancels the operation and lets the completion report; `close()` resolves a parked operation inline and a listener's `close()` resolves a parked accept; a non-overlapped attempt before each operation, so bytes already there cost no operation and no turn; `setReceiveDeadline`; `shutdownWrite` as an awaitable with its precondition asserted; `SO_EXCLUSIVEADDRUSE` on the listener |
| `src/core/net/windows/IocpSocket.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/IocpSocket.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B7b. `IocpAttachment` becomes `IocpAssociation`; `IsAttached()` goes, because a socket the port would not take answers every operation with the refusal instead (a constructed object is usable); `IocpListener::Bind`/`Adopt` become `bind`/`adopt` answering `std::expected`, and `adopt` adopts rather than refusing, because core-cpp's `adoptListener` is a public verb on every platform |
| `src/core/net/windows/IocpSocket_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/IocpSocket_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B7b. Upstream's 13 cases, rewritten onto an `EventLoop` and parking real flows, because an operation here is issued when AWAITED rather than in the verb: the round trip, the listener that releases its socket without `close`, the AcceptEx/WSARecv/WSASend in flight at teardown (#465), both `cancelRead` halves and the two #884 races, the abort and the reset each converted rather than tabled. The `Adopt` refusal is dropped (core-cpp's adopt adopts; `windows/AdoptListener_test.cpp` covers it). Added: the default backend and the factories following the loop, `close()` resolving inline, a flow stop that keeps received bytes, the receive-deadline contract, and a completion resuming in the loop's turn rather than inside the backend's walk, the abandoned read that never reaches the caller's buffer, an accept destroyed or stopped while parked leaving the next client to the next accept, the overlapped write's bytes checked across the 256KiB copy bound, and (Release only) a write armed over a parked one keeping it reachable |
| `src/core/net/windows/IocpCanary.cpp` | origin: core-cpp | - | - | Task B7a: the two refusals that are assertions and so cannot be provoked from inside a Catch case -- a second thread dequeuing the port (G1) and a handle associated with one port twice (G4). A program per refusal, each judged by its own stderr marker, in the shape `HostDrivenCanary.cpp` established. Upstream asserted neither: fastcached's `IsOnWorkerThread` existed but nothing violated it in a test |
| `src/core/net/windows/NetworkEvents.cpp` | origin: core-cpp | - | - | the one place this module takes a Winsock readiness indication off an event; extracted from `WindowsSocket::latchNetworkEvents` so `WindowsListener::accept` stops reaching for `WSAResetEvent`, which loses a recorded indication (Task A12, gate pass 4, finding 2) |
| `src/core/net/windows/NetworkEvents.hpp` | origin: core-cpp | - | - | declares `consumeNetworkEvents` and states why `WSAResetEvent` is never a substitute |
| `src/core/net/windows/NetworkEvents_test.cpp` | origin: core-cpp | - | - | drives a listening socket into the state the accept race ends in, and pins both arms: the consume reports the pending connection, the bare reset leaves the event dark for good |
| `src/core/net/windows/SocketsWin32.cpp` | contour-terminal/contour | `src/net/windows/SocketsWin32.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; `ensureWinsockInitialized()` is `core::platform`'s; the `addrinfo` walk is a `while` that steps to the next candidate first; no file-wide `#ifdef _WIN32` guards, around its body or its `<winsock2.h>` block: `SOURCES_WINDOWS` selects the file (Ruling R42) Task B8: as `posix/SocketsPosix.cpp` -- `connect` and its `discardEvent` helper move to the portable `Sockets.cpp` over `makeConnector`, and `listen(loop, ListenOptions)` plus `adoptListener` arrive Task B7b: `listen` and `adoptListener` build an `IocpListener` when the loop lends a completion port. |
| `src/core/net/windows/WaitCompletionPacket.cpp` | origin: core-cpp | - | - | Task B7a: `NtCreateWaitCompletionPacket` and friends behind a `GetProcAddress` probe, never a link against `ntdll` -- core-cpp is a library inside other people's builds and may not add an undocumented import to their executables. The kernel's own bridge from a waitable HANDLE to a completion port, taken when the probe answers and an ordinary fallback to the thread-pool wait when it does not |
| `src/core/net/windows/WaitCompletionPacket.hpp` | origin: core-cpp | - | - | Task B7a: the reusable packet object and the probe, with `cancel()` documented as an OWNERSHIP answer rather than a status -- `true` means no packet will be delivered and none is sitting in the port, so the caller still holds the arm's share; read the other way round it is a use-after-free on one side and a leak on the other |
| `src/core/net/windows/WfmoBackend.cpp` | contour-terminal/contour | `src/net/PollEventSource.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | its Windows half (Ruling R40). Task B3 rewrote it behind `IoBackend`: readiness is DISPATCHED to the callbacks on the `ReadinessHandler` a caller registers, instead of being collected into a `WaitOutcome` of tokens for the caller to route; `classifyWait`, the chunked >64-handle sweep over `detail/WaitChunking.hpp` and the fair rotation are unchanged, and `collectSignalled` now reports `Readiness` per registration -- a handle that has become invalid as `Readiness::Failed`, which is this platform's POLLNVAL Task B7b: refuses `HandleKind::Completion` with `Unsupported`. |
| `src/core/net/windows/WfmoBackend.hpp` | contour-terminal/contour | `src/net/PollEventSource.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | Task B3: contour's Windows event source as a class of its own, `WfmoBackend` (Ruling R40 -- a file's directory says which platform it is for). Task B3 rewrote it behind `IoBackend`: readiness is DISPATCHED to the callbacks on the `ReadinessHandler` a caller registers, instead of being collected into a `WaitOutcome` of tokens for the caller to route. Windows' default for one release; Task B7 makes IOCP the default and keeps this as its fallback (core-cpp#6) |
| `src/core/net/windows/WfmoBackend_test.cpp` | origin: core-cpp | - | - | Task B3 fix round: the backend's USE of `detail/WaitChunking.hpp` past `MAXIMUM_WAIT_OBJECTS`, which a unit test of the arithmetic cannot reach. 80 registrations, one signalled at a time at each boundary index; coverage of a late chunk turns out to be defended twice (sweep breadth and rotation), so only removing both reddens it |
| `src/core/net/windows/WindowsListener.cpp` | contour-terminal/contour | `src/net/windows/WindowsListener.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; `PeerAddress.hpp` from `detail/`; the `addrinfo` walk is a `while` that steps to the next candidate first; no file-wide `#ifdef _WIN32` guards, around its body or its `<winsock2.h>` block: `SOURCES_WINDOWS` selects the file (Ruling R42); it keeps the `#ifndef IO_REPARSE_TAG_AF_UNIX` definition, for SDKs that lack it Task B8: `adopt`. An adopted listener owns no socket FILE, because this process did not create one and removing a path on close would delete somebody else's |
| `src/core/net/windows/WindowsListener.hpp` | contour-terminal/contour | `src/net/windows/WindowsListener.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; no file-wide `#ifdef _WIN32` guard: `SOURCES_WINDOWS` selects the file (Ruling R42) Task B8: `localPort` is `boundPort`, and `adopt` joins `bind`/`bindUnix` |
| `src/core/net/windows/WindowsLoopback.cpp` | contour-terminal/contour | `src/net/platform/WindowsLoopback.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; moved from `platform/` to `windows/`, since a `core::net::platform` namespace would hide `core::platform`; no file-wide `#ifdef _WIN32` guards, around its body or its `<winsock2.h>` block: `SOURCES_WINDOWS` selects the file (Ruling R42) |
| `src/core/net/windows/WindowsLoopback.hpp` | contour-terminal/contour | `src/net/platform/WindowsLoopback.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces; moved from `platform/` to `windows/`, since a `core::net::platform` namespace would hide `core::platform`; no file-wide `#ifdef _WIN32` guards, around its body or its `<winsock2.h>` block: `SOURCES_WINDOWS` selects the file (Ruling R42) |
| `src/core/net/windows/WindowsSocket.cpp` | contour-terminal/contour | `src/net/windows/WindowsSocket.cpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; no file-wide `#ifdef _WIN32` guard: `SOURCES_WINDOWS` selects the file (Ruling R42) |
| `src/core/net/windows/WindowsSocket.hpp` | contour-terminal/contour | `src/net/windows/WindowsSocket.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | namespaces and includes; no file-wide `#ifdef _WIN32` guard: `SOURCES_WINDOWS` selects the file (Ruling R42) |
| `src/core/net/windows/InvalidSocket.hpp` | origin: core-cpp | - | - | Task B13: `INVALID_SOCKET` as a `SOCKET`, defined once for `windows/` -- the macro's inner `~0` is a signed `int`, which made 29 comparisons in Windows-only files read as signed/unsigned to the pinned clang-tidy. The completion-port files' three local copies were folded into it |
| `src/core/net/windows/WinsockError.hpp` | origin: core-cpp | - | - | Task B7b: the Windows half of "a platform socket error is classified in one table" -- the dial's `fromDialError` moved out of `windows/DialPrimitives.cpp`'s anonymous namespace so the completion socket, listener and dial classify through it too, and answering every `WSAE*` code through Task B9's `detail::classifySocketError`, so `WSAECONNABORTED` and `WSAENETRESET` are `SystemError` as on every other Windows transport (both were `ConnReset` before the rebase onto B9; POSIX likewise keeps `ConnReset` for `ECONNRESET` alone); its own rows are `ERROR_OPERATION_ABORTED` as `Cancelled` and a missing family or protocol as `Unsupported` |
| `src/core/net/ConnectFlow.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/ConnectFlow.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. `RunConnectFlow` is `detail::runConnectFlow`, and its `IReactor*`/`IClock*` pair becomes `EventLoop*`/`platform::IClock*` -- the clock stays SEPARATE from the loop even though `EventLoop::clock()` exists, because that is what lets the total-budget rule be a `ManualClock` unit test with no loop turning at all. `Detail::DialStep`'s `TimePoint` is `platform::SteadyTimePoint` |
| `src/core/net/ConnectFlow.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/ConnectFlow.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. camelBack; every `NetError` is built through `makeNetError` rather than aggregate-initialised, which is how this tree spells one |
| `src/core/net/ConnectFlow_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/ConnectFlow_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. Over `testing::TestLoop` and `platform::ManualClock` rather than upstream's `TestReactor`, with a scripted resolver that COSTS clock -- so the total-budget rule and the per-candidate share are asserted as arithmetic rather than as a sleep. New here: a dial with no budget is handed `SteadyTimePoint::max()` and not a default-constructed one, which is the already-expired shape that turns an opt-out into an instant failure |
| `src/core/net/Connector.cpp` | origin: core-cpp | - | - | Task B8: ONE connector, where fastcached shipped an `EpollConnector` and a `KqueueConnector` whose bodies were identical but for the reactor type they named. core-cpp has one `EventLoop` over an `IoBackend`, so there is nothing left for a per-platform connector to vary over, and `Net/PlatformConnector.hpp`'s `#if` ladder has nothing to select Task B7b: the connector asks the loop, per candidate, whether it lends a completion port, and dials through `detail::dialCompletion` when it does. |
| `src/core/net/Connector_test.cpp` | origin: core-cpp | - | - | Task B8: the case this task exists for -- an injected resolver records the thread it was called on, and a dial asserts that thread is not the loop's. It could not be written against contour's `connect()` at all, which called `getaddrinfo` inline with no seam to inject through. Also the R101 refusal through the public `connect()` on every backend, and `boundPort` reporting the port a bind to 0 actually got |
| `src/core/net/IAdmissionControl.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/IAdmissionControl.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. camelBack: `AllowAccept`, `OnConnectionStarted`, `OnConnectionEnded`, `InFlight` and `SetMax` become `allowAccept`, `onConnectionStarted`, `onConnectionEnded`, `inFlight` and `setMax`; the `_in` member is `_inFlight`, since `inFlight()` is what reads it. Reshaped in B8's fix round rather than renamed: `allowAccept` + `onConnectionStarted` are one atomic `tryAdmit()` returning an `AdmissionLease` whose destructor is what `onConnectionEnded` was, and `setMax` is gone -- a check-then-act across two calls let two loops exceed the cap, and `setMax` wrote a plain field other threads read |
| `src/core/net/IAdmissionControl_test.cpp` | origin: core-cpp | - | - | Task B8 fix round: upstream had no test. Two threads released together from a barrier race for the last slot, round after round, and exactly one may win each |
| `src/core/net/IAsyncAddressResolver.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/IAsyncAddressResolver.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. camelBack; `IReactor*` is `EventLoop*`, keeping the nullable convention (null means resolve inline and never suspend). `ResolveFailure` is `resolveFailure` and builds its error through `makeNetError` |
| `src/core/net/IConnector.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/IConnector.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. `Connect` is `connect`; `DialOptions` is declared here, which is where upstream keeps it too -- `Net/DialOptions.*` does not exist at the pin. `makeConnector(EventLoop&, IAsyncAddressResolver&)` is new and replaces `Net/PlatformConnector.hpp` |
| `src/core/net/KeepAlive.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/KeepAlive.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. The namespace, and the upstream issue links restated as the facts they carried rather than as URLs into another project's tracker |
| `src/core/net/ReadinessDial.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/ReactorDial.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. **De-templatised.** Upstream's `DialReadiness<Traits>` was templated on a `{Reactor, Handler, Socket}` triple PLUS a `Traits::Settle` callback -- four facts, not the three its own comment names -- because `EpollReactor` and `KqueueReactor` had no common base. `EventLoop` is that base, so what is left is one free function, and the platform difference moves to `detail/DialPrimitives.hpp`. The file-wide `#if defined(__linux__) || defined(__APPLE__)` is gone: the dial is portable, Windows included |
| `src/core/net/ReadinessDial.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/ReactorDial.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. `ReadinessDialOp` is `DialOp` and keeps the property that made it right -- it lives in the dialling coroutine's own frame -- and `SettleDial`/`DialPark` keep their shape over `EventLoop::registerPark`. New: stop-awareness, which upstream's dial has none of. A `StopCallback` routes a stop from any thread through `EventLoop::requestCancel`, and a cancelled dial UNWINDS rather than reporting a value, matching `ResultAwaitable::await_resume`. `Traits::Settle`'s three hooks collapse to one `Interest::Write` registration, because `selectReadinessCallback` gives a watched direction priority on every backend |
| `src/core/net/ReadinessDial_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/EpollConnector_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. ONE suite over `testing::BackendMatrix`, where upstream had an epoll-only file and no kqueue counterpart at all -- which is how the macOS divergence Ruling R101 exists to prevent gets written back in. The refusal case is R101 in its natural habitat. New here: the backlog-saturation arrangement, which SKIPs rather than passing when a stack will not leave a dial outstanding, and a descriptor-leak check that reads the lowest free fd on POSIX |
| `src/core/net/SocketAddress.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/SocketAddress.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. camelBack. The BIND half is deliberately NOT imported -- `BindAndListen`, `BoundListener`, `BoundPortOf`, `PeerAddressOf`, `ReusePort`, `ExclusiveBindOption` -- because core-cpp's listeners already own their bind sequence, and a second one would be a second answer to who owns an address. `FormatPeerAddress` is `formatPeerAddress` and delegates to the module's existing `detail::formatPeer` |
| `src/core/net/SocketBuffers.hpp` | origin: core-cpp | - | - | v0.1.1: `SocketBufferSizes`, the kernel buffer sizes `ListenOptions` and `DialOptions` ask for; fastcached sized both to 1 MiB in its own socket setup |
| `src/core/net/detail/StreamSocketOptions.hpp` | origin: core-cpp | - | - | v0.1.1: the one helper every dial and every accept calls; before it only the dial set `TCP_NODELAY` |
| `src/core/net/posix/StreamSocketOptions.cpp` | origin: core-cpp | - | - | v0.1.1; `armKeepAlive` and `wholeSeconds` moved here unchanged from `posix/DialPrimitives.cpp` |
| `src/core/net/windows/StreamSocketOptions.cpp` | origin: core-cpp | - | - | v0.1.1; `armKeepAlive` moved here unchanged from `windows/DialPrimitives.cpp` |
| `src/core/net/AdoptSocket_test.cpp` | origin: core-cpp | - | - | v0.1.1: `adoptSocket` over a connection accepted with plain socket calls, on every backend |
| `src/core/net/StreamSocketOptions_test.cpp` | origin: core-cpp | - | - | v0.1.1: reads `TCP_NODELAY` and the buffer sizes back from the kernel, for accepted and dialled sockets on every backend |
| `src/core/net/SocketAddress.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/SocketAddress.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. Only the four functions the header declares; the `addrinfo` walk is a `while` that steps to the next candidate first, as the listeners' already are. `GaiMessage` is a file-local `resolverMessage` |
| `src/core/net/SocketAddress_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/SocketAddress_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. The cases for the half that was imported. Every resolver case uses a LITERAL, so none depends on a working name server, and the one case that needs a failed lookup SKIPs where a wildcard-answering provider resolves an RFC 2606 `.invalid` name -- the network lying, not this code failing |
| `src/core/net/SocketDeadline.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/SocketDeadline.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. camelBack; `IReactor*` is `EventLoop*` and `Clock().Now()` is `clock().now()`. A target holding a null socket arms nothing as well -- upstream checked the target pointer but not the socket in it, and the same caller reaches both |
| `src/core/net/SocketDeadline_test.cpp` | origin: core-cpp | - | - | Task B8 fix round: upstream had none. A non-positive ceiling arms nothing, and an expiry records `expired` BEFORE it closes -- observed by a socket that reads the flag at the instant of its own close, since reading it afterwards cannot tell the two orders apart |
| `src/core/net/Sockets.cpp` | origin: core-cpp | - | - | Task B8: contour's `connect()` re-implemented OVER `makeConnector` rather than beside it, which is what moves `getaddrinfo` off the loop's thread. Portable, so the two platform `Sockets*.cpp` files no longer carry a `connect` each. The overload taking an `IAsyncAddressResolver*` and `DialOptions` is new |
| `src/core/net/ThreadedAddressResolver.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/ThreadedAddressResolver.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. camelBack; `IReactor*` is `EventLoop*`. `defaultAsyncResolver()` is new: the free `connect()` has nowhere to take a resolver from, so the process default is given a NAME -- the shape `defaultAddressResolver` and `platform::defaultSteadyClock` already use -- rather than being a private static nothing can reach past |
| `src/core/net/ThreadedAddressResolver.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/ThreadedAddressResolver.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. camelBack; `Submit` is `EventLoop::submit` and `Detail::Parked` is `async::detail::Parked`. `std::vector<std::jthread>` is `std::vector<std::thread>` joined explicitly, for the reason `core/async/ThreadPoolExecutor.hpp` records: `__cpp_lib_jthread` is exactly what `StopToken.hpp` carries a fallback for |
| `src/core/net/ThreadedAddressResolver_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/ThreadedAddressResolver_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8. A scripted blocking inner that RECORDS the thread each lookup ran on, so a name never resolving on the loop's thread is an assertion rather than a paragraph; a holding latch makes the full-queue and stopped-while-queued states arranged rather than raced. No name server is involved in any case here |
| `src/core/net/IDatagramSocket.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/IDatagramSocket.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. camelBack; `Send`/`Receive`/`Close`/`BoundAddress` as `send`/`receive`/`close`/`boundAddress` |
| `src/core/net/UdpSocket.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/UdpSocket.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. `OpenUdpSocket` is `openUdpSocket` and returns `std::expected<..., NetError>` where upstream returned a null pointer; `MaxIpv4DatagramPayload` (65507) and `MaxIpv6DatagramPayload` (65527), chosen by the bound family, replace the private 8 KiB bound, and a datagram longer than the buffer is `DatagramWait::MessageTooLarge` rather than delivered cut short |
| `src/core/net/posix/UdpSocket.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/UdpSocket.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9: the POSIX half of upstream's `#if defined(_WIN32)` split. Close-on-exec (`makeDatagramSocket`); errors through `detail/SocketErrors.hpp`, so `EMSGSIZE` is `MessageTooLarge`; one receive buffer per socket |
| `src/core/net/windows/UdpSocket.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/UdpSocket.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9: the Windows half. `SO_EXCLUSIVEADDRUSE` for an exclusive socket fails the candidate rather than being ignored, as upstream |
| `src/core/net/detail/DatagramAddressing.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/UdpSocket.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9: `DatagramAddressOf` and the `getaddrinfo` call both halves share; an owning `AddressList` replaces a `freeaddrinfo` at each exit; the port through `SocketAddress.hpp`'s one `portOfSockaddr` |
| `src/core/net/detail/DatagramReceiveBuffer.hpp` | origin: core-cpp | - | - | Task B9 fix round: the receive buffer by family, and the short-buffer seam the truncation test opens a socket through |
| `src/core/net/UdpSocket_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/InMemoryDatagram_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9: upstream's real-UDP cases, moved beside the socket they test. New: `MessageTooLarge`, a payload past the old 8 KiB bound, and a close stopping a receive loop |
| `src/core/net/SharedPortDatagram.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/SharedPortDatagram.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. camelBack; `AnswerFromOwnAddress` returns `std::expected` (`BadHandle` for a missing half), and `openSharedPortUdpSocket` the first failed bind's error |
| `src/core/net/SharedPortDatagram.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/SharedPortDatagram.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. camelBack |
| `src/core/net/SharedPortDatagram_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/SharedPortDatagram_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. camelBack; the two `[smoke]` cases are tagged `[udp]` |
| `src/core/net/testing/InMemoryDatagram.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/InMemoryDatagram.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. In `core::net::testing`; camelBack |
| `src/core/net/testing/InMemoryDatagram.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/InMemoryDatagram.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. camelBack |
| `src/core/net/testing/InMemoryDatagram_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/InMemoryDatagram_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9: the bus cases; the real-UDP ones moved to `UdpSocket_test.cpp` |
| `src/core/net/testing/DatagramPayload.hpp` | LASTRADA-Software/fastcached | `src/tests/DatagramPayload.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. In `core::net::testing`, camelBack |
| `src/core/net/testing/InMemorySocket.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/InMemoryTransport.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. Renamed, because `testing/InMemoryTransport.hpp` here is contour's REAL socket pair. Over `ResultAwaitable`'s arm and retire hooks; `waitReadable` parks, a bounded write waits instead of failing `WouldBlock`, the slots are claimed through `core::net::contract`, and the EOF latch is kept. `InMemoryListener::accept` is a coroutine over a parked awaitable |
| `src/core/net/testing/InMemorySocket.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/InMemoryTransport.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. The pipe no longer wakes anyone from `push` or `pull`: the socket wakes its peer LAST, after recording what it needs, because the peer may own and destroy it. The destructor abandons a parked operation rather than completing it |
| `src/core/net/testing/InMemorySocket_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/InMemoryTransport_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. camelBack. New: a parked `waitReadable`, `cancelRead`, the EOF latch, and a bounded write that waits, fails with the reset, and is retired by `close` |
| `src/core/net/SocketClosedStates_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/SocketClosedStates_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. The parity property kept: one table over the fake and over a real pair. The real pair is two loop sockets run a step at a time, rather than a reactor socket against a blocking one on a thread; `EnumTable` is replaced by arrays checked against their enums at compile time. New: the zero-deadline case and its control, which since Task B7b runs on Windows too: the platform loop's socket there is `IocpSocket`, which has a receive deadline |
| `src/core/net/testing/SocketDecorator.hpp` | LASTRADA-Software/fastcached | `src/tests/SocketDecorator.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9: `SocketDecorator` alone, forwarding every virtual `ISocket` now has, `readWithFd` and `setReceiveDeadline` included |
| `src/core/net/testing/ParkingReadableSocket.hpp` | LASTRADA-Software/fastcached | `src/tests/SocketDecorator.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9: `ParkingReadableSocket`, `ParkingWritableSocket`, and `CompleteCancelled` as `completeCancelled`; `FailingReadSocket` not imported. The destructors abandon rather than complete |
| `src/core/net/testing/ParkingReadableSocket_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/SocketDecorator_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9: the fake-contract case. New: the counters, and `SocketDecorator` forwarding a zero deadline and the defaulted verbs |
| `src/core/net/BlockingSocket.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/BlockingSocket.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9: `BlockingSocket` alone; `BlockingListener` and the `Detail` helper layer are not imported. `waitReadable` BLOCKS rather than inheriting the answer-`1` default, a non-positive `setReceiveDeadline` removes the bound, and `setSendDeadline` is new |
| `src/core/net/BlockingSocket.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/BlockingSocket.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9: the socket over `detail/BlockingPrimitives.hpp`, with no platform branch of its own |
| `src/core/net/BlockingSocket_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/BlockingSocket_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9: the hung-up-peer, SIGPIPE-disposition and round-trip cases, against a loop listener; the `BlockingListener` and `AcceptRaw` cases are not ported. New: `waitReadable`, the half-close, the deadline and its zero, and a closed socket |
| `src/core/net/detail/BlockingPrimitives.hpp` | origin: core-cpp | - | - | Task B9: the OS calls a blocking stream socket and a blocking dial need, one declaration and one implementation per platform |
| `src/core/net/posix/BlockingPrimitives.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/BlockingSocket.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9: the POSIX half of upstream's send, receive and timeout helpers, and `BlockingConnector.cpp`'s `poll` wait; `SO_NOSIGPIPE` or `MSG_NOSIGNAL`, per socket |
| `src/core/net/windows/BlockingPrimitives.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/BlockingSocket.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9: the Winsock half. The dial waits on the dial's own `WSAEVENT`, and clears the event selection before restoring blocking mode |
| `src/core/net/detail/SocketErrors.hpp` | origin: core-cpp | - | - | Task B9: one socket-error table per platform, declared once |
| `src/core/net/posix/SocketErrors.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/BlockingSocket.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9: `TranslateErrorImpl`'s POSIX table, plus `EMSGSIZE` |
| `src/core/net/posix/SocketErrors_test.cpp` | origin: core-cpp | - | - | Task B13: every row of the one socket-error table, asserted -- every transport and the dial read their errors through it, and nothing else in the suite provokes most of these codes |
| `src/core/net/windows/SocketErrors.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/BlockingSocket.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9: `TranslateErrorImpl`'s Winsock table, plus `WSAEMSGSIZE` |
| `src/core/net/windows/SocketErrors_test.cpp` | origin: core-cpp | - | - | Task B13: every row of the one socket-error table, asserted -- every transport and the dial read their errors through it, and nothing else in the suite provokes most of these codes |
| `src/core/net/windows/WindowsSocket_test.cpp` | origin: core-cpp | - | - | Task B13: a read parked on a destroyed `WindowsSocket` unwinds without writing to it, observed on poisoned storage the case owns rather than on a freed block only a sanitizer could read |
| `src/core/net/BlockingConnector.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/BlockingConnector.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9, ruled from B8 because it constructs a `BlockingSocket`. camelBack |
| `src/core/net/BlockingConnector.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/BlockingConnector.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. Over the shared `detail::runConnectFlow` and B8's dial primitives, rather than a socket/connect/`SO_ERROR` sequence of its own |
| `src/core/net/BlockingConnector_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/BlockingConnector_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. Against loop listeners, since `BlockingListener` is not imported |
| `src/core/net/TcpClient.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/TcpClient.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. `ConnectTcp`, `SendAll` and `RecvExactly` as `connectTcp`, `sendAll` and `receiveExactly` |
| `src/core/net/TcpClient.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/TcpClient.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. camelBack |
| `src/core/net/TcpClient_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/TcpClient_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. The accepted end is a loop socket. New: the silent-peer case asserts a floor as well as a ceiling |
| `src/core/net/HealthProbe.hpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/HealthProbe.hpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. `HttpHealthProbe` is `httpHealthProbe`, no longer `noexcept`, with a timeout parameter; `probeHttpStatus` is new and returns the status |
| `src/core/net/HealthProbe.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/HealthProbe.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. Rewritten over `BlockingConnector` and `BlockingSocket` rather than raw socket calls of its own; the three status digits must be followed by a space or the line end |
| `src/core/net/HealthProbe_test.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/HealthProbe_test.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B9. Rewritten against `core::net::serve` on a loop thread; assertions on the returned status replace upstream's reached and answered flags |
| `src/core/net/testing/InMemorySocketCanary.cpp` | origin: core-cpp | - | - | Task B9: the fake refusing a second read and a second write over a parked one, judged on a marker as `SocketContractCanary.cpp` is |
| `src/core/net/detail/DialPrimitives.hpp` | origin: core-cpp | - | - | Task B8: the handful of OS calls a dial needs, declared once so `ReadinessDial.cpp` is written once. `DialHandles` carries a socket AND a readiness handle because they are the same value on POSIX and different on Windows, where readiness for a socket arrives through an associated `WSAEVENT` Task B7b: `dialCompletion` is declared here, implemented in `windows/IocpDial.cpp` and answered `Unsupported` in `posix/DialPrimitives.cpp`. |
| `src/core/net/posix/DialPrimitives.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/SocketAddress.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8: the POSIX half. `ArmKeepAlive`'s body is upstream's, intervals-before-flag order included; the socket/connect/SO_ERROR sequence around it is lifted from `Net/ReactorDial.hpp`. `fromDialErrno` is new, and is what makes `ConnRefused` and `HostUnreach` reach a caller instead of `SystemError` Task B7b: `dialCompletion` answers `Unsupported`, because no POSIX loop lends a completion port. |
| `src/core/net/windows/DialPrimitives.cpp` | LASTRADA-Software/fastcached | `src/FastCache/Net/SocketAddress.cpp` | `0708dd54dc7ee72622c8c0783c2bd4a06f0e9b21` | Task B8: the Windows half, and deliberately NOT `Net/IocpDial.hpp` -- that one issues `ConnectEx` against an `IocpSocket` this tree does not have yet (Task B7b). This dials through `WSAEventSelect` readiness, which is what `WindowsSocket` already is, and closes the dial's own event before handing the socket over because Winsock allows exactly one event object per socket. `ArmKeepAlive`'s `SIO_KEEPALIVE_VALS` body is upstream's Task B7b: `fromDialError` becomes `detail::fromWinsockError` (`windows/WinsockError.hpp`), shared with the completion socket, listener and dial. |
| `src/core/net/posix/AdoptListener_test.cpp` | origin: core-cpp | - | - | Task B8: `adoptListener` over a listening descriptor made with the platform's OWN API rather than with `core::net::listen` -- which is the only arrangement that tests what the function is for, a handle this library did not make. POSIX-only as an ARRANGEMENT, not as a behaviour; `windows/AdoptListener_test.cpp` asserts the same three things in the Winsock spelling |
| `src/core/net/windows/AdoptListener_test.cpp` | origin: core-cpp | - | - | Task B8: the Winsock half of `posix/AdoptListener_test.cpp`, and the only coverage `WindowsListener::adopt` has |
| `src/core/platform/CMakeLists.txt` | origin: core-cpp | - | - | - |
| `src/core/platform/Clock.hpp` | contour-terminal/endo | `src/platform/Clock.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | merged with contour `src/net/platform/Clock.hpp` (`6777ff05014f8ff163b071e8b0e942830119db80`) and fastcached `src/FastCache/Core/Clock.hpp` (`b461e8b6d367ed22e4bf2935717fa59360a64b7d`): fastcached's virtual no-op `refresh()`, `CachedClock`, `IWallClock`, `SystemWallClock`, `ManualWallClock`, `WallClockRef` and `defaultSystemWallClock()`, camelBack; `TimePoint`/`Duration` are `SteadyTimePoint`/`SteadyDuration` |
| `src/core/platform/Clock_test.cpp` | contour-terminal/endo | `src/platform/Clock_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | merged with fastcached `src/FastCache/Core/Clock_test.cpp` and `src/FastCache/Core/WallClockRef_test.cpp` (`b461e8b6d367ed22e4bf2935717fa59360a64b7d`), duplicates dropped; the concurrent `CachedClock` case uses `std::thread` and a bounded start gate instead of fastcached's `tests/BoundedWait.hpp`, and is compiled only where threads exist; the `WallClockRef` asserts over fastcached's own retainer types are dropped; the retainer and forwarder rows are test-local stand-ins for fastcached's retainer types |
| `src/core/platform/EnvironmentProvider.hpp` | contour-terminal/endo | `src/platform/EnvironmentProvider.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `userName()` returns the optional it read rather than re-wrapping its value; declares `nativeEnvironmentProvider()` (core-cpp) |
| `src/core/platform/EnvironmentProvider_test.cpp` | contour-terminal/endo | `src/platform/EnvironmentProvider_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | plus two core-cpp cases for the native provider (export, read-through); the native cases go through the factory; runs under Emscripten too; plus core-cpp cases for `homeDirectory()` and `configHome()` over a `TestEnvironmentProvider`; plus core-cpp cases for an empty and a long value, which the Windows provider answered wrong (A10) |
| `src/core/platform/FileInfoProvider.hpp` | contour-terminal/endo | `src/platform/FileInfoProvider.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `FileEntry`'s string members have `{}` initializers, so designated initializers may omit them; declares `nativeFileInfoProvider()` (core-cpp) |
| `src/core/platform/FileInfoProvider_test.cpp` | contour-terminal/endo | `src/platform/FileInfoProvider_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the fixture directory is a `ScopedTempDir`, not a fixed path; plus a core-cpp case for `nativeFileInfoProvider()`; the lstat(2) cases test `PosixFileInfoProvider` and run under Emscripten too, where a symlink's target reads resolved against its directory |
| `src/core/platform/FileSystem.hpp` | contour-terminal/endo | `src/platform/FileSystem.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `Generator` is `core::Generator`; `openWrite()` and `copyFile()` take `WriteMode`/`OverwritePolicy` rather than a bool, and `openReadWrite()` documents its one position (A10) |
| `src/core/platform/FileSystem_test.cpp` | contour-terminal/endo | `src/platform/FileSystem_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | reads a stream through a string stream, not `std::istreambuf_iterator`; the break-out case breaks conditionally (MSVC C4702); plus core-cpp cases for the write streams over both backends, a key the ANSI code page cannot spell, a symlink to a directory, `createDirectory()`'s two failures and the recase error (A10); plus core-cpp cases driving the two-hop recase over a scripted rename: the hops succeeding, the second hop failing, and the rollback failing after it (A10, ruling R53); plus core-cpp cases for the key round-trip and for a write, a remove and a rename under an open stream (A10 fix round 1); plus a put-back case comparing the model with the native backend only where the standard pins the answer down (A10 fix round 2, rulings R58/R59) |
| `src/core/platform/FileUri.cpp` | contour-terminal/endo | `src/platform/FileUri.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/platform/FileUri.hpp` | contour-terminal/endo | `src/platform/FileUri.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/platform/FileUri_test.cpp` | contour-terminal/endo | `src/platform/FileUri_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/platform/GlobMatch.cpp` | contour-terminal/endo | `src/platform/GlobMatch.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `namespace endo` renamed `core::platform`; the bracket expression is matched by a helper (readability-function-cognitive-complexity); the `[` arm is reached before the literal one, and only for a bracket expression a `]` closes (A10) |
| `src/core/platform/GlobMatch.hpp` | contour-terminal/endo | `src/platform/GlobMatch.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `namespace endo` renamed `core::platform` |
| `src/core/platform/GlobMatch_test.cpp` | origin: core-cpp | - | - | the upstream file has no test |
| `src/core/platform/MessageQueue.hpp` | contour-terminal/endo | `src/platform/MessageQueue.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `setWakeup()` and the signalling in `push()`/`shutdown()` run under the queue's mutex (A10) |
| `src/core/platform/MessageQueue_test.cpp` | contour-terminal/endo | `src/platform/MessageQueue_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `std::thread` rather than `std::jthread`; the producer loop is a range-for; plus a core-cpp case that registers a wakeup against a live producer (A10) |
| `src/core/platform/NativeFileSystem.cpp` | contour-terminal/endo | `src/platform/NativeFileSystem.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the `increment(ec)` loops are `while` loops; `strerror()` became `std::error_code::message()`; the recase temporary is `<name>.recase-<n>`; `isExecutableFile()` classifies through the followed status; `createDirectory()` names an existing directory; `rename()` reports the recase's own error and where a failed rollback left the entry; `createTempFile()` is wchar_t end to end on Windows (A10); `rename()` and the two-hop recase go through the injected primitive (A10, ruling R53) |
| `src/core/platform/NativeFileSystem.hpp` | contour-terminal/endo | `src/platform/NativeFileSystem.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `openWrite()` and `copyFile()` take `WriteMode`/`OverwritePolicy` (A10); the rename primitive is injected at construction (`RenameFunction`, `nativeRename()`), defaulting to `std::filesystem::rename` (A10, ruling R53) |
| `src/core/platform/PathUtils.cpp` | contour-terminal/endo | `src/platform/PathUtils.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `stripTrailingSeparator()` and `isCaseOnlyRename()` go through `normalizePath()`, never `generic_string()` or a path rebuilt from a narrow string (A10) |
| `src/core/platform/PathUtils.hpp` | contour-terminal/endo | `src/platform/PathUtils.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/platform/PathUtils_test.cpp` | contour-terminal/endo | `src/platform/WindowsPlatform_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the `PathUtils` cases (`normalizePath`, `resolveDevicePath`, `canonicalCasePath`, `stripTrailingSeparator`, `isCaseOnlyRename`), split out so the WebAssembly subset can run them; plus core-cpp cases for a path the native narrow encoding cannot spell (A10) |
| `src/core/platform/PlatformError.hpp` | contour-terminal/endo | `src/platform/PlatformError.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/platform/PlatformError_test.cpp` | contour-terminal/endo | `src/platform/PlatformError_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/platform/SignalHandler.cpp` | contour-terminal/endo | `src/platform/SignalHandler.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the state is file-scope, not static members; `sigprocmask()` became `pthread_sigmask()`; `restore()` deregisters the interrupt wakeup as well as the callback (A10) |
| `src/core/platform/SignalHandler.hpp` | contour-terminal/endo | `src/platform/SignalHandler.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the private static members (and their NOLINTs) moved into `SignalHandler.cpp` |
| `src/core/platform/SignalHandler_test.cpp` | origin: core-cpp | - | - | the upstream file has no test |
| `src/core/platform/StringUtils.hpp` | contour-terminal/endo | `src/platform/StringUtils.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/platform/StringUtils_test.cpp` | origin: core-cpp | - | - | the upstream file has no test |
| `src/core/platform/SystemInfo.cpp` | contour-terminal/endo | `src/platform/SystemInfo.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/platform/SystemInfo.hpp` | contour-terminal/endo | `src/platform/SystemInfo.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/platform/SystemInfo_test.cpp` | origin: core-cpp | - | - | the upstream file has no test |
| `src/core/platform/SystemPipe.cpp` | contour-terminal/endo | `src/platform/SystemPipe.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | merged with contour `src/net/platform/SystemPipe.cpp` (`6777ff05014f8ff163b071e8b0e942830119db80`): both POSIX ends non-blocking and close-on-exec, `MSG_NOSIGNAL`, and a write the full channel refused reports done; endo's API (`PlatformError`) kept; `read()` returns a `ChannelResult` (core-cpp); the Windows write socket is non-blocking and answers WSAEWOULDBLOCK as done; both directions clamp the byte count to INT_MAX; `makeLoopbackPair()` verifies the two ends are each other's peer and retries (A10) |
| `src/core/platform/SystemPipe.hpp` | contour-terminal/endo | `src/platform/SystemPipe.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | merged with contour `src/net/platform/SystemPipe.hpp` (`6777ff05014f8ff163b071e8b0e942830119db80`): documents the non-blocking contract; `ChannelResult`, which `read()` returns (core-cpp) |
| `src/core/platform/SystemPipe_test.cpp` | contour-terminal/endo | `src/platform/SystemPipe_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | plus three core-cpp cases for the non-blocking contract; the read cases rewritten for `ChannelResult`: bytes, empty, end of stream, failure, on every platform; the full-channel case runs on every platform, on a thread with a bounded wait, plus a Windows case for the pair's addresses (A10) |
| `src/core/platform/Types.hpp` | contour-terminal/endo | `src/platform/Types.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | includes no `<windows.h>`: on Windows `NativeHandle`/`ProcessId` are spelled `void*`/`unsigned long` and the Windows API calls are out of line in `windows/WindowsTypes.cpp`; the Windows fallback `STDIN_FILENO`/`SIG*` macros are dropped |
| `src/core/platform/Types_test.cpp` | contour-terminal/endo | `src/platform/WindowsPlatform_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the `platformRead` case, split out, and run under Emscripten too, where the drained pipe reads -1 with EAGAIN; plus core-cpp cases for `InvalidHandle`, the standard handles and the header not including `<windows.h>` |
| `src/core/platform/UserPaths.hpp` | contour-terminal/endo | `src/platform/UserPaths.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | reads through a `core::Environment`, a `core::LiveEnvironment` by default, rather than `std::getenv()` |
| `src/core/platform/UserPaths_test.cpp` | contour-terminal/endo | `src/platform/WindowsPlatform_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the `homeDirectory`/`configHome` cases, over a `core::testing::FakeEnvironment` instead of the process environment; the zero-argument call is checked to compile, not run |
| `src/core/platform/Wakeup.hpp` | contour-terminal/endo | `src/platform/Wakeup.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/platform/Wakeup_test.cpp` | contour-terminal/endo | `src/platform/Wakeup_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `std::thread`, joined before the assertions, rather than `std::jthread` |
| `src/core/platform/WinsockInit.cpp` | contour-terminal/endo | `src/platform/WinsockInit.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | identical to contour `src/net/platform/WinsockInit.cpp` (`6777ff05014f8ff163b071e8b0e942830119db80`) but for the namespace |
| `src/core/platform/WinsockInit.hpp` | contour-terminal/endo | `src/platform/WinsockInit.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | identical to contour `src/net/platform/WinsockInit.hpp` (`6777ff05014f8ff163b071e8b0e942830119db80`) but for the namespace |
| `src/core/platform/linux/LinuxWakeup.cpp` | contour-terminal/endo | `src/platform/linux/LinuxWakeup.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the eventfd is created in the member initializer |
| `src/core/platform/posix/PosixEnvironmentProvider.cpp` | contour-terminal/endo | `src/platform/posix/PosixEnvironmentProvider.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | reads through `core::LiveEnvironment` and writes through `core::setProcessEnvironmentVariable()` / `unsetProcessEnvironmentVariable()` instead of `getenv()`/`setenv()`/`unsetenv()`; defines `nativeEnvironmentProvider()` (core-cpp) |
| `src/core/platform/posix/PosixEnvironmentProvider.hpp` | contour-terminal/endo | `src/platform/posix/PosixEnvironmentProvider.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/platform/posix/PosixFileInfoProvider.cpp` | contour-terminal/endo | `src/platform/linux/LinuxFileInfoProvider.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `LinuxFileInfoProvider`, renamed `PosixFileInfoProvider` and moved to `posix/`: it uses nothing Linux-specific, and is the provider on Linux, macOS, the BSDs and Emscripten; defines `nativeFileInfoProvider()` (core-cpp) |
| `src/core/platform/posix/PosixFileInfoProvider.hpp` | contour-terminal/endo | `src/platform/linux/LinuxFileInfoProvider.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `LinuxFileInfoProvider`, renamed `PosixFileInfoProvider` and moved to `posix/` |
| `src/core/platform/posix/PosixWakeup.cpp` | contour-terminal/endo | `src/platform/posix/PosixWakeup.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/platform/testing/InMemoryFileSystem.cpp` | contour-terminal/endo | `src/platform/testing/InMemoryFileSystem.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `removeAll()` uses `std::erase_if`; the symlink hop loop is a range-for; the read-write stream carries one position, overwrites in place and re-establishes its get area after every write and seek (A10); keys convert back to paths through one `pathFromKey()` helper, never the narrow constructor; the write streams share the file's storage and the read-write one caches no pointer into it (A10 fix round 1); the read streams share the file's storage too, so a read sees later writes (A10 fix round 1, ruling R56); pbackfail() accepts unget() and putback(), including of a character the file does not hold (A10 fix round 2, ruling R58) |
| `src/core/platform/testing/InMemoryFileSystem.hpp` | contour-terminal/endo | `src/platform/testing/InMemoryFileSystem.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `openWrite()` and `copyFile()` take `WriteMode`/`OverwritePolicy` (A10); the file map holds shared content, so a stream outlives a remove and follows a rename (A10 fix round 1) |
| `src/core/platform/testing/MockFileInfoProvider.hpp` | contour-terminal/endo | `src/platform/testing/MockFileInfoProvider.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/platform/testing/TestEnvironmentProvider.hpp` | contour-terminal/endo | `src/platform/testing/TestEnvironmentProvider.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `namespace core::platform::testing`, matching its directory (A10) |
| `src/core/platform/windows/WindowsEnvironmentProvider.cpp` | contour-terminal/endo | `src/platform/windows/WindowsEnvironmentProvider.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | defines `nativeEnvironmentProvider()` (core-cpp); `get()`, `unset()` and `exportVariable()` delegate to `core::LiveEnvironment` and the process-environment writer, as the POSIX provider does (A10) |
| `src/core/platform/windows/WindowsEnvironmentProvider.hpp` | contour-terminal/endo | `src/platform/windows/WindowsEnvironmentProvider.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/platform/windows/WindowsFileInfoProvider.cpp` | contour-terminal/endo | `src/platform/windows/WindowsFileInfoProvider.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | defines `nativeFileInfoProvider()` (core-cpp) |
| `src/core/platform/windows/WindowsFileInfoProvider.hpp` | contour-terminal/endo | `src/platform/windows/WindowsFileInfoProvider.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/platform/windows/WindowsTypes.cpp` | contour-terminal/endo | `src/platform/Types.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the Windows definitions of `Types.hpp`, moved out of line so the header needs no `<windows.h>` |
| `src/core/platform/windows/WindowsWakeup.cpp` | contour-terminal/endo | `src/platform/windows/WindowsWakeup.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/testing/CMakeLists.txt` | origin: core-cpp | - | - | - |
| `src/core/testing/CatchMain.cpp` | origin: core-cpp | - | - | `core::testing_main`: Catch2's `main()`, the exit-code contract and the `LOG` filter |
| `src/core/testing/CatchMain_test.cpp` | origin: core-cpp | - | - | covers the `LOG` filter |
| `src/core/testing/EnvHelper.hpp` | contour-terminal/endo | `src/testing/EnvHelper.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | POSIX writes go through `core::setProcessEnvironmentVariable()`, and `ScopedEnv` reads through `core::LiveEnvironment`, instead of `setenv()`/`unsetenv()`/`getenv()`; on Windows an empty value reaches the Win32 block too (A10) |
| `src/core/testing/EnvHelper_test.cpp` | origin: core-cpp | - | - | the upstream file has no test |
| `src/core/testing/Environment.hpp` | contour-terminal/contour | `src/crispy/testing/Environment.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | `crispy::testing::FakeEnvironment` renamed `core::testing::FakeEnvironment` |
| `src/core/testing/ExitCode.cpp` | origin: core-cpp | - | - | - |
| `src/core/testing/ExitCode.hpp` | origin: core-cpp | - | - | - |
| `src/core/testing/ExitCode_test.cpp` | origin: core-cpp | - | - | - |
| `src/core/testing/ScopedTempDir.hpp` | contour-terminal/endo | `src/testing/ScopedTempDir.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/testing/ScopedTempDir_test.cpp` | contour-terminal/endo | `src/testing/ScopedTempDir_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/testing/ScopedWorkingDirectory.hpp` | contour-terminal/endo | `src/testing/ScopedWorkingDirectory.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/testing/ScopedWorkingDirectory_test.cpp` | origin: core-cpp | - | - | the upstream file has no test |
| `src/core/testing/SuppressWindowsDialogs.cpp` | contour-terminal/contour | `src/crispy/SuppressWindowsDialogs.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | the out-of-line body added in core-cpp (`26de633`); same four-way merge as `SuppressWindowsDialogs.hpp` below |
| `src/core/testing/SuppressWindowsDialogs.hpp` | contour-terminal/contour | `src/crispy/SuppressWindowsDialogs.hpp` | `6777ff05014f8ff163b071e8b0e942830119db80` | merged with contour `src/coro/testing/SuppressWindowsDialogs.hpp` (same commit), endo `src/testing/SuppressWindowsDialogs.hpp` (`f774a210ce989e5947b8f61d715068b1dc96088c`) and fastcached `src/tests/WindowsErrorPopups.hpp` (`eb9c9c68da8fadfd43b0b36366919cb462689f48`) |
| `src/core/testing/SuppressWindowsDialogsAtStartup.cpp` | contour-terminal/endo | `src/testing/SuppressWindowsDialogsAtStartup.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | verbatim |
| `src/core/tui/.clang-tidy` | origin: core-cpp | - | - | the one directory that departs from the root `.clang-tidy`, and says why |
| `src/core/tui/Box.cpp` | contour-terminal/endo | `src/tui/Box.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Box.hpp` | contour-terminal/endo | `src/tui/Box.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Buffer.cpp` | contour-terminal/endo | `src/tui/Buffer.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the OSC 8 `id=` is the bare hash; endo prefixed it `endo-`, and no consumer's hyperlinks carry another project's name |
| `src/core/tui/Buffer.hpp` | contour-terminal/endo | `src/tui/Buffer.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/CMakeLists.txt` | origin: core-cpp | - | - | endo's `src/tui/CMakeLists.txt` is one `add_library`; this is the module-table form, and where the `core::tui_output` split is spelled |
| `src/core/tui/Canvas.cpp` | contour-terminal/endo | `src/tui/Canvas.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Canvas.hpp` | contour-terminal/endo | `src/tui/Canvas.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Cell.cpp` | contour-terminal/endo | `src/tui/Cell.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Cell.hpp` | contour-terminal/endo | `src/tui/Cell.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/CommandPalettePopup.cpp` | contour-terminal/endo | `src/tui/CommandPalettePopup.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: the completer types are qualified `completer::` |
| `src/core/tui/CommandPalettePopup.hpp` | contour-terminal/endo | `src/tui/CommandPalettePopup.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: the completer types are qualified `completer::` |
| `src/core/tui/CommandPalettePopup_test.cpp` | contour-terminal/endo | `src/tui/CommandPalettePopup_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/CommandRegistry.cpp` | contour-terminal/endo | `src/tui/CommandRegistry.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/CommandRegistry.hpp` | contour-terminal/endo | `src/tui/CommandRegistry.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/CommandRegistry_test.cpp` | contour-terminal/endo | `src/tui/CommandRegistry_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Completer_test.cpp` | contour-terminal/endo | `src/tui/Completer_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: `using namespace core::tui::completer` beside `core::tui` |
| `src/core/tui/CompletionPopup.cpp` | contour-terminal/endo | `src/tui/CompletionPopup.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: the completer types are qualified `completer::` |
| `src/core/tui/CompletionPopup.hpp` | contour-terminal/endo | `src/tui/CompletionPopup.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: the completer types are qualified `completer::` |
| `src/core/tui/CompletionPopup_test.cpp` | contour-terminal/endo | `src/tui/CompletionPopup_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: `using namespace core::tui::completer` beside `core::tui` |
| `src/core/tui/Component.cpp` | contour-terminal/endo | `src/tui/Component.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Component.hpp` | contour-terminal/endo | `src/tui/Component.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/CursorShape.hpp` | contour-terminal/endo | `src/tui/CursorShape.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Dialog.cpp` | contour-terminal/endo | `src/tui/Dialog.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | A7 fix round 1: every `Rect` is built as `{ .x = column, .y = row }`, which endo had transposed, and all three dialogs clamp their width on a terminal narrower than the border |
| `src/core/tui/Dialog.hpp` | contour-terminal/endo | `src/tui/Dialog.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Dialog_test.cpp` | origin: core-cpp | - | - | endo's `src/tui/Dialog.cpp` has no test; added by A7 fix round 1 for the frame's position and the narrow-terminal case |
| `src/core/tui/EditAction.hpp` | contour-terminal/endo | `src/tui/EditAction.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Element.hpp` | contour-terminal/endo | `src/tui/Element.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Error.hpp` | contour-terminal/endo | `src/tui/Error.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/FilesystemImageProvider.cpp` | contour-terminal/endo | `src/tui/ImageProvider.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | renamed with the class it defines; see `FilesystemImageProvider.hpp` |
| `src/core/tui/FilesystemImageProvider.hpp` | contour-terminal/endo | `src/tui/ImageProvider.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `FilesystemImageProvider` and `isRemoteImageSource()` split out of `ImageProvider.hpp`, because they are the part that needs a decoder and so exist only with `CORE_CPP_WITH_IMAGES` |
| `src/core/tui/FuzzyMatch_test.cpp` | contour-terminal/endo | `src/tui/FuzzyMatch_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: `using namespace core::tui::completer` beside `core::tui` |
| `src/core/tui/FuzzyPickerPopup.cpp` | contour-terminal/endo | `src/tui/FuzzyPickerPopup.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: the completer types are qualified `completer::` |
| `src/core/tui/FuzzyPickerPopup.hpp` | contour-terminal/endo | `src/tui/FuzzyPickerPopup.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: the completer types are qualified `completer::` |
| `src/core/tui/FuzzyPickerPopup_test.cpp` | contour-terminal/endo | `src/tui/FuzzyPickerPopup_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: `using namespace core::tui::completer` beside `core::tui` |
| `src/core/tui/GenericSyntaxHighlighter.cpp` | contour-terminal/endo | `src/tui/GenericSyntaxHighlighter.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | A7 fix round 1: `toLowerInto()` takes a `std::span<char>` and bounds the copy itself; endo's took a bare `char*` and three of its seven call sites overran a 64-byte stack buffer. A7 fix round 1b: the well-known-filename table has no `.endo-format` row |
| `src/core/tui/GenericSyntaxHighlighter.hpp` | contour-terminal/endo | `src/tui/GenericSyntaxHighlighter.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/GenericSyntaxHighlighter_test.cpp` | contour-terminal/endo | `src/tui/GenericSyntaxHighlighter_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | A7 fix round 1 adds the oversized-assembly-token case. A7 fix round 1b asserts `.endo-format` is `LanguageId::None` |
| `src/core/tui/GhostTextHelper.hpp` | contour-terminal/endo | `src/tui/GhostTextHelper.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/HoverState.cpp` | contour-terminal/endo | `src/tui/HoverState.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/HoverState.hpp` | contour-terminal/endo | `src/tui/HoverState.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/HyperlinkEmitter.cpp` | contour-terminal/endo | `src/tui/HyperlinkEmitter.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/HyperlinkEmitter.hpp` | contour-terminal/endo | `src/tui/HyperlinkEmitter.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/ImageLoader.cpp` | contour-terminal/endo | `src/tui/ImageLoader.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `readClipboardImage()`, which was an `#ifdef` with a platform in each arm, is in `posix/` and `windows/` (Ruling R41); the `#pragma clang diagnostic` around stb's headers is gone, because core-cpp includes stb as a SYSTEM directory |
| `src/core/tui/ImageLoader.hpp` | contour-terminal/endo | `src/tui/ImageLoader.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/ImageLoader_test.cpp` | contour-terminal/endo | `src/tui/ImageLoader_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `generateMinimalPng()`, a 39-line helper no case called, is deleted |
| `src/core/tui/ImageProvider.hpp` | contour-terminal/endo | `src/tui/ImageProvider.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the interface and its configuration only; `FilesystemImageProvider` moved to a header of its own |
| `src/core/tui/InputEvent.hpp` | contour-terminal/endo | `src/tui/InputEvent.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/InputField.cpp` | contour-terminal/endo | `src/tui/InputField.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | spells the constants `InputField.hpp` renames |
| `src/core/tui/InputField.hpp` | contour-terminal/endo | `src/tui/InputField.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | four private `constexpr` constants renamed to CamelCase: `DoubleClickTimeout`, `DoubleClickTolerance`, `MaxUndoHistory`, `MaxKillRing` |
| `src/core/tui/InputField_test.cpp` | contour-terminal/endo | `src/tui/InputField_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/KeyBindings.cpp` | contour-terminal/endo | `src/tui/KeyBindings.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/KeyBindings.hpp` | contour-terminal/endo | `src/tui/KeyBindings.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/KeyBindings_test.cpp` | contour-terminal/endo | `src/tui/KeyBindings_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/KeyCode.hpp` | contour-terminal/endo | `src/tui/KeyCode.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the upstream `NOLINT(readability-enum-initial-value)` on `enum class KeyCode` is gone -- `src/core/tui/.clang-tidy` disables the check for this directory instead; `isPrintable()`'s private-use test is rewritten by De Morgan |
| `src/core/tui/List.cpp` | contour-terminal/endo | `src/tui/List.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/List.hpp` | contour-terminal/endo | `src/tui/List.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/LogPanel.cpp` | contour-terminal/endo | `src/tui/LogPanel.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | spells the constants `LogPanel.hpp` renames |
| `src/core/tui/LogPanel.hpp` | contour-terminal/endo | `src/tui/LogPanel.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | private `constexpr` constants renamed to CamelCase: `MaxEntries`, `MaxVisibleExpanded` |
| `src/core/tui/MarkdownHtml.cpp` | contour-terminal/endo | `src/tui/MarkdownHtml.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/MarkdownHtml.hpp` | contour-terminal/endo | `src/tui/MarkdownHtml.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/MarkdownHtml_test.cpp` | contour-terminal/endo | `src/tui/MarkdownHtml_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/MarkdownImage_test.cpp` | contour-terminal/endo | `src/tui/MarkdownImage_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/MarkdownInline.hpp` | contour-terminal/endo | `src/tui/MarkdownInline.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/MarkdownRenderer.cpp` | contour-terminal/endo | `src/tui/MarkdownRenderer.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/MarkdownRenderer.hpp` | contour-terminal/endo | `src/tui/MarkdownRenderer.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/MarkdownRenderer_test.cpp` | contour-terminal/endo | `src/tui/MarkdownRenderer_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | a `firstOf()` helper replaces four `output.find(...)->n` dereferences, which would have been undefined had the op not been found |
| `src/core/tui/MarkdownTable.cpp` | contour-terminal/endo | `src/tui/MarkdownTable.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/MarkdownTable.hpp` | contour-terminal/endo | `src/tui/MarkdownTable.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/MarkdownTable_test.cpp` | contour-terminal/endo | `src/tui/MarkdownTable_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/MockTerminalOutput.cpp` | contour-terminal/endo | `src/tui/MockTerminalOutput.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `syncGuard()` returns `{}` where endo returns `SyncGuard()`; otherwise left exactly as endo has it, so no renderer test changes meaning |
| `src/core/tui/MockTerminalOutput.hpp` | contour-terminal/endo | `src/tui/MockTerminalOutput.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Modifier.hpp` | contour-terminal/endo | `src/tui/Modifier.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/PopupKeyDispatch.hpp` | contour-terminal/endo | `src/tui/PopupKeyDispatch.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/PopupKeyDispatch_test.cpp` | contour-terminal/endo | `src/tui/PopupKeyDispatch_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/QuestionComponent.cpp` | contour-terminal/endo | `src/tui/QuestionComponent.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | spells the constants `QuestionComponent.hpp` renames |
| `src/core/tui/QuestionComponent.hpp` | contour-terminal/endo | `src/tui/QuestionComponent.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | private `constexpr` constants renamed to CamelCase (`LeftBarWidth`, `BarPadding`, `HeaderHeight`), and `QuestionConfig`'s members are `{}`-initialized |
| `src/core/tui/QuestionComponent_test.cpp` | contour-terminal/endo | `src/tui/QuestionComponent_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Rect.hpp` | contour-terminal/endo | `src/tui/Rect.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Renderer_test.cpp` | contour-terminal/endo | `src/tui/Renderer_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Screen.cpp` | contour-terminal/endo | `src/tui/Screen.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `flushInline`'s C-style loop is a `while`, with the `++row` written out on each of its three exit paths |
| `src/core/tui/Screen.hpp` | contour-terminal/endo | `src/tui/Screen.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Screen_test.cpp` | contour-terminal/endo | `src/tui/Screen_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/ScrollableSelection.hpp` | contour-terminal/endo | `src/tui/ScrollableSelection.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/ScrollableSelection_test.cpp` | contour-terminal/endo | `src/tui/ScrollableSelection_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/SemanticBlockClient.cpp` | contour-terminal/endo | `src/tui/SemanticBlockClient.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/SemanticBlockClient.hpp` | contour-terminal/endo | `src/tui/SemanticBlockClient.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/SemanticBlockClient_test.cpp` | contour-terminal/endo | `src/tui/SemanticBlockClient_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/SgrBuilder.cpp` | contour-terminal/endo | `src/tui/SgrBuilder.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/SgrBuilder.hpp` | contour-terminal/endo | `src/tui/SgrBuilder.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Sixel.cpp` | contour-terminal/endo | `src/tui/Sixel.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Sixel.hpp` | contour-terminal/endo | `src/tui/Sixel.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Sixel_test.cpp` | contour-terminal/endo | `src/tui/Sixel_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/SmartCaseMatch_test.cpp` | contour-terminal/endo | `src/tui/SmartCaseMatch_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: `using namespace core::tui::completer` beside `core::tui` |
| `src/core/tui/Spinner.cpp` | contour-terminal/endo | `src/tui/Spinner.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | spells the constants `Spinner.hpp` renames |
| `src/core/tui/Spinner.hpp` | contour-terminal/endo | `src/tui/Spinner.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | private `constexpr` constants renamed to CamelCase: `FilledChar`, `EmptyChar`, `PartialChars` |
| `src/core/tui/StatusBar.cpp` | contour-terminal/endo | `src/tui/StatusBar.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/StatusBar.hpp` | contour-terminal/endo | `src/tui/StatusBar.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/StbImageImpl.cpp` | contour-terminal/endo | `src/tui/StbImageImpl.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the warning pragmas are gone: the module's CMakeLists.txt compiles this file with warnings off and `-fno-sanitize=undefined`, as per-source PRIVATE options |
| `src/core/tui/StyledText.cpp` | contour-terminal/endo | `src/tui/StyledText.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/StyledText.hpp` | contour-terminal/endo | `src/tui/StyledText.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/StyledText_test.cpp` | contour-terminal/endo | `src/tui/StyledText_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Terminal.cpp` | contour-terminal/endo | `src/tui/platform/TerminalShared.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | plus every `Terminal` member endo defined identically in `src/tui/platform/Terminal.cpp` and `src/tui/platform/TerminalWin32.cpp` (same commit) |
| `src/core/tui/Terminal.hpp` | contour-terminal/endo | `src/tui/Terminal.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/TerminalInput.cpp` | contour-terminal/endo | `src/tui/platform/TerminalInput.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the members that touch no OS state, which endo had in both platform files |
| `src/core/tui/TerminalInput.hpp` | contour-terminal/endo | `src/tui/TerminalInput.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the OS state is the opaque `NativeState` the platform sources define, so this header includes neither `<windows.h>` nor `<termios.h>` (Ruling R41) |
| `src/core/tui/TerminalOutput.cpp` | contour-terminal/endo | `src/tui/platform/TerminalOutput.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `SyncGuard` and `syncGuard()` are here rather than per platform, because the sequences now go through `writeToDestination()`; the members that only compose bytes, which endo duplicated in `src/tui/platform/TerminalOutputWin32.cpp` (same commit); `copyToClipboard()` encodes through `core::base64::encode()` rather than a third copy of a base64 encoder. A7 fix round 1: `~SyncGuard()` and its move-assignment flush before writing the end sequence |
| `src/core/tui/TerminalOutput.hpp` | contour-terminal/endo | `src/tui/TerminalOutput.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | post-import fix: `SyncGuard` holds the `TerminalOutput` it brackets instead of a native handle, so its `#if _WIN32` `NativeHandle` alias is gone (Ruling R41); `isTerminal()` added. A7 fix round 1 restores "and flushes" to the destructor's description |
| `src/core/tui/TerminalOutput_test.cpp` | origin: core-cpp | - | - | the upstream file has no test; written for the `SyncGuard` and `isTerminal()` fix. A7 fix round 1 replaces `a default-constructed SyncGuard writes nothing`, which asserted on an unrelated capture, with the two flush cases |
| `src/core/tui/TerminalProtocols.hpp` | contour-terminal/endo | `src/tui/TerminalProtocols.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/TerminalProtocols_test.cpp` | contour-terminal/endo | `src/tui/TerminalProtocols_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | A7 fix round 1: the sample OSC 8 id is `1f2e`, not `endo-1f2e` |
| `src/core/tui/TerminalQuery_test.cpp` | contour-terminal/endo | `src/tui/TerminalQuery_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/TestHelpers.hpp` | contour-terminal/endo | `src/tui/TestHelpers.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: the completer types are qualified `completer::` |
| `src/core/tui/Text.cpp` | contour-terminal/endo | `src/tui/Text.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Text.hpp` | contour-terminal/endo | `src/tui/Text.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/TextDecorator.hpp` | contour-terminal/endo | `src/tui/TextDecorator.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Theme.cpp` | contour-terminal/endo | `src/tui/Theme.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | A7 fix round 1b: the path-gradient colours are described as blue and teal, not as one application's signature |
| `src/core/tui/Theme.hpp` | contour-terminal/endo | `src/tui/Theme.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | A7 fix round 1b: `categoryColorFromIndex()`'s Doxygen names a host's own highlighter rather than `endo::TokenCategory` |
| `src/core/tui/TimerUtils.hpp` | contour-terminal/endo | `src/tui/TimerUtils.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Tooltip.cpp` | contour-terminal/endo | `src/tui/Tooltip.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Tooltip.hpp` | contour-terminal/endo | `src/tui/Tooltip.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/TreeTableView.cpp` | contour-terminal/endo | `src/tui/TreeTableView.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/TreeTableView.hpp` | contour-terminal/endo | `src/tui/TreeTableView.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/TreeTableView_test.cpp` | contour-terminal/endo | `src/tui/TreeTableView_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Unicode.cpp` | contour-terminal/endo | `src/tui/Unicode.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Unicode.hpp` | contour-terminal/endo | `src/tui/Unicode.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/Unicode_test.cpp` | contour-terminal/endo | `src/tui/Unicode_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/VtParser.cpp` | contour-terminal/endo | `src/tui/VtParser.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | a condition in `dispatchCsi` is rewritten by De Morgan; A7 fix round 1 bounds `_pasteBuf`, `_paramBuf` and `_dcsBuf`, which grew without limit from untrusted bytes |
| `src/core/tui/VtParser.hpp` | contour-terminal/endo | `src/tui/VtParser.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | A7 fix round 1 adds the public `MaxPasteLength`, `MaxCsiParamLength` and `MaxDcsLength` caps and the private `abandonIfOverlong()` |
| `src/core/tui/VtParser_test.cpp` | contour-terminal/endo | `src/tui/VtParser_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | A7 fix round 1 adds the three bounded-buffer cases |
| `src/core/tui/completer/Completer.cpp` | contour-terminal/endo | `src/tui/completer/Completer.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `addProvider()` sorts stably, so equal priorities keep registration order; core-cpp#30: the namespace is `core::tui::completer`, the one its directory names (endo's TUI is one flat `namespace tui`) |
| `src/core/tui/completer/Completer.hpp` | contour-terminal/endo | `src/tui/completer/Completer.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: the namespace is `core::tui::completer`, the one its directory names (endo's TUI is one flat `namespace tui`) |
| `src/core/tui/completer/CompletionItem.hpp` | contour-terminal/endo | `src/tui/completer/CompletionItem.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: the namespace is `core::tui::completer`, the one its directory names (endo's TUI is one flat `namespace tui`) |
| `src/core/tui/completer/CompletionProvider.hpp` | contour-terminal/endo | `src/tui/completer/CompletionProvider.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: the namespace is `core::tui::completer`, the one its directory names (endo's TUI is one flat `namespace tui`) |
| `src/core/tui/completer/FuzzyMatch.cpp` | contour-terminal/endo | `src/tui/completer/FuzzyMatch.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | A7 fix round 1b: the substring-first comment's worked example is a generic file name; core-cpp#30: the namespace is `core::tui::completer`, the one its directory names (endo's TUI is one flat `namespace tui`) |
| `src/core/tui/completer/FuzzyMatch.hpp` | contour-terminal/endo | `src/tui/completer/FuzzyMatch.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: the namespace is `core::tui::completer`, the one its directory names (endo's TUI is one flat `namespace tui`) |
| `src/core/tui/completer/SmartCaseMatch.cpp` | contour-terminal/endo | `src/tui/completer/SmartCaseMatch.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: the namespace is `core::tui::completer`, the one its directory names (endo's TUI is one flat `namespace tui`) |
| `src/core/tui/completer/SmartCaseMatch.hpp` | contour-terminal/endo | `src/tui/completer/SmartCaseMatch.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | core-cpp#30: the namespace is `core::tui::completer`, the one its directory names (endo's TUI is one flat `namespace tui`) |
| `src/core/tui/detail/XtVersion.hpp` | contour-terminal/endo | `src/tui/platform/TerminalOutput.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | `parseXTVersionName()` and the unscroll terminal list, which endo had verbatim in `src/tui/platform/TerminalOutputWin32.cpp` too; spelled `parseXtVersionName()` and `supportsUnscroll()` here |
| `src/core/tui/posix/ImageLoader.cpp` | contour-terminal/endo | `src/tui/ImageLoader.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the POSIX `readClipboardImage()` and its helper |
| `src/core/tui/posix/PosixIO.hpp` | contour-terminal/endo | `src/tui/platform/PosixIO.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | moved out of `tui/platform/`, which Ruling R40 does not keep |
| `src/core/tui/posix/Terminal.cpp` | contour-terminal/endo | `src/tui/platform/Terminal.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the destructor, `initialize()` and `shutdown()`: what SIGWINCH makes different. The NOLINTs on the file-scope signal state are gone with the `g` prefixes. A7 fix round 1: the SIGWINCH handler saves and restores `errno` and reads `activeInput` as a lock-free `std::atomic` |
| `src/core/tui/posix/TerminalInput.cpp` | contour-terminal/endo | `src/tui/platform/TerminalInput.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the POSIX half, over `TerminalInput::NativeState`, which this file defines: Ruling R41 keeps `<termios.h>` out of `TerminalInput.hpp`. A7 fix round 1: both ends of the resize self-pipe are `O_NONBLOCK`, so the signal handler's write cannot block |
| `src/core/tui/posix/TerminalOutput.cpp` | contour-terminal/endo | `src/tui/platform/TerminalOutput.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | only what goes through the OS: the write, the window size, the XTVERSION read and `isTerminal()` (core-cpp) |
| `src/core/tui/runtime/InputSource.hpp` | origin: core-cpp | - | - | Task B12. What is left of endo's `src/tui/runtime/EventSource.hpp` once the waiting is `core::net::EventLoop`'s: the handles to watch and the decode behind them, with no `wait()`, no fd registry and no readiness vocabulary. Nothing of that file's text survives, so this is new code rather than a rename of it |
| `src/core/tui/runtime/Modal.hpp` | contour-terminal/endo | `src/tui/runtime/Modal.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/runtime/ModalComponent.hpp` | contour-terminal/endo | `src/tui/runtime/ModalComponent.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | - |
| `src/core/tui/runtime/Modal_test.cpp` | contour-terminal/endo | `src/tui/runtime/Modal_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | Task B12: the three cases are endo's, restated over a `core::net::EventLoop` and a `ScriptedInputSource`; the cancellation case drives a real `core::platform::Wakeup` where upstream pushed a scripted interrupt |
| `src/core/tui/runtime/TerminalInputSource.hpp` | contour-terminal/endo | `src/tui/runtime/TerminalEventSource.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | Task B12: `TerminalEventSource` becomes an input adapter. What it keeps is the terminal-facing calls -- `takePending`, `readReadyInput`, `drainResize`, `parserTimeout`, `consumeProtocolReports`; what it loses is the multiplexed `wait()` and both of its platform bodies (`platform/TerminalEventSourcePosix.cpp` and `TerminalEventSourceWin32.cpp`), whose job the event loop already does on every platform. The agent and interrupt wakeups and the signal fd move to `TuiRuntime`, so this names none of them |
| `src/core/tui/runtime/TuiRuntime.cpp` | contour-terminal/endo | `src/tui/runtime/TuiRuntime.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | Task B12 rewrote it onto `core::net::EventLoop`. What survives from upstream is the input semantics -- the decoded-event buffer, the report filter, the waiter slots and the interrupt policy; what is gone is every scheduler part of it: `pumpOnce`, `drainReadyQueue`, `computeTimeoutMs`, the timer min-heap, `fireExpiredTimers`, `wakeAllWaiters` and `requeueForCancellation`. The multiplexed wait is four coroutines parked on `loop.waitReadable()`, one per handle |
| `src/core/tui/runtime/TuiRuntime.hpp` | LASTRADA-Software/fastcached | `vendor/endo/tui/runtime/TuiRuntime.hpp` | `5389e29a5eeca9c2319f43757bd7d6d0ac1c1a13` | endo `f774a210` plus fastcached `6483abd8`, which makes `DelayAwaiter::await_ready()` a constant and decides an elapsed deadline in `await_suspend()`: MSVC 19.44's ARM64 code generator loses the enclosing `try` of a `co_await` on an awaiter whose `await_ready()` reads the clock through a virtual `now()`. A7 fix round 1 drops `static` from `DelayAwaiter::await_ready()`, keeping the constant return. Task B12 rewrote the class onto `core::net::EventLoop`: `DelayAwaiter` and `WaitFdAwaiter` are gone -- `delay`, `sleepUntil`, `waitReadable` and `waitWritable` return the loop's own awaitables -- so the ARM64 workaround recorded above now lives in `core::net::DelayAwaiter`, which carries it. The four input awaiters are upstream's, plus the stop-callback each of them lacked (core-cpp#18) |
| `src/core/tui/runtime/TuiRuntime_test.cpp` | LASTRADA-Software/fastcached | `vendor/endo/tui/runtime/TuiRuntime_test.cpp` | `5389e29a5eeca9c2319f43757bd7d6d0ac1c1a13` | endo `f774a210` plus fastcached `6483abd8`, which makes `DelayAwaiter::await_ready()` a constant and decides an elapsed deadline in `await_suspend()`: MSVC 19.44's ARM64 code generator loses the enclosing `try` of a `co_await` on an awaiter whose `await_ready()` reads the clock through a virtual `now()`. Beyond that delta, the pipe case reads `got->bytesRead() == 1` where upstream reads `*got == 1` (`:531`), because core-cpp's `SystemPipe::read()` returns a `ChannelResult` rather than a count. A7 fix round 1 replaces the `static_assert` on `DelayAwaiter::await_ready()` with a case that calls it on an awaiter, since the function is no longer static. Task B12 rewrote the file for the composed runtime. The input, delay and teardown cases are upstream's scenarios restated; the `EventSource` registry cases are gone with the type, the `DelayAwaiter::await_ready` case belongs to `core::net` now, and the four deferred defects (core-cpp#16 to #19) gain a case each |
| `src/core/tui/runtime/testing/ScriptedInputSource.hpp` | origin: core-cpp | - | - | Task B12, replacing endo's `src/tui/runtime/testing/MockEventSource.hpp`. That double owned a `wait()` and an fd-token registry -- it impersonated a scheduler -- and neither has an equivalent here: readiness is scripted at `core::net::testing::ScriptedBackend` or taken from a real kernel through a `core::platform::SystemPipe`, and only the decode is scripted here. `pushEvents` is the one member that carries the same name and meaning |
| `src/core/tui/windows/ImageLoader.cpp` | contour-terminal/endo | `src/tui/ImageLoader.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the Windows `readClipboardImage()` |
| `src/core/tui/windows/Terminal.cpp` | contour-terminal/endo | `src/tui/platform/TerminalWin32.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | as `posix/Terminal.cpp`, for Windows; the file-wide `#if defined(_WIN32)` is gone, because SOURCES_WINDOWS selects the file |
| `src/core/tui/windows/TerminalInput.cpp` | contour-terminal/endo | `src/tui/platform/TerminalInputWin32.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | the Windows half, over `TerminalInput::NativeState`, which this file defines; the file-wide `#if defined(_WIN32)` is gone, because SOURCES_WINDOWS selects the file |
| `src/core/tui/windows/TerminalInput_test.cpp` | contour-terminal/endo | `src/tui/TerminalInputWin32_test.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | a Windows-only test source; the file-wide `#if defined(_WIN32)` is gone, and the named-mutex serialisation of its cases is kept |
| `src/core/tui/windows/TerminalOutput.cpp` | contour-terminal/endo | `src/tui/platform/TerminalOutputWin32.cpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | as `posix/TerminalOutput.cpp`, for Windows; the file-wide `#if defined(_WIN32)` is gone, because SOURCES_WINDOWS selects the file |
| `src/core/tui/windows/Win32Utf.hpp` | contour-terminal/endo | `src/tui/platform/Win32Utf.hpp` | `f774a210ce989e5947b8f61d715068b1dc96088c` | moved out of `tui/platform/`, which Ruling R40 does not keep |
