# cli

Command-line parsing and an application scaffold. Namespace `core::cli`, directory
`src/core/cli/`, target `core::cli`.

!!! note "Status"
    Not imported yet. Task A3 of the
    [implementation plan](https://github.com/contour-terminal/core-cpp/blob/master/docs/superpowers/plans/2026-09-18-core-cpp.md)
    imports contour's crispy `CLI` and `App` (namespace `crispy::cli` today) at `6777ff05`.

Depends on [base](base.md) and [log](log.md). Builds for WebAssembly.
