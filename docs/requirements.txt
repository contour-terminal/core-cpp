mkdocs==1.6.1
mkdocs-material==9.5.39
pymdown-extensions==10.11
# Pinned: mkdocs-material only requires pygments~=2.16, so an unpinned install
# picks up 2.20.0, whose formatter passes filename=None into html.escape() via
# pymdown-extensions 10.11 and aborts the build with an AttributeError. Every
# other dependency here is already pinned; this one was the gap.
pygments==2.19.1
