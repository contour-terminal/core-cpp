# SDD ledger — plan: docs/superpowers/plans/2026-09-18-core-cpp.md

Spec: docs/superpowers/specs/2026-09-18-core-cpp-design.md (reachable; binding).
Briefs: .superpowers/sdd/2026-09-18-core-cpp/brief.sh <ID>. The skill's task-brief only matches numeric IDs; this plan uses A1..C8.

Task 0: complete (commit 4abb140; done by the controller as bookkeeping: spec and plan persisted, memories saved)

## Pre-flight scan

### Cross-task rows (shared file or interface)
| Tasks | Producer → consumer | Finding |
|---|---|---|
| A1 ↔ A3 | A1 creates `src/core/testing/CatchMain.cpp`; A3 Step 4b adds the `LOG` filter via `core::log::configure` | consistent: A1 no longer references configureLogFilter |
| A1 ↔ A3..A7 | A1's hygiene scan refuses `NOLINT`; imported contour/endo code contains NOLINT (e.g. vtparser NOLINTBEGIN, coroutine hooks) | conflict → Ruling R3 |
| A1 ↔ A1 | hygiene prefix rule vs `portable/CompileCache.cmake` and `FetchTransferBound.cmake` unprefixed options | consistent: exempt by path (Part I §3) |
| A1 ↔ A1/A4 | hygiene "no diagnostic-muting pragma" vs `SuppressWindowsDialogsAtStartup.cpp` `#pragma init_seg(lib)` | consistent: allowlist row with reason |
| A2 ↔ A3..B13 | A2 creates build.yml jobs (emscripten, sanitizers, consumer-smoke) before the modules exist; later tasks extend them | consistent: jobs run the skeleton until modules land |
| A2 ↔ B13 | A2 creates stub module doc pages (mkdocs strict needs them); B13 fills coro/net/tui pages | consistent |
| A3 ↔ A4 ↔ A5 | A4 creates the `coro` module row (INTERFACE, only Generator.hpp) that `platform` depends on; A5 imports Task etc. into it | consistent |
| A4 ↔ A6 | A4 merges contour `net/platform/Clock` into `core::platform::Clock`; A6 deletes the net copies of Clock/SystemPipe/WinsockInit | consistent |
| A6 ↔ B2 | A6 puts contour NetError/IoResult in `core::net_types`; B2 merges the enum | consistent |
| A7 ↔ B12 | A7 keeps tui/runtime's own EventSource; B12 replaces it with EventLoop | consistent |
| A8 ↔ B5 | consumer-smoke (c) builds base+coro only until B5; B5 extends it | consistent |
| A5 ↔ B1 | `<stop_token>` usage requirement (emsdk 3.1.56 libc++) ↔ B1 ThreadPoolExecutor `#error` under single-threaded Emscripten | consistent |
| B3 ↔ B4 | B3 adds `isHostDriven`/`armWakeAt` to IoBackend; B4 calls `armWakeAt` after each turn | consistent |
| B6 ↔ B10 | B6 changes ISocket read/write to awaitables; B10 adapts the helpers | consistent (B10 expects failures until adapted) |
| B7 ↔ B12 | IOCP console parking (B7) is what B12's TUI runtime relies on | consistent; B12 must follow B7 |
| B13 ↔ C* | B13 tags v0.1.0; every C task pins `GIT_TAG v0.1.0` | consistent |
| C0 ↔ C1..C4, C6 | renames.json and rewrite.py profiles consumed by the codemods | consistent |
| C3 ↔ C4 | C4 branches from C3's branch | consistent; sequential |
| C7 ↔ C8 | C8 branches from C7's branch | consistent; sequential |
| C1, C2 ↔ C6 | contour's PR may become ready only after endo's and tuidu's merge | consistent; C6 stays draft |
| Global "local Windows AND WSL green" ↔ A1 6b | WSL may not reach the Windows fastcached daemon | consistent: module falls back; CI `compile-cache` covers the positive path |

### Per-task self-consistency rows
| Task | Finding |
|---|---|
| A1 | consistent. The test-first exit-code fixture needs `core::testing_main`, created in the same task |
| A2 | consistent. Outward-facing repo creation → Ruling R2 |
| A3 | consistent; the Emscripten job is configured here, and later tasks widen it |
| A4 | consistent; the `coro` row is created here before A5 |
| A5 | consistent |
| A6 | consistent |
| A7 | consistent; SyncGuard fix is test-first |
| A8 | consistent; Step 0 (CORE_CPP_TARGETS) precedes the vendor self-test |
| B1–B13 | consistent with the Phase B preamble; B7 must precede B12; B13 is outward-facing (release) → Ruling R2 |
| C0–C8 | consistent; per-repo worktrees as Global Constraints say; outward-facing PRs and issues → Ruling R2 |
| Plan-wide | A-task texts name `D:\fastcached\…` paths → Ruling R5 |
| Plan-wide | imported code may contain C-style index loops → Ruling R4 |

### Rulings
- Ruling R1: core-cpp tasks run directly on `D:\core-cpp` `master`, not in a worktree — the repo is brand new with no base to branch from, and the approved plan names `master` the default branch and pushes to it from A2 on — cost if wrong: history would need moving to a branch; nothing is lost.
- Ruling R2: GitHub repo creation, Pages, pushes to core-cpp master, the v0.1.0 release, consumer draft PRs and tracking issues proceed without further confirmation — the user approved a plan that lists each of these as authorized outward-facing actions — cost if wrong: a public repo or PR exists earlier than wanted, and it can be closed or deleted.
- Ruling R3: imported contour/endo/fastcached code never keeps `NOLINT`. The importer removes each one and resolves the underlying finding with a `.clang-tidy` naming IgnoredRegexp (coroutine/STL hooks) or a code fix — the Global Constraints forbid NOLINT and A1's hygiene scan enforces it — cost if wrong: extra import effort; an unfixable tidy finding goes to a core-cpp issue plus a documented check exclusion in `.clang-tidy`.
- Ruling R4: imported files have their C-style index loops converted during the import task (grep `for *(.*;.*;` per imported file) — Global Constraint "no C-style for(;;) index loops" — cost if wrong: import tasks take longer; behaviour is covered by the imported tests.
- Ruling R5: every fastcached path the plan names is read as an `origin/master` blob (`git -C D:\fastcached -c core.autocrlf=false -c core.eol=lf show origin/master:<path>`), never from the `D:\fastcached` working tree — Global Constraints plus the user's worktree requirement — cost if wrong: none.
- Ruling R6: Phase C tasks in different repositories may be dispatched in parallel (no shared files). Within a repo they stay sequential (C3→C4, C7→C8) — the skill's no-parallel rule exists to avoid file conflicts, which separate repositories cannot have — cost if wrong: more concurrent CI load.

## Progress
Task A1: dispatched (BASE 4abb140, implementer impl-A1, model opus)
Task A1: report DONE_WITH_CONCERNS (commits 6a2001e..f0c951c; 4 local configs green; fastcache-cc selected with HITs on Windows and WSL)
- Ruling R7: `core::testing` (dialog suppression, and later the ScopedTempDir/EnvHelper helpers) builds unconditionally on native and the WebAssembly subset. Only `core::testing_main` needs Catch2; it is gated by `CORE_CPP_CATCH2_MAIN` (default `${CORE_CPP_TESTING}`, from the build design §4.2), with the Catch2 row `WHEN CORE_CPP_TESTING OR CORE_CPP_CATCH2_MAIN` — C1 (endo) moves its test helpers onto `core::testing` while configuring core-cpp with `CORE_CPP_TESTING OFF` — cost if wrong: one extra small static library per consumer build.
- Ruling R8: `FetchTransferBound.cmake` is included only from `CoreCppTopLevel.cmake`, not from `cmake/CPM.cmake` — spec §3: only the top-level file may touch process or global state — cost if wrong: a subproject fetch of core-cpp's deps has no transfer bound (the parent owns that).
- Ruling R9: accept the `HeaderFilterRegex '.*[/\]src[/\]core[/\].*'` deviation — the literal spec regex measurably drops every header finding on Windows paths — cost if wrong: none; it is a superset match.
- Ruling R10: accept the `key: value` pin-file format (`version: clang-format version 22.1.8`) — it is the organisation's format, which fastcached and the format-on-edit hook read — cost if wrong: none.
- Ruling R11: the compile-cache assertion (A2 CI job; plan A1 6b) greps `CORE_CPP_CXX_COMPILER_LAUNCHER` in CMakeCache.txt or `fastcache-cc` in build.ninja, not `CMAKE_CXX_COMPILER_LAUNCHER` — the verbatim CompileCache.cmake sets the launcher as a normal variable, never cached — cost if wrong: none; it checks the same fact.
- Ruling R12: endo keeps its own env-gated product dialog variant (`WindowsDialogs.cmake`, `SuppressWindowsDialogsAtStartup.cpp`) per the C1 plan text. core-cpp's unconditional startup TU serves core-cpp's own tests — cost if wrong: C1 adds a core-cpp variant later.
Task A1: pre-review fix 26de633 (R7, R8); review dispatched (review-A1, opus) on review-4abb140..26de633.diff
- Ruling R13: filed LASTRADA-Software/fastcached#1531 (clang-cl cache hits replay no /showIncludes, so Ninja keeps no header deps) — a measured finding from A1; fastcached's rules say findings become issues. Carried note for every later Windows verification: a `clangcl-*` tree populated from fastcache-cc hits must be rebuilt with `--clean-first` after header edits before a result counts as green — cost if wrong: one public issue that may duplicate unknown work.
Task A1: review 1 (review-A1, opus): spec ✅; Important ×2; Minor ×9. Full review in task-A1-review.md.
- Ruling R14 (plan-mandated finding, Important #2): `normalisedExitCode` ends with `return rawExitCode == 0 ? 0 : 1;` instead of `return 0;`, with a fixture row and unit test for Catch2's non-zero-without-failures case (e.g. an unmatched test spec) — spec §3 says 0 = pass, and a non-zero Catch2 verdict is not a pass — cost if wrong: none.
Task A1: minor (deferred): hygiene scanner is line-based; PUBLIC/INTERFACE flags via continuation lines, variables or set_property escape it. Carry to A8: configure-time check that CORE_CPP_TARGETS have empty INTERFACE_COMPILE_OPTIONS/DEFINITIONS/LINK_OPTIONS.
Task A1: minor (deferred): -Wno-c2y-extensions applies to library targets too (CoreCppToolchain.cmake:101); should be test targets only.
Task A1: minor (deferred): an undefined WHEN variable silently disables a module or dependency (CoreCppModules.cmake:61, CoreCppDependencies.cmake:133).
Task A1: minor (deferred): generated core/Config.hpp is in no FILE_SET — carry to A3 (base owns Config.hpp).
Task A1: minor (deferred): row-split helper duplicated (CoreCppToolchain.cmake:131-134/:153-156, check-cmake-hygiene.cmake:165-169, selftest :52-55/:111-114).
Task A1: minor (deferred): WindowsDialogCanary.cpp:27 hard-codes 77 instead of CORE_CPP_SKIP_EXIT_CODE.
Task A1: minor (deferred): check-exit-codes.cmake discards fixture output on mismatch.
Task A1: minor (deferred): framework refusal paths (layering, disabled DEPS, FETCH_DEPS=OFF, subproject sanitizers) have no automated test.
Task A1: minor (deferred): SOURCES_POSIX matches UNIX, which is true under Emscripten — carry to A3/A4, where the wasm-subset / SOURCES_EMSCRIPTEN selection must exclude POSIX sources.
- Ruling R15: A2's build.yml leaves out `consumer-smoke` (A8 adds it, together with tests/consumer-*), and A2's downstream.yml starts with only the CompileCache/FetchTransferBound drift job. The consumer builds are added to downstream.yml after Phase C (carry: a post-C8 step, listed in the ledger) — jobs with nothing to run would be red or placeholder noise — cost if wrong: nightly consumer drift detection starts later.
- Carry (post-C8): extend downstream.yml with nightly builds of contour/endo/fastcached/tuidu/Lightweight-dbtool/morph against core-cpp master.
Task A1: fix round 1/5 (2 addressed, 0 open; commits 26de633..0d80b3e; re-review rereview-A1-r1 sonnet)
Task A1: complete (commits 4abb140..0d80b3e, review clean after 1 fix round)
- Ruling R16: A2 adds `emscripten` configure/build/test presets (Release, no `-pthread`, `CORE_CPP_WITH_TUI=OFF`, `CMAKE_CROSSCOMPILING_EMULATOR=node`, toolchain from `$env{EMSDK}`), because A2's CI emscripten job needs one and A3 Step 4 already refers to `--preset emscripten` — cost if wrong: none.
Task A2: dispatched (BASE 0d80b3e, implementer impl-A2, model opus)
- Ruling R13 update: fastcached#1531 fixed upstream at ca8dfc32 (PR #1533); the cause was fastcache-cc not recognising clang-cl's -clang:-MF depfile flag. CompileCache.cmake is unchanged. The --clean-first workaround applies only while the local fastcache-cc binary predates ca8dfc32 (it does on this machine until rebuilt). impl-A2 was told to phrase the build-and-toolchain rule accordingly.
Task A2: report DONE_WITH_CONCERNS (commits 5d9f259..2a524ad; repo contour-terminal/core-cpp public; Pages https://contour-terminal.github.io/core-cpp/; ci-ok + Docs green on 2a524ad)
- Ruling R17: branch protection, when configured, requires only `ci-ok`. docs.yml keeps its path filters and so cannot be a required check; it stays gated by its own run on docs changes — cost if wrong: a docs-breaking PR could merge without Docs having run; the next docs-touching run catches it.
- Ruling R18: Dependabot PRs #1 (actions major bumps), #2–#4 (docs pip pins) stay open, unmerged, for the user — merging dependency bumps is a merge the user should decide, and docs/requirements.txt mirrors fastcached's pins — cost if wrong: open PRs linger. Listed for the final summary.
- Ruling R19: filed LASTRADA-Software/fastcached#1538 (the auto-started v0.2.0 daemon needs libyaml-cpp.so.0.8 and reports only exit 127). core-cpp CI installs libyaml-cpp0.8 as the workaround — cost if wrong: one public issue.
- Ruling R20: accept `-fexceptions` in the emscripten presets (Catch2 SKIP/REQUIRE throw; measured) — amends R16 — cost if wrong: none.
- Confirmed gap (for the review): the plan's "Open work after v0.1.0" items were not created as core-cpp issues, and `.agent/rules/README.md` has no `## Open work` entries yet.
- Note for the final summary: impl-A2's local compile-cache simulation ran `fastcache-cc --zero-stats` in WSL, which cleared the user's WSL fastcache-cc statistics.
Task A2: review dispatched (review-A2, opus) on review-0d80b3e..53ef21f.diff
Task A2: review 1 (review-A2, opus): spec: 3 Important (open-work issues missing [plan-mandated/confirmed gap]; vcpkg binary cache broken, OpenSSL rebuilt every run; no namespace=directory check), 9 Minor. Full review in task-A2-review.md.
- Ruling R21: the namespace=directory check (spec §4 `style` row) is added now as a hygiene rule with self-test cases. The build-design excerpt that narrowed it was the plan's mistake; the spec is binding — cost if wrong: none; A3–A7 imports need it.
Task A2: minor (deferred): ccache keys are prefixes of other keys (windows-cl-release ⊂ -tls; linux-clang-22 ⊂ -arm64/-cxx26), so prefix restores hit 0 times.
Task A2: minor (deferred): cpp-guidelines.md:148-150 cites a clang-tidy padding check that .clang-tidy:86 disables.
Task A2: minor (deferred): cpp-guidelines.md:185-186 misdescribes the clang-tidy preset (CORE_CPP_CLANG_TIDY_EXE is not set; find_program + version warning).
Task A2: minor (deferred): docs/modules/testing.md:48-50 SKIP_RETURN_CODE 4 claim is wrong for Catch2 3.8 (42 on failure).
Task A2: minor (deferred): library-hygiene.md:32 describes CORE_CPP_TARGETS as existing (A8 adds it).
Task A2: minor (deferred): build.yml:538-541 libyaml workaround does not link fastcached#1538.
Task A2: minor (deferred): AGENT.md:69-70 tripwire says CMakeCache does not show the launcher; CORE_CPP_CXX_COMPILER_LAUNCHER does.
Task A2: minor (deferred): one CPM cache key per OS; an Emscripten (TUI OFF) save could pin a cache without libunicode once it exists.
Task A2: minor (deferred): no CMake `coverage` target (done inline in the workflow).
Task A2: fix round 1 commits 53ef21f..2a7b79f; re-review dispatched
- Carry to C8: link core-cpp#9 (the morph follow-up) to the morph issue C8 Step 6 files.
Task A2: fix round 1/5 (3 addressed, 0 open; commits 53ef21f..2a7b79f; re-review rereview-A2-r1 sonnet)
Task A2: complete (commits 0d80b3e..2a7b79f, review clean after 1 fix round)
Task A3: dispatched (BASE 2a7b79f, implementer impl-A3, model opus)
- USER DIRECTIVE (2026-09-18): account for upstream progress (fastcached is where active development happens). Amendment drafted in plan-amendment-upstream.md. Apply it to the plan and spec after A3 completes (no commit while impl-A3 is active on master), then regenerate the briefs. It adds Task A3b (provenance table), Task B12b (catch-up sweep) and delta gates in C1/C3/C4/C6.
- USER DIRECTIVE update (2026-09-18): the user checked the fastcached board; coro/net are not moving (open items are business logic). The amendment is scaled down to a provenance table (A3b), one catch-up check (B12b) and a start-of-task delta check in C1/C3/C4/C6 — cost if wrong: an unexpected upstream change is caught at C-task start instead of continuously.
Task A3: report DONE (commits 1709dac..a8c8429; ci-ok green on a8c8429)
- Ruling R22: accept `CRISPY_*` → `CORE_*` for Defines.hpp macros, and the Assert split (`fatal`/`SoftRequire` → `core::log`, `<core/log/Assert.hpp>`), which layering forces. Both go into C0's rename map — cost if wrong: a rename in C0's table.
- Ruling R23: accept Tracy's own `option()`s landing in the parent's cache when core-cpp fetches Tracy with CORE_CPP_WITH_TRACY on — every add_subdirectory of Tracy does that; it only happens on opt-in — cost if wrong: none.
- Ruling R24: the latent upstream bugs A3 noticed get fixed in core-cpp during A3's fix round, test-first: `FNV<char>` infinite recursion for non-char V, and `TimesIterator::operator--(int)` incrementing. core-cpp now ships them — cost if wrong: none; contour gets the fixes through C6.
- Ruling R25: emsdk 3.1.56 ships libc++ 17.0.4 (measured), not 18/19. The spec, plan and Global Constraints text get corrected in the amendment commit; A5's `<stop_token>` probe must handle libc++ 17 (`-fexperimental-library`) — cost if wrong: none.
- Carry to C0: rename map gains crispy::fatal → core::log::fatal (+ <core/log/Assert.hpp>), crispy::App → core::cli::App, CRISPY_* → CORE_*, global Overloaded → core::Overloaded.
- Carry to C6: contour's crispy::nextPowerOfTwo had a rounding bug (fixed in core 3e63739); contour hash-table sizing changes when it switches; verify contour's tests.
- Amendment applied: commit 371e73a (plan+spec); briefs and global-constraints regenerated.
- Ruling R26: core::cli's throwing parse errors (core-cpp#13) are converted to std::expected in A3's fix round, not deferred — the Global Constraint allows no exception type but OperationCancelled, and deferring would make contour/endo/tuidu migrate twice — cost if wrong: the A3 fix round grows.
- Note: commits 2e57ff7..1b2401d fail the gcc-release CI legs (fixed forward in fe62488). Accepted for a new repo; no history rewrite.
- USER DIRECTIVE (2026-09-18): Ruling R26 reversed. The core::cli std::expected refactor (core-cpp#13) moves to the END of the task list, after C8 (and after the post-C8 downstream.yml carry). A3's fix round covers only its review findings plus R24 (FNV<char> recursion, TimesIterator post-decrement).
- Carry (end of plan): core-cpp#13, core::cli parse errors → std::expected, with follow-up updates in the consumers that parse CLIs through core::cli (contour, tuidu, endo).
Task A3: review 1 (review-A3, opus): spec ✅, Approved; 0 Critical/Important; 11 Minor. Full review in task-A3-review.md.
- Ruling R27: `core::ranges` (Iota/FoldLeft fallbacks mirroring std::ranges) is an allowed nested namespace; add it to the list in spec §1 and cpp-guidelines.md — cost if wrong: a rename before 0.1.0.
- Ruling R28: A3 gets one fix dispatch, despite the Approved verdict, for R24 (FNV<char> recursion, TimesIterator operator--(int) incrementing, and TimesIterator/Times2DIterator operator++(int) returning *this after pre-increment), R27, minor #2 (the consumer-migration.md rename map gets every A3 delta) and minor #3 (contour-specific wording and stale docs). The reviewer recommends 1–3 before Phase C, and C0 builds on the rename map — cost if wrong: one short extra round.
Task A3: minor (deferred): Doxygen for enumerate (Utils.hpp:142-143) gives the wrong reason.
Task A3: minor (deferred): views::enumerate (Utils.hpp:147) and nextPowerOfTwo (Utils.hpp:700) lack [[nodiscard]].
Task A3: minor (deferred): nextPowerOfTwo tests do not instantiate uint8_t/uint16_t (the digits>8 branch is untested).
Task A3: minor (deferred): testing_main links core::log PUBLIC; PRIVATE suffices (testing/CMakeLists.txt:24).
Task A3: minor (deferred): CORE_CPP_WITH_TRACY is neither forced OFF nor refused under Emscripten (CoreCppOptions.cmake:51-58).
Task A3: minor (deferred, record only): master is not bisectable across 2e57ff7..1b2401d (gcc-release) and 2e57ff7..861baa6 (clang-tracy).
Task A3: minor (deferred): bool parameters in imported log API (Sink ctor, setEnabled, enable, installLogging) and missing [[nodiscard]]; needs a tracking issue.
Task A3: fix round 1/5 (6 addressed, 0 open; commits 371e73a..6ebb78d, the 371e73a amendment excluded; re-review rereview-A3-r1 sonnet)
Task A3: complete (commits 2a7b79f..6ebb78d, review clean)
Task A3b: dispatched (BASE 6ebb78d, implementer impl-A3b, model sonnet)
Task A3b: report DONE (commit 6dda244; CI green); review dispatched
Task A3b: review 1 (review-A3b sonnet): Approved, no issues.
Task A3b: complete (commits 6ebb78d..6dda244, review clean)
Task A4: dispatched (BASE 6dda244, implementer impl-A4, model opus)
- Verified 2026-09-18: fastcached#1531 closed/fixed (PR #1533, ca8dfc32). Local fastcache-cc binaries predate it (Windows d4451c3b, WSL 73fb0457, both Sep 16), so the --clean-first workaround stays in force locally until they are updated (the user was offered the update).
Task A4: report DONE (commits fe95520..48aae7b; CI run 35356388834 success)
- Ruling R29: core-cpp#14 (Wakeup's constructor throws) joins #13 at the END of the task list, like the user's CLI directive. ScopedTempDir/ScopedWorkingDirectory throwing in test fixtures is intended — cost if wrong: none.
- Ruling R30: the bool parameters in imported API (A3: log Sink/setEnabled/enable/installLogging; A4: FileSystem::openWrite(append), copyFile(overwrite)) get ONE tracking issue, filed at the end-of-plan cleanup and fixed there — enum-class-over-bool is a guideline, and changing these now would make consumers migrate twice — cost if wrong: an API break after 0.1.0 (0.x allows it).
- Carry to C1: endo's process code must define its own <windows.h> include and SIG*/STD*_FILENO Windows fallbacks, which core::platform::Types.hpp no longer provides.
- Carry (end of plan): core-cpp#14, the Wakeup ctor → std::expected factory.
- USER DIRECTION (2026-09-18): core::coro StopToken/StopSource/StopCallback alias std:: where __cpp_lib_jthread is defined, with a core-cpp fallback otherwise (like Generator), instead of the -fexperimental-library usage requirement. Plan and spec amended in commit 0cf5bf7; the A5 brief is regenerated. Carry to C6: contour's global <stop_token> probe becomes removable.
Task A4: review 1 (review-A4 opus): Approved; 0 Critical/Important; 9 Minor. Full review in task-A4-review.md.
- Ruling R31: one A4 fix dispatch for minors #1 (WallClockRef retainer rows), #2 (public `core::platform::nativeEnvironmentProvider()` / `nativeFileInfoProvider()` factories, because endo's composition roots include the private posix/windows/linux provider headers; migrate-once), #3 (UserPaths zero-arg test), #4 (endo/shell wording), #6 (writer: short-circuit an identical publish, fix the mutex comment, fork/exec caveat), #7 (the WebAssembly EOF contract in platformRead's doc and platform.md), #8 (record the fixture-exception carve-out in cpp-guidelines.md) — #2 is load-bearing for C1, the rest are cheap — cost if wrong: one short round.
Task A4: minor (deferred to B3/B4): SystemPipe::read maps EAGAIN and real failures both to PlatformError::IoError; the event-loop work decides on a WouldBlock enumerator.
Task A4: minor (deferred): the task-A4 report's CI placeholder (report-only).
- USER DIRECTION on the A4 minors (2026-09-18), sent to impl-A4:
  - #2: a real default provider per platform via public factories (including a generic POSIX FileInfoProvider for macOS/BSD);
  - #3: tests use mocks;
  - #4: reword meaningfully;
  - #5: NOW, not deferred: SystemPipe::read → std::expected<ChannelResult, PlatformError>, with ChannelResult empty on an empty channel and end-of-stream explicit;
  - #7: tests for the Emscripten behaviour;
  - #8: exceptions only for unrecoverable conditions (plus OperationCancelled), and ScopedTempDir setup failure counts as unrecoverable; cpp-guidelines.md is updated by impl-A4, while the spec/plan Global Constraint wording is amended by the controller after A4 lands;
  - #9: nothing to decide (a report placeholder only).
- Pending controller action after A4: amend the spec, plan and Global Constraints exceptions wording; ask the user whether core-cpp#14 (the Wakeup ctor throwing on OS resource exhaustion) falls under "unrecoverable" and closes, or stays for the std::expected factory at the end.
- USER DECISION: core-cpp#14 is closed; the Wakeup ctor keeps throwing (unrecoverable). impl-A4 was told to update the #14 references and the docs.
Task A4: fix round 1 commits 48aae7b..629629e (the controller's 0cf5bf7 inside); CI 35365981481 green; re-review dispatched
- Controller: the exceptions wording in the Global Constraints was amended per the user's rule (904b6bc); the briefs are regenerated.
Task A4: fix round 1/5 (9 addressed, 0 open; commits 48aae7b..629629e; re-review rereview-A4-r1 opus; new Minor: an Environment.cpp:129 duplicate-entry short-circuit regression; platform.md:94 contradiction; 2 nits)
- Ruling R32: fix round 2 for the Environment.cpp short-circuit regression (with duplicate `X=old`, `X=same`, setting X=same skips a publish while readers see `old`; A4's fix introduced it) plus the platform.md:94 contradiction and the two nits. A correctness regression we introduced is fixed now, not deferred — cost if wrong: one short round.
- Ruling R33: A5's CORE_CORO_FORCE_STOP_TOKEN_FALLBACK build is a separate test executable (core-cpp-coro-fallback-test / ctest core-cpp.coro-fallback) that compiles all coro test sources with the macro — why: TUs with and without the macro in one binary give inline functions in Cancellation/Task two definitions (an ODR violation), and it exercises Task/whenAll/whenAny cancellation over the fallback — cost if wrong: one extra test binary.
Task A4: fix round 2/5 (4 addressed, 0 open; commits 9e5922e..a92e0e9; CI 35367968230 green; re-review rereview-A4-r2 sonnet clean)
Task A4: complete (commits 6dda244..a92e0e9, review clean)
Task A5: dispatched impl-A5 (opus), BASE a92e0e9
Task A5: implementer DONE_WITH_CONCERNS; commits 6cde65a..074b7da; CI 35374365149 green; review package review-a92e0e9..074b7da.diff
- Ruling R34: the brief's `noStopState` is renamed `NoStopState` and becomes `inline constexpr` (std's is constexpr). The Global Constraints say constants are CamelCase, and `.clang-tidy`'s ConstexprVariableCase is CamelCase; the plan's spelling slipped against its own constraint, and `inline const` only dodged the check — fix-round item — cost if wrong: a one-identifier rename.
- Filed core-cpp#15: deep chains of synchronously completing co_awaits overflow the stack on GCC -O0 and emsdk 3.1.56 (symmetric transfer is not a tail call there). Carry to B1: decide the fix there (a trampoline via bool await_suspend is preferred), since B1 ports fastcached's symmetric-transfer depth test; the done-when is in the issue.
Task A5: review review-A5 (opus) — Needs fixes: 2 Important (StopToken.hpp:195 the unordered stop_possible read; :243-253/:479-483 the address-based wait of an inline-run, unregistered callback can deadlock), 5 Minor.
- Ruling R35: fix round 1 covers Important 1-2 (each with a test; #2 deterministic), R34 (NoStopState), Minor 3 (Emscripten key !__wasm_tail_call__; name -Og/-O1 in the comment and docs; no stack-growth probe, since #15 holds the real fix), Minor 4 (docs: a loop overflows too, link #15), Minor 5 (docs: the fallback is live on libc++ before 20; a configure-time message(STATUS) in the coro test CMake says which StopToken branch each job takes), Minor 6 (three test gaps), plus a local clang-tsan `--repeat until-fail:200` of coro-fallback [threads], and a check of the macOS CI log for the branch. Minor 7 (the inherited #ifndef _WIN32 exception-case guards) is carried to B1, which rewrites those tests — cost if wrong: a slightly larger round.
- Carry to B1: remove the inherited `#ifndef _WIN32` guards on Task_test.cpp:203 and WhenAny_test.cpp:162 if cl/clang-cl pass (WhenAll's unguarded throwing cases already pass there).
- core-cpp#15 updated with the reviewer's GCC -O level table.
- USER DECISION (2026-09-18): the coroutine module is renamed coro → async: directory src/core/async/, namespace core::async, target core::async (real target core-cpp-async). Namespaces and directories stay lowercase (the user considered CamelCase Core::Async and chose lowercase); CMake targets keep core::<module>. Generator moves to base as core::Generator (src/core/Generator.hpp), because core::async::Generator would read as an async generator; platform then no longer depends on the coroutine module. clang-tidy gains NamespaceCase: lower_case. It runs as a new Task A5b between A5 and A6, after A5's review is clean; the controller amends the spec and plan first.
- Controller: the spec and plan are amended for the async rename (8e29bb5, local, pushed with impl-A5's fix round): new names from Part I through C8, the lowercase NamespaceCase rule in §1 and the Global Constraints, and the new Task A5b (brief task-A5b-brief.md). Completed A4/A5 text is left as history.
- Controller: C6's plan text now removes contour's <stop_token> probe (replacing 'keep it'), which closes impl-A5's concern 3 (local commit, pushed with the next task).
Task A5: fix round 1 commits 241d814..2605c23 (8e29bb5 controller's inside); CI 35379331171 green; re-review rereview-A5-r1 (opus) dispatched
- Ruling R36: A5b adds no CHANGELOG Breaking entry, because v0.1.0 is unreleased and no consumer ever saw core::coro; the [Unreleased] entries are rewritten to the new names (plan amended locally) — cost if wrong: one CHANGELOG line.
Task A5: fix round 1/5 (Important 1-2, R34 and Minor 3-6 addressed, 0 open; commits 241d814..2605c23; CI 35379331171 green; re-review rereview-A5-r1 opus clean; 2 cosmetic nits and 2 doc observations folded into A5b's dispatch; the #15 and C6 observations are already handled)
Task A5: complete (commits 6cde65a..2605c23, review clean)
Task A5b: dispatched impl-A5b (sonnet), BASE 36c4eeb
Task A5b: implementer DONE; commits 16478ae..e45f730 (e45f730 was a CI-driven fix: the App.cpp `namespace CLI` alias became lowercase); CI 35386108380 green; review review-A5b (sonnet) dispatched on review-36c4eeb..e45f730.diff
- A6 prep: contour origin/master is still 6777ff05, so there is no drift in src/net or src/coro. The A6 dispatch is written (task-A6-dispatch.md).
- Ruling R37: no `core::net::platform` namespace (it would hide core::platform inside core::net). contour's net/platform/PeerAddress goes to core::net, and WindowsLoopback to net/windows/ — cost if wrong: two file moves.
- Ruling R38: in A6, `net` is PLATFORMS native and `net_types` is any; B3–B5 switch net to wasm-subset. A PUBLIC link to Threads::Threads is allowed as a usage requirement (not a flag) — cost if wrong: none until B3.
- Ruling R39: CORE_CPP_WITH_TLS=ON in the hidden unix preset, and CI Linux jobs install libssl-dev, so Tls_test runs on Linux, macOS and FreeBSD, not only on cl-release-tls — cost if wrong: slightly longer CI.
- Carry (next fix round or A6): .agent/rules/build-and-toolchain.md gains a rule to rebuild the clang-tidy preset with --clean-first after editing .clang-tidy, because it is not a build input and a stale object hides new findings (A5b's e45f730 came from exactly this).
Task A5b: review review-A5b (sonnet) Approved, 0 Critical/Important; 3 Minor, all no-action (the README module-row staleness sweep is folded into A6)
Task A5b: complete (commits 16478ae..e45f730, review clean)
Task A6: dispatched impl-A6 (opus), BASE e45f730
- USER DIRECTION (2026-09-18): platform-dependent net files go into platform subdirectories.
- Ruling R40 (sent to impl-A6 mid-task): Epoll → linux/, Kqueue → bsd/ (SOURCES_BSD = APPLE|BSD). PollEventSource.cpp is split along its #ifdef into posix/ and windows/, with the header portable and _waitRotation unconditional. WaitChunking and PeerAddress → detail/ (superseding R37's placement; R37's namespace ban stands). FdPassing_test and UnixSocket_test → posix/. DefaultEventSource.cpp keeps its #ifdefs until B3. The layout rule is documented. Spec §1 and plan B3/B7 are amended to match (local commit, pushed with A6): no backend/ directory, one DefaultBackend.cpp per platform — cost if wrong: file moves.
Task A6: implementer DONE_WITH_CONCERNS; commits cb73274..def7325 (d07e136 controller's inside); CI Build 35395135341 green and Portability 35395187310 (FreeBSD) green on def7325; review package review-A6.diff (non-net) plus net faithfulness checked against the upstream blobs
- Carry to B4 (EventLoop rewrite): the contour EventLoop use-after-free when a parked flow exists and an exception leaves blockOn (A6 concern 4) — B4 needs a regression test. Also contour's EventLoop throwing where the exceptions rule wants std::expected (concern 3).
- Carry to B10/B11: coroutine parameters taken as std::string_view in the imported net/TLS/HTTP code (the guideline is by-value owning types); the "contour-daemon" default CN goes with SelfSignedOptions (B11); the two-reactor TLS tests can hang instead of failing, so B11 makes every wait bounded.
- Dependabot PR: needs a rebase onto def7325; it stays with the user (R18).
- A7 prep: endo origin/master is still f774a210, so src/tui/platform/testing have no drift. fastcached origin/master 5389e29a: its vendor/endo matches endo's pins except tui/runtime/TuiRuntime.{hpp,_test.cpp}. fastcached 6483abd8 (2026-09-18) decides DelayAwaiter's elapsed deadline in await_suspend, because MSVC 19.44 ARM64 loses the enclosing try block when await_ready reads the clock through a virtual now(). A7 imports that delta (the upstream sync discipline).
- Carry to B4/B5: core::net EventLoop.hpp:364 DelayAwaiter::await_ready reads `_loop.clock().now()`, the same pattern. The rewritten DelayAwaiter decides an elapsed deadline in await_suspend with a constant await_ready, and the rule goes into async-and-net.md with fastcached 6483abd8 cited. Carry to B13: consider a windows-11-arm CI leg (MSVC ARM64), which would have caught it.
- Ruling R41 (A7): public tui headers are platform-clean. endo's TerminalInput.hpp <windows.h>/<termios.h> goes behind a pimpl or an opaque state in posix/ and windows/, and TerminalOutput.hpp's `using NativeHandle = void*` becomes core::platform::NativeHandle (or a base-level alias if the tui_output leaf must stay base-only). endo's tui/platform/ is split into posix/, windows/ and detail/ per R40, and runtime/{EventSource,PollEventSource} stay until B12 — cost if wrong: header churn in one module.
- A7 dispatch written (task-A7-dispatch.md); A7 is dispatched after A6's review is clean.
Task A6: review review-A6 (opus), Needs fixes — Important 1: the file-wide guards R40 banned remain in the posix/ tests; Important 2: target rows have no DEPS, so the layering check can't bound net_types or (next) tui_output. 4 Minor. The import itself is verified faithful (all 66 files diffed against upstream; the 6dd73ea clock refresh is correct per §2 and pinned by its test).
- Ruling R42: fix round 1 covers every finding (task-A6-fixround1.md). Important 1 is extended to drop every file-wide platform guard in net's platform directories (CMake selects them), and target-row DEPS land now, before A7's tui_output. Two agent messages were lost this session (R40 to impl-A6, and review-A6's result), so dispatches now point to files and are checked on disk — cost if wrong: a slightly larger round.
- impl-A6 never received the fix-round message: it sat idle for about 4 h with no file, process or CI activity. Stopped it, and dispatched fix-A6 (opus, fresh) for fix round 1 from task-A6-fixround1.md.
Task A6: fix round 1 commits c7712a3..c4a083a (fix-A6); new tests/cmake/check-layering.cmake with 12 configure scenarios (9 failed on the old mechanism, including net_types -> core::async); CI Build 35413031946 and Portability 35413048140 green on c4a083a; re-review rereview-A6-r1 (sonnet) dispatched
- Ruling R43: fix-A6's concern 1 is the rule, not a hole: a module's targets may link each other, so `net` -> `net_types` configures. A target's own row bounds only what it links OUTSIDE its module. This is documented where the DEPS mechanism is. Concern 2 (a row's DEPS naming a sibling with fewer platforms) is parked for the final review; nothing does it today — cost if wrong: a later layering tightening.
- Note: teammate messages are delayed, not lost. impl-A6's R40 message and review-A6's verdict both arrived hours late, after I had already routed around them by writing files. Keep using files as the source of truth, and check disk rather than waiting for messages.
Task A6: fix round 1/5 (6 addressed, 0 open; commits c7712a3..c4a083a; CI 35413031946 and FreeBSD 35413048140 green; re-review rereview-A6-r1 sonnet clean; its 2 out-of-scope notes folded into A7's dispatch)
Task A6: complete (commits cb73274..c4a083a, review clean)
Task A7: dispatched impl-A7 (opus), BASE c4a083a
- USER DIRECTIVE (2026-09-19): every task and stage ends with the /code-review skill over its changes, and its findings are addressed in the same fix round as the subagent review's. Applies from A7 on; the Phase A gate (after A8) runs it over A1-A6 too. A finding I decide against becomes a ruling with its reason.
Task A7: implementer DONE_WITH_CONCERNS; commits e8c81ab..7618e80 (176 files, +47299/-25); CI Build 35475447624 and Portability 35475468092 green on 7618e80; review-A7 (opus, spec+quality) and the /code-review skill (high, c4a083a..7618e80) both dispatched — first task under the new /code-review directive
- /code-review (xhigh) on A7 found 15 defects; most are endo's, imported faithfully, and ours to fix now.
- Ruling R44: A7 fix round 1 fixes the 9 that are cheap, local or ours (task-A7-fixround1.md): the GenericSyntaxHighlighter stack overflow (3 unguarded toLowerInto call sites; 4 others in the same file guard correctly), the Dialog Rect x/y swap at 6 sites plus a first Dialog_test, ~SyncGuard's missing flush, the SIGWINCH errno clobber and blocking pipe write, VtParser's three unbounded buffers, InputDialog's negative width, the "endo-" OSC 8 id prefix (AGENT.md: no consumer concept in core-cpp), Completer's unstable sort, and the SyncGuard test that asserts nothing. The 4 runtime defects (core-cpp#16-19) are deferred to B12, which deletes that code before v0.1.0 ships in B13; #20 (CESU-8 on Windows) and #21 (Alt+Backspace) need design and are filed. Plan B12 amended locally — cost if wrong: the deferred four ship if B12 slips before a release, which B13 gates.
Task A7: review review-A7 (opus) Needs fixes — 0 Critical, 6 Important: provenance records only `-` for 79 of 84 files that differ beyond the mechanical rewrite (B12b and the consumer delta checks would silently revert A7's house-style conversions, including removed NOLINTs and pragmas); a dead public signalFd() accessor where A6 used [[maybe_unused]]; a global tidy relaxation that belongs in the module; the cognitive-complexity check disabled instead of thresholded; readability-static-accessed-through-instance disabled although await_ready need not be static; a stale network note. Plus 4 Minor.
- The two reviews' findings are merged into one fix round (task-A7-fixround1.md, items 1-16; /code-review item 9 and the spec review's first Minor are the same finding). fix-A7 (opus, fresh) dispatched; impl-A7 had already gone idle.
- fix-A7 died on a weekly usage limit mid-item-1, leaving 5 uncommitted paths (items 1 and 2, partly). fix-A7b (opus) resumed from that state, told to verify the inherited work before continuing.
Task A7: fix round 1 commits 63555d8..04ffa19 (fix-A7b, 12 commits); all 16 items done; CI build 35496614625 + 35497139755, docs 35496614582, portability 35496620406 green; issues core-cpp#22 (cognitive complexity payoff) and #23 (hygiene rule vs forward declarations) filed and cited in the files they constrain.
- Ruling R45: the other two consumer-named strings (`.endo-format` in the highlighter's language table, `endo-signature` in Theme.cpp) are the same AGENT.md violation as item 7 and get fixed now in a 1b follow-up; where the bytes are a format a consumer already emits or reads, they stay with a comment and a CHANGELOG line instead — cost if wrong: one small commit.
- fix-A7b's other concerns settled: the 4 MiB paste cap stands (chunking is an unrequested behaviour change), the vcpkg z-applocal failure is infrastructure (file only if it recurs), and item 4 rightly has no test.
Task A7: fix round 1b commit dc9f63f (4 consumer-named strings neutralised; the `.endo-format` recognition row deleted, which C1 gives back on endo's side); CI build 35498496844 green, 22 jobs.
- Ruling R46: LanguageId::Endo, registerEndoHighlighter(), the `.endo` extension row and the `endo` fence tag are the same AGENT.md violation but need a registration seam, not a patch. Filed core-cpp#24 and amended the plan (3f46e34): B13 closes it before the v0.1.0 tag because it breaks public API, and C1 registers endo's highlighter on endo's side — cost if wrong: one API design in B13.
Task A7: fix round 1/5 (16 items + R45 addressed, 0 open; commits cb68bdc..dc9f63f; CI green; re-review rereview-A7-r1 opus clean; 2 Minor found in the fix itself, folded into A8's dispatch)
Task A7: complete (commits e8c81ab..dc9f63f, review clean)
- Phase A gate (after A8): run /code-review over the tasks that never had one, in four passes — A1-A3 (build framework, base/log/cli), A4 (platform), A5+A5b (async), A6 (net) — since the per-task directive started at A7.
Task A8: dispatched impl-A8, BASE 3f46e34
Task A8: implementer DONE; commits 4be64a5..2177c41; vendor self-test 14/14, all three consumer smokes green locally and in CI; CI Build 35502877055 and Portability 35502138081 green on 2177c41; review-A8 and /code-review dispatched
- Carry to C8 (morph): its WebAssembly build needs CMAKE_CXX_SCAN_FOR_MODULES OFF, as tests/consumer-wasm does.
- Carry (next, before Phase B): re-sync cmake/portable/CompileCache.cmake verbatim from fastcached master; the nightly Downstream job has been failing on the drift since 2026-09-19. Its README and NOTICE record the source SHA.
Task A8: review review-A8 (opus) Needs fixes — 1 Critical: release.yml calls -DMODE=export, which the tool refuses, so the first tag would attach no vendor tarball or SHA256SUMS. 2 Important: REPO defaults to the script's parent, so running sync from inside a vendored copy resolves the ref in the consumer's repo and silently refills the copy from it; and an empty/headers-only MANIFEST passes check over an emptied copy. 6 Minor.
- Ruling R47: fix round 1 covers all of them (task-A8-fixround1.md), and item 1 must be proven by running the release step's commands locally, not by reading the workflow — an unexecuted release path is not verified.
- /code-review (xhigh) on A8 found 14 more, several verified by running the tool: a failed sync leaves its staging dir inside a good copy (so the consumer's own check then fails, contradicting docs/vendoring.md); the wasm leg's pthread guard can never fire (find_package makes a directory-scoped IMPORTED target the parent cannot see); the CPM leg's PUBLIC/INTERFACE-flag assertion skips INTERFACE targets, which is where such a flag would reach a consumer; a branch is accepted as REF although tool and docs say tag-or-SHA; the smoke echo server's single read makes the gating job intermittently red; the vendor self-test fails instead of skipping without git; MODULES can omit an unconditional module; and the SyncGuard asymmetry was documented rather than fixed.
- Both lists merged into task-A8-fixround1.md (17 items, overlaps named). fix-A8 (opus) dispatched, told to prove item 1 by running the release step's commands locally.
Task A8: fix round 1 commits 77c593a..8d1fc18 (fix-A8b, after fix-A8 died on a monthly spend limit); all 17 items; vendor self-test now 28 cases with 11 mutations each reddening exactly its own case; CI Build 35511575686 and Portability 35511352144 green on 8d1fc18; re-review rereview-A8-r1 (opus) dispatched
- The inherited self-test from fix-A8 had never been run: cmake_parse_arguments unset an absent one-value keyword, so its first case died, and two cases were wrong (a MODULES list that lost its semicolon through ${ARGN}, and a fixture hitting the occupant refusal rather than the tree refusal). fix-A8b rewrote and re-proved them. Telling a resuming agent to verify inherited work rather than trust it is what caught this; keep that in resume dispatches.
Task A8: fix round 1/5 (17 addressed, 0 open; commits 77c593a..8d1fc18; CI green; re-review rereview-A8-r1 opus clean apart from 4 Minor introduced by the fix)
- Ruling R48: fix round 2 covers those four (task-A8-fixround2.md) — the self-test header's over-claim plus an unreachable tool branch, a stale SyncGuard comment, the avoidable hygiene allowlist row (remove the glob and the row; an escape hatch we do not need is one we should not keep), and two docs plus the CHANGELOG describing the old behaviour. The item-15 residual (the fixture pins none of the real module table's shape) is answered in the report, not built — cost if wrong: one short round.
Task A8: fix round 2/5 (4 Minor addressed; commits 18e273e..8581313; CI Build 35513381325, Docs 35513381346, Portability 35513384532 green; the hygiene allowlist row is gone, so the tree carries no vendor exemption)
- Ruling R49: fix round 3 makes sync's replacement phase recoverable (move the old copy aside, restore and refuse on failure, name both trees if a restore fails), because it is the last path on which a sync can damage a consumer's copy and a rename can fail on Windows. Proof required, or the restore path tested directly with the reason stated — cost if wrong: one short round.
- Carry to B13 (or wherever check-layering is next touched): close item 15's residual the way fix-A8b proposed — read the real cmake/CoreCppModules.cmake twice, by include() and by the vendoring tool's regex, and require the name sets and the empty-WHEN sets to agree. No fixture: a fixture pins the imitation and goes stale silently.
Task A8: fix round 3 commits 08dcb40..b1db039 — the replacement is two directory renames through DEST.core-cpp-vendor-old, with restore-and-refuse and name-both-trees-delete-neither; self-test 30 cases, 4 mutations each reddening its own case; a leftover backup now refuses rather than being deleted; CI Build 35514743433, Docs 35514743431, Portability 35514743303 green on b1db039. Re-review rereview-A8-r2 (opus) dispatched over rounds 2 and 3.
- Honest gap recorded by the implementer: FAILED-KEEP-BOTH (both renames failing) is reachable in production but unreached by any test, for the same structural reason the second rename cannot be forced to fail; named in both file headers.
Task A8: fix rounds 2-3 re-reviewed clean (rereview-A8-r2, opus): all verdicts ADDRESSED, every replacement ordering walked; the copy ends up neither old nor new only in FAILED-KEEP-BOTH (intended, both trees named) and when DEST never existed. 5 Minor found.
- Ruling R50: fix round 4 is A8's last (task-A8-fixround4.md) — the regression where a regular-file DEST is silently destroyed, the FAILED-KEEP-BOTH message recommending a manifest-less copy, the crash window between the swap and the manifest (keep the backup until the manifest is on disk), the leftover-backup check moving beside the occupant guard, two stale docs sentences, and the self-test taxonomy missing the replacement's own messages — cost if wrong: one short round.
Task A8: fix round 4 commits 6990dcf..f7a89f3 — the manifest moved inside the staged tree, so the new copy is whole before the first rename and no crash ordering loses the old copy; IS_DIRECTORY guard for a regular-file DEST (proved: without it the tool turned someone else's file into a 214-file directory and reported success); leftover-backup refusal moved to the caller; docs and taxonomy corrected. Self-test 31 cases. CI Build 35516528273, Docs 35516528296, Portability 35516528221 green on f7a89f3. Re-review rereview-A8-r3 (sonnet) dispatched.
Phase A gate: /code-review pass 1 of 4 dispatched over 4abb140..6dda244 (A1 framework, A2 docs/CI, A3 base/log/cli, A3b provenance). Passes 2-4 follow: 6dda244..a92e0e9 (A4 platform), a92e0e9..e45f730 (A5 async + A5b rename), e45f730..c4a083a (A6 net).
Task A8: fix round 4/5 (7 addressed, 0 open; commits 6990dcf..f7a89f3; re-review rereview-A8-r3 sonnet clean, with an independently derived crash-ordering table)
Task A8: complete (commits 4be64a5..f7a89f3, review clean after 4 fix rounds)
PHASE A COMPLETE: A1-A8 all reviewed clean. Filed core-cpp#25 for the untested symlink DEST (pre-existing, not a regression).
Phase A gate pass 1 (4abb140..6dda244) found 15 defects in base/log/cli, all inherited from contour's crispy and all still present at HEAD: CLI.cpp:611 unsigned underflow (--help segfaults in a narrow terminal or with a long option), CLI.cpp:841 (~4 GB allocation from a verbatim placeholder), CLI.cpp:597 (text[SIZE_MAX]), Utils splitKeyValuePairs reading past the view via strlen, Utils.cpp threadName resize(SIZE_MAX) plus a leak, App screenWidth accepting ws_col==0, Flags::operator&= doing the opposite of operator& (and no Flags_test.cpp exists), Base64 decodeLength's dead scan, Escape not round-tripping, FNV hashing padding bytes, toLower/toUpper UB on high-bit bytes, readFileAsString's trailing NULs and ANSI path, installLogging's destruction order silently redirecting logs to stdout, isStdErrTty hard-coded true on Windows (also a platform-rule violation), and cli::parse throwing where its contract says nullopt.
Task A9: dispatched impl-A9 (opus) over task-A9-gate1.md, test-first, with core-cpp#13 explicitly out of scope.
Phase A gate pass 2 dispatched: /code-review 6dda244..a92e0e9 (A4 platform).
CompileCache re-sync: complete (commit b505db8). fastcached moved eb9c9c68 -> 5a9dca04, adding FASTCACHE_AUTO_INSTALL_HOST_{SYSTEM,PROCESSOR} and an _fc_auto_install_host() helper; behaviour unchanged when unset. FetchTransferBound.cmake was byte-identical and untouched. README and provenance record the new SHA; NOTICE deliberately left, since its block is the original-import record shared with CPM.cmake. Downstream 35517620663-era run green, Build success on b505db8 — the nightly drift failure is resolved.
Phase A gate pass 2 (6dda244..a92e0e9, platform) found 15 defects: an InMemoryFileSystem use-after-free (cached get-area pointers into a string that reallocates on write), a MessageQueue data race on _wakeup, SignalHandler::restore leaving a dangling Wakeup*, WindowsEnvironmentProvider carrying the exact empty-vs-unset bug A4 fixed in LiveEnvironment (it hand-rolls instead of delegating), EnvHelper deleting a variable when restoring an empty value on Windows, Windows SystemPipe write blocking (the never-stall guarantee is POSIX-only) and its unclamped int cast, makeLoopbackPair accepting any local connection, isExecutableFile accepting a symlink to a directory, GlobMatch's unreachable bracket branch, PathUtils' lossy generic_string, createDirectory's wrong diagnosis for an existing directory, rename discarding recaseError, bool parameters in the public FileSystem interface, and TestEnvironmentProvider's namespace not matching its directory.
Task A10: dispatched impl-A10 (opus) over task-A10-gate2.md, running concurrently with impl-A9 on disjoint files, told to rebase before pushing. Finding 14 closes the plan's standing bool-parameter item for this module.
Task A9: implementer DONE; commits 3af2245..2e68c34 (pushed within b505db8..1556119); all 15 findings fixed test-first plus 7 lower-severity items; 3 new test files (Escape, Flags, App); finding 15 resolved by making the documentation true for cli::parse and the return true for its two bool-returning callers, leaving the reason where App::run can print it. review-A9 (opus) dispatched.
- Controller fixed a clang-cl build break impl-A10 had committed (SystemPipe.cpp:388, FIONBIO signedness) as 1556119, because it blocked impl-A9's push and the tree was clean; both agents told. impl-A10 now also builds clangcl before pushing.
- macOS red on both jobs is impl-A10's FileSystem_test.cpp:472: the fixture creates two NAME_MAX directories differing only in case, which APFS folds into one, so the setup REQUIRE fails before the assertion under test. Diagnosed and routed with the fix (detect case-insensitivity, SKIP with the reason, as the NAME_MAX skip two lines below already does).
- Ruling R51 (A9's two deferred items, to fix in its next round): Times2D::operator[] returns the same type its value_type declares, and joinHumanReadableQuoted's separator stops being a deducible template parameter so its default is usable. Both are public-API changes that cost nothing before v0.1.0 and are breaking after it; no Breaking CHANGELOG entry, per R36, since nothing shipped.
Phase A gate pass 3 dispatched: /code-review a92e0e9..e45f730 (A5 async + A5b rename).
Task A9: review review-A9 (opus) — all 15 gate findings FIXED, but the round introduced a Critical regression: CLI.cpp:598-604 makes --help hang forever on any help text with a space before a line feed (confirmed against the built library, 20 s to a timeout kill), because the index advances by the trimmed chunk's length while the skip loop skips only line feeds. Plus 3 Important (five breaking changes recorded under Fixed instead of Breaking; readServerFileAsString's Doxygen promising a contract file_size() throws out of; the LogSink #ifdef) and 5 Minor.
- Ruling R52: fix round 1 covers all of them plus R51's two API items (task-A9-fixround1.md). On the LogSink item I ruled for the split: platform.md allows a module to host the interface itself with posix/ and windows/ sources, which needs no new module edge and is what R40 does everywhere else — so core::log is not an exception.
Phase A gate pass 3 (a92e0e9..e45f730, async) found 15 defects. Critical: whenAny's parent-to-child cancel bridge can destroy the awaiter — including the StopSource whose request_stop() is on the stack and the callback executing — from inside that call; our fallback survives by holding a shared_ptr copy, so the std::stop_token branch (libstdc++, MSVC) is a use-after-free, and the existing test cannot reach it because its fake event registers no stop callback. Important: core::async links no threads although its fallback uses mutex/condition_variable/this_thread, so a consumer linking only core::async on a fallback platform fails to link (our binaries hide it via testing_main -> core::base); whenAny throwing away a winner that already ran, against async-and-net.md's "the data wins"; the variadic overloads accepting lvalues they cannot compile; and the lowercase-namespace gate checking only a file's first namespace while claiming every segment.
Task A11: dispatched impl-A11 (opus) over task-A11-gate3.md — 12 items, with Task<T>'s null-handle default, WhenAll's duplicated exception capture and the runner copy-paste deferred to B1 (plan amended).
Task A10: implementer DONE; 15 commits ed1e9fe..3cdb0ce; all 15 findings fixed test-first (finding 1 an ASan heap-use-after-free, finding 2 a TSan data race before their fixes); finding 14 named WriteMode{Truncate,Append} and OverwritePolicy{Refuse,Replace}, both under Breaking with migrations; CI Build 35522418404 and Portability 35523249853 green on 3cdb0ce, all 24 jobs. Filed its three deferred items as core-cpp#26 (path.string() in ~20 error messages, which MSVC throws on), #27 (InMemoryFileSystem's two divergences from the native backend) and #28 (inheritable Windows sockets). Routed its hygiene-hole finding to impl-A11 (the rule derives the expected namespace from the first directory segment only, so a file under platform/testing/ declaring core::platform passes) and its report of a missing provenance row to impl-A9.
- Ruling R53 (A10): add an injectable rename primitive to NativeFileSystem, defaulting to today's behaviour, and test the two-hop recase path — the direct rename failing, the second hop failing (the caller must get the second hop's error), and the rollback failing after that. Reason: that code can strand an entry under <name>.recase-N and its rollback can fail, yet modern APFS and NTFS rename case natively, so CI reaches it on no volume at all. A10's cc27c2a already added a success-path case that runs on both kinds of volume — cost if wrong: one seam, and A10 reports back if it grows beyond a modest change.
Task A10: review review-A10 (opus) Approved — all 15 FIXED against their failure scenarios, 0 Critical, 2 Important follow-ups in InMemoryFileSystem (the path fix covers only the write half of the key space; the streams still hold a raw pointer into the file map, so writeFile/remove/rename dangle it). Ruling R54 folds both into the R53 round.
- R53 landed as a518402: RenameFunction injected at construction with a nativeRename() default, no new virtual and no change to the FileSystem interface, plus three scripted cases (both hops succeed; second hop fails and the caller gets its reason; rollback fails and the message names both spellings and the temporary). The middle case was proven non-vacuous by reverting the fix locally. cc27c2a's volume-independent case stays: it pins the contract on real filesystems, the scripted one pins the implementation.
Task A9: fix round 1 done (through af392a6) — the Critical --help hang fixed by advancing the index by what a chunk consumed rather than what it emitted, with the rest of the wrapper swept for the same shape; five entries moved to Breaking with migrations; readFileAsString answers a missing file as empty; the terminal query split into log/posix and log/windows with no #ifdef left; R51's two API items done (Times2D::operator[] returns the tuple, matching *it and value_type, which uncovered that Times::size()/operator[] had never been instantiated and do not compile under -Wsign-conversion).
- Ruling R55: escape() gains [[nodiscard]] in the same Breaking entry (a third is no worse than two, and the inconsistency would be the surprise), LogSink.cpp's processId() #ifdef gets the same posix/windows split as the terminal query, and Times::size()/operator[] are fixed and instantiated by a test so they cannot rot again.
Task A10: fix round 1 (R53+R54) pushed as ab623bc — pathFromKey() is the only way out of the key space, plus two sites the review did not list (addSymlink narrowed its target on the way in, so a link to a non-ASCII name resolved to nothing); _files holds shared_ptr<std::string> with a single fileAt() creator, and MemoryIOBuf now caches no pointer at all (integer position, empty get area, every read through underflow/uflow/xsgetn), because a get area spanning the string is read without entering the class. REDs: 8 of 11 assertions on cl-debug for item 1; three ASan heap-use-after-frees for item 2.
- Ruling R56: openRead() shares too. The fake stands in for the real filesystem, where a read descriptor sees later writes; a snapshot is an invisible divergence, and after this round a stream already survives a remove and follows a rename, so a frozen read stream would be the odd one out. Its own commit and case, and core-cpp#27 gains the divergence list.
- Concurrency friction noted: three agents share one git index, so A10's CHANGELOG and provenance edits were swept into another agent's commit (content intact, attribution wrong). Also a gcc-release "double free" that was a file-swap RED demo across a class-layout change, not a defect — the lesson goes into .agent/rules/testing.md.
Task A11: review review-A11 (opus) Approved — all 12 findings plus the added hygiene item FIXED, 0 Critical, 0 Important defects; the reviewer independently walked the cancellation ownership on both StopToken branches and confirmed the test reaches the bug by the awaitables' own route. Two process items: f28462f (the second hygiene hole) was local-only and un-CI'd, and the completer deferral had allowlist rows without an issue. Filed core-cpp#30 (the eight src/core/tui/completer/ files declare core::tui, not core::tui::completer — rename or flatten, before v0.1.0 either way, since it is public API) and told impl-A11 to push, cite it in the rows and correct the report.
- Ruling R57 (core-cpp#30): rename src/core/tui/completer/'s namespace to core::tui::completer rather than flattening the directory — runtime/ and testing/ in the same module are already namespaces, docs/modules/tui.md has been promising that spelling, flattening moves five public headers' include paths and loses a real subsystem boundary, and endo and tuidu rewrite their namespaces wholesale when they migrate. impl-A11 does it, deleting its eight allowlist rows in the same commit, before v0.1.0 — cost if wrong: a mechanical rename in the other direction.
- impl-A11 also fixed the deeper hygiene rule (f28462f, c4c3153, d70c31e): the expected namespace follows the whole directory path, skipping the layout directories core_cpp_add_module() holds private headers in, with self-test cases for both shapes (25 cases) and a clean 401-file tree scan.
Task A10: fix round 2 — item 1 settled empirically rather than documented: a libstdc++ probe showed ifstream/fstream accept unget(), putback(same) and putback(different), the next read returns the put-back character and the file is unchanged, so pbackfail() now matches; MSVC's answer was established by running one script over both backends and comparing (the standalone probe died with STATUS_STACK_BUFFER_OVERRUN), which passes on cl-debug and clangcl-release. RED: with pbackfail() disabled the native assertions pass and fromModel == fromNative fails. Minor 3 took the preferred route (copyFile, addFile and createTempFile go through fileAt(), so copyFile onto an open destination overwrites in place as the native backend does), while rename()'s moves were kept deliberately: they preserve identity, which is POSIX rename.
- Process lesson: three agents share one working tree and index, so an unqualified `git add -A`/`commit -a` sweeps another agent's in-flight edits into the wrong commit (happened twice, content intact, attribution wrong). Told impl-A11 to commit with explicit pathspecs; put this in future concurrent dispatches.
- R57 done as cfff6ee: core::tui::completer, with the eight allowlist rows deleted in the same commit and core-cpp#30 closed; ~360 unqualified references handled by five `using` lines rather than 360 edits; 1010/1010 tui cases on clang-debug, ASan and gcc-release, 1014+1 skipped on cl-debug.
- I was wrong in R57 about the CHANGELOG section: impl-A11 filed the namespace rename under Breaking with a migration note, not Changed, because the file's own preamble says every break goes there and whenAny's rename already sits in that section. Confirmed its choice; a consumer scanning Changed would have missed the entry that breaks their build.
Phase A gate pass 4 dispatched: /code-review e45f730..c4a083a (A6, core::net: the event loop, sockets, TLS and the HTTP server, plus its fix round).
- impl-A11 audited its own history and found it caused both sweeps (26a820a took nine of A9's provenance rows; cfff6ee took A10's row and CHANGELOG entry). Its diagnosis is the useful part: explicit pathspecs protect against the wrong files, not the wrong hunks in a file all three sessions edit. The hunk-level procedure (read every hunk, `git apply --cached` a trimmed patch, confirm with `git diff --cached`) is now in global-constraints.md for future concurrent dispatches.
Phase A gate pass 4 (e45f730..c4a083a, net) found 15 defects. The serious ones: HttpServer's parseHead continues past a bare-LF blank line, so we and a front-end can disagree where a request ends (request smuggling), in a parser that already rejects Transfer-Encoding for that reason; WindowsListener clears a readiness event Winsock will not raise again, so the listener can stop accepting permanently (WindowsSocket already solves this with WSAEnumNetworkEvents); Tls passes unchecked BIO_new results to SSL_set_bio, so an allocation failure crashes instead of returning null as documented, and flushOut reports success after dropping ciphertext, hanging both ends; PosixSocket::write reads a stale errno on a zero return, so it spins, retries forever or fabricates an error. Plus a WriteQueue null dereference in a noexcept teardown, AsyncBufferedReader's stale scan offset, the unmet isClosed contract, the two poll backends disagreeing about FdInterest::None (untested by the parity suite whose job that is), a non-idempotent detach in the scripted source, a CLOEXEC window on the listening socket, a missing <stdexcept>, a REQUIRE inside a whenAll arm that turns a red into a hang, an RLIMIT restore with no RAII, and four unchecked std::expected dereferences in Tls_test.
Task A12: dispatched impl-A12 (opus) over task-A12-gate4.md — all 15 fixed here rather than deferred to Phase B, so the rewrite inherits correct behaviour and the tests that pin it. Findings 8 and 9 need a decision between two defensible behaviours; the implementer makes it and records it.
PHASE A GATE: all four passes dispatched; 60 defects found across them.
Task A12: implementer DONE; 13 commits e49dc81..36e6bf3; 9 findings with a captured RED (7 cases / 10 assertions before, 130 cases + 684 assertions after), 4 defensive branches fixed by inspection with each commit naming the branch it could not drive; decisions — isClosed latches on EOF via a separate flag, and FdInterest::None mutes on every backend (its documented meaning; 2 of 4 already did, and epoll's level-triggered EPOLLHUP would otherwise spin the pump), both under Breaking with migrations, as is WriteQueue now throwing on a null socket. Windows listener race verified by driving a listening socket into the state the race ends in: the control arm shows WSAResetEvent leaving the event dark for 250 ms with a connection pending.
- Ruling R59 (A10's put-back case, red on macOS and FreeBSD): libc++ refuses putback() of a character the file does not hold, while libstdc++ and MSVC accept it — and the standard is on libc++'s side, since only one put-back is guaranteed. Keep the model permissive, compare model against native only where the standard guarantees an answer (unget and putback of the character just read), assert the model's own behaviour for the differing case without comparing, and record it in core-cpp#27. Lesson: a probe of two implementations measures those two, not the standard.
Task A11: re-review rereview-A11-r1 (sonnet) clean — both the second hygiene hole and R57's rename ADDRESSED, all eight allowlist rows gone, the four rule shapes verified, and no name in core::tui collides with the nine completer types the new using-directives pull in (every apparent free function is a static member needing qualification). Asked for one last line: drop `backend` from the private-directories exempt list, since R40 put the backends in linux/bsd/posix/windows and the plan creates no backend/ directory.
Task A12: review review-A12 (opus) Approved — all 15 FIXED at their named file:line, both judgement calls right and consistent across four backends and three socket classes. 3 Important: the finding-2 regression guard hangs rather than fails (1500 s, then a Timeout naming nothing); finding 13's sweep missed the sibling shape (an arm returning early without stopping its sibling) at five sites; and the head parser still trims whitespace before a header colon, which RFC 9112 makes a MUST to reject — same class as finding 1. Ruling R60 dispatched, plus two report corrections (six behavioural REDs, not nine; and "no seam exists" for finding 5 reads oddly beside the rename primitive built in the same window).
Task A10: re-review rereview-A10-r2 (opus) — all items ADDRESSED, no new Critical/Important breakage; pbackfail() walked path by path (exact position arithmetic, a different character can never reach the file's storage, rename()'s retained moves are POSIX unlink-on-overwrite). 1 Minor open (the CHANGELOG still asserts the claim libc++ refuted) plus two flags: copyFile has no test in either backend though two documents now assert its behaviour, and an unnamed divergence (unget() after the pushed-back character has been read returns the file's byte, where filebuf returns the put-back one). Ruling R61 dispatched as A10's last round.
- My slip: the diff package I generated for that re-review was missing its CHANGELOG hunks; the reviewer regenerated them. Include the CHANGELOG path explicitly when filtering a package by module path.
Task A11: complete (12 gate findings + both hygiene holes + R57's rename; commits ccdec61..73ec045, re-review clean). Its last commit also narrowed the exempt list to exactly the directories core_cpp_add_module() holds private headers in, with the comment naming that source rather than a list to append to.
- Operational correction from impl-A11: `git pull --rebase` cannot run in this shared checkout at all, because the other sessions always have unstaged work; rebase refuses and stashing would take their edits. All three share one working tree and one local master, so commits already stack and a push is a fast-forward. global-constraints.md now says fetch-and-push, never rebase, never stash, and never format a file another session is editing.
- Its narrow-staging procedure caught its first real case: two pending CHANGELOG hunks, one its own and one the platform session's; it staged only its own and left the other untouched in the tree.
Task A9: re-review rereview-A9-r1 (opus) — all findings and both rulings ADDRESSED, no new Critical/Important breakage. It verified readFileAsString's contract empirically (directory, missing path, unreadable file, FIFO, /dev/null), checked the POSIX/Windows/Emscripten build graphs each select the right platform pair, and proved the help wrapper terminates by sweeping 9,175,005 inputs through the lifted function. 5 Minor open; Ruling R62 dispatched as A9's last round: the surviving localtime #ifdef goes into the existing log/posix and log/windows files so the three claims about no #ifdef become true; the two new Times.hpp [[nodiscard]]s join the entry that reads as exhaustive; the trailing-space rendering change gets recorded; A9's entries move to Breaking so the CHANGELOG stops answering the same question two ways (the preamble wins — my earlier Changed ruling was wrong); and the cli test gets a TIMEOUT.
- My packaging slip, twice: review-A9-fix.diff omitted CHANGELOG.md, Base64.hpp and Utils_test.cpp, and review-A10-fix2.diff omitted its CHANGELOG hunks. When filtering a review package by module path, list the shared files explicitly or diff without a path filter and let the reviewer scope itself.
Task A10: round 3 committed as ec7c41d — the CHANGELOG now states what the standard guarantees and names which libraries differ; copyFile gained a comparison case over both backends for the three answers [fs.op.copy.file] pins down, plus a model-only in-place-overwrite case that is red without the fileAt() change; and the unget-after-put-back divergence is recorded rather than matched, because filebuf's behaviour comes from its own slot, the standard describes neither, and libc++ refuses the put-back that creates the situation — matching would mean copying libstdc++'s internals and calling it a contract, the same error R59 corrected. R59 confirmed green on macOS and FreeBSD (Build and Portability on f4cdb08), so that red is closed.
- Lesson to carry into any task that changes documented behaviour: a correction is not finished until every document that carried the wrong version carries the right one. Three artefacts were fixed within the hour here; the fourth was invisible because it had been written earlier.
Task A9: round 3 — the localtime #ifdef moved into log/posix and log/windows behind a private header, so grep -c ifdef is 0 for every file at core::log's root and the three claims are true; the split exposed that nothing asserted the timestamp's content, only that it can be suppressed, so a failed conversion would have rendered zeroes and passed the suite (there is a case now, RED-probed by zeroing localTime()); the trailing-space rendering change turned out to emit a line break and a continuation indent per trailing space, so two renderings changed and both are recorded; entries moved to Breaking; TIMEOUT 60 on core-cpp.cli.
- Ruling R63: add a default TIMEOUT to core_cpp_add_test() now, with a per-test override and the number in one place sized against a loopback or sanitizer binary on a loaded runner. One framework file, every binary at once, including ones written after us; the other sessions are in src/, not cmake/.
- Ruling R64 (collision on cmake/CoreCppTargets.cmake): impl-A9 found impl-A12 had that file uncommitted, adding the TIMEOUT keyword — the override half of R63 — and stopped rather than clobber it. My premise that the other sessions were not in cmake/ was stale and unchecked. Option 2: impl-A12 lands both halves in one commit using impl-A9's measurement (slowest binary core_cpp_add_test registers is core-cpp.tui at 11.1 s under a sanitizer; default 300 s, about 27x, so a hang is named in five minutes instead of ctest's 1500; vendor-selftest at 87 s is registered by tests/ and out of reach). impl-A9 dropped R63 and closed.
Task A9: round 3 green on every local check (base/log/cli 3/3 and hygiene 8/8 on three Linux presets, emscripten 16/16 under node, clang-tidy 0 findings, 22/22 on both Windows presets); pushed inside 50c3c2d, with Build 35537221014, Docs 35537221002 and Portability 35537251076 dispatched on that head so FreeBSD covers this round rather than an earlier one.
Task A9: complete (15 gate findings + a Critical regression of its own + 3 rulings across 4 rounds; commits 1968931..50c3c2d; Build 35537221014 and Portability 35537251076 both green on 50c3c2d).
Task A10: complete (15 gate findings + R53/R54/R56/R59/R61 across 3 rounds; commits ed1e9fe..b70fab9, all in origin/master; the last round rode 50c3c2d's green Build 35537221014 and Portability 35537251076, which is the FreeBSD/macOS run R59's fix needed).
PHASE B begins. Task B1: dispatched impl-B1 (opus) over task-B1-dispatch.md — fastcached 0708dd54's Task/ParkedWork/IExecutor/ResumeOn/ThreadPoolExecutor/AsyncQueue grafted onto contour's Task, plus the three defects A11's gate deferred here. Runs alongside impl-A12's fix round; disjoint modules (async vs net).
- Ruling R65: core::async stays KIND INTERFACE and ThreadPoolExecutor is header-only (its upstream .cpp is 92 lines of worker loop). Reason: the module table in the spec, AGENT.md, cmake/CoreCppModules.cmake and docs all say INTERFACE, "std only"; STATIC changes what every consumer links and what the WebAssembly subset builds. Cost if wrong: one kind change plus a CHANGELOG entry, and the implementer reports rather than deciding it alone.
- Ruling R66: DetachedTask.hpp and SyncRun.hpp get their own headers, as the plan names them, although upstream keeps both inside Task.hpp — every other piece of this module's vocabulary is already a header of its own and the public API is the FILE_SET.
Task C0: dispatched impl-C0 (opus) over task-C0-dispatch.md, out of plan order — it touches only tools/migrate/, so it runs beside impl-A12 (net) and impl-B1 (async) with no file overlap, and the consumer PRs need the table long before Phase C starts.
- Ruling R67: the codemod tests run under stdlib unittest, not pytest (installed on neither this machine nor the CI images), registered as a ctest with label hygiene so they actually run. Cost if wrong: a pip install and one line of ctest.
- Ruling R68: renames.json is validated against the delivered headers by a checker C0 writes and registers under hygiene, and from now on every task that renames a public symbol updates the table in the same commit (added to global-constraints.md). Reason: six consumers read that table; drift there costs six debugging sessions.
- Ruling R69: semantic_rename.py's libclang test skips loudly by name where the bindings are missing, and one CI job installs them and runs it for real — a tool whose only test always skips is untested.
- Carried into the dispatch: core::coro does not exist (A5b made it core::async), Generator is core::Generator in base, and the completer is core::tui::completer (R57) — three places where the spec's prose is older than the tree.
Task B13a: dispatched impl-B13a (opus) — B13's first bullet (core-cpp#24, the highlighter registration seam) pulled forward, because src/core/tui/ is free while the rest of Phase B rewrites net and async, and it removes public API so it has to precede v0.1.0 either way.
- Measured, rather than assumed, what B12b will cost: `git log <pin>..origin/master -- <path>` over contour's src/crispy and endo's src/tui, src/platform and src/testing returns **0 commits** on all three. The non-fastcached half of B12b is a verification and a record, not a port, so it is not worth a parallel implementer now.
Task A12: fix round 1 done (edb1340..50c3c2d) — the head parser refuses whitespace before a field-name colon and an empty field name (RED: all four spellings parsed as valid before, all four are 400 now, with a companion case pinning that whitespace after the colon still works); the listener guard runs under a 10 s budget and fails naming what it waited for; the sibling-stop shape fixed at all five sites, the two TLS ones through post() because the listener must die on its own loop's thread. R64's both halves landed: core_cpp_add_test() takes TIMEOUT and defaults to 300 s, with the measurement in the comment.
- The finding worth keeping from that round: A12's first sentinel for the listener guard PASSED while the defect was present, because whenAny cancels the loser and a cancelled accept() resolves with an error rather than throwing, so the accept arm returns cleanly on the way out. The timer's own flag is the sentinel now. A sentinel that the bug's own cleanup path can set is not a sentinel.
- And the sweep shape, for whoever writes the next combinator test: "an arm that gives up early while a sibling is parked on something only that arm could have produced". The first sweep looked for assertions inside an arm; this one looks for exits. The question at every early return is "if I return here, what wakes my sibling?"
- The staging hazard bit from the other direction: A12's fix-round CHANGELOG hunk reached master inside another lane's commit (78824d1), which staged the file whole while A12's edit was in the tree. Nothing was lost or altered. The discipline protects the file from you; it does not protect you from a lane that stages it whole. Both halves of the rule are now in global-constraints.md.
Task A12: re-review rereview-A12-r1 (opus) dispatched over edb1340^..50c3c2d, scoped to the three R60 items plus R64's default TIMEOUT, and told explicitly to read CHANGELOG.md from the tree because this round's entries reached master inside another lane's commit and are not in the diff. Asked it to reason the sentinel's cancellation path itself and to run its own sweep for a sixth early-return site, since two sweeps have already missed sites in this file set.
Task B2: dispatched impl-B2 (opus) over task-B2-dispatch.md — the merged NetError vocabulary (contour's ∪ fastcached's, with BadFileHandle -> BadHandle and Other -> SystemError), fastcached's isDeadlineExpiry with the #824 reasoning, and three decisions it has to make and record: the toString() rendering, the answers for the three new codes, and whether Ok stays in an enum that sits in a std::expected's error channel.
Task A12: re-review rereview-A12-r1 (opus) — items 1, 2 and 4 ADDRESSED, item 3 PARTIALLY: the five are right and its own sweep found a sixth. It reasoned the sentinel's cancellation path independently and confirmed it (DelayAwaiter::await_resume throws on a stopped token, WhenAny requests the child stop before the loser resumes, so timedOut is unreachable on the cancelled path, and nothing swallows the cancellation there the way AcceptLoop does between accept() and its caller). It also measured the 300 s justification empirically (core-cpp.tui 9.78 s on cl-debug, 11.10 s under asan/ubsan, matching the comment) and read three reconfigured trees' CTestTestfile.cmake to confirm all 12 registrations are bounded with the explicit overrides winning.
- Ruling R70: the sixth site (posix/UnixSocket_test.cpp:121-122) gets its own round now rather than folding into a later one — one branch plus a listener close, and it is exactly what the round was sent to close; leaving it means the next gate pass pays for the whole cycle again. Cost if wrong: one short round.
- The generalisation, which is the useful part: **a sibling's shape decides whether an early return is safe.** connectAndProbe's comment says the server "sees this socket close and finishes", but echoOnce is a draining loop -- read returns 0, it `continue`s rather than returning, and goes back into accept() with *served still false. A sibling that co_returns on EOF finishes on its own; one that continues on EOF does not. The question is not "does my peer observe my close" but "does observing my close end my peer's flow". A12 had written the correct pairing 300 lines away (unixProbe closes the listener on that branch, facing the same sibling shape).
- My own slip, caught by the reviewer: I verified R64's default TIMEOUT by grepping the working tree and read it as landed. It was uncommitted at that moment; A12 committed it as a13070c afterwards. Grep answers "is this written", not "is this in history" -- check `git log -- <file>` or `git show origin/master:<file>` when the question is whether something shipped.
- A12's deliberate deviation from R64, and it was right: I said "one commit", but the TIMEOUT keyword had already shipped as f5ccf0e by the time my message arrived, so the default landed as a follow-up (a13070c) rather than an amend. Published history is not rewritten to satisfy a controller's phrasing. Confirmed. It verified the result from the generated CTestTestfile.cmake on both platforms rather than from the call sites, which is how it established that core-cpp.cli's tighter 60 wins (its set_tests_properties runs after the call).
- Cross-lane courtesy that is now house practice: A12 found src/core/async/ParkedWork.hpp failing the pinned clang-format and did NOT format it -- rewriting another lane's work-in-progress under them is the same mistake as staging their hunks. It reported instead; I relayed it to impl-B1 so they hear it before push time rather than discovering it at the gate.
- Controller check while the lanes ran (read-only, no edits in the shared tree): audited every `## Open work` entry in .agent/rules/ against GitHub. Eight entries, leading references core-cpp#5,6,7,8,9,10,11,12 -- all still open, so no rule has gone false. The README is honest about the missing gate rather than claiming it ("until core-cpp has that check (core-cpp#12), keeping entries true is a review question here"), so this is a manual pass standing in for the checker until B13 writes it. core-cpp#123 in README.md is an illustrative format example, not a live reference.
Task A12: fix round 2 done (dda4050, 98937f7) — the sixth give-up site fixed with a real RED (forced branch, pre-fix: no failure, runs until SIGTERM at a 45 s timeout; post-fix: fails in 0.107 s on REQUIRE(served)), and A12 corrected 50c3c2d's "every early return that remains is accounted for" claim rather than quietly fixing the site. Its own sharpening of the lesson is better than mine: *the comment it wrote there is what made the site look swept* — the sweep's question was right, the answer was wrong, because it checked whether the peer would OBSERVE the close rather than whether observing it would END the peer's flow. Item 2 is the same rule with the opposite answer (unixEcho's client needs no listener close, because returning destroys its socket and echoServer co_returns on the EOF); it fixed it anyway rather than arguing reachability — "thin is not examined". Build 35538763899 green on a13070c, all 24 jobs, so the default timeout is covered.
Task A12: re-review rereview-A12-r2 (sonnet) dispatched over dda4050^..98937f7 — 35 lines, so a cheap model is proportionate; its one real job is to check item 2's reachability claim against echoServer's actual shape, since that is the same reasoning that was wrong last round.
- Routed to impl-B1 immediately, because it is GCC-only and a clang-only local loop will not see it before CI: gcc-release does not build on src/core/async/IExecutor.hpp:56, "'virtual void IExecutor::submit(ParkedWork)' was hidden [-Werror=overloaded-virtual=]". That is fastcached#1041 by name, which the spec's §2 item 6 already requires `using IExecutor::submit;` in every derived class for. Told B1 to make its planned compile-time overload-selection check fail first rather than accept "the build stopped erroring" as evidence — clang and MSVC say nothing here, so the next derived class that forgets the using-declaration reintroduces it silently.
Tasks B2, C0, B13a all pushed while A12 closed: ddddc67 (the rename table and codemods), 8a88ce0 + e6dd748 (the merged NetError vocabulary and its docs), f61d57b + d0345d0 (the tui registration seam and its fixtures). R68's discipline paid on its first day: C0 followed B2's rename with 748142f, "the NetErrorCode::Other rows name the target B2 delivered" -- the table tracked the API inside the same hour, without me routing it.
Task B3: dispatched impl-B3 (opus) over task-B3-dispatch.md — the pivot from contour's token-registry EventSource to fastcached's callback-dispatching IoBackend, with Rule 1 (backends dispatch, the loop resumes) as the load-bearing property, the HostDrivenBackend tested on every OS rather than only under Emscripten, and an explicit scope boundary: adapt EventLoop minimally, do not write B4's answer, and stop and report if the adaptation is larger than the backends themselves.
- Flag for B3's review, noticed while reading B2's merged enum: NetErrorCode gained a `Last` sentinel for table-driven coverage. It is useful, but it also has to be handled in every `constexpr switch` that deliberately has no `default` — the property that makes a new code a compile error everywhere. Worth checking that the sentinel did not buy iteration at the cost of that.
Task C0: implementer DONE_WITH_CONCERNS; one commit ddddc67 plus its follow-up 748142f; 51 stdlib-unittest cases and the drift gate, both ctest under label hygiene, green on WSL clang-debug, Windows cl-debug and CI Build 35539983375 (all 23 jobs), with the style job reporting "438 rows, 412 with a delivered core-cpp target, 8 pending, 0 failures".
- **The most valuable finding of the session so far, and nobody would have reasoned it out:** three sessions share one `.git/index`, and that is sharper than sharing one working tree. C0 found the index already holding a second lane's STAGED CHANGELOG.md on top of a third lane's unstaged edits to the same file, and a plain `git commit` there would have committed a tree that DELETED all of tools/migrate/. It committed through a private GIT_INDEX_FILE, then had to repair the shared index it left stale. The rule is now in global-constraints.md and sent to all three other lanes: never a bare `git commit`, `git commit -a` or `git add`; `git commit --only -- <paths>` for exclusive files (reads the working tree, disregards what is staged, leaves other lanes' entries alone); a private GIT_INDEX_FILE for hunk selection in a shared file; `git show --stat` after every commit.
- Ruling R71: pin ruff, as C0 recommended and correctly declined to decide alone. tools/migrate/ roughly triples this repository's Python, and "a gate that does not report reads as passed" is not a C++-only rule — a style checked by hand is a style that drifts the first time nobody has time to check it by hand. Scoped tight: pinned in scripts/tool-versions.py with the same wrong-version refusal as clang-format, a `ruff format --check` step beside it over scripts/ and tools/, line length 110 so one number governs the repository, CHANGELOG under Added, and no CoreCppDependencies row because nothing here enters a consumer's build. C0 does it as round 2.
- C0's libclang call was right and stands: pinned in the workflow rather than tool-versions.py, so `--check` does not start failing for developers who have no reason to install it.
- My dispatch error, caught by C0: I gave tuidu's cli drift as optionList/flagStore/helpDisplayStyle; src/tuidu/Cli.cpp spells them option_list/flag_store/help_display_style. It carried both rather than silently picking one.
- Ruling R72: the tests/ registrations get bounds too (vendor-selftest at 87 s among them), and impl-A12 does it rather than a fresh agent — it has the measurement and the reasoning loaded, and routing a ten-line change elsewhere costs more than doing it. Per-check numbers measured on one Linux and one Windows preset, with a comment saying why they do not inherit core_cpp_add_test()'s 300 (they are not test binaries, their runtimes differ by an order of magnitude, and the function cannot reach them).
- A12 also rewrote minor 3 from a fact into a rule, unprompted and correctly: "is unbounded today" expires and would have been wrong in the other direction once the async lane lands, where "a bare add_test() keeps ctest's default unless it sets its own bound" is true in every future. It closes with "adding a bare add_test() means deciding its bound with it", turning a description into an instruction. Carry that shape into any comment written at a gate.
- **Lesson that generalises past this session, and A12 found it about its own process:** the fastcached#1041 report only happened because gcc-release was in its local loop at all. The fastest path through the workflow checklist is the clang presets, and that path would have shown a clean tree and produced no report. -Woverloaded-virtual is diagnosed by GCC and silently accepted by clang and MSVC, so the toolchain that costs the most to keep in the loop is the only one that catches this class. That is the argument for the checklist's ordering that nobody had written down.
- Ruling R73, with the mechanism verified rather than assumed: A12's round-2 commits have no CI run of their own, and neither do several other lanes'. build.yml sets `cancel-in-progress: false` for pushes, but that only protects a run that has already STARTED; GitHub always supersedes a PENDING run in a concurrency group. `gh run view 35540400481 --json jobs` reports zero jobs — it never executed anything. With four lanes pushing every few minutes against a ~20-minute 24-job Build, intermediate heads routinely never run. So a task now closes against a completed green run on any commit that CONTAINS its commits, not one on its own head: master is linear, so a green Build at head H proves everything in H's history builds and passes together, which is the property we actually want. Cost if wrong: we lose per-commit attribution on a red, and bisect locally instead — cheaper than quadrupling CI to run every intermediate head.
- R73's first covering green: Build 35540278214 succeeded on 98937f7, which contains A12's round-2 functional commits (dda4050, 98937f7), B2's merged error vocabulary (8a88ce0, e6dd748), C0's rename table and its follow-up (ddddc67, 748142f) and B13a's first two tui commits (f61d57b, d0345d0). Four lanes' work verified together by one run. Not yet covered: f90aacb (comment reword), aa49c53 (tui), ac9e2e2 (ruff) — ac9e2e2's Build is next in the queue.
- The queue behaviour observed end to end, for the record: exactly one Build runs at a time, it takes ~20 minutes, and every head that queues behind it is cancelled before executing a job. That is roughly three verified heads an hour, each covering everything before it, and it self-heals the moment pushes pause. No workflow change needed.
Task B2: implementer DONE_WITH_CONCERNS; commits 8a88ce0, e6dd748, 748142f; 12 cases / 155 assertions, RED first as a compile failure (the codes did not exist), then three mutations each failing exactly the expected cases and only those — including "a new code with no answer" failing the suite without the test file being edited. Covered green by Build 35540278214 on 98937f7 (748142f is an ancestor, verified by merge-base), all 27 jobs.
- Its three decisions, all recorded with reasons: toString() keeps contour's rendering, because fastcached's printed the enumerator's POSITION -- which this task renumbers, so old and new log lines that read alike would mean different codes -- and because std::format would enter core::net_types, which links nothing and which fastcache-cc links alone in C4. The switch stays defaulted-less with the header now saying why and saying not to add one. Ok stays: it is the zero enumerator cpp-guidelines requires for the absent case and it has live callers outside an error channel in both lineages; the "a constructed object is usable" objection is answered by NetError defaulting to SystemError rather than Ok, and asserted.
- My earlier flag about the `Last` sentinel is answered, and B2 was right to raise it rather than assume: .agent/rules/design-principles.md:97-102 prescribes a trailing Last, precisely because a static_assert anchored on an enumerator by name fires only when nothing is wrong. Handed to the reviewer as something to verify by adding a code and running, not by reading the test.
- Ruling R74: check-renames.py's PENDING arm cannot see an enum-scoped symbol land -- _checkPending looks for a namespace literally named core::net::NetErrorCode -- so such rows would read "not landed" forever, while the DELIVERED arm walks prefixes and finds it. That is "a gate that does not report reads as passed" inside the gate we built to stop drift, and a pending row that can never resolve is worse than no row because it teaches the next person to ignore the pending list. Routed to C0 with the self-test case as the part that matters more than the fix; none of its 51 cases had that shape.
- Recorded for Task B11 (TLS): cl-release-tls cannot be configured on this machine -- find_package(OpenSSL) fails and the pre-existing tree has OPENSSL_INCLUDE_DIR-NOTFOUND too, so it predates any of our changes. CI's windows leg covers it; a local TLS task will need vcpkg's OpenSSL installed first.
- Staging note: B2 landed before the private-index rule went out and worked around two lanes' staged CHANGELOG.md hunks by building a HEAD+mine blob, setting the entry with `git update-index --cacheinfo`, committing, then restoring their staged state exactly (48 lines, 46 insertions, 2 deletions, before and after). Careful and correct, but the private GIT_INDEX_FILE route in global-constraints.md is simpler and leaves no window at all.
- Two unprompted follow-throughs worth recording: B2 landed 73d4569, "toString() says what renders a code's number wrong, not just why words are better" — it took the suggestion to put the enumerator-position hazard into the header where the next person to simplify that function will hit it, rather than leaving it in a report nobody will read. And A12 landed R72 as 9d0bbc3, "every check in tests/ carries its own bound".
- Task B5's dispatch is pre-written (task-B5-dispatch.md), built around the one property that distinguishes a real fix from a faster poll: a cancel must wake the sleeper with ManualClock FROZEN and leave the park table and timer heap empty. An interval-based implementation passes every test that lets real time advance, and a timer left in the heap after cancellation is core-cpp#16's shape.
Task A12: complete (15 gate findings + a sixth give-up site its own sweep missed + R60/R64/R70/R72 across three rounds; commits e49dc81..9d0bbc3). Closes Phase A entirely.
- **A12's correction, in its words, and the most portable lesson of the session: "in a tree three lanes are writing to, an hour-old measurement is not a measurement."** It told me the tests/ checks were unbounded; seven were, but two had been bounded by the tools/migrate lane after the grep it quoted. It caught itself twice today on the same axis -- once on wording with an expiry date, once on a grep with one -- and both times by re-running rather than re-reading.
- Its bounds are measured worst-of-two across clang-debug and cl-debug, and the judgement I would not have specified is right: the three cheapest checks share a FLOOR of 60 rather than a multiple, because at 0.1 s a multiple measures process startup rather than the work. vendor-selftest was the only one that moved between runs (84 s, 93 s), so it gets the most in absolute terms and the least in multiples. The compute-bound premise is stated in the comment because the numbers rest on it.
- Agreed, not overruled: no CHANGELOG entry for R72. tests/ checks are not public API, not a dependency and nothing a consumer observes; and adding a hunk to a file another lane is mid-rewrite in invites the sweep that already took one of A12's.
Task B12b part 1: dispatched impl-A12 (opus) over task-B12b-1-dispatch.md — build the upstream catch-up checker provenance.md's 381 rows need before v0.1.0, self-test it, and run it over the stable rows. Deliberately off the critical path. Two things aimed at A12 specifically: my zero-drift measurement is handed over as a result to confirm or refute, since a directory-granularity `git log` cannot see a file that moved, was deleted, or was touched from outside those directories; and cmake/portable/CompileCache.cmake is supposed to be verbatim with downstream.yml failing on drift, so check whether that nightly diff exists and works -- a gate nobody has watched fail is a gate nobody has watched.
- A12's reframing of R73 is the better argument for it than my own, and replaces mine in the record: Build 35540278214 verified four lanes at once — A12's round 2, the merged error vocabulary, the rename table and the tui seam, all queued behind it. **"Green on a descendant" is a STRONGER claim than "green on my own head", not a weaker one.** I had justified the ruling by what it costs; the real justification is what it buys.
- The operational tell, now in global-constraints.md: a superseded run reports `conclusion: cancelled` with ZERO jobs executed, and in a run list that is indistinguishable from a real failure. `gh run view <id> --json jobs --jq '.jobs | length'` separates them in one line. A12 was one step from citing a cancelled run as evidence, and so was I.
- A12 also corrected the containment picture by checking ancestry rather than assuming: ac9e2e2's in-progress run does not contain 9d0bbc3 (ac9e2e2 predates it), so it cannot close R72 however green it goes. It is re-checking with `git merge-base --is-ancestor` after each fetch so the watch self-corrects as heads move — which is the right shape for a watch under R73.
- Second covering green: Build 35540789314 on ac9e2e2, which adds f90aacb (A12's comment reword), aa49c53 (B13a's tui compile-time detection tables) and ac9e2e2 (C0's ruff pin) to the verified set. B13a's three tui commits are now all covered, across two runs. Still outstanding: 73d4569 (B2's header comment) and 9d0bbc3 (A12's R72), both later than ac9e2e2 -- run 35541190425 on 9d0bbc3 contains both and closes them when it lands.
Task B13a: implementer DONE_WITH_CONCERNS; commits f61d57b, d0345d0, aa49c53; 1023 cases / 3540 assertions green on four presets, with RED captured verbatim at three stages (golden tables 60 == 59 and 70 == 69, 20 compile errors for the registry API, and { "let x" } == { "l", "et x" } for the Markdown fence path). Both covering greens landed: 35540278214 and 35540789314.
- Its best finding is about the gate B13 was going to use: **`git grep -i endo src/core/tui` cannot be that gate**, because the substring matches ordinary English -- `vendored`, `clearToEndOfLine`, `isEndOfStream` are 16 of its 30 hits -- and the 14 word-boundary hits that remain are provenance comments the upstream-sync discipline requires. It proposes the gate the code actually supports (`\b(LanguageId::Endo|registerEndoHighlighter)\b` and `"\.?endo"` empty, plus a word-boundary consumer-name scan excluding comment lines), with its three golden-table cases as the durable half. Handed to the reviewer to test against the tree rather than accept.
- Ruling R75: renames.json gains a `removed` kind, as an INVERSE gate. B13a was right not to add an ordinary row -- every existing kind drives a mechanical rewrite to an existing target, and both removals change the call shape, so a row would generate code that compiles into the wrong thing, where a compile error at the call site is the better signal. But a `removed` row that asserts the symbol is ABSENT buys three things: it catches a re-introduction (nothing would notice today; the golden tables catch the table rows coming back, not the enumerator), it documents the migration for six consumer PRs next to every other rename they apply, and it must be impossible for rewrite.py to treat as a rewrite source. Routed to C0 with R74, same files, same kind of self-test.
- B13a's two open judgement calls both stand: LanguageId::Last is the same question as NetErrorCode::Last with the same rulebook answer (design-principles.md:97-102), and the reviewer is asked to note whether the two tasks answered it consistently; and the golden tables' 128 duplicated rows are the point, not the cost -- the two-place edit is what makes a silently added or removed row a red test.
- d0345d0 (renaming a FuzzyMatch fixture from endo.exe to demo.exe) was off the dispatch's list and it did it in its own commit so reverting costs nothing. Right instinct; the reviewer checks that demo.exe preserves every property the fuzzy-match regression was written for.
Task C0: round 2 (R71, ruff) done as ac9e2e2, covered green by Build 35540789314. Round 3 dispatched with three rulings:
- Ruling R76: drop check_renames_test.py::test_this_tree_passes_the_gate. C0 recommended it and explicitly declined to do it alone, because "removing an assertion while it is red needs someone else to agree it is not just making the red go away" — the standard I want applied to me too. The case for dropping is independent of the current red: a unit test must not take the mutable working tree as its fixture, because that makes its verdict depend on what three other lanes have half-written, and it reports one true fact twice. The tree-level assertion belongs to the ctest, which already has it. Its own commit, reasoning in the message, so it never reads as silencing a red.
- Ruling R77: Python in this repository is snake_case, including tools/migrate/. Our camelBack rule is a rule about C++; every Python reader's expectations are set by PEP 8 and ruff's linter assumes it. Cheap now, expensive once six consumer PRs quote these names in their migration notes.
- Ruling R78: enable ruff's DEFAULT lint set (E4, E7, E9, F) in the same round — undefined names, unused imports, syntax-level errors, nothing stylistic. Together with R77 because the naming rule is what would flag the rename, so it is one churn instead of two; and because a formatter without a linter catches layout while missing an undefined name, which is the class that breaks a codemod in a consumer's tree. Told to stop and report rather than mass-fix if the default set finds more than a handful.
- **The more valuable half of R71, and it came from C0 refuting its own earlier report:** I approved the ruff pin on the argument that a hand check drifts; C0 then showed it had ALREADY drifted (112-, 111- and 114-column lines in scripts/*.py), and that the awk behind its original claim had been truncated by a `head -20` the new tools/migrate lines filled. **"A hand check read through `head` is the check that drifts."** It corrected the report in place rather than quietly reformatting.
- And a bug only running finds: scripts/ruff.py shadowed the `ruff` package it imports to locate the pinned binary, so it reported "no ruff found" on a machine that had it. Renamed ruff-format.py, with the reason in the docstring where the next person to shorten the name will read it.
- B1 has landed its two main commits: b21229d (teardown-safe ownership, detached tasks, executors and AsyncQueue from fastcached) and f72cabc ("one runner and one join behind whenAll and whenAny" -- the third defect A11's gate deferred here, the ~200 lines of near-identical runner/state/awaiter collapsed behind one parameterised runner). Not yet reported. C0 landed R75 the same hour as a8e5212, "a removed symbol is a row that must stay absent".
- Third covering green: Build 35541190425 on 9d0bbc3, which closes 73d4569 (B2's header comment) and 9d0bbc3 (A12's tests/ bounds). Task A12 is now green-covered end to end; Task B2's four commits all are.

=== STATUS as of 2026-09-21, three covering greens in (98937f7, ac9e2e2, 9d0bbc3) ===
Phase A: COMPLETE (A1-A12). Four /code-review gate passes found 60 defects the per-task reviews missed.
Phase B: B1 pushed (b21229d, f72cabc), not yet reported. B2 complete, review running. B3 running (the IoBackend pivot, the long pole). B13a complete, review running. B12b-1 dispatched to impl-A12.
Phase C: C0 complete through round 2 (ddddc67, 748142f, ac9e2e2, a8e5212); round 3 dispatched (R76 drop the tree fixture, R77 snake_case, R78 ruff's default lints). C1-C8 wait on v0.1.0.
Remaining critical path: B3 -> B4 -> {B5, B6} -> {B7, B8, B9, B10, B11} -> B12 -> B12b-2 -> B13 -> tag v0.1.0 -> C1..C8.
Dispatches pre-written and ready: task-B4-dispatch.md, task-B5-dispatch.md.
Rulings so far: R32-R78.
Task B2: review review-B2 (opus) **Approved** — spec compliance PASS (the enum is the plan's union verbatim and in order, both renames applied, isDeadlineExpiry behaviourally identical to fastcached's, all four named tests present), task quality PASS with 2 Important, both in documentation and test-guard, none in the shipped header.
- **I-2 is a real hole in B2's own guard, and the reviewer proved it rather than argued it:** append a code AFTER `Last` with a proper toString case and `static_assert(SystemError < Last)` still holds, -Wswitch is satisfied, and all 12 cases pass green — the new code is invisible to every property the suite advertises. B2's decision #4 closes the append hole only for appends BEFORE Last. That is exactly the trap design-principles.md:96-100 names. The reviewer wrote and ran the six-line fix (scan [CodeCount, 256) for "unknown error"): 156/13 unmutated, fails exactly on the mutation, both directions proven.
- I-1: the CHANGELOG promises a classification that does not ship. AddressNotAvail, HostUnreach and PermissionDenied have ZERO producers in core-cpp today (PosixSocket maps only ECONNRESET/EPIPE/EBADF, WindowsSocket the four WSA equivalents, the ladders only EADDRINUSE/ECONNREFUSED), while upstream fastcached produces all three at five sites — so a migrating caller's `== HostUnreach` branch compiles and is silently dead until B6-B8. The scoping is right; the changelog over-reads.
- Ruling: fold both into one follow-up commit now, not deferred — B3 is about to add code to this module and I-2's reasoning is cheapest while fresh.
- **Ruling R79, the most portable thing out of today's reviews:** the reviewer did not spot-check the 53-site rename, it proved it PURE BY CONSTRUCTION — for each of the 16 non-NetError files, apply only the intended substitution to the pre-image, strip whitespace, assert byte-identity with the post-image. Any unintended edit breaks identity and is named by file, including in Windows paths it could not compile. Routed to C0 to write into .agent/guides/consumer-migration.md, because six consumer migrations are about to run rewrite.py over hundreds of files each and "I read the diff and it looked right" does not scale past the first hundred. It pairs with idempotence: idempotence says running twice changes nothing; this says running once changed nothing but what was intended.
- Fourth covering green: Build 35542004684 on a8e5212, covering B1's two async commits (b21229d, f72cabc) and C0's R75 inverse gate (a8e5212). That run includes gcc-release, so the fastcached#1041 using-declaration fix is verified on the one toolchain that diagnoses it.
Task B13a: review review-B13a (opus) — spec compliance MET, task quality CHANGES REQUESTED: 1 Critical, 2 Important, 5 Minor. Its departure on the 14 endo provenance comments is recorded, argued and correct, and is not a finding.
- **Critical, and the reviewer RAN it rather than reasoning it:** the cross-registry safety claim is false and the test named for it cannot fail for it. Ids are dense from 128 in registration order with no discriminator, so with registry a = toy-then-doll and b = doll-then-toy, b.highlightLine(line, a.find("toy")) runs DOLL's highlighter -- a different language, not unstyled. The documented guarantee "degrades to unstyled, never to a different language" is the recorded justification for the identity choice, and it fails in exactly the two-registry scenario the design advertises; the test asserts against an EMPTY registry, the one case where the claim holds.
- Ruling R80: retract the claim, keep the identity choice, document the precondition. I considered making the claim true with a process-wide id counter and rejected it -- ambient mutable state, which design-principles refuses, bought for a guarantee nobody asked for. The replacement is an ordinary C++ contract, the one std::vector::iterator has: a registered LanguageId belongs to the registry that issued it. Built-in ids are below 128 and ARE portable; say that too, because it is the half that is true. The claim lives in the header, the CHANGELOG and docs/modules/tui.md, and in the migration note -- all four change, per the ledger's own lesson about grepping for the claim rather than the file.
- Important I would not have predicted: there is a THIRD built-in table, FilenameLanguageTable, and it is the one that actually held `.endo-format`. Nothing guards it -- the reviewer added {".endo-format", Yaml} and the suite stayed green at 3540/1023, and B13a's own grep gate misses it. The golden tables cover two of three, and the uncovered one is the table that carried the consumer's concept. Asked B13a to say how it found the third table this time, since the method that found two missed it.
- Ruling R81 (the gate B13 will use): keep the symbol grep, verified empty; DROP the tree-wide consumer-name scan. It is not empty today (it fires on XtVersion.hpp:80, where "contour" is a terminal being DETECTED, not a consumer concept), `grep -P` fails outright in Git Bash here, and its exit codes are inverted because git grep exits 1 on the pass case. A gate that fires on a true negative, cannot run on one of our two shells, and reports backwards is three ways worse than no gate. The golden tables are the durable half, over all three tables.
- Ruling R82: B13a now owes two renames.json rows. Its "no row" was right when written and right about why; the gap closed while it worked, when C0 landed kind: "removed" in a8e5212 -- using B13a's two symbols as its fixtures.
Task B2: supplementary review pass — all six findings verified FIXED by running (mutation D2, a code appended after Last with a real description, now fails at :143 where it passed green before), and B2's M-2 rewrite judged STRONGER than what the reviewer had suggested. It also decomposed the prediction B2 gave me: the count rise is 49->52 in the description case and 16->17 in the deadline case -- **two independent cases grew, not one**, which a hand-written list would have left flat. And it checked something neither of us did: whether the renumbering is a WIRE break. It computed both enumerations and grepped both upstreams for any serialization of a code to a file, socket or IPC frame; there is none, so the CHANGELOG's log-text-only coverage is complete rather than merely believed.
- One finding, about evidence rather than code: every line number in B2's mutation transcripts is consistently 8 low, taken from a working copy predating two static_assert blocks and never re-run against what landed. Substance reproduces exactly, so it is citation hygiene -- but the framing is why it matters: **a reviewer who checked :97, found a constexpr array, and concluded the transcript was fabricated would have been reasoning correctly from bad evidence.**
- **Incident, self-reported by impl-B2 before anyone could notice, and handled well:** it ran `git commit --amend` to add a paragraph to its own message, using the private-index recipe to avoid B3's staged deletions. Between its commit and the amend, the drift-checker lane committed 8514854 on top, so `read-tree HEAD` read THEIR tree and the amend rewrote THEIR commit with B2's message. Verified independently: same tree, same parent (6dfb8c5), `git diff 8514854 438f40b` empty, 8514854 now orphaned. Content intact; a careful 38-line message replaced, already pushed.
- **Ruling R83: never rewrite a commit in this checkout** -- no `--amend`, `reset`, `rebase`, or cherry-pick onto a moving branch. The private-index recipe protects the INDEX from other lanes and says nothing about HEAD, which moves just as freely; the object a rewrite acts on is chosen after you last looked. B2 drafted this wording itself, which is the right instinct after an incident: turn it into the rule that stops the next one. In global-constraints.md now.
- Repair, non-destructive: attached the original 38-line message to 438f40b as a `git notes` explaining what happened, and pushed both `refs/notes/commits` and `refs/recovered/upstream-drift-checker-message` to origin so the pre-amend commit cannot be garbage-collected and the message is recoverable by anyone, not just in this working copy. No history rewritten; no force-push.
- What B2 did right after breaking it, and it is the standard: it stopped touching history immediately, pinned the overwritten commit with `git update-ref` so the object could not be collected, saved the full message to a file, reported to me before messaging the affected lane, refused to force-push, and drafted the constraint that would have prevented it. It also noted the consequence for its own commit -- 6dfb8c5's message does not describe the I-1 fix its content contains -- and supplied the missing paragraph for the record rather than amending again.
Task B12b part 1: done (8514854 + fa7b569) — scripts/check-upstream-drift.py and its self-test, registered under hygiene, bounds measured (checker 11.3 s Windows / 58 s WSL -> 600; self-test 4.2 / 1.8 -> 120, after a SHA cache cut per-row validation from 92 s to 58 s).
- My zero-drift measurement CONFIRMED at row granularity: contour 108 rows, endo 220 rows, zero drift after a fetch. With the right caveat, which I would not have stated: it proves those upstreams have not moved, not that the rows are CORRECT -- a row naming the wrong upstream file reports clean if that file was untouched. Recorded as a known limit in the script's docstring; content comparison is not built now, because "a `-` is not byte-identical" makes it harder than it sounds.
- Ruling R85: re-sync cmake/portable/CompileCache.cmake to f6ec49f3 (+67/-3, BEHAVIOURAL -- it reworks host detection and changes which prebuilt fastcache-cc row a host selects). A12 does it: the rule is verbatim, so there is no judgement in the content, which is exactly why the lane that found the drift should land it. Verify by reading build.ninja, not CMakeCache.txt, per the rulebook.
- The sharper half of that finding, now to be written into cmake/portable/README.md: downstream.yml compares against fastcached MASTER while the row records a PIN, so it answers "are we verbatim with master today" and the provenance table answers "are we verbatim with what the row claims". They diverge every time upstream moves -- which is why the nightly has already fired twice (35430889453, 35499910731), been acted on (b505db8) and will fire again tonight. One sentence saying which question each gate answers, so nobody "fixes" one to agree with the other.
- **Ruling R84, A12's finding and the more dangerous half of R83:** a private-index commit moves HEAD without updating the SHARED index, so another lane's index then encodes "delete the files you just added". A12 saw its own two new scripts staged for deletion in the shared index; had the IoBackend lane committed, they would have gone out inside that lane's commit. The failure lands on someone else, silently. Refined rule, in global-constraints.md: private index ONLY for a file whose other hunks are not yours; `--only` for a file wholly yours. Repair is a path-scoped `git reset -q -- <your paths>`, then verify the other lane's staged work survived.
- Two defects its checker found that nobody was looking for: provenance.md:90 is malformed (the async lane's row writes `ThreadPoolExecutor.{hpp,cpp}`, a brace pattern `git log --` cannot read, so the row silently stops being CHECKABLE -- the same class as R74's unresolvable pending arm), and 13 stale rows naming src/core/net/ files the IoBackend lane is deleting. It reported the 13 WITHOUT reddening, because check-cmake-hygiene already refuses exactly that "and two gates for one defect gets both ignored". Both routed.
- It let its own gate land red on the async lane's malformed row rather than weaken the checker to be green on day one. Correct call; the one-token fix is routed as a priority.
- Self-test: 14 cases, every one proven able to fail by mutating the checker one way at a time -- including two that assert an exit status of 0 (drift and a deleted upstream must NOT redden), which no output assertion would have caught. The A12 sentinel lesson applied before anything went wrong rather than after.
- **A12 corrected R83 within the hour, and it was right: my own two rules contradicted each other.** R83 as written banned `git reset` outright, which forbids the path-scoped index repair R84 actually REQUIRES. `git reset -- <paths>` moves no HEAD, rewrites no history and touches no worktree file -- it chooses nothing, which is the entire principle R83 rests on. Its argument for why this matters is the part worth keeping: *a rule people must break routinely stops being read.* A blanket ban would have left a dangerous shared index with no remedy.
- The constraint is now one bullet in A12's framing, which is better than my two: the private index protects the INDEX from other lanes but leaves HEAD free to move under you; an amend trusts HEAD but is blind to other lanes' commits. Both are "the object I act on was chosen after I last looked". Banned: anything that moves HEAD or the worktree. Permitted and sometimes required: `git reset -- <paths>` for your own files' index entries.
- A12 also checked what my incident report did not, because it was the likelier casualty: **the amending lane's own work survived too.** The amend did not replace their commit with A12's -- it produced a SECOND commit with their message and A12's tree. Both sides intact; residue is one duplicated subject line. Its reason for saying so is the right one: from inside that mistake the natural fear is that you destroyed your own work, and they had not. Relayed to them plainly.
- Its own checker's `not an ancestor` and `not a commit in this checkout` cases are the provenance analogue of the same hazard, for when an upstream rewrites a SHA a row records.
- Fifth covering green: Build 35542238141 on 7838b53, covering B1's two async commits plus its rulebook commit, and B2's 73d4569. Uncovered: 78bf537, 6dfb8c5, 438f40b, fa7b569.
- Ruling R86: the checkout-free half of the provenance validation moves into check-cmake-hygiene's existing `provenance` rule. core-cpp.upstream-drift runs --no-fetch and SKIPs wherever the upstream checkouts are absent, which is EVERY CI runner -- so the part A12 said the registration gates ("a row whose SHA, whose upstream path or whose core-cpp path is wrong, a defect in the table itself") only ever runs on this machine. The brace-pattern row needs no checkout and no network to detect, yet CI would never catch the next one. Deliberately NOT a second test, on A12's own argument that two gates for one defect gets both ignored: the hygiene provenance rule already owns table validation, so it learns one more way a row can be wrong (literal path not a pattern, 40-hex SHA), with a self-test case in the existing shape.
Task C0: round 3 (R75) done as a8e5212 — 51 -> 66 cases, covered green by Build 35542004684 and again by 35542238141; style job reports "440 rows, 412 delivered, 8 pending, 2 removed, 0 failures".
- **C0 corrected R74's premise and the correction improves the ruling rather than retiring it.** The shipped pending arm already walked prefixes -- it had fixed that in round 1 before ddddc67, and the proof is that 748142f could not otherwise have resolved B2's rows, so B2 read an intermediate state. But C0's own conclusion is the one that matters: nothing TESTED the shape, so nothing would have stopped it regressing -- and nothing did stop the same hole being written into the `removed` arm a round later. That is the argument for one declaresQualified() rather than three arms that happen to agree.
- **The best RED of the session:** neuter the shared prefix walk and three cases fail, while test_the_delivered_arm_reads_the_same_shape KEEPS PASSING, because that arm had its own walk. The test that proves the fix necessary is the one that stays green under the mutation -- which is also exactly why the hole was invisible from outside.
- R75 landed with B13a's reasoning preserved rather than overturned: `removed` rows carry no `to` and no `target`, the gate asserts absence, and that no rewrite tool can see one is structural through three doors each with a case (the schema refuses a `to`, a `target`, or any `apply` but `none`; textRows()/semanticRows() filter on apply; _patternsFor() raises), plus a case running the real table over all four profiles. Its first two rows are B13a's two symbols, each carrying the registry call in its `note` for C1 to paste.
- Confirmed R76 to C0 explicitly (it asked twice rather than assume, which was right) and re-sent R77/R78/R79 as round 4, with R79 to be coordinated with the B2 lane rather than both editing consumer-migration.md.
- Sixth covering green: Build 35542633374 on fa7b569, 24 jobs — closes B2 entirely (6dfb8c5), A12's B12b-1 (438f40b, fa7b569) and B1's docs commit (78bf537).
- **R86 confirmed empirically rather than argued:** that run is green with 24 jobs while `ctest -L hygiene` is RED on this machine for a real defect (the async lane's brace-pattern provenance row). The drift checker SKIPs wherever the upstream checkouts are absent, which is every CI runner, so the table-validation half it was registered to gate never runs where it would catch the next one. Exactly why the checkout-free half moves into check-cmake-hygiene.
Task B1: implementer DONE_WITH_CONCERNS; commits b21229d, f72cabc, 7838b53, 78bf537 (non-contiguous — four other lanes pushed during the task); 318 assertions in 78 cases per binary across core-cpp.async and core-cpp.async-fallback, every new case seen RED first, green on six local configurations and CI Build 35542633374 (all four commits, 23 jobs including both Emscripten legs, AppleClang and clang-tidy). Review dispatched.
- Ruling: the #1041 negative control may be weaker than the defect's own shape. A faithful reproduction cannot compile here without a NOLINT or a pragma and both are forbidden -- a test that requires breaking a rule to exist is not a test we can keep -- and two real diagnostics refusing the shape (GCC's -Woverloaded-virtual and clang-tidy's bugprone-derived-method-shadowing-base-method) is stronger than one control that reproduces it. Asked the reviewer to EXAMINE whether the weakened control still proves what it claims rather than assume it, since this is the one place where a weaker test is arguably correct.
- Ruling on deferred defect 1: a null-handle result()/await_resume() throws std::logic_error from one place, confirmed. An assert is a silent wrong value in Release, which is the defect being removed. The Global Constraints' "the only exception type is OperationCancelled" governs FALLIBLE APIs; this is a precondition violation, which AGENT.md already covers under "exceptions only for unrecoverable conditions", and std::optional::value() is the precedent. Requirement attached: the header must say it is a precondition violation reported as an exception, not a recoverable error, or someone will catch it as flow control and the empty state becomes a supported path.
- core-cpp#15 stays open and B1 is right about why: tail-call-ness is a compiler and Wasm property, not an ownership one, so no amount of work in this module can close it. It replaced the doc's "to be decided in Task B1" with the reason -- a stale pointer to a task that cannot answer is worse than an open issue.
- Routed to B3 from B1's report: a C-style `for` at src/core/net/detail/ReadyBatch.hpp:121, which the guidelines forbid outright. Reporting another lane's rules violation by file and line is more useful than reporting that the tree does not build.
Ruling R85 done as f0ef11a — and **my dispatch's premise was wrong and A12 refused to write it into the CHANGELOG.** I took "reworks host system/processor detection, changes which prebuilt fastcache-cc row a host selects" from A12's own earlier report and repeated it into a ruling without checking: exactly the error I had spent the evening correcting in other lanes. The real delta of f6ec49f3 to this file is a `_fc_first_line()` helper plus `_fc_maybe_start_daemon` capturing the child's streams where it passed OUTPUT_QUIET ERROR_QUIET, so a loader's 127 reads as the library it could not resolve rather than "not found" for a binary the module just staged (fastcached#1538). Host detection and the prebuilt-row table untouched. A12 wrote the entry to say what actually changed and explicitly declined my sentence about launcher selection -- a changelog entry that repeats a controller's wrong premise is worse than none, because it is the version a consumer will believe.
- It verified anyway on both hosts from build.ninja rather than CMakeCache.txt (319 LAUNCHER lines in WSL, 318 on Windows, all resolving to fastcache-cc), and then BUILT through the launcher, because "a status line is a claim and the objects are the fact". It also pinned both verbatim cmake files at f6ec49f3 so the README's same-commit claim stays true, and found NOTICE carrying a third, staler pin (eb9c9c68) that the previous re-sync never reached.
- **Ruling R87, A12's finding and the most dangerous git hazard of the day:** the worktree CHANGELOG.md was 13 lines behind HEAD because another lane had committed to it through a private index without updating the worktree. A patch built from `git diff` of that worktree would have staged those 13 lines as DELETIONS and silently reverted their commit. Rule, in its wording: for a shared file, build the commit's content from HEAD's blob plus your own transform, never from the worktree -- in a checkout where lanes commit through private indexes, the worktree is not a base, it is one lane's stale copy. Line-ending normalisation went in beside it (two LF files picked up CRLF while being edited; .gitattributes protects the blob, but a CRLF worktree file makes every other lane's diff unreadable).
- Finding 2 confirms R86 from the other side: core-cpp.upstream-drift exits 77 on every runner, which is WHY Build 35542633374 was green with the malformed row present. A12 documented the two gates' different questions in cmake/portable/README.md (downstream.yml diffs against upstream master and FAILS; the script walks the table and REPORTS). Documentation is necessary and does not close the hole; R86 still stands.
- Ruling R88: no gate reads NOTICE's pins. A12 caught a stale one by hand and nothing would have caught the next. Every SHA NOTICE records for a vendored or verbatim file must agree with provenance.md, checked in the same hygiene rule as R86. Three documents carry pins and only one is checked -- the shape of A12's own earlier lesson about a correction not being finished until every document carries the right version.
- Full drift run after the re-sync: 350 rows checked, 350 up to date, 0 drifted, 1 malformed (the async lane's brace-pattern row).
- Seventh covering green: Build 35543400100 on b528631 — covers C0's rounds 3 and 4 (1dba5fa R76, aee02ec R77+R78, b528631 R79) and B2's c7102e2 ("six NetError case names could not select themselves" — a test you cannot run by name is one nobody runs in isolation). Uncovered: f0ef11a.
Task C0: review review-C0 (opus) dispatched over its seven commits, framed as infrastructure rather than a script: tools/migrate/ is what six consumer migrations run over hundreds of files each, so a defect here is the same bug six times in six repositories, found by six different people. Pointed hardest at rewrite.py's anchored regexes against text that is NOT code (string literals, comments, raw strings, CMake/Markdown/JSON), idempotence over a PARTIALLY converted tree, and whether the `removed` kind's three doors are really three or one door wearing three names -- to be verified by disabling two at a time.
Task C0: all four rulings landed (1dba5fa R76, aee02ec R77+R78, b528631 R79), green on Build 35543400100 (24 jobs), with the style log carrying all four gates. R78 found nothing on either side of the rename, and C0 proved the linter refuses rather than trusting the green: a planted undefined name was reported by file, line and column with exit 1.
- **C0's most portable finding: a diagnostic-muting comment is the Python spelling of NOLINT.** Enabling the linter retired all three `# noqa`s. Two were inert once a real rule set existed; the third (E402 under a sys.path.insert) went away for a BETTER reason than suppression -- Python already puts a script's own directory first on sys.path, so the surgery was never needed. A suppression comment is usually a fix nobody looked for. To be written where the NOLINT rule lives, generalised to any language.
- Ruling R89: a bare invocation of scripts/clang-format.py or scripts/python-style.py must be an error naming `--all` or explicit paths; CI passes --all. C0 was right that formatting every Python file git knows about is a loaded gun in a four-lane checkout, and right that changing one and not the other would be worse. The current default contradicts a standing constraint -- never format a file another lane is editing -- and a rule the tooling defaults to breaking is a rule that gets broken.
- **Third instance today of one failure class, now named: a mechanical check believed from its summary rather than its output.** C0's first rename pass used a `(?<![\w.])` lookbehind and silently skipped every attribute access (`def text_rows` renamed while `table.textRows(...)` stayed); the replacement count looked plausible and only reading the result caught it. Same shape as its own `head -20` claim, as A12's stale grep, and as B2's stale transcript line numbers. The byte-identity proof is precisely the defence, so the lesson goes beside it in the migration guide.
- Into global-constraints.md from C0: patches are written to a file in BINARY, never piped through anything that touches text (piping `git diff --no-index` through `python -c` corrupts the patch on Windows, and `git apply` then reports "patch does not apply", which reads as a conflict rather than an encoding fault).
- **C0's CRLF report on another lane's script does not hold, and I nearly repeated the error.** `git ls-files --eol` reports `i/lf w/lf attr/text=auto eol=lf`; that lane had normalised before committing. My own `grep -c $'\r'` also suggested CR bytes. Rule added: ask `git ls-files --eol`, not grep -- it prints both sides and the attributes in force, which is the actual question. Two false CRLF reports in one evening, one of them mine.
- R79 collision was prospective, not actual: C0's guide section landed in b528631 before my "let B2 write it" instruction reached it, and C0 checked the file for uncommitted work from any lane before replying. Told B2 to EDIT that section rather than write a second, on C0's own reasoning that its version is second-hand -- derived from my description of B2's review rather than from having done the work.
- Two rulings so the guide says one thing:
  1. **Strip whitespace before comparing** (B2's version wins). A rename changes line lengths, clang-format reflows, and reflow is the one legitimate difference a mechanical pass produces; without the strip the proof reports false differences on every reflowed line and is abandoned after its first real use. But the cost gets written down: a stripped comparison cannot see a whitespace-only change, and `clang-format --check` is what covers that blind spot. Two checks, each covering the other's.
  2. **An explicit substitution list, independently written -- NOT rewrite.py's per-file report.** Deriving "the intended substitution" from the tool's own account of what it changed is automatic and cannot drift, which is attractive, and it is circular: a tool that is wrong about itself would prove itself correct. The entire value of the method is independence from the tool. The reason goes in the guide, because the circular version will look like an improvement to the next reader.
- C0 recorded my R74 framing verbatim and sharpened the decay argument past mine: nothing stopped the same hole being written into the `removed` arm a round later **by C0 itself, with the correct pending-arm implementation on screen**. Three arms that happen to agree is a state that rots on its own -- which is the argument for one shared function rather than three correct ones.
- **Ruling R90, earned by B2's comma finding, and the most serious defect found tonight: the exit-code contract is false.** testing.md and the build contract both say 2 means nothing ran. B2 measured that a Catch2 name filter matching nothing gives exit **0**, because normalisedExitCode returns 2 only when `totals.testCases.total() == 0 && rawExitCode != 0`, and Catch2 reports no error for a filter that matched nothing. So a bisect script, a flake hunt or a CI shard that selects a case by name gets a GREEN for a case that never ran -- silent, and in the machinery that tells us whether anything is broken. That is a defect in src/core/testing/ExitCode.cpp, not a documentation gap, and it outranks the comma fix that exposed it. B2 fixes it with RED first, using Task A1's existing ExitCodeFixture/check-exit-codes table, adding both a "filter matched nothing -> 2" row and a row pinning whichever legitimate no-tests invocation must stay 0, so the distinction cannot be simplified away.
- The finding that led there (c7102e2): a comma splits a Catch2 test spec, so a name containing one cannot select itself. Six of B2's thirteen names had one -- and 42 of the tree's 1581 TEST_CASE names do, so over a seventh of every instance in core-cpp sat in this one file. The part B2 identified as worth carrying back into the rule is that **here the miss is a PASS, not a failure**, which is what makes it dangerous.
- B2's citation repair is the standard for "fix the evidence": it re-ran all five mutations plus RED 1 against the committed files rather than editing numbers, and reproduced RED 1 against a header that no longer exists by extracting 8a88ce0~1's blob into an include directory ahead of src/ -- making the transcript reproducible by anyone today. It also annotated the two citations that needed a WORD rather than a correction (the mutated header's line; Catch2 reporting a two-line CHECK at its closing parenthesis).
- Its independent wire recomputation is stronger than the reviewer's, because it found the asymmetry: fastcached's numbering moves far more than contour's (ConnRefused 8->7, ConnReset 9->6, AddressInUse 6->8), so **the lineage whose rendering printed the number is the lineage whose numbers move most**. That turns the rendering decision from a judgement into a fact.
- And the habit worth keeping, in its words: "I had reported the total because it was what I had measured; the reviewer measured the thing the total is evidence of."
- R79 landed as 7ddbe1d (B2 editing C0's section, all three rulings applied). **B2's circularity finding is a full step sharper than my ruling and is the most dangerous thing anyone nearly shipped tonight:** C0's version did not merely take the substitution list from the tool's report -- it RE-RAN rewrite.py on the pre-image to produce the expected side. So a tool wrong about a row is wrong identically on both sides, byte-identity holds, and in B2's phrase **the check passes precisely when it should fail**. Not a weak proof: an inverted one, strongest exactly where the tool is right and silent exactly where it is wrong -- and it would have shipped into six consumer migrations looking like rigour. The guide now states the reason, because the automatic version reads as an improvement.
- Best small moment of the task: B2 first wrote "re-wrapped a dozen makeNetError( calls" from memory, noticed it was inside a section warning about numbers nobody measured, and measured it -- 34 changed lines across sixteen files contain none of the renamed token. Applying a section's own rule to the sentence you are writing in it.
- **B2's timing note is the useful half of R87's story, in its words:** my "no uncommitted changes" check was true when I made it and had stopped being true by the time the instruction reached B2. In a four-lane checkout a check of the tree is a statement about a MOMENT, and the instruction carrying it arrives later than the moment. B2 built from HEAD's blob anyway and would have asked rather than written over C0 -- the only behaviour that survives that staleness.
- B2 verified ancestry with `git merge-base --is-ancestor` for every run rather than reading it off the log; all six of its commits are now covered by completed green runs (35540278214, 35542238141, 35542633374, 35543400100), with 7ddbe1d's pending.
- R86 done as 1596744: check-cmake-hygiene's provenance rule now also refuses an upstream path that is a pattern rather than one file, and a synced SHA that is not a full 40 characters, with `origin: core-cpp` rows exempt. A12 added a THIRD case unasked, and it is the better half: a data row too short to hold those cells would have skipped both new checks in silence -- a gate bypassed by malformed input reads as passed, the same defect one level down. It proved the rule on the REAL table rather than only a fixture, by restoring both historical defects into a scratch copy (the brace path and IExecutor's truncated SHA) and confirming the scanner names both by row -- a fixture proves the regex, the real table proves the rule would have caught THESE.
- **My error, and A12 checked before contradicting me:** I told it 438f40b and fa7b569 were uncovered, citing the previous green run. Build 35542633374 on fa7b569 covers both, and my own ledger had recorded that correctly an hour earlier -- the message contradicted my own record because I wrote it from memory instead of reading back. Third instance today of "a mechanical check believed from its summary rather than its output", and this one is mine.
- **New constraint, from A12's near-miss and in its words: assert the expected failure count; never read an empty output as a pass.** Running the new scanner over a table deliberately seeded with two defects printed nothing, and nothing is exactly what a passing scanner prints -- for a moment it had "the rule works and the table is clean". The cause was `cmake: command not found`, Git Bash having no cmake on PATH while every other cmake run that session went through WSL or the VS dev shell. It was caught only because it had PREDICTED two refusals and got zero. That is the operator-side corollary of "a gate that does not report reads as passed", and we did not have it written down.
- Noted for the shared checkout generally: our gates read the LIVE tree, so a single red can be another lane's half-second (core-cpp.migrate-renames failed once mid-run while renames.json was being edited, and passed on re-run). Re-run before reporting one.
Task B2: complete — six commits (8a88ce0, e6dd748, 748142f, 73d4569, 6dfb8c5, c7102e2, 7ddbe1d), all covered by completed green runs with ancestry verified by merge-base. R90 (the false exit-code contract) is carried as its own follow-up.
**Task B3's first commit landed: e7963de "net: IoBackend replaces EventSource; backends dispatch, never resume".** The pivot of Phase B is in; the host-driven half (HostDrivenBackend, IHostScheduler, ManualHostScheduler, the Emscripten scheduler) is the second commit still to come.
- C0's R89 landed as 6d92c54, "a bare run of either formatter is an error, not 'format everything'". A12's R86 as 1596744 and a README correction as 18ee360.
Task B13a: fix round 1 landed as 002af92 (R80's retraction, the third table's guard, the three uncovered call sites, the minors, R82's two removed-rows). Scoped re-review dispatched, told to verify by RUNNING rather than reading: reintroduce {".endo-format", Yaml} and confirm the suite now fails; un-thread the three call sites ONE AT A TIME and confirm a distinct failure for each, because one test that fails for all three is one test, not three; and check the retraction reached all four places, since a correction is not finished until every document that carried the wrong version carries the right one.
- Worth recording from B13a's report tail: it found another lane's scripts/python-style.py mid-edit and unparseable (an f-string with a literal newline), verified its own five files against HEAD's copy instead, and restored their in-progress copy byte for byte. It also noted their change is a good one -- which it is; that is R89.
- **Ruling R90 WITHDRAWN.** B2 re-measured and retracted its own finding before anyone edited ExitCode.cpp, and I verified the retraction independently rather than accepting it: tests/cmake/check-exit-codes.cmake:26 has carried the row "2;[nothing-has-this-tag]" since Task A1, and :27 goes further with "1;-w;UnmatchedTestSpec;...". That check is a ctest under hygiene and has been green all day. The exit-code contract is correct, implemented and tested, and the rawExitCode != 0 guard does exactly the job we both guessed -- it separates "asked to list" from "asked to run and matched nothing". Nothing was changed; no working code was edited on a bad measurement.
- How the false reading happened, and it is the harness: `$B` in a `wsl -- bash -lc '...'` probe expanded in the OUTER Git Bash, so an empty command ran against a filename and `$?` reported the outer shell's status. The earlier half of the pair had `... | tail -6; echo $?`, which reports tail's status. B2 named it as its own second bullet from the guide it had committed an hour before -- *the tool answered a smaller question than it was asked* -- and walked into it in the same session.
- **New constraint, earned: read a status inside the script that produced it, never through a nested shell and never after a pipe.** Write the probe to a file and run the file; print the values you are about to use; and **check your result against any existing gate that already asserts the same thing before you report it**. That last habit is the one that would have caught this instantly -- `ctest -R exit-codes` was corroborating evidence sitting in front of B2 the whole time.
- What survives: the comma finding itself. A comma does split a Catch2 test spec, six of B2's thirteen names could not select themselves, and 42 of the tree's 1581 names have the same problem. That measurement held because it was taken a different way -- through a script file, grepping the OUTPUT for "All tests passed" rather than reading `$?`. The sound and unsound measurements differed in exactly the way the new rule names.
- Told B2 not to apologise: escalating a suspected defect in the machinery that reports whether anything is broken is the right instinct, and the failure mode to fear is the opposite one -- a wrong measurement defended.
- Eighth covering green: Build 35543744367 on f0ef11a, closing R85 (the CompileCache re-sync). Uncovered and queued: 002af92 (B13a's fix round), 6d92c54 (R89), **e7963de (B3's IoBackend commit -- the one whose CI result matters most tonight)**, 7ddbe1d (R79), 1596744 (R86), 18ee360.
- Phase C prep, done read-only while the lanes worked: all six consumer repositories exist, are public, and have default branch `master` (contour-terminal/{endo,tuidu,contour}, LASTRADA-Software/{fastcached,Lightweight,morph}). Local checkouts present for all six. Only D:\fastcached-worktrees exists so far; the rest get created per task.
- **One thing to carry into C1: D:\endo is checked out on branch `fastcached/upstream`, not master.** Harmless for us, because worktrees are created from `origin/master` and the standing constraint is to never touch a main checkout -- but it is evidence that something or someone else is using that checkout, so the "fetch and worktree add only" discipline applies to endo exactly as it does to fastcached.
Task C0: review review-C0 (opus) — spec compliance PASS, task quality CHANGES REQUESTED: 1 Critical, 7 Important, 8 Minor, nearly all verified by running. The hardest review of the night, and it earned it.
- **Critical: semantic_rename.py does not follow virtual overrides.** Run with libclang: ISocket::Read renamed, TcpSocket::Read OVERRIDE and every call typed to the derived class left behind -- the migrated tree does not compile. Ruling R91: fixed in C0, not deferred to C4. It is a fitness failure against the plan rather than a breach of the dispatch (which asked only for DeclRef/MemberRef/TypeRef), but renaming an interface's methods across an override hierarchy is the exact job C4 commissioned the tool for, and discovering it mid-migration costs a benchmark gate and a full preset matrix. The fixture must have the defect's SHAPE -- C0's is two UNRELATED classes, which structurally cannot see it. A fixture that cannot fail for the bug, in the tool whose job is finding things.
- **The answer to "what is most likely to bite the first consumer migration": the byte-identity proof fails on EVERY consumer PR, on the first attempt.** Run verbatim against a codemod commit that does what the guide's own step 4 describes (convert, delete the superseded copy, add a file), it reports NOT PURE for every added and deleted path, produces an EMPTY substitution log (it re-runs over the already-converted tree), and **writes to the working tree**. A verification step that mutates the thing it verifies is not a verification step. Fix: `--diff-filter=M -z` plus one deleted line.
- Ruling R92: a `pending` row must carry its `target`. One is asserted by nothing in either direction and has already gone stale -- a row that cannot be checked is a comment wearing a row's clothes, and being checked later is the pending list's entire job.
- **Ruling R93: make the two structural claims true, not merely correct the prose.** The `removed` kind has TWO doors, not three -- with schema and _patterns_for disabled, the loader alone rewrote the row, because text_rows() filters on `apply`, not `kind`. And declares_qualified() IS bypassed by the delivered arm, which carries its own copy of the walk, so the docstrings asserting "every arm reads through this one function" are false and R74's "one implementation, not three" was not delivered. Two walks that happen to agree today -- exactly the state C0's own decay argument describes.
- The reviewer's adversarial method is the reusable part: it disabled two doors at a time to test whether three mechanisms were really three. They were two. Ask that of any claim of defence in depth.
- R88 done as 549822b: the provenance rule now holds NOTICE's pins against the table's. **The `(verbatim)` restriction is the design, not a scope reduction, and is recorded as such.** A merged file is deliberately listed in NOTICE under the commit of EACH upstream it drew from while the table names only its primary -- Clock.hpp under fastcached's b461e8b6 in one and endo's f774a210 in the other, both correct -- so a gate comparing those would fire on a true negative, which is exactly why R81 had us delete a gate. A12 found the one subset that is checkable and the subset that actually rotted, and they are the same subset. A verbatim file outside the table's scope is checked for existence only, because it has no row to be held against.
- **New practice, from A12 and better than what I had been asking for: capture a RED by running the new test against HEAD's code, not by disabling your own.** It proves the case discriminates between the old behaviour and the new one, and it cannot leave a mutation behind in a tree four lanes are writing to. Boundary written in: mutation when the defect is in data or in a branch unreachable from HEAD; HEAD whenever the change is to code the test exercises directly.
- A12 applied the expected-failure-count rule to both the RED and the probe within the hour of it being written -- two predicted, two observed, on each. And it ran a full WSL build through the re-synced launcher (344/344, exit 0) rather than waiting on repeatedly-superseded CI, because a probe proves the launcher is selected and a full build proves it drives the build.
- Its epistemics on the 8 red hygiene violations are the standard: it checked each one individually rather than assuming, **because a rule it had just added to that exact gate was the obvious suspect and "my change made it red" was the easy wrong conclusion.** All 8 are the IoBackend lane's untracked files.
- B12b part 2 waits on the IoBackend lane landing: running a drift check against a table mid-rewrite would measure the rewrite, not the drift.
**MASTER WENT RED — CI run 35544422449, two causes, both routed, neither B13a's.**
1. `core-cpp.cmake-hygiene` fails on the async lane's brace-pattern provenance row, which fails `style` and the `Test` step on every platform. Verified directly against origin/master: the fix is in the worktree and was never committed. Routed to B1 as the next thing it does, with the R87 recipe, because that file carries other lanes' hunks.
2. `consumer-smoke` fails with `fatal error: 'core/net/DefaultEventSource.hpp' file not found` — B3's e7963de deleted the header and `tests/consumer-shared/ConsumerSmoke.hpp` still includes it at :20 and calls makeDefaultEventSource() at :138. Verified against origin/master. **That fixture is the one place our public API is exercised the way six consumers will exercise it**, and it compiles only in CI, so no local green was ever evidence about it. Routed to B3.
- New constraint from that: deleting a public header means grepping the WHOLE tree, not src/ -- tests/, docs/, .agent/ and tools/migrate/renames.json all name public headers, and tests/consumer-* compiles only in CI.
Task B13a: fix round 1 complete (002af92) — 1033 cases / 3870 assertions, up from 1023/3540, green on four presets with clang-tidy, mkdocs --strict and ruff clean.
- **R80's claim lived in FOUR places, not the three my ruling named** — B13a found `SyntaxHighlighterRegistry::highlightLine()` promising the same falsehood and not on my list. RED first: `{ 3, 3, 3, 3, 3 } == { 0, 0, 0, 0, 0 }` — Cat::String, doll's highlighter, from toy's id.
- **How it found the third table, since the method that found two missed it:** the first pass took the dispatch's list as the inventory and grepped the header, which cannot find a table the header does not hold. `git grep -n "to_array<LanguageToken>" -- src/core/tui` returns three, and reading detectLanguageFromPath()'s body finds it too, because the file-name table is the one it consults FIRST. A dispatch's list is a starting point, never an inventory.
- New constraint from its self-reported mistake: `git checkout -- <file>` is not a single-hunk undo. It used it to remove one probe row and lost its whole fix round in that header; caught immediately, redone, nothing shipped wrong. A whole-file weapon with an undo-shaped name.
- Flag to resolve with B3: B13a reports renames.json is being REGENERATED rather than hand-edited, and a hand edit of its note was dropped. Several lanes are adding rows by hand on my instruction; if that is not durable the instruction has to change. Asked B3 directly.
- **A12 found that it had implemented R86 as a COPY when I ruled a MOVE, and completed it unasked (921e80d).** A brace row reddened both gates -- the exact outcome R86 exists to prevent, and the one A12 itself argued for in B12b-1 when it demoted the core-cpp-path case. Confirmed to it that completing a ruling is right and asking first would have been wrong: a ruling is closed when it is implemented, not when I say the word, and a round trip would have left the duplicate gate live. The four hygiene-owned defects now print under OTHER GATE without setting exit status -- reported, not silently dropped, because that would trade one bad outcome for a worse one.
- **Best find of the night, and it is in A12's own checker (093d3a4): a silently swallowed row.** The checker decided whether a line was DATA by matching PROSE -- it recognised the header by testing the whole line for the substring "core-cpp path", so `src/core/async/ThreadPoolExecutor.hpp`'s notes, which read "a row keys on its core-cpp path", were read as a second header and the row vanished: not checked, not counted, not reported, while the run printed clean. **The swallowed row is the brace-pattern row itself** -- the one defect that gate existed to catch, invisible to it, found by a different gate.
- New constraint: **structure is decided by structure, never by content.** The header is the row whose FIRST CELL is the column name. Prose in a notes column cannot decide whether a row is data. Applies to every parser any lane writes here.
- **A number I was given was wrong and A12 corrected it precisely: "350 rows checked, 0 drifted" was 350 of 351 presented as all of them.** The zero-drift conclusion survives. That is the third form of one failure class tonight -- a mechanical check believed from its summary rather than its output -- and this time **the summary WAS a count**, which is the hardest version to doubt.
- How it was found is the whole argument for the expected-failure-count rule, written an hour earlier: A12 predicted two OTHER GATE lines and got one. Nothing failing, exit status 0, every test green, and the only signal in the world was a number not matching one it had committed to in advance. Its words: "I nearly wrote it off."
- Also right, and worth carrying: it verified each commit green STANDALONE before landing the split, rather than assuming the split was clean -- a split that leaves a red commit in history is a bisect trap nobody finds until they are already lost.
- **Controller intervention: I fixed the consumer smoke fixture myself and pushed 13bdcd4.** Master was red on 22 of 24 jobs and every lane's CI evidence was blocked behind a three-line change queued behind B3's host-driven work. Checked first that nobody had it in flight (`git status -- tests/` empty), then: DefaultEventSource.hpp -> IoBackend.hpp, makeDefaultEventSource() -> makeDefaultBackend(), EventLoop takes the IoBackend& exactly as it took the EventSource&. Told B3 not to duplicate it, and swept the tree: the remaining old-API hits are all places where naming it is correct (CHANGELOG, provenance, the migration guide, renames.json, the plan and spec).
- **My own slip while doing it, recorded because it is the rule I have been enforcing:** to test whether the fixture was already unformatted at HEAD I used `git stash push -- <path>` in a four-lane shared checkout. It was scoped to a single file only I had modified, so nothing of anyone else's was touched -- but stashing in this tree is exactly what the rebase constraint forbids for the general case, and `git show HEAD:<path>` into a scratch file would have answered the same question with no worktree mutation at all. The safe form is the one that reads, never the one that moves.
- Confirmed by that check, incidentally, that the formatter was right and I was wrong about include order: clang-format reordered three lines of my edit.
- **Ruling R94: the member-row key must include `scope`.** Verified at tools/migrate/renames.py:231 -- the key is `(profile, row.kind, row.source)`, so two semantic rows for the same member name in the same profile collide even across different classes. B1 hit it immediately: AsyncQueue::Close cannot have a row because ISocket::Close holds that name in the fastcached profile, and one Submit row is serving IExecutor and every derived executor. Read, Write, Stop and Close are exactly the members Phase B renames one-per-profile, so B4-B11 hit it repeatedly. A table that cannot express the renames it exists to drive gets worked around, and a worked-around table drifts -- the failure R68 exists to prevent.
- **Ruling R95: an unresolvable `${...}` must be refused, not silently read as absent.** check-renames resolves a `${...}` in HEADERS through a `set()` but not through a `list(APPEND)`, so B1's module made **every core::async rename row read as private** -- the gate reported nothing, and the rows it could not see were exactly the ones it exists to check. B1 worked around it by making its list literal again, which fixes its module and leaves the blind spot for the next one. The ruling is not "also resolve list(APPEND)" but "when the resolver cannot determine a header list, say so and fail, never conclude absent" -- the fourth instance tonight of a gate whose silence is indistinguishable between *clean* and *did not look*.
- B1 found R95 the same way A12 found its row-swallow: **a row count that looked wrong.** Two independent lanes, two different gates, same tell.
- Recorded as deliberate so nobody "fixes" them later: `syncRunWith` has no row, because the table keys on the source symbol and FastCache::SyncRun is spoken for, so a two-argument call fails to compile and names the site -- better than one that compiles and means something else. `AsyncQueuePush::accepted` has none because its TYPE changed with its name. Both are the judgement the `removed` kind encodes: where a mechanical rewrite would produce something that compiles and is wrong, a compile error is the better tool.
Task B2: COMPLETE. Seven commits; six covered by completed green runs with ancestry verified, 7ddbe1d (markdown) pending. Two errors made and corrected in the open before anyone acted on them -- the amend that overwrote another lane's commit message, and the exit-status measurement that produced a defect report against working machinery. Its own closing note is the one I want kept: being checked rather than believed is what made it safe to report the second retraction quickly instead of re-measuring privately first, "which, with a defect claimed against the machinery that tells us whether anything is broken, would have been the more dangerous instinct."
- New practice from A12: **exercise a gate the way CI invokes it, not the way you invoke it.** A registered ctest and a direct script run are different programs — the registration adds `--no-fetch`, sets SKIP_RETURN_CODE and bounds a TIMEOUT — and a gate can pass one form while silently skipping the other. A12 had only been running the direct form. In global-constraints.md.
- **Expected red, do not re-diagnose:** Build 35545166786 is executing on 921e80d, which contains B1's brace-row fix but NOT my consumer-smoke fix (13bdcd4 came after it). It will fail consumer-smoke for a cause already fixed. The first run that can be green is the one on 13bdcd4 or later; d7a7bee is queued behind it.
- B1 landed d7a7bee, "an empty task's refusal is a precondition, not an error to catch" — the documentation requirement I attached to the null-handle ruling, so nobody wraps it in a catch and turns the empty state into a supported path.
- **My own lesson landed on me for the second time tonight.** I told B1 its brace-row fix was uncommitted; my measurement was real (1 occurrence via `git cat-file -p origin/master:...` before sending) but 66dfc9a landed between that read and the send. Justified at the instant I measured, stale at the instant it arrived — word for word B2's lesson: *a check of the tree is a statement about a moment, and the instruction carrying it arrives later than the moment*. I recorded that lesson and then made it. The habit that works is B1's: re-read the remote before acting on someone else's claim about it, and say what you verified. Likewise I should have said "the red run predates your fix" rather than "the fix is uncommitted" — run 35544422449's head 18ee360 is an ancestor of 66dfc9a, so it is evidence about the tree it built, not the tree now.
Task B1: COMPLETE — six commits (b21229d, f72cabc, 7838b53, 78bf537, 66dfc9a, d7a7bee); review running over the first four. d7a7bee closes the null-handle requirement in all four @throws lines, docs/modules/async.md and the rulebook, not just at the function — the correction discipline this project has had to learn three times tonight.
- B1 reached R94 and R92 independently from the other end, which is the best confirmation either could get. Its framing of R92 is sharper than mine and is now the record: **"a pending row with no target checks nothing — the R74 shape, in the table rather than in the checker."**
- **R95 amended: it has two arms and I had only ruled the loud one.** B1 drove check-renames.py's own helpers directly rather than re-describing its symptom, and the cases separate cleanly: an unknown `${X}` resolves to `[]` (silently ABSENT — the arm I ruled), while a known `${X}` that a `list(APPEND)` later extends resolves fine and is silently INCOMPLETE. `_set_variables` reads `set(...)` only; `list(APPEND|INSERT|PREPEND <var> ...)` is invisible to it. **An "unresolvable reference" check passes arm (b) outright, because the reference is perfectly resolvable — it is just short.** Both arms now ruled. B1's reason for insisting is the part to carry: *an empty list is at least suspicious on inspection, while a list that is merely one short looks entirely normal* — the quieter failure is the more dangerous one.
- B1 also narrowed its own claim honestly: it had told me `list(APPEND)` made EVERY core::async row read as private, then re-read its own output and found only the ThreadPoolExecutor rows failed. Real defect, smaller blast radius, corrected before anyone sized a fix to the overstatement.
- C0's round now carries R91, R92, R93, R94, R95 (both arms) and seven Importants with I5 first. Offered to split it rather than have one lane carry it whole — with the condition that the round may not shrink silently.
Task B13a: re-review rereview-B13a-r1 (opus) — **all nine items ADDRESSED**, every one verified by running in a PRIVATE DETACHED WORKTREE at 549822b with its own build tree, so nothing in the shared checkout was mutated. It reintroduced {".endo-format", Yaml} itself (red, 9 == 8), un-threaded the three call sites one at a time (three separate reds), and found the retraction had reached FIVE places rather than the four my ruling named — B13a had already caught the fifth I missed.
- **New Important, ruled into the round rather than filed: LanguageId::Last's new doc names a guard that does not fire.** Measured both ways: append `Rust` after `Last` and the build fails on -Wswitch (so "still satisfies the switch" is false, and the switch is the only thing that notices); append `Rust` AND the switch case the compiler demands, with no table row, and the build is clean and the suite green at 3870/1033 — because builtinLanguageTableIsIndexedByLanguageId() compares size() against Last and appending after Last moves neither, so it fires only once the table row is added, i.e. once the mistake is half corrected. **It is the Critical's own shape — a public header promising a guard that is not there — introduced by the edit meant to bring this enumerator up to NetErrorCode::Last's standard**, which is scrupulous about exactly this and names a runtime test instead. Closing the round with it would leave the round having fixed an instance without the lesson.
- New practice, from the reviewer's method: **mutate in a private detached worktree, never in the shared checkout.** Proving a test can fail means breaking something, and breaking it here breaks it for three other lanes; a mutation left behind is indistinguishable from a defect. `git worktree add --detach <scratchpad>/wt-<name> <sha>` also lets you mutate a commit that is no longer HEAD.
- **B13a's method for finding the third table is the durable part of the task, and it indicts my dispatches:** its first pass took my list as the inventory and grepped the header, which cannot find a table the header does not hold. **A dispatch's list is a starting point, never an inventory** — true of every task I send, including the ones where I am confident the list is complete.
- R89 landed as 6d92c54: both formatters now require paths or an explicit `--all`, every branch verified rather than assumed (bare -> exit 2; `--all` WITH paths -> exit 2, since it means two things at once; `--all --check` -> 0). Two second-order fixes it forced: both scripts' "run this to fix it" hints named a command that is now refused, and **the linter enabled last round caught an f-string with no placeholder in brand-new code on its first outing** — R78 paying for itself inside one round.
- The rulebook now bans the ACT, not the spelling: `# noqa`, `# type: ignore`, `// eslint-disable`, `#[allow(...)]`, ending on *a suppression comment is usually a fix nobody looked for*.
- **Fourth instance of the summary-versus-output class, and C0 found it in its own CRLF report:** what misled it was `git diff`'s *"CRLF will be replaced by LF"* — **a line describing what git WOULD do to a blob, not what the file IS** — which was true when first seen and stale when reported. `git ls-files --eol` gives `i/lf w/lf`. Corrected in place.
- **Ruling R96: tree-level checks run once in CI, not in every matrix leg.** C0's observation from tonight's outage: Build 35544422449 failed 22 of 24 jobs and the whole matrix came from ONE LINE, because check-cmake-hygiene is a hygiene-labelled ctest and every build job runs ctest. Its framing is the problem exactly — **correct behaviour and terrible triage** — it reads as catastrophe and buries whichever job actually has a platform bug, which on a 24-leg matrix is the one thing we cannot lose in noise. Routed to A12: classify which checks are tree-level (answer cannot vary by platform because the input is the source tree), give them a label, exclude from the per-job ctest, run them in `style`, keep `ctest -L hygiene` running everything locally. Told to prove the triage improves with a deliberate violation — every leg red before, exactly one named after — and warned that a check which stops running somewhere is one step from one that stops running anywhere, so `style` must FAIL on them and that must be verified by making it red.
- R79's coordination resolved itself as a correction rather than a conflict, and C0 named why: it flagged its uncertainty about the two specifics instead of picking a side, so B2 could correct the circular derivation rather than argue with a committed position.
**Real platform defect found by CI in B3's IoBackend work — the first thing tonight that no local preset could have caught.** Build 35545166786 on 921e80d, both macOS legs (appleclang and llvm-22), identically:
```
src/core/net/BackendParity_test.cpp:1026: FAILED:  CHECK( muted.readable >= 1 )  with expansion: 0 >= 1
```
core-cpp.net: 146/147 cases, 846/847 assertions. Linux, Windows and both sanitizers green on the same commit — so kqueue and only kqueue. The case is B3's own and its comment states the contract: *"muting is not a one-way door: the registration that was silent arms and is reached without ever having been detached and re-attached."* After setInterest(muted.handler, Interest::Read) on a registration previously muted with Interest::None, kqueue delivers nothing. Likely EV_DELETE/EV_DISABLE on mute without the matching EV_ADD/EV_ENABLE on re-arm. **The test is right** — it tests exactly the Interest::None semantics the Phase A gate had to unify when it found the two poll backends disagreeing. Routed to B3 with the instruction to use CI as the instrument deliberately (Portability dispatches FreeBSD on demand; macOS runs every Build), putting the hypothesis in the commit message rather than guessing twice at a fix it cannot test.
- The same run's consumer-smoke failures are already resolved (13bdcd4), and the brace row is fixed (66dfc9a), so the macOS failure is the only live one on master.
Task B2: CLOSED, with its accounting correction taken — six of seven commits covered, 7ddbe1d pending. I had written "all seven" in a message while my own ledger said "six plus one pending": the second time tonight I contradicted my own record by writing from memory.
- **B2's diagnosis of why master was red was stale, and the trap is R87 one level up.** It read ` M .agent/reference/provenance.md` in git status and concluded the async lane's fix was uncommitted. Verified against the branch: the brace row has been gone since 66dfc9a and the consumer fixture since 13bdcd4. What the worktree held beyond HEAD was the IoBackend lane's 25 uncommitted rows. **A dirty-file marker says the file differs from HEAD; it does not say whose difference it is, or that the change you are looking for is missing.** Now in global-constraints.md, with the two commands that do answer the question.
- B2 has been on both sides of that in one evening — building from HEAD's blob correctly when it mattered, and reading a status marker as a claim when it did not. That pairing is why it is a rule rather than a commit message.
Task B1: review review-B1 (opus) — spec compliance PASS (every line of §2 item 6, nothing beyond, all three deferred A11 defects addressed, four upstream-layout departures each recorded and defensible); task quality CHANGES REQUESTED: 1 Critical, 3 Important, 6 Minor. Four of the six things I asked it to attack SURVIVED: the whenAny cancellation bridge intact on both branches with latch-then-decrement preserved verbatim through the collapse, the null-handle execution, the ThreadPoolExecutor inlining line-by-line against upstream's .cpp, and AsyncQueue::pop.
- **Critical, and the reviewer segfaulted it rather than arguing it:** a whenAll/whenAny fan-out gives the SAME chain root to N parks, and detail::Parked destroys it ONCE PER PARK. Twelve lines of this module's own vocabulary print two identical abandon addresses and crash at -O2; ASan says heap-use-after-free. Both combinators, both StopToken branches. The existing cases are the right shape at ARITY ONE, which is exactly why they pass. The missing invariant — *at most one live park per unowned root* — was true of a linear chain and became false the moment unownedRoot propagated through a combinator, which is B1's own addition.
- **Ruling R97: refcount the abandon root.** Single-holder trades a double free for a leak whose trigger is scheduling order. The fan-out reclaiming its children is arguably the correct ownership but moves the loop's abandon-to-fixpoint pass from children to join, and B3/B4/B5 are being written against ParkedWork right now — a redesign to schedule, not to land in a fix round. Refcounting is invisible to them.
- **Ruling R98: a join may span threads, and the counters become atomic.** JoinState::remaining is a plain size_t decremented from whatever thread resumed the child, and B1 is the task that makes a cross-thread child EXPRESSIBLE (it ships ResumeOn and ThreadPoolExecutor). Forbidding it would make the module contradict itself and IExecutor.hpp:21-24 already tells the reader the opposite. The reviewer's tell: **the state is refcounted atomically while the counter it protects is not.** Settles Q4 with it — the shared_ptr stays, because a join that may cross threads needs state outliving whichever thread finishes last.
- **My error, propagated to three lanes: "GCC is the only one of our three toolchains that diagnoses -Woverloaded-virtual" is false.** CoreCppToolchain.cmake:84 is CORE_CPP_GCC_OR_CLANG, so clang diagnoses it too and the claim was about MSVC. Worse, the reviewer COMPILED it: both in-tree executors declare both overloads, so every `using IExecutor::submit;` is currently a NO-OP and deleting all of them leaves nine static_asserts green. The negative control stays (it proves the concept discriminates); the comment must say what genuinely closes the hazard — submit(ParkedWork) is pure virtual, so no concrete executor can override only the borrowing half.
- Filed two gaps the review found while not looking for them: **core-cpp#31** (no hygiene rule compiles a header standalone, so the self-contained-header rule is unenforced and B1's dispatch line about the header self-check was satisfied vacuously) and **core-cpp#32** (the `no-tsan` label is excluded by build.yml:429 and applied to no test, so the exclusion is a no-op — the same defect as a gate that cannot report, wearing the opposite sign).
- **B2 unified the evening's failures better than my framing did, and it goes in the constraints as one thing: trusting a shorthand instead of asking for the thing itself.** Four instances from one careful lane in one evening — a **base** (the worktree, which is one lane's stale copy), a **variable** (expanded in the outer shell), an **exit status** (the pipeline's, not the command's), and a **dirty-file marker** (which says only that the file differs from HEAD, naming no author and implying nothing about what is in it). Each shorthand was close enough to pass a glance. The answering commands are all a few characters longer than the shorthand that misleads.
- **And a convergence worth naming: B2's stale grep did not distinguish the provenance rule's PROSE at line 25 from a data row** — which is precisely the bug A12 found in its own checker two hours earlier, where a note quoting a column name was read as the table header. Two lanes, same class, one in a tool and one in a human's grep, found independently. That is the strongest evidence yet that "structure is decided by structure, never by content" belongs in the rulebook and not just in a checker.
- B2's own closing position, which I am keeping: "I would rather the pattern be on the record than tidied away."
**Task B3's second commit landed: 0fd01f5 "net: a host-driven backend so an event loop can run inside the browser's".** Both commits of the plan's B3 are now in, plus 9f7eb5d "the two places outside src/ that still named the deleted event sources" — it ran the whole-tree sweep after the consumer-smoke breakage rather than only src/. `git status -- src/core/net` is clean, so B3 has landed everything and its report should follow. The kqueue re-arm defect is not yet fixed and becomes its fix round 1.
- C0 landed c973592 "fix(migrate): the codemod changes only the bytes it was asked to" — the CRLF normalisation finding (I4), where every changed file was being silently CRLF->LF converted including untouched lines, invisible to both the tests and the purity proof.
- **The signal is isolated.** Build 35545405016 on d7a7bee: failing jobs are `macos (llvm-22)`, `macos (appleclang)` and `ci-ok` reporting them — nothing else. Both consumer-smoke legs pass now (13bdcd4 a verified ancestor) and the hygiene row is long gone. So master carries exactly one live defect, B3's kqueue re-arm, on 2 of 24 jobs, and a push with the fix is an unambiguous single-variable experiment: green is the fix, anything else is a new fact rather than a confound. Told B3 to put its hypothesis in the commit message, since it cannot run macOS or BSD here and a fix it cannot test is worth less than a hypothesis CI can kill — and that Portability dispatches FreeBSD on demand as a second witness, because the two kqueue implementations are not the same code and a fix satisfying only one is worth knowing about before Task B7.
Task C0: fix round 1 complete (c973592, 6d93980, 4bde234, a4d91f5) — 90 unittest cases OK with 0 skipped (the libclang cases run for real), ctest migrate 2/2, check-renames 0 failures over 484 rows and 155 public headers, ruff clean. The round was not split and nothing shrank; C0's reason is the general one: **it divided by ARTEFACT, not by finding**, so carrying it whole cost one extra RED/GREEN cycle rather than a merge.
- **R95 arm (b) had already cost something, and this is the finding of the round: `src/core/async/CMakeLists.txt` carried a comment explaining that it avoids `list(APPEND)` in HEADERS BECAUSE THE GATE CANNOT SEE THROUGH ONE.** Somebody had shaped a build file around the bug — so the defect had stopped being a defect and become a constraint on how people write here, propagating as folklore rather than as a red. That is the most expensive form a silent gate failure takes. Header count 155 before and after says it exactly: the fix removes a blind spot rather than filling one, because the module that would have hit it was written around it.
- **I5 had a third defect neither the review nor I named: the snippet printed a count it never computed.** It quoted `pure: 16  not pure: 2` while only echoing failures — and the fix needed reading from a file rather than a pipe, because a `while` on the right of a pipe runs in a subshell and brings both counters back as **zero, which reads exactly like a clean run**. A guide whose subject is "do not believe a summary you did not compute" shipped a summary nobody computed, failing as a confident zero. C0's phrase: *this guide's own subject with the guide's name on it.*
- R93 claim one, in C0's words: **"Two doors were documented, one existed."** text_rows()/semantic_rows() excluded a `removed` row only BECAUSE the schema forces apply to none — a consequence standing in for a mechanism. Table._rewritable() now tests the kind.
- R93 claim two: **the five existing delivered-arm cases all passed against the copy**, which is why nothing caught it — the same shape as the reviewer's own RED observation, that the test proving a fix necessary is the one that stays green under the mutation.
- Unasked and not in any ruling: **validate() now reads the tree BEFORE the table**, so a table that fails to load cannot swallow a fact about the tree. Closes a hole I had not seen.
- R92's door closed on a gap already walked through once and repaired; R94's key is (profile, kind, scope, source).
- C0's concern 1 handled the way I want reports written: `core-cpp.async-link-smoke` is Not Run because the link fails with LNK1104 on kernel32.lib in a non-developer shell — the source compiles, the failure is environmental, and C0 refused to claim it green. **"Benign, but unverified by me" is a different object from a verified pass, and only one of them is evidence.**
- C0's concern 2 was sound reasoning with one gap, and I ruled it must run mkdocs: **docs/changelog.md is a single line, `--8<-- "CHANGELOG.md"`**, with `base_path: [".", "docs/snippets"]`, so the CHANGELOG **is** a rendered page even though it does not live under docs/. The config's own comment at mkdocs.yml:79 warns that a missing snippet is *silently dropped* — this evening's subject, sitting in the build configuration.
- **Second time tonight a correct principle applied to an incomplete map of the repository produced a wrong answer** (the other: a lane taking my dispatch's list as an inventory of the tables in a file). Both times the fix is the same: **ask the repository, not the model of it.**
Task C0: CLOSED. Five commits (c973592, 6d93980, 4bde234, a4d91f5, b23f838); 90 tests with 0 skips, both migrate ctests, check-renames 0 failures over 484 rows, ruff clean over 12 files, mkdocs clean; one item honestly carried as unverified rather than claimed.
- **The private-index hazard's THIRD costume, and the nastiest to read.** A private-index commit moves HEAD without telling the shared index, which keeps caching the pre-commit blob — so `git status` reads `MM <file>`: index-versus-HEAD differing by YOUR OWN CHANGE, INVERTED. It looks exactly like another lane staged a revert of what you just landed, and someone would have "fixed" it by re-applying on top and made it worse. Fourth step now in global-constraints.md: snapshot `git status --porcelain` before, `git reset -- <paths>` after, diff the snapshots. C0 caught it by diffing against a snapshot taken in advance — the expected-failure-count rule in another form: it knew what the answer should be before it looked. (R84 was the index deleting your new files; A12's was staging a reversal of a whole commit; this is a single hunk.)
- **Best reasoning of the round: "a conditional that is always true is a conditional people learn to evaluate wrongly rather than a rule they follow."** Checklist item 4 said run mkdocs *if docs/, mkdocs.yml or a public header's comments changed* — and step 5 requires a CHANGELOG entry on EVERY change, and the changelog is a page. So the condition was true every time it was ever evaluated, and it taught C0 to reason about when the gate applies instead of running it. The condition is gone; at 0.45s it was never buying anything.
- And the map was incomplete in THREE places, not the one I corrected: docs/contributing/index.md and docs/contributing/cpp-guidelines.md are the same one-line snippet pattern for CONTRIBUTING.md and .agent/rules/cpp-guidelines.md. C0 went and looked rather than taking my correction as the new complete map — the right response to a correction. b23f838 records all three in AGENT.md where someone reasoning about it will be looking.
- **C0's closing sentence is the second of the evening's two unifying lessons, and it is now the framing for a whole family of rules: "it is only a check if you commit to the answer first."** Deciding in advance what the output should be — how many failures, which rows, what git status will print — is what turns looking into testing. Without it, silence reads as success, a count reads as complete, and a clean run reads as a clean tree: three separate defects tonight, each caught only because someone wrote the expected answer down first. It is the companion to B2's "trusting a shorthand instead of asking for the thing itself": that one is about WHAT YOU LOOK AT, this one about WHETHER LOOKING WAS A TEST AT ALL.
- And C0's argument for why the framing I added to the private-index rule earns its words: the two older rules leave staleness that reads as *your work is missing*, which prompts caution; its own reads as *someone staged a revert of your work*, which prompts **action** — and the obvious action, re-applying on top, compounds the damage. A rule saying only "refresh the index" would not have stopped that. **Saying what the wrong reading looks like is what does.**
- Expected red, verified not re-diagnosed: Build 35546843248 on aae25fc failed the same two macOS legs. **B3's kqueue fix `1709a3c "fix(net): a muted registration's un-muting is asked where the answer is portable"` came AFTER that head** (`git merge-base --is-ancestor` says the fix is not in the failing run), and its own run is in progress — that is the decisive one.
- B13a's fix round 2 landed as aae25fc, "fix(tui): LanguageId::Last's doc named a guard that does not fire". A12's R96 landed as 898110b, "a tree-level check is asked once, not twenty-four times".
- Ruling on R96's open question: **vendor-selftest is labelled tree-level AND kept on one Windows leg**, overruling A12's single-witness proposal while taking its flag seriously. The vendoring contract's own subject is "reads git blobs with core.autocrlf=false, refuses CR bytes, refuses symlinks" — every one of those hazards is most likely to fail on Windows, so a Linux-only witness means the check cannot fail for the thing it exists to prove, on the platform where it would fail. Two legs at 93 s is still ~92% of the saving against 24.
- **Ruled the correspondence gate in now rather than filed:** nothing ties the `tree-level` label to the style job's list, so a new tree-level check with no style step runs NOWHERE, silently — the hole A12 just walked up to, since two of its own checks were about to vanish that way. Self-tested in both directions, because the second (a style step naming a check that no longer carries the label) is how it rots quietly after a rename.
- A12's list-checking is the pattern worth keeping: **my inventory was wrong in both directions** (vendor-selftest tree-level and the slowest check at 93 s, so the largest single source of waste; async-link-smoke not tree-level because it links a binary), and it found that by enumerating the registrations rather than implementing my list. 10 and 2, not the 9 and 1 I wrote.
Task B13a: fix round 2 complete (aae25fc) — core-cpp.tui 1035 cases / 4445 assertions, up from 1033/3870, green on four presets with clang-tidy and mkdocs clean. A cheap scoped re-review is running.
- **B13a's third probe is the finding, and it is worse than the reviewer or I knew.** We had established that a code appended after `Last` with a bare break was invisible. It appended one that **dispatches to a real highlighter**: clean build, green suite. Not a latent inconsistency — **a language that WORKS, reachable by id, invisible to every [0, Last) walker in the module.** Nobody asked for that shape; it tested it because the first two probes left a question open, and the answer changed what the guard had to be.
- And my sentence was BACKWARDS, not merely incomplete: the doc said an appended code "still satisfies the switch", and measuring showed the switch is the only thing that OBJECTS. A comment wrong in the opposite direction is worse than a vague one, because it tells the reader the compiler has their back.
- **The guard's comment records what it cannot see** (an enumerator above Last whose case only breaks and which no table names — C++ cannot enumerate enumerators), and B13a's reason is the one to copy: *a test comment that overclaims is how this finding happened in the first place.* A guard that names its own limit cannot become the next version of the defect it closes.
- Carried into Task B13's brief, in B13a's framing: `no_language_hides_above_Last` closes the USABLE hidden language, not the inert one; certainty about the enumerator SET rather than about behaviour needs a source scan over the enum block, outside C++. B13a declined to add it on its own authority — right, because it is a gate and gates are decided where the other gates are. Asked the re-reviewer for a second opinion before ruling.
- Third sighting of the shared-index staleness, this time with the index lock held by another lane's live git process. B13a routed around it with a private index and did NOT remove a lock that was not its own. The whole family is now in global-constraints.md.
**My kqueue hypothesis was refuted on two independent grounds, and my error is this session's own class.** B3 showed the Catch2 section header said `backend=poll`, **two lines above the failure in the log I had already fetched and grepped** — I read the file path and the test name, saw KqueueBackend.cpp in the directory, and wrote a hypothesis on top of a summary of evidence that was in front of me. FreeBSD, running the same PollBackend.cpp and KqueueBackend.cpp, had been green on 9f7eb5d, so kqueue was never a candidate. The second ground is worse because it was not haste: setInterest(h, Interest::None) never reaches arm(), it routes to disarm() which ends `armed = false`. B3's reformulation answers my own design question: **read `armed` as "has filters in the kernel" rather than "is live", and "should a muted registration count as armed?" answers itself** — muting removes the filters, so it must be false.
- **B3's actual finding, in its words: "I wrote the careless form one case away from the comment explaining why it is careless."** The case armed a second registration on a descriptor the first was still watching, then asked whether the second was dispatched to. Linux's and FreeBSD's poll(2) fill in every matching pollfd; macOS's reports the descriptor once; IoBackend deliberately promises neither, and the sibling case two rows below asserts `first || second` for exactly that reason. Fixed in 1709a3c by detaching the control first, so the property is asked of a descriptor with one registration.
- **renames.json is GENERATED as well as hand-edited, and B3's instruction replaces mine.** Its flow reads the whole file, parses, inserts and writes back, so any edit committed between its read and its write is silently reverted — it dropped another lane's hand edit and, separately, its own rows. "Hand-edit, do not generate" fixes nothing, because an editor's buffer predating a commit has the identical failure mode. R87 one layer up: not the index, the file. Re-run the transform against the current HEAD blob, produce a HEAD -> yours patch in a scratch tree, `git apply --cached` it. In global-constraints.md.
- **A catch I would not have made: `removed` rows must be fully qualified.** B3 spelled them `core::net::FdToken` and `core::net::WaitOutcome`, because `core::tui::runtime::FdToken`, `FdInterest` and `WaitOutcome` still exist and stay legitimate until B12 — **a bare `FdToken` row would have told every consumer to delete a type that is still there**, a codemod confidently breaking six repositories on a name collision. Now a rule.
Task B4: dispatched impl-B4 (opus) on B3's word that its return follows the CI result rather than waiting on it. Carried three of tonight's lessons into the dispatch explicitly: a dispatch's list is a starting point and mine have been wrong in both directions today; it is only a check if you commit to the answer first; and the whenAll/whenAny chain-root double-free is being fixed under R97 right now in the same object its teardown work touches, so read that fix rather than collide with it.
Ruling R96 CLOSED (898110b + 6821256): 12 tree-level checks asked once in `style` instead of 24 times, with vendor-selftest keeping a Windows witness on cl-debug (21.7s there) — A12 accepted that my framing was the better one: not "its platform-invariance is the weakest claim" but **the check cannot fail for the thing it exists to prove, on the platform where it would fail**.
- **The correspondence gate found a defect in ITSELF before it found anything else, and no fixture could have produced it.** Its first pattern read the step's own DOCUMENTATION PROSE — *"carries a `# covers:` marker"* — as a marker covering a backtick, so the gate refused a tree that was entirely correct. R81's shape a third time, and **the false positive lived in the documentation A12 had just written**. Only running it against the real files caught it. Strongest case yet for proving a gate on the real artefact rather than only on a constructed one.
- Three design points recorded: **the link is declared, not inferred** (it refuses to compare the step's command with the test's, because they differ on purpose — the step runs before anything is configured, so a drifted table is the first thing a PR hears rather than the twenty-second, and a comparing checker would forbid the better arrangement); **the parser has a control that refuses to disagree with itself** (counts label sites and fails on a mismatch, explicitly because of tonight's row-swallowing bug — the lesson applied one level up rather than merely noted); and **the gate is itself tree-level with a step of its own**, "the smallest case it has to get right", because a gate that does not cover itself is the first gap in its own coverage.
- R84 recurred and was repaired with `git reset -- <paths>` — the form A12 argued I should unban and I did. Simpler than the update-index --cacheinfo dance it used before that ruling existed, so the correction paid for itself within the evening.
Task B12b part 2: dispatched to impl-A12, **reframed as a SURVEY rather than a port.** The plan says port each behavioural upstream commit test-first and bump the row; doing that against core::net and core::async while B4 and B6 rewrite those files would collide, and worse would port a fix INTO CODE THAT IS BEING REPLACED. So: produce the table (upstream commit -> core-cpp file -> behavioural or not, one-line reason), port nothing, bump nothing, and hand each behavioural commit to the task that owns that file for me to route into its brief. **If fastcached fixed something in its reactor after 0708dd54, B4 and B6 should be writing that fix in rather than having it ported into their freshly written code three tasks later.** A survey now feeds the tasks; a port now fights them. B13 re-runs it to confirm nothing behavioural remains unported, which is the check the plan actually wants and will be cheap by then.
**MASTER IS FULLY GREEN. Build 35546989595 on 1709a3c: success, 24 of 24 jobs, zero non-success.** Verified independently, including the empty failed-job list. It contains every lane's work through that head: B13a's seam and both fix rounds (f61d57b, aae25fc), B3's IoBackend, host-driven backend and the poll fix (e7963de, 0fd01f5, 1709a3c, 149659a), B1's async grafts (b21229d, f72cabc), B2's error vocabulary (8a88ce0), the consumer-smoke repair (13bdcd4) and the provenance fix (66dfc9a). Portability (FreeBSD) is green on the same head.
- **So B3's diagnosis is confirmed and mine is refuted by the machine as well as by argument:** the defect was macOS's poll(2), the fix was in the test asking a portable question, and the two macOS legs cleared without a line of kqueue changing.
Task B13a: COMPLETE (f61d57b..aae25fc) — the registration seam, the retraction of a false cross-registry guarantee, the third built-in table brought under the golden treatment, three uncovered call sites covered, and a public header's guard made real. 1035 cases / 4445 assertions on four presets. CI green on a head containing it, attribution confirmed by evidence rather than argument.
- **B13a's method note is the most reusable thing it said, and it is deliberately not a story about inspiration:** *"the credit belongs to the first two probes leaving a question shaped like a question. Once 'bare break is invisible' and 'the compile-time check fires only when half-corrected' were both true, 'what if the thing above Last actually works' was the next thing to ask... the method is to keep probing until the remaining gap is one you can STATE, not one you can only gesture at."*
- And on the backwards doc sentence: *"the thing that caught it was measuring a claim I had written myself and believed."*
- B13a also sharpened why the private-index variant C0 found is the dangerous one: the staleness reads as someone staging a revert of your work, and **the instinct it provokes is to "restore" your files — which writes the stale state back into the shared index for everyone.**
Task B3: returned DONE_WITH_CONCERNS, e7963de..1709a3c (six commits; the plan named two, four are follow-ups). 821 assertions / 139 cases in core-cpp.net and 85/19 in core-cpp.net_backend, green on seven configurations including emscripten, with **16 arm-removal REDs captured verbatim** (8 backends, 5 host-driven, 3 refusal paths). Build 35546989595 24/24 and Portability 35547462130 success. Task review dispatched.
- **Ruling: `ReadinessHandler::slot`'s omission is UPHELD.** The spec declares it; B3 omitted it because nothing in B3 reads or writes it. A public field with no reader has no defined meaning and the first writer would define it, which is backwards from "the spec is a contract". B3's worry — that adding it later is a public-header change — is true and CHEAP, precisely because nothing has shipped. B7 is the task with the real user (IOCP's refcounted, generation-checked slot, lpOverlapped never pointing into the handler). Recorded as a spec deviation; in B7's obligations; reviewer asked to check the one thing B3 could not, that its absence does not make some other part of the contract unimplementable.
- **Ruling: the EBADF probe substitution is accepted and the original routed to B4.** B3 is right that "what does a backend do with a descriptor closed underneath it" is a question about who closes and in what order — loop lifetime, B4's. Its substitute, *a dead handle in the wait set does not blind a backend to a live one*, is the property B3 can actually answer, and answering the answerable question beats gesturing at the other. Sent to B4 explicitly, because **between two tasks is exactly where a deferral becomes a hole.**
- **Ruling: the Wfmo >64-handle sweep gets a case — overruling B3.** Its argument is that core-cpp#6 removes the backend once IOCP is green. True, but it stays one release as IOCP's fallback so a consumer can select it, and "it is going away" has been the argument for every piece of code that then did not. The chunking LOGIC in detail/WaitChunking.hpp is tested on every OS; what is untested is Wfmo's USE of it past the limit, and the integration is what fails in the field. Told to measure before arguing if it turns out expensive rather than merely fiddly.
- Sharper than my note: a host-driven loop today does something WORSE than refuse run() — nothing consumes isHostDriven(), so a caller gets a **hot spin**, because wait() returns immediately by design. Routed to B4, which already has the death test in its dispatch.
- **B3's §7 is now a constraint, in its framing: a local green is not evidence about something your local run cannot reach.** Both its defects that reached master shared that one shape — ConsumerSmoke.hpp compiles only in CI, and the platform that disagreed is one none of us runs. Naming the shape rather than the two incidents is what makes it usable by the next task.
- **Controller audit of the CHANGELOG's [Unreleased] section after five concurrent editors found a FOURTH document carrying the stale CompileCache pin — and it is in the same file as the correction.** CHANGELOG.md:1070's **Imported** table still says the two verbatim cmake files came from eb9c9c68, while CHANGELOG.md:676 in `Changed` correctly says the pin moved to f6ec49f3. So one document gives two answers, and the one that looks authoritative — the table whose whole job is "which commit each file was taken at" — gives the old one. NOTICE and provenance.md are both correct; I checked all three rather than assuming A12's fix had reached them.
- The shape that makes it easy to miss: **the row is not wrong, it is wrong for two of its five subjects.** The bounded CPM bootstrap, the Windows error-popup suppression and the .clang-tidy IgnoredRegexp genuinely came from eb9c9c68 and stay; only the two verbatim cmake files moved. It is a row split, not an edit.
- **And R88's gate cannot see it**: it holds NOTICE's (verbatim) pins against the provenance table, which is the right pair and is why NOTICE is correct — but the CHANGELOG's Imported table is a THIRD statement of the same fact and nothing checks it. A12's own lesson one document further out, in the version where they are all in the same repository and two of them in the same file. Asked A12 to decide whether the gate extends, suggesting the same (verbatim) restriction it argued for NOTICE, since the table's non-verbatim rows describe merges whose pins legitimately differ.
- Worth recording how it surfaced: not from a gate, and not from anyone working on that file — from reading a section five lanes had been writing to concurrently, looking for exactly that kind of divergence. An argument for the periodic audit as much as for the gate.
Task B13a: re-review rereview-B13a-r2 (sonnet) — **all three items ADDRESSED, no new Critical/Important.** Verified by running in a private detached worktree at aae25fc, removed afterwards, nothing in the shared checkout touched. It reproduced the third probe itself (Rust after Last with a case dispatching to highlightCpp, no table row) and saw the guard catch it at `LanguageId value 15`, full suite 1034 passed / 1 failed; **built the exact inert shape the guard's comment says is invisible and confirmed the suite stays green at 4445/1035** — so the comment is accurate rather than modest; and verified the streaming split's asymmetry by breaking each call site in turn. It also checked both renames.json notes' pasted code against the REAL signatures rather than merely confirming the notes exist.
- **Ruling: Task B13's gate does NOT get a source-level enumerator scan.** Implementer and reviewer agree independently, and their reasoning holds: the behavioural guard closes the harmful case (a hidden language that WORKS), and the residual case is inert BY CONSTRUCTION — unreachable, unnamed, observably identical to None/Last — so no runtime probe could close it and no user can trigger it. Standing up a source-scanning mechanism to catch a three-line diff a reviewer sees is a bad trade, and tonight has produced three separate instances of a gate firing on a true negative. It stays the documented limitation it already is; no follow-up ticket, because an issue nobody will act on is another thing that reads as tracked.
Task B13a: **CLOSED** — f61d57b..aae25fc, four commits across a task and two fix rounds. Delivered: the registration seam that takes endo's language out of core-cpp (core-cpp#24), a false cross-registry guarantee retracted and replaced by a documented precondition, the third built-in table brought under the golden treatment, three uncovered call sites covered, and a public header's guard made real. 1035 cases / 4445 assertions on four presets; CI green on a head containing it, attribution confirmed by evidence.
- core-cpp#31 and #32 are recorded as Open work entries (9dbac94), both claims verified before writing rather than taken from my message: `cmake -DLIST_RULES=ON` lists all eighteen hygiene rules and **every one reads text — none compiles anything**, so a header needing another included first passes every check this tree has and breaks only for whoever includes it first, **which in a library is a consumer**; and a tree-wide grep finds `no-tsan` in exactly one place, the exclusion itself. A12 also confirmed both issues OPEN before writing, because the rulebook makes the leading reference load-bearing.
- Useful refinement for whoever implements #31: enforcing a self-contained header is a **per-leg** test, not a tree-level one — whether a header stands alone is a question for each toolchain. That distinction did not exist before R96 created the categories.
- **My map of the repository was incomplete for the THIRD time tonight.** I pointed A12 at AGENT.md's label list; `.agent/rules/testing.md` holds the **canonical** one, and it did not mention `tree-level` at all — the label that now decides where a check runs. The other two: a dispatch list taken as an inventory of the tables in a file, and a docs gate judged inapplicable because the changelog does not live under docs/. Same fix each time: **ask the repository, not the model of it** — and when correcting someone else's map, check that mine is complete first.
- A12's reason for keeping the sentence as well as the gate, which is why I asked for it: **a gate tells you that you broke a rule and the sentence tells you what the rule is — they are not substitutes.**
- And C0's third-document discovery is already paying: A12 ran `mkdocs build --strict` for a rulebook edit because `docs/contributing/cpp-guidelines.md` snippet-includes that file. A fact found two hours ago by a different lane, applied without being told.
- **B13a corrected the credit, and its version of the rule is better than mine.** "A dispatch's list is a starting point, never an inventory" did not indict my dispatch so much as its reading of it: my dispatch named four places and said *"that seam is what you build"* — describing the API surface to remove, not claiming to enumerate the tables. It **treated a scope statement as a completeness statement**, and those look identical on the page. The transferable rule is on the READER's side and is now in global-constraints.md in its framing: *when a list is handed to you, ask what question it was answering, because it was probably not the question you are about to use it for.*
- It also preferred my justification for the enumerator-scan ruling over its own, and said why: it argued from COST (a scanning mechanism is a lot of machinery for a three-line diff); "inert by construction" argues the residual **is not a defect at all** — something unreachable, unnamed and observably identical to None is not yet a language. And it flagged that declining the follow-up ticket is the part it would have got wrong: its instinct was to file one.
- **Intelligence for Task B12, from the lane that owns core::tui, worth an hour when it opens:** `runtime/PollEventSource.cpp` is the one file in the module that still picks its platform with an `#ifdef`, so `.agent/rules/platform.md`'s rule lands in the same task; and **`core::tui` does not link `core::net` yet, so `cmake/CoreCppModules.cmake`'s `tui` row must be extended in the same change or configure refuses the link.** Both go into B12's dispatch.
- Its note on why the external check mattered: it wrote the guard's limitation *from the inside*, reasoning about what its own test could see — "having someone else build the shape and watch the suite stay green is a different kind of evidence, and it's the kind that would have caught me if I'd been wrong about the limit in the same way I was wrong about -Wswitch."
Task B12b part 2 (the drift survey): **EMPTY, and for a structural reason A12 proved rather than assumed — contour's `origin/master` IS `6777ff05` and fastcached's IS `0708dd54`, the exact pins.** Zero upstream commits after the pins at row level (20 async + 70 net rows) and zero at the deliberately wider directory level. It did not stop there: a control from earlier bases returns 11 fastcached and 37 contour commits, proving the probe can see commits when they exist. Its own framing is the one to keep — **a zero that cannot become non-zero proves nothing** — and it is the C0 rule (*it is only a check if you commit to the answer first*) applied to a negative result, which is the harder direction.
- **The substantive consequence, and it answers the worry that made me reframe the task:** B4's and B6's dispatches say "fastcached at `0708dd54`, read as blobs" — and that IS current master, so those lanes are already reading the newest upstream. My concern that they might be writing code a later fastcached fix would have to be ported into three tasks later **does not arise**. Clean no. B13 still re-runs the survey, which will be cheap and is now the check the plan actually wanted.
- **A12 separated a gap from rot before reporting either: 74 of fastcached's 82 non-test Async/Net files have no provenance row.** That is Phase B's remaining import work — `IReactor.hpp` to B4, `DeadlineTimer`/`SleepUntil`/`InterruptibleSleep` to B5, `IocpReactor` to B7 — not a coverage failure. Its reason for checking: *"'0 drift across 90 rows' could otherwise be mistaken for '0 drift across the upstream', and it is not the same claim."* Recorded here so nobody later reads the 74 as decay.
**Ruling R100: a verbatim row in the CHANGELOG's Imported table may name only verbatim files, enforced by refusing a semicolon in such a row.** A12's refinement to R88's extended gate, and one I had not specified — I gave it "restrict to (verbatim) rows" and stopped. NOTICE's grammar is one bullet per file; the Imported table's is one row per many subjects in prose. So a gate that honours a row's `verbatim` claim and walks its paths would examine only the subjects before the first separator and report a pass over the rest — **a check quietly examining less than it claims, which is tonight's drift-checker bug one level up.** Refusing the row rather than parsing around it makes the document say what the gate can check. Both mutations proved on the real CHANGELOG with counts predicted first: the row as it stood → 1 refusal, split but stale → 2. Landed as `455b21b`, closing the fourth stale `CompileCache.cmake` pin.
**Ruling R99 is VOID — it never existed.** `task-B1-fixround1.md`'s title reads "Rulings R97–R99"; only R97 and R98 were ever assigned, and I3/I4 carry no ruling number. B1 found this by grepping for R99 across the session and reporting that it appears in that title and nowhere else, rather than inventing content for it or silently skipping a finding it could not identify. It treated both as ordinary findings and did them in full, which is correct. The number is retired rather than reused, so the audit trail stays readable; the next ruling is R100 above.
- The failure is mine and it is the same one as the label list and the dispatch inventory: **I wrote a range from memory instead of counting.** A range in a title is a claim about how many things follow, and nothing checked it but the reader.
Task core-cpp#31 (the header self-check): dispatched to impl-A12, **before the v0.1.0 tag rather than after.** A12's own finding — eighteen hygiene rules, every one reading text, none compiling a header standalone, so the C++ guidelines' self-contained rule is enforced by nothing and breaks first for whoever includes a header first, **which in a library is a consumer.** Six consumer migrations are about to do exactly that in orders nobody here has tried. Per-leg by A12's own distinction, so it cannot ride the `style` job.
- **The dispatch makes the cost question the design question**: ~155 public headers is ~155 TUs per leg, so measure on `clang-debug` and `cl-debug` with fastcache-cc running normally and report wall-clock BEFORE choosing a shape. If the thorough shape is unaffordable, say so with the number and propose the trade — because a check covering half the headers while claiming all of them would be tonight's recurring failure written into the gate that exists to catch it.
- Told to **report the first run's real defects rather than fix them**: some will land in files B1, B3 and B4 have in flight, and a stranger's fix in another lane's file is the concurrency hazard behind half tonight's rules.
**Controller audit #2, while three lanes ran: the vocabulary sweep came back CLEAN, and the count did not.** I grepped `.agent/`, `docs/`, AGENT.md and README for pre-rename vocabulary (`EventSource`, `FdInterest`, `makeDefaultEventSource`, `core::coro`, `IReactor`, `PlatformReactor`, `TestReactor`, `BadFileHandle`); every hit is a migration-guide old-to-new mapping, an upstream filename in provenance, or fastcached's own name used in context. Nothing stale. Recording the clean result rather than only the finds, because an audit reported only when it hits is an audit whose hit rate cannot be read.
- **Then I counted test binaries and found a two-document divergence.** `.agent/rules/testing.md:13` states the rule as *"one test binary per module ... and a second one where a module's tests must also run under a DEFINITION that changes what its headers declare"* — while lines 21-25 of the same file correctly document `core_cpp_add_test(net NAME net_types ...)`, a second binary existing for a reason the rule bullet says is not a reason. **One document, two answers, the authoritative-looking one wrong, both on the same screen.**
- Counted, not remembered: 13 registrations over 8 modules. base/log/cli/platform/testing have one each; `async` has 2, `tui` has 2, **`net` has 4**. Of the five extras only `async-fallback` is a definition — `net_types`, `net_tls` and `tui_output` link a narrower or conditional TARGET, and `net_backend` exists for PLATFORM AND LABEL scope (builds under Emscripten, carries neither `loopback` nor the timeout).
- **Why it drifted is the reusable part: `net_types` came from B2, `net_tls` from A6, `tui_output` from A7 and `net_backend` from B3, and not one of those lanes owned the sentence.** Each addition was correct and documented in its own CMakeLists with a good comment; the prose claiming how many there are belonged to nobody. Same shape as the fourth stale pin and my own R99 range: **a completeness claim with no owner and no check.**
- **Routed by file ownership, not by authorship.** `.agent/rules/testing.md` to A12 as an addendum to #31 — it is the file A12 must read to register the new per-leg check correctly, so a rule pointing at the definition form would actively misdirect it; the fix is to name the three grounds rather than enumerate the binaries, because an enumeration is the completeness claim that rotted. `AGENT.md:130`'s misleading parenthetical to B1, which is already opening AGENT.md for an unrelated minor this round. Each lane told explicitly to leave the other's file alone. Authorship would have pointed at B3, which only added the fourth; the sentence was already false before B3 existed.
- **B3 landed the Wfmo >64-handle case I overruled it on: `870d12b`, "the >64-handle sweep is asked of the backend, not only of its arithmetic".** The commit subject is the ruling's own distinction back in its words — the chunking logic in `detail/WaitChunking.hpp` was always tested; what was not was Wfmo's USE of it past the limit, and integration is what fails in the field. Recording it because overruling an implementer is only justified if the result is worth what it cost, and this one is.
Task B7 dispatch WRITTEN (`task-B7-dispatch.md`), ahead of B6 rather than after it, while B3's backend design is still cheap for me to hold. It carries: the readiness bridge as the part that does not exist upstream (threadpool wait for a waitable HANDLE, zero-byte `WSARecv` for read, `WSAEventSelect` + threadpool wait for write) framed as **the merge itself — one backend serving a server's sockets and a TUI's console input**; `ReadinessHandler::slot` as B7's obligation with the reason it was left (B3 had no reader, B7 is the first writer, and `lpOverlapped` must never point into the handler); G1/G3/G4 as asserted rather than documented, with the G1 second-thread canary; and fastcached's three load-bearing scars each with its issue URL (#465 the op holds the Impl, #884 data beats a later stop, and the `MSG_PEEK` classification at `IocpSocket.cpp:327-334`, whose failure mode is a spurious EOF that reproduces once a week in production and never in a test). `NtAssociateWaitCompletionPacket` is named as an optimisation behind a probe, explicitly not the path.
- Written now for a reason worth stating: B5, B6 and B7 are all queued behind B4, and a dispatch composed under time pressure is where my completeness claims have failed tonight. Three of them are written before they are needed.
Task B3 fix round 1: landed as `870d12b`, 5 files +199/−3, 29/29 on `cl-debug` including all 14 hygiene tests. The Wfmo >64-handle case I overruled B3 to get **cost 21 ms** for all four boundary indices at 80 registrations each — the wait set is waitable HANDLEs, so a registration is a `CreateEvent` rather than a loopback connection. **My ruling was right for the wrong reason:** I argued from "it stays one release as IOCP's fallback"; B3 measured and found cost was never the question. Its own verdict: *"my 'it is going away' argument was wrong on the facts as well as on the principle."*
- **The most valuable thing in the report is that B3's first arm-removal RED came back GREEN, and it read that as data rather than as a broken case.** Remove the multi-chunk sweep: green, because the rotation reaches chunk 1 on the next `wait()`. Pin the rotation and keep the sweep: green, because a full sweep walks every chunk. **Only removing both reddens**, and then exactly at indices 63 and 79 while 0 and 62 stay green. So the property is defended TWICE and the case pins the property rather than either defence. The consequence is the durable one: **`nextWaitRotation` is not redundant, it is the second of two independent guarantees**, and someone would eventually have deleted it with a passing suite as their evidence. Now a constraint.
- **`WfmoBackend` requires LEVEL-TRIGGERED handles, and nothing said so.** Its wait is a detector (`WaitForMultipleObjects`) plus a rescan (`WaitForSingleObject(h, 0)` per registration) — and `WaitForMultipleObjects` **consumes** an auto-reset event when it returns it, so an auto-reset handle is eaten by the detector, invisible to the rescan, and dispatched to nobody; the flow parked on it hangs. A precondition rather than a defect (a `WSAEventSelect` WSAEVENT is manual-reset, and so is the wakeup channel's), now stated in `WfmoBackend.hpp`. **Found because the fixture's own control failed alongside everything else** — the case reporting that the CASE was broken, read that way instead of hunting the backend. A control that fails on its first run has earned its place; most never fire.
- **B3 dropped a starvation case it had written**, because `collectSignalled`'s rescan is global and serves everything whatever the sweep did, so it asserted nothing the boundary case does not. *Assert what distinguishes* cuts both ways — it removes cases as well as demanding them, and a dropped case reported with its reason is worth more than a kept one asserting nothing.
- **B3 removed its own host-driven header hunk on finding B4 had already written one** in the working tree (`IoBackend.hpp` now reads *"it advances only when the host pumps it, and `@c EventLoop` asserts both"*, with `setPump` virtual and `HostDrivenBackend` given `override`). Correct, and better than the alternative it avoided: a "neither refuses yet" warning contradicting a file another session is actively editing would have read as fact to whoever saw it next. Concern 3 is B4's and is closing.
- **R84 bit exactly as warned:** after committing through the private index, the shared index encoded `D src/core/net/windows/WfmoBackend_test.cpp` — a deletion of the file B3 had just added. Caught by `git diff --cached --stat`, repaired by path-scoped `git reset`. The fourth step of the private-index procedure earning its place again.
**New constraint, and it is the operationally urgent one: a shared checkout's working tree is not a buildable state and was never promised to be one.** B3 found `core-cpp-net` failing with ~30 errors because B4's new `EventLoop.hpp` is in the tree while `EventLoop.cpp` is not. Five lanes edit concurrently, so at any moment some header is ahead of its source, and **a red build there is not evidence about your change until you look at whose files are in the errors.** The standing answer is B3's: a throwaway `git worktree` at `origin/master` plus your own files — one configure, complete isolation. Broadcast to B1, which is building right now. **No blame lands on B4**; a partial edit mid-task is what a shared checkout is, and framing it as someone's failure would produce the wrong fix.
- **A12 corrected its OWN instrumentation, unprompted, and named the failure as the one it had been flagging in others all evening:** its CI monitor watched `898110b`, an ancestor of its later commits, so it could have fired green on a run not containing `455b21b`. Replaced with one watching `455b21b` that names failing jobs on a red. Now a constraint — **arm a monitor on the commit you want covered, never on an ancestor** — and worth recording that it came from the lane auditing itself rather than from a review.
- **Range hazard I am carrying, not B3: `review-B3` is running over `e7963de..1709a3c`, six commits, and `870d12b` is a seventh outside it.** It is a fix round for my concerns rather than for review findings, so the review will return without having seen it. I will carry `870d12b` into the re-review rather than let a clean review read as a statement about it. Told B3 explicitly not to read a clean review that way. **This is the same defect as A12's ancestor-monitor, one level up: a verdict whose range does not cover the work it appears to bless.**
- **Controller verification of B3's level-triggered precondition: TRUE on both halves, and the sentence carrying it is tonight's recurring defect.** I chased it rather than took it, because a wrong precondition here is a hang. The wakeup channel: `SystemPipe.cpp:397` `WSACreateEvent()` associated at `:407` via `WSAEventSelect(pair[0], event, FD_READ|FD_CLOSE)` — manual-reset by definition, and `:218` already resets it deliberately before draining. The WSAEVENT path generally: `SocketsWin32.cpp:87`, `WindowsListener.cpp:190,246`. B3's own fixture: `CreateEventW(nullptr, TRUE, FALSE, nullptr)`, so the control is honest about what it tests.
- **But the sentence names two handle classes and there are three today, four at B12.** The third is `core::platform::Wakeup`, which a consumer registers directly and `windows/WindowsWakeup.cpp:14` creates as `CreateEvent(nullptr, TRUE, FALSE, nullptr)` — manual-reset, so the rule holds, but it is neither a WSAEVENT nor the wakeup channel. **The fourth is the console input HANDLE that B12 registers, and it is not manual-reset in Win32's terms at all** — it is signalled by input-buffer state, so waiting does not consume it and the property holds for a different reason than the sentence gives. A reader checking a console handle against "manual-reset" finds it is neither and cannot tell whether that is safe.
- **So: an enumeration standing in for a rule, with no owner and no check, in a set about to grow — written tonight in direct response to that shape.** Same defect as the CHANGELOG's Imported row, the test-binary count and my void R99 range; that is four instances in one evening, which is enough to call it the dominant failure mode of this project rather than a run of bad luck. Routed to B3 for fix round 2 rather than a commit of its own: state the PROPERTY (a handle whose signalled state survives the detector, whether by being manual-reset or by being signalled by state rather than by an event), give the current handles as examples, and add the instruction an enumeration cannot give — **check a new handle class against that property when you register one**, which is the sentence B12 needs and would not have got.
Task B3 review (review-B3, over `e7963de..1709a3c`): **spec PASS** with three additive/defensible divergences; **quality approved with changes** — 2 Important, 6 Minor, nothing Critical. Verified by running in private worktrees: WSL `clang-debug` clean with **zero warnings**, 25/25; Windows `cl-debug` 26/27, the one failure `core-cpp.vendor-selftest` failing to `git add` in its own `%TEMP%` scratch repo — environmental, not B3, and all four `net` binaries pass.
- **It cleared the things I most wanted attacked, by running them.** Rule 1 is **structurally impossible to break** rather than merely untested: `detail/ReadyBatch.hpp:129-132` is the ONLY call site in the module that invokes a `ReadinessCallback`, it raises the guard, and the loop's only resume site asserts it is down — verified by grep across every backend including `ScriptedBackend`. The `Interest::None` fix is right, and it swept every exact-count assertion in the parity suite for the same carelessness. `HostDrivenBackend`'s four dispatch requirements all have cases on every OS including Emscripten. It would uphold the `slot` ruling independently, on a better argument than mine: B7's need is a different mechanism — **a kernel-queued completion cannot be withdrawn** — and the field is purely additive.
**Ruling R101 — F1 inverts: the defect is the "instead of", and the two backends the reviewer called wrong are the two that are right.** The reviewer measured a peer hangup as `readable=0 writable=0 failed=1` on poll and epoll, against `onReadable` on kqueue and Wfmo, with `IoBackend.hpp:95-99,145-155` and the rules file all stating it as uniform, no parity case asserting it (`grep '\.failed'` returns nothing) and `EventLoop::registerFdWaiter` nulling `onError` so production cannot see it. **B6 is the first code that will set it.** I took neither offered remedy.
- **`selectReadinessCallback` (`IoBackend.hpp:176-190`) returns exactly ONE callback and `Failed` with `onError` set beats a watched direction.** So the moment B6 sets `onError`, a hangup arriving as `POLLIN|POLLHUP` — **a socket with buffered data still unread** — returns `onError` alone on poll and epoll, the reader is never woken, and those bytes are never read. Not a diagnostic divergence: **data loss, armed and waiting for the first handler that sets the field.** kqueue and Wfmo return `onReadable`, the reader drains then reads 0. Remedy 1 would have propagated the defect to the platforms that got it right.
- **And mapping `EV_EOF` to `Failed` would be its own regression**: `EV_EOF` on a read filter is an ordinary EOF — the peer called `shutdown(WR)` — so macOS would begin reporting every normal close as a failure. Recorded in the fix round so the next reader of F1 does not reach for it.
- **What decided it: ask what the real users need.** A reader needs waking so it can read 0; a dial needs waking and then checks `SO_ERROR`, the portable connect idiom everywhere, which never consults which callback fired. **Neither needs `onError` to be correct** — so it is diagnostic, never load-bearing, and "instead of" made it harmful by stealing the wakeup both depend on. Ruling: a watched direction wins; `onError` only when `Failed` arrives with NEITHER direction set (the `POLLERR`-only failed connect it exists for); `Failed` documented best-effort; the "called **instead of** the two above" clause deleted, because that sentence IS the bug; and the parity case pins what is portable — **a peer hangup wakes the watched direction on every backend.** B3 told to stop and report rather than implement under protest, since B6/B7/B8 are downstream.
**Ruling R102 — F2: G1 and G3 are B4's, and the hand-off was the defect.** G2 is covered (it is the Rule 1 machinery). G1/G3 have no assertion anywhere — no `thread::id`, no `isOnWorkerThread`, no `teardownIsSerialisedWithDispatch`, though the spec declares the last two. Sent to B4 while it runs, with the motivation that makes it a case rather than a comment: `PollBackend::wait` keeps a `static thread_local` pollfd vector (`posix/PollBackend.cpp:107`), so a second thread builds its own vector from a `_registrations` another thread is mutating — **worse than a crash**, a torn read under TSan at best and a silently wrong wait set at worst; and the Rule 1 guard is itself thread-local (`ReadyBatch.hpp:42`), so a cross-thread resume would not trip it. B3's part is one paragraph in §4. The reviewer's transferable point: **§4 is the document B4 is written against, and a guarantee that appears in neither the code nor the hand-off list is how one gets lost.**
**Ruling R103 — the workflow checklist overstates, and B3 is right.** Steps 4-5 read as "every change edits `CHANGELOG.md`"; four same-day commits do not, **including `c99ca38`, which wrote the words**. `455b21b` does and is the one that fits — a new hygiene gate is something a release-notes reader needs. No eighth commit from B3; `870d12b` correctly has no entry.
- **Step 4 survives on its OTHER leg, and that distinction is the ruling.** Its justification had two halves: "every change edits the CHANGELOG" (false) and "the condition it replaces cannot be evaluated without reading `mkdocs.yml`" (true, and stronger alone). **A condition you cannot check is more expensive than the check it guards.** B3 ran it retroactively rather than argue from the version of the rule it had read first — clean in 0.43 s.
- Exact replacement text handed to B1 so the edit costs it no judgment; B1 already holds AGENT.md this round, so file ownership beats authorship again (`b23f838` was C0's). C0 told what happened to its text and why the half of its reasoning that was right is the half kept. **B3's framing into the constraints: a rule that is visibly not followed stops being read as a rule, and takes the steps around it down with it.**
- **F5 routed early and sequenced across two lanes:** `ScriptedBackend::wait` ignores `Interest`, so a B4 mute case would get the wrong answer and read as a defect in B4's loop. B3 asked to land it first; B4 told not to build one until it does, and to report if that blocks rather than work around it.
- Build `35548899124` on `455b21b`: **success.** Covers A12's Imported-table gate and B1's `0a77bfa`, `4d20406`, `c99ca38`. `ceb471d` superseded; `870d12b` pending.
- **B3 verified the two handle classes I cited rather than taking them** (`WindowsWakeup.cpp:14` is `CreateEvent(nullptr, TRUE, FALSE, nullptr)`; `WindowsTypes.cpp:19` is `GetStdHandle(STD_INPUT_HANDLE)`, which B12 turns into a registration) **and then found a sharper version of my own critique.** `HandleKind::Waitable`'s doc in `IoBackend.hpp` has read *"an event, console input, a WSAEVENT"* since `e7963de` — so the precondition's enumeration was **already narrower than its own file, written by the same hand, in a comment whose entire purpose was to record a general property.**
- **That changes the mechanism of the whole failure mode, and the constraints are rewritten around it.** I had it as decay: the set grows, nobody owns the sentence. B3's version is that the list is **born stale** — when you write "the X are A, B and C" you are not recording what you know, you are recording **what came to mind just then.** The failure happens at the keystroke, not over months, and the set does not have to grow for the list to be wrong. Which is also why the remedy is to state the property rather than promise to keep the list current: *a list you promise to maintain is a list you will write from memory again.*
- **One tightening sent back, and it is the same defect one level down.** B3's replacement draft classifies by *manual-reset, or signalled by STATE*. A semaphore is signalled by state — its count — and `WaitForMultipleObjects` **consumes** it; so does a mutex, so does a synchronization waitable timer. All three read as "state-signalled" and all three fail the property, so the classification admits a handle that hangs. **The property is non-consumption; "state" merely correlates with it**, and B3's own next clause already said so ("reading the records is what clears it") — promoted that instead. Also asked for "Two kinds of handle satisfy that" to become illustrative rather than exhaustive, since as written it is a completeness claim about the replacement for a completeness claim.
- B3 recorded the wording as §9 of its report with both file:line citations **specifically so it survives a context loss**, and committed nothing — holding it for round 2 as asked rather than spending a commit. Good instinct, unprompted.
- **R101 propagated into the dispatches it was ruled for, rather than left in the ruling.** B6's dispatch gains a section saying plainly that **B6 is the first code in the repository that will set `onError`** and that this is why the ruling exists: what it may rely on (a watched direction always wins, on every backend), what it may not (`Readiness::Failed` is best-effort — kqueue and Wfmo do not set it for a hangup, by design), what `onError` actually is (a fast path carrying the errno sooner, for a failure with neither direction set), and that its dial checks `SO_ERROR` because a dial depending on `onError` is wrong even where it works. B7's gains a shorter one, because IOCP **synthesises** `Readiness` rather than receiving it — so the ruling binds it in the direction of not faking `Failed` where a direction is watched, which would reintroduce the defect on the platform that has to invent the bits.
- Worth naming why this was worth doing now: both dispatches were written BEFORE the ruling, and a dispatch is read once, by a fresh agent, as its contract. **A ruling recorded only in the ledger and the fix round would have reached the lane that caused it and not the lane it was for.** That is the same hand-off defect as F2 — a guarantee living in neither the code nor the document the next task is written against.
Task C0: **fix round 1 was landed and accepted earlier in this session, and I asked for a status as though it had not been.** C0 corrected me with evidence; I verified each commit with `git merge-base --is-ancestor` rather than taking it: `c973592` (I1 raw string, I2 comment mask, I3 non-UTF-8, I4 CRLF, M6), `6d93980` (R91 override chain, I6, I7, M7), `4bde234` (R92, R93 both claims, R94, R95 both arms), `a4d91f5` (I5, M2-M5), `b23f838` (the AGENT.md follow-up). All in `HEAD`. Gates at close: 90 tests 0 skips, both migrate ctests, `check-renames.py` 0 failures over 484 rows, ruff clean, `mkdocs --strict` clean; one item explicitly carried unverified (`core-cpp.async-link-smoke` will not link in a non-developer shell, `LNK1104: kernel32.lib`; `cl-debug` in CI covers it). **My own rule broken by me for the second time tonight: wrote from memory instead of reading back, and the cost landed on C0, which spent a turn proving something already true.**
- **And my fix-round text for I5 was stale in a way that is now a constraint.** I forwarded the review's four items as current. Two — "writes to the working tree" and the empty substitution log — were **B2's, closed in `7ddbe1d` before C0's round started**, when it replaced the `rewrite.py` re-run with hand-written `sed` and `/tmp` scratch. The review was correct against `b528631`; I passed it on without asking whether it still was. C0 fixed what was actually left (`--diff-filter=M`) **plus two the review did not name**: `-z` with `read -r -d ''` against word-splitting, and a recipe printing `pure: 16   not pure: 2` while never computing it. **A review is a statement about a commit; forwarding it as a task makes it a statement about now.**
- **I built a tautology while checking C0's claim about unchecked claims.** `git log -20 --name-only -- CHANGELOG.md` FILTERS to commits touching that file, so it printed "20 of 20" and I nearly reported it as confirming C0. Re-ran per-commit: **3 of 20**, exactly C0's number. Now a constraint: *a measurement whose answer cannot come out wrong is not a measurement* — before reporting a number, ask what input would have made it come out differently.
- **C0's self-assessment, recorded in its words because it is sharper than anything I would have written:** *"I asserted it without asking the repository, in the same commit whose whole subject was that I had reasoned from an incomplete map instead of looking. That is the failure one layer up from the one I was fixing."*
- **And its addition to B3's rule is genuinely new, not a restatement:** an unconditional step 4 justified by a false premise **invites the next reader to discard step 4 along with the premise**. A true conclusion resting on a false argument is more fragile than no argument, because whoever checks the argument throws out both. Constraint added beside B3's.
Task C0 (new): **survey whether `renames.json` still describes Phase B.** C0 owns the 484 rows and `check-renames.py`; every B task is meant to add rows in the commit that renames the symbol, and **C1-C6 migrate off that file**, so a row never added is a consumer that silently does not migrate, discovered in someone else's repository. Asked for the table, the method, and **a control proving the probe can find a missing row** — because "0 missing across 484 rows" and "0 missing across Phase B" are different claims, which is A12's B12b distinction reused deliberately. Two things named as beliefs to be tested rather than confirmed: `removed` rows fully qualified, and `pending` rows carrying their `target` since R92 (back-filling `FastCache::SyncRun` was never the ruling; refusing a NEW one was). Report to `task-C0-renames-survey.md`.
Task B1 fix round 1: **DONE**, `ac0ff76..ceb471d` (five commits). 343 assertions / 83 cases on both `core-cpp.async` and `core-cpp.async-fallback` across `clang-debug`, `gcc-release`, ASan, TSan, `cl-debug`, `clangcl-release` and — new this round — **Emscripten under node, 3/3**. All three REDs captured against the committed cases: C1 as an ASan `heap-use-after-free` symbolized to `parkTwoUnderWhenAll` destroying a frame the sibling park had released; I2 as a TSan data race on the 48-byte join state with `addr2line` naming `Join.hpp:168` (pool thread T4) against `Join.hpp:292` (main), **reported by ctest as a failure rather than a printed warning**; I4 as `-Werror,-Wunused-result` on a discarded `push()`. Re-review `rereview-B1-r1` dispatched.
- **R97 had a SECOND HALF the finding did not name, and B1 found it: `ThreadPoolExecutor::submit(ParkedWork)` delegated to `submit(work.resume)`, dropping the claim.** Harmless against a raw handle; **a free of a live chain against a refcount** — every ASan tag went SIGSEGV until the pool's queue became `std::deque<detail::Parked>`. **Now a cross-lane rule, already sent to B4**, because `EventLoop::submit(ParkedWork)`, `resumeSoon` and `schedule(tp, ParkedWork)` are all that shape and B4 is writing them now: *any executor written against `ParkedWork` must hold the claim, not just the handle* — a queue's element type is `detail::Parked`, never `std::coroutine_handle<>`. Asked B1 to put it in `.agent/rules/async-and-net.md`, whose whole premise is that every rule in it has already been a bug.
- **Two self-corrections, both settled by compiling rather than by arguing.** `whenAny`'s awaiter is immovable **by the standard** — its registered `StopCallback` pins it — so only `whenAll` regressed, found by asserting movability of both and watching one fail to compile. And B1's own comment justifying the restored move gave an example (`return whenAll(...)`) that **copy elision makes legal**; it compiled both forms to find the one actually rejected (`ceb471d`). Both are claims about what compiles, settled by compiling; the re-review is told to apply the same method to them.
- **B1's CI statement is the shape I want from every lane:** it named `35548899124` / `455b21b` / 23 of 23 as the run covering the four substantive commits, and **explicitly declined to claim `ceb471d`**, which rides on `870d12b`'s still-running run. Saying which of your own work is uncovered is worth more than a green.
- **Hygiene regression reported, not fixed — correctly.** `ctest -L hygiene` went 14/14 to 12/14: `cmake-hygiene` wants provenance rows for `src/core/net/{PlatformLoop,detail/ParkTable,detail/WorkerIdentity,testing/TestLoop}.hpp` and `migrate-renames` wants three `core::net::*` rows delivered — **all B4's uncommitted in-flight work.** Told B4 to land the rows in the same commit as the files, or the gate is red for whoever builds next and they spend the time B1 just spent establishing it is not theirs. **Independent confirmation of the C0 survey's premise**: those three renames rows are exactly the drift it was dispatched to find, surfacing from a second direction before its report.
- **`LNK1163: invalid selection for COMDAT section` on a `cl` tree**, against objects predating a layout change, cleared by `--clean-first`. AGENT.md's stale-object rule names `clangcl-*` trees only; this is the same family one toolchain over. Recorded for the lane that next holds AGENT.md rather than routed now — B1 already has three prose lines this round and the rule is not wrong, only narrow.
Task core-cpp#31 (the header self-check): **LANDED as `3012952`, and it vindicated itself nine seconds into its first run.** Measured before deciding, nothing narrowed: 159 headers on `clang-debug` (~12.5 s cold, 4.48 s warm), 158 on `cl-debug` (11.01 s cold, 1.26 s warm), 155 at pristine `HEAD` — **~7% cold and under 3% warm** against a 346-target, 2 m 51 s full build. One TU per header, one `OBJECT` library per publishing target, built as part of the build, so the check IS the build and a failure is the compiler naming the header and what it needed. The three differing counts are the mechanism working: the list is each target's `FILE_SET HEADERS`, so `ThreadPoolExecutor.hpp` needed no special case under Emscripten — it is in no file set there.
- **Two real defects, both B4's, neither fixed by A12.** `PlatformLoop.hpp:66` and `testing/TestLoop.hpp:61`, the same defect twice: **`member '_backend' found in multiple base classes of different types`** — `detail::OwnedBackend` declares `std::unique_ptr<IoBackend> _backend`, `EventLoop` declares `IoBackend& _backend`, `PlatformLoop` derives from both, and lookup finds both before access control notices `EventLoop`'s is private. Routed to B4 with the reasoning; its call and its file.
- **The whole argument for #31, made by the tree instead of by me: nothing in the repository includes either header.** Public, broken, invisible to 346 targets. **The first person to write `#include <core/net/PlatformLoop.hpp>` would have been a consumer**, mid-migration, in their own repository. "Eighteen rules, all of them reading text" was worth saying out loud, and A12 is the one who said it.
- **The best thing in the report is a false green caught before it could exist.** A12 made `CORE_CPP_HEADER_TARGETS` a list of its own rather than reuse `CORE_CPP_TARGETS`, because the latter holds only COMPILED libraries — and `core::async` is INTERFACE and publishes twenty headers. **A check reading that list would have covered none of that module and reported exactly the same green**: 100% of what it looked at, looking at the wrong set. A passing check that covers nothing is indistinguishable from one that covers everything. It also refuses an empty target list outright.
- **Its RED is honest about a hard case, and the honesty is the point.** `HEAD` had no such module, so there was nothing to run the new test against; A12 took it against a **stub** whose function does nothing — the actual state of the tree before the commit. One failure predicted, one observed. Then the detail that makes it a check rather than a formality: **the clean case still passed under the stub**, so the violating case discriminates rather than merely reddens. A generator refusing everything would also have "caught" it.
- **Proving the check green at pristine `HEAD` BEFORE deciding it was landable was the right call for a reason beyond correctness:** it was about to redden the build on another lane's uncommitted files, and landing first would have made the new gate the obvious suspect — the worst possible first impression for a gate that has to be trusted, and the same hour B1 spent establishing a red gate was not its doing. 155 of 155, exit 0, in a worktree carrying only its change, is what separates "your files are broken" from "my check is broken."
Task B3, F5: **pushed as `7e2e3ae`; B4 unblocked and told.** Worse than the review described — the double ignored `Interest` **and reported directions the registration never asked for**, so a B4 case could have parked a reader, been woken by a write, and looked for the defect in its own loop, where there was none. Both closed the same way: a muted or wrong-direction step dispatches nothing while the wait still **consumes** it. The asymmetry B3 chose is the careful part: `Readiness::Failed` survives the direction filter (POLLERR/POLLHUP/POLLNVAL arrive unasked everywhere, and filtering a failure by direction would make an error case unwritable) but **not** muting, because a muted registration is not in the wait set to fail.
- **"The double had no cases of its own, which is how it drifted" is the finding underneath the finding.** A test double is code that other people's tests are evidence about; when it is wrong, every case built on it is wrong in the same direction and all of them still pass. Three cases now, in the portable `net_backend` binary, for a reason better than "add tests": **the double has no kernel in it, so a fidelity rule it keeps is one every platform can check.** RED first, 3 cases / 6 assertions. The load-bearing number is that `core-cpp-net-test` was **unchanged at 1524/123** — no existing case was leaning on the old behaviour, which could not have been known by reading.
- **B3 accepted R101 after working it through, no protest, and its POSIX framing is better than the one I ruled with:** *you never trust `revents` to tell you what went wrong — you do the `read`/`write` and let that report.* Mine argued from what breaks; B3's argues from what the platform already does, which tells B6's author how to write the code rather than what to avoid. Put into B6's dispatch, credited.
- **Its one-callback caveat is in B6's dispatch too**, as a fact B6 meets in a brief rather than discovers: `selectReadinessCallback` returns exactly one, so the reorder changes which fires and never fires both; a handler wanting to know a failure *accompanied* readable data must look, and should take it from the `read` that follows. Right to flag rather than propose changing — a second callback is a real interface change for a hypothetical caller.
- **B3 checked my F6 list rather than trusting it**: `detail/WaitChunking.hpp:14` was already fixed in `870d12b`, so the review's list had gone stale between writing and forwarding. **Second time tonight a forwarded finding described a repository that no longer existed, and both times the lane caught it rather than the controller.**
- Build `35549323301` on `870d12b`: **success.** `ceb471d` is an ancestor, so **B1's fix round 1 is CI-green end to end** — `35548899124`/`455b21b` for the four substantive commits, this run for the fifth. Told B1, because it had deliberately declined to claim that one: it stopped watching with four jobs still running and said so rather than rounding up. Still uncovered by a completed run: `7e2e3ae` (B3's F5) and `3012952` (A12's #31).
Task B8 dispatch WRITTEN (`task-B8-dispatch.md`), the fourth composed ahead of need. Headline framed as the reason the task exists rather than as a feature list: **contour's `connect()` calls `getaddrinfo` on the caller's thread, and on a loop thread that is an unbounded stall — a dead resolver is seconds, and every coroutine on that loop waits, including the ones with nothing to do with the network.** The deliverable is named as the SEAM rather than the thread: `IAsyncAddressResolver` is what a test injects and `ThreadedAddressResolver` is merely what ships.
- **R101 lands on B8 harder than on anyone, and the dispatch says why with B3's review's own scenario:** on Linux a failed connect reaches `onError` and the dial completes with the errno; on macOS the write filter fires with `EV_EOF`, the backend reports `Writable`, and **a dial that trusted the callback believes the connect succeeded** and discovers otherwise on its first write — macOS-only, invisible to a parity suite that does not assert it. So: a dial checks `SO_ERROR`, `Failed` is best-effort and never branched on for correctness, `onError` is a fast path carrying the errno sooner, and B3's framing is the one to write code by. **A dial written to depend on `onError` is wrong even on the platform where it works.**
- Two cases named as the ones the task exists for: an injected resolver recording the calling thread, asserted **not** to be the loop's, written against the current `connect()` first so the RED is the point of the task; and a refused connect completing with the refusal on poll, epoll and kqueue — R101's case in its natural habitat, and the one that would have caught the macOS divergence.
- Carries the evening's constraints as they apply: renames and provenance rows **in the same commit as the code** (with B1's hour as the reason), the shared tree not being a buildable state, a measurement whose answer cannot come out wrong, and a report that **names what it leaves for B9 and B10 explicitly** — F2's lesson, that a guarantee appearing in neither the code nor the hand-off list is how one gets lost.
Task `.agent/rules/testing.md` (the second-binary rule): **fixed as `5801090` — grounds, not binaries.** A12 ran the count itself rather than take mine: 13 registrations over 8 modules, five with one binary, three with more, and of the five extras **only `async-fallback` rests on a definition**. The rule now states three grounds (a definition that changes what the headers declare; a narrower or conditional target; a platform-or-label scope) and enumerates no binaries.
- **Its reason for grounds over binaries is the load-bearing one and is now a constraint:** the four missing binaries came from A6, A7, B2 and B3, **none of which had any reason to open that file**. Enumerating them would have been correct on the day and wrong by Friday. A rule that must be revisited by every lane touching something else is a rule that decays structurally, not through carelessness.
- **New rule, and a better statement of the problem than "the list went stale": a reason recorded twice is a reason that can disagree with itself.** That is exactly how the bullet came to contradict the mechanism paragraph eight lines below it. The ground is now recorded only in the module's `CMakeLists.txt`, beside the split it explains — which was already true in the comments.
- **A12 caught the defect inside its own correction:** its first draft said *"of which there are three"* and *"the five splits that exist"* — live counts written into the sentence deleting live counts. Removed on re-reading; the reader now sees three grounds because they are the next three bullets, not because a number promised them. **Third recursive catch tonight** (B3's enumeration already narrower than its own file; my tautology built while checking someone else's claim about unchecked claims; this). The consistency is the finding: **this defect is not a lapse of care, it is what writing feels like from the inside.**
- **Ruling: NO gate, on A12's argument, which I accepted over my own inclination — I would have approved a cheap one had it proposed one.** The defect was prose generalising from one instance, not a missing binary: nothing was unregistered, untested or unexplained. The only mechanical form is a count, which **re-introduces the completeness claim the fix deletes** and reddens whenever a lane legitimately adds a binary — a gate on a true negative, the fourth refused tonight and the first that would have been built deliberately. **The structural point, now a constraint: the fix removes the thing a gate would have had to watch.** A gate keeping two lists in agreement is worse than one list, because it makes the duplication permanent and calls the arrangement safe. Same move as the Imported-table split.
- **A12 declined to apply my monitor rule mechanically, and was right.** I said arm it on the commit you want covered; it reasoned about WHICH commit is worth covering — `3012952` is the real build change, `5801090` is markdown that `mkdocs --strict` already settles — and concluded that chasing the newest SHA each commit is churn, not precision. The rule used with judgment rather than obeyed.
Task core-cpp#32 (the `no-tsan` no-op) dispatched to A12, **framed so that deletion is an allowed outcome and probably the right one.** The label is consumed by `build.yml:429` and applied to no test, so the exclusion is a no-op — the shape A12 has been finding all evening, this time in CI's own configuration. Two honest outcomes named: label the tests that genuinely cannot run under TSan, each with a stated reason; or delete the label, the exclusion and the rulebook row. **Warned explicitly against guessing in the labelling direction**: B1's I2 was a real data race TSan found tonight, and B8 runs a resolver thread where TSan is not optional, so labelling something `no-tsan` because it is merely slow silently removes coverage from the tool that has already caught a real defect here. Told to report before acting if the answer is not clean, because "delete the label" and "label four tests" are different enough that the choice is mine.
**MASTER WAS RED ON EVERY POSIX TOOLCHAIN, CI MISSED IT, AND A LANE FOUND IT BY BUILDING.** `7e2e3ae` (B3's F5) wrote `.handle = nullptr` in `ScriptedBackend_test.cpp`; `NativeHandle` is `void*` at `platform/Types.hpp:26` and **`int`** at `:48`, so it compiles on Windows and on nothing else. B1 found it in a clean worktree at `origin/master`, reported and did not touch it. **Already fixed by the time I read it — `8d05bb7` is the head and the line now reads `.handle = core::platform::InvalidHandle`.** I verified the defect AND the fix at source rather than taking either.
- **The tree moved between B1's build and my read, which is the same shape as when I told B3 its fix was uncommitted and its push had landed between my read and my send.** *A check of the tree is a statement about a moment*, and it has now caught both of us. B1's report was accurate when made, which is all a report can be.
- **R73's hazard realised for the first time tonight, and it is exactly the harm the warning was written for.** `7e2e3ae`'s run `35549980333` is `cancelled` with **zero jobs**, superseded by `ba48a50`; that head's run superseded by `3012952`; that one by `5801090`. **Four heads landed and exactly one run was in progress. The single head that would have caught this never ran.** We have been quoting the zero-job pattern as something to check before claiming coverage; this is it causing the damage rather than merely obscuring it.
- **Warned A12 pre-emptively**, because `3012952` contains `7e2e3ae` and not `8d05bb7`, so its run carries the broken intermediate and **the header self-check is about to look like the suspect on its first outing** — the precise outcome A12 went out of its way to avoid by proving the check green at pristine `HEAD` first. Closed by a path neither of us could have blocked.
- **B1 corrected my generalisation and the constraint is amended.** I wrote *a red build in a shared checkout is not evidence about your change* and left the implication that it is therefore a blameless uncommitted edit. That is one of two cases and I collapsed them: the other is **master is broken and CI missed it**, which is urgent and which my wording told B1 to shrug off. Its version is the rule now — the two look identical from a failed build, and `git log -- <the file in the error>` distinguishes them in one command.
- **A SIXTH test binary exists and my count missed it: `core-cpp-async-link-smoke`, a bare `add_test` that deliberately does not link `core::testing_main`.** I found thirteen registrations by grepping `core_cpp_add_test(` — **I counted the instances I thought to look for**, which is my fourth enumeration failure tonight and the first where the method itself was the blind spot rather than memory. It also makes A12's fix more right than either of us knew: `5801090` replaced the count with three grounds an hour earlier, so a corrected count would already have been wrong again.
- B1 applied the rewritten step 5 to the commit that rewrote it — prose, so no CHANGELOG entry, and `mkdocs build --strict` run anyway (0.42 s, clean). The cheapest possible demonstration that the new wording is livable.
- **B3 self-reported the Linux break before CI did, fixed it in `8d05bb7`, and took a better rule from it than the obvious one.** Its own count: third time in this task it shipped something its local run could not see — `ConsumerSmoke.hpp`, the macOS `poll(2)` divergence, and this. **The distinction it drew is what makes the rule usable:** the first two were platforms it cannot run, so no rule would have helped; this one it could, and skipped because F5 was urgent and Windows was green. *"Urgent is exactly when the shortcut gets taken."* So the rule is **a new file gets built on a second platform before it is pushed, however small** — narrow enough to survive the next urgent thing, which "run everywhere" is not. Four minutes.
- **B3 nearly shipped B4's uncommitted work, and this is the bigger finding of the two.** Copying `IoBackend.hpp` from the shared tree into its build worktree pulled in B4's `virtual setPump`, its `IHostScheduler` include and its doc edit. Caught **only** because `HostDrivenBackend.hpp` at `HEAD` lacks the matching `override`, so Linux failed to compile — B3 named that as luck rather than diligence. On Windows alone it would have gone out. **R87 is amended: it applies to any source file another lane has open, not only `CHANGELOG.md` and generated tables — and the failure mode in this direction is worse than the one it was written for.** Reverting someone's work is visible and repairable; publishing their half-finished work under your name is neither. B3 rebuilt the file as `HEAD` plus its four edits and **asserts in its script that none of B4's three markers survive** — a check committed to, not an inspection hoped for.
- B4 told, framed as concerning its work rather than its conduct, with the practical consequence: a finished piece of its `IoBackend.hpp` work is now better landed small than held, because holding it makes it something another lane can pick up by accident; a mid-design piece is fine but the state is visible. Also asked B4 directly whether my four routings have crowded out its core work, since I would rather split them than have B4 itself slip.
**R101 is demonstrated on a real kernel rather than argued, which I could not have produced myself.** B3 predicted 2 failures and got exactly 2: `backend=poll` and `backend=epoll`, `CHECK( reader.readable >= 1 )` → `0 >= 1`, on a socketpair written to and then closed so **the hangup and the unread byte arrive together and the reader is not woken at all.** I ruled from reading `selectReadinessCallback` and reasoning about what a handler needs; B3 put it on a kernel and it did exactly what the argument said. **That is the difference between a ruling I believe and one I know.** GREEN after on Linux (843/141, 105/22) and Windows (1524/123, 104/22); the unit RED was 2 as predicted, with the third assertion — `onError` still taking a failure on a direction the handler does not watch — passing unchanged.
- **The companion case is the better piece of design and nobody asked for it.** A hangup with NOTHING buffered asserts only `total() >= 1`, because which callback fires there is a genuine divergence, so pinning it would pin the divergence instead of the property. **The macOS `poll(2)` lesson applied unprompted by the lane that learned it**, in the one case where the contract and the local behaviour are easiest to confuse. Now a constraint.
Task C0 renames survey: **`renames.json` still describes Phase B — 0 missing rows**, and the zero means something because of the control. B1 5 public symbols vanished / 0 covered (all `detail::`), B2 1/1, B3 22/20, B13a 2/2; **all 7 uncovered decided by `git grep` at four consumer pins rather than by a rule about `detail::`** — every one is named only inside the files core-cpp replaces (`src/net/`, `src/coro/`), which is a fact about the consumers rather than an assumption about namespaces. B4's three rows are `pending` with targets; B5-B12 have not run.
- **The control is three mutations by row kind, each surfacing exactly what it should and nothing else:** dropping the `net::EventSource` rename surfaces `core::net::EventSource`; dropping the `core::net::FdToken` removed row surfaces it; dropping the `EventSourceKind` enum row surfaces the enum **and all three enumerators**. Three PASS.
**Ruling R104: `removed` rows must require `::` and a leading `core` component, enforced by the schema — short round approved.** C0's finding is the best of the night and it is better than the missing row I sent it to look for: **I asked it to test a belief, it found the belief TRUE (8 of 8) and found my reason for holding it WRONG**, which is strictly more useful than confirming or refuting.
- **A badly spelled `removed` row is not wrong, it is INERT.** `qualified_failure()` with one component only looks for a `#define`, so it reports absent and passes. Bare `FdToken` — silent. `net::FdToken`, the consumer's spelling — silent. `core::tui::runtime::FdToken`, the control that exists — FIRES. So the guard I was worried would false-positive against the live TUI-runtime collision instead **guards nothing, forever, and reads exactly like one that works.** A false positive is noisy and gets fixed; a false negative indistinguishable from success never gets looked at again.
- **And the surrounding convention actively teaches the broken spelling.** `removed` is the **only** kind whose `from` is a core-cpp name; every other kind's `from` is the consumer's spelling. A fresh agent pattern-matching its neighbours writes `net::FdToken` and produces an inert row that nothing in the file, the schema or the gate can distinguish from a working one. **Not a discipline problem — a design that punishes the way the rows actually get written.**
- **What decided the ruling: 8 of 8 are qualified by discipline, not construction**, and cross-lane discipline is precisely what failed four times tonight in four documents written by four lanes. B6, B7 and B8 will each add `removed` rows, each written by a fresh agent reading its neighbours. Waiting costs nothing today and the whole guard tomorrow. The collision is live until B12 — `core::tui::runtime` still declares `FdInterest`, `FdToken`, `WaitOutcome`, `FdRegistration`, `FdRegistry`.
- Two smaller ones taken in the same round: the two `macro` rows carrying no `target.symbol` (their header is asserted but not that the macro is still defined in it — `defines_macro()` already exists, so it is nearly free, and **a header that exists while the macro inside it was renamed is the same inert shape one level down**); and the `to` side, compared against the tree only for `include` rows, held up by `to` agreeing with `target.symbol` for 237 of 238 symbol rows. **Asked whether that agreement is enforced or merely observed** — if observed, it is another thing true by discipline and I would rather know now than in C1.
- **C0 reported two wrong intermediate answers and both are in its report:** 83 from an extractor matching any `identifier(` and promoting every member call to namespace scope, then 22 from asking the table about `core::net::EventSource` when a row's `from` is the consumer's `net::EventSource`. Both over-reports, the safe direction — but *"the first number I could have sent you was wrong by an order of magnitude."* Now a constraint: **a lane that reports only its final number is a lane whose final number has to be taken on faith.**
- **B12 intel from C0, joining B13a's two:** when the TUI runtime moves onto `core::net::EventLoop`, endo's uses of the five `core::tui::runtime::` names need rows **in the same commit** — a second set for names the table already carries once under `core::net::`.
Task core-cpp#32: **CLOSED as `80bbe1c` — delete was right, and the label was not merely inert.** The verdict is settled in the right order: `sanitizers (clang-tsan)` is **green in CI while excluding nothing** (Build 35548899124 ran 27 of 27 under TSan and passed), which is a stronger fact than the grep that follows it; `no-tsan` is carried by no test anywhere; and **no test opts out by any other route** either — no `__SANITIZE_THREAD__`, no `thread_sanitizer`, and the only ThreadSanitizer mentions in test sources are comments explaining races it CATCHES (`StopToken_test`, `ThreadPoolExecutor_test`, `MessageQueue_test`). The suite was authored TSan-clean. The rule now says so where the label was listed, including the temptation I flagged: do not reintroduce it for a test that is merely slow or flaky under TSan.
- **The real finding is larger than #32 and A12 introduced it in R96 without noticing: `ctest` ANDs repeated `-LE` options — a test is excluded only if it matches EVERY one.** The sanitizers leg ran `-LE tree-level ${{ matrix.ctest_args }}` → `-LE tree-level -LE no-tsan`, and since nothing carries `no-tsan` the intersection was empty, so **that leg excluded nothing and has been running the whole suite, tree-level checks included.** Measured: `-LE tree-level -LE no-tsan` selects **28**; `-LE tree-level` selects **16**. The discriminator that pins it as AND rather than last-wins is `-LE tree-level -LE hygiene` selecting 16 rather than the 13 that `-LE hygiene` alone gives — the step most people would skip. The CI log corroborates independently: that TSan job lists `Start 16: core-cpp.cmake-hygiene`, `layering`, `vendor-selftest`, `tree-level-coverage`.
- **Direction of the harm, which I asked A12 to state plainly because it was not stated: NO GREEN WAS EVER FALSE.** The leg ran MORE than intended, not less, so nothing was hidden. What it cost is three things: R96's central claim was untrue for one leg of seven; CI time went on asking twenty-five questions with one answer; and **a failing tree-level check would have been reported on the sanitizers leg too, which is exactly the noise R96 existed to remove.** A correctness-of-claims defect and a waste, not a coverage hole.
- **A12's self-diagnosis is the sharpest rule of the night and is recorded verbatim: *the row that differs is the row to check.*** It verified R96 by counting labels and by a worktree experiment and never checked what the sanitizers leg actually ran **because it was the one row with an extra variable in it** — the row it had most reason to check, skipped for the very thing that made it interesting.
- The durable half kept: `ctest_args` removed, one `-LE`, and a comment that it must stay one — **extend the regex, never add a second flag.** The label is gone; the trap is a property of ctest and is not.
- **Ruling: the `no-tsan` references in the PLAN (`:322`) and SPEC (`:303`) are removed, and A12 takes them; `AGENT.md:134` stays with B1.** The reasoning, since editing a spec is normally wrong: a spec and a plan record what was DECIDED, and editing them to match today's tree makes them lie about that — **but a label list is not a decision, it is a statement of fact about the build**, and that fact was changed by a later explicit ruling. Removing it corrects a fact rather than rewriting a decision. Both documents are live in the way that matters: lanes read briefs extracted from that plan, and AGENT.md points at that spec as *the design*. Told not to soften it into a note — a list naming a label and then disclaiming it is worse than either choice.
- A12 deleted `testing.md`'s `## Open work` heading along with its last entry, citing the rulebook's own grammar — the correct authority for that call. `ctest -L hygiene` 13 of 15, the same two failures as before (B4's untracked provenance rows; `migrate-renames` waiting on B4).
**Ruling R105: the CI concurrency group becomes per-SHA for pushes — it is a bug against the workflow's OWN STATED INTENT, which is what makes the ruling easy.** `.github/workflows/build.yml:24-27` sets `group: ${{ github.workflow }}-${{ github.ref }}` with the comment *"Only a pull request's superseded run is worth cancelling; a push to master is a record."* The intent is exact and the implementation does not deliver it: `cancel-in-progress: false` stops GitHub cancelling a RUNNING job and does nothing about a **queued** run in the same group, which is superseded regardless. Every master push shares one group, so each push destroys its predecessor's pending run.
- **A12 caught that my own read was stale** — I called `35550435259` on `8d05bb7` "pending, the one that matters"; it was cancelled by the time A12 looked. CI state is the fastest-moving thing we have, and *a check of the tree is a statement about a moment* now applies to me twice tonight.
- **The tally that forced the ruling: five consecutive heads at zero jobs** (`7e2e3ae`, `ba48a50`, `5801090`, `8d05bb7`, `c5e2db6`), one failure (`3012952`, the predicted break), one live run. **The break shipped unverified and then the fix for it shipped unverified too** — the zero-job harm compounding rather than occurring once, in A12's framing.
- **Cost accepted deliberately and stated rather than discovered:** every master push gets a full matrix, which with five lanes pushing is a real increase in CI minutes. The alternative is what we have — a library one task from a v0.1.0 tag whose master could be broken with nothing to tell us. **Coverage we do not get is not cheaper than minutes we do not spend.** A12 told to check the expression rather than take mine (I wrote it from memory, which has gone badly three times tonight) and to **report before landing if it does not behave as expected**, since a concurrency change that silently cancels MORE would be worse than the problem.
- Monitor confirmed `3012952 failure 35550061379`, as predicted and already explained to A12 before it fired. No alarm raised: known cause, already fixed in `8d05bb7`.
- **A12's footnote is now a constraint and it is about trust, not correctness:** *"the reason I could say 'not mine' instantly is the pristine-`HEAD` run I did before landing."* **A new gate's first red decides whether anyone believes it** — and a gate nobody believes gets disabled by whoever is tired at the time. Ten minutes bought the difference between a footnote and a morning.
Task B1: wrote the `ParkedWork` rule as **`c5e2db6`** after confirming it was NOT already in `.agent/rules/async-and-net.md` — line 98's *"resumes what it is handed, or frees it"* named `detail::Parked` but said nothing about a queue's ELEMENT TYPE, which is exactly the half that was the bug. Stated generally, placed as that rule's mechanical corollary, naming `EventLoop::submit`, `resumeSoon` and `schedule(timePoint, ParkedWork)` explicitly, cited to fastcached#1025 for the origin and `ac0ff76` for where it was a bug — **and it did not invent a core-cpp issue number when none existed.** The tell it wrote down, so B4 can grep for it: *a `submit` overload whose body names `work.resume` and nothing else has taken half of a two-part value.*
- **B1 named its own weakest reasoning for the re-review, unprompted:** `AbandonState::claim()`'s relaxed `fetch_add`, and the disarm/release interleaving in `Parked::resume()` where the claim outlives the call by design — *"that is the part I reasoned about rather than tested directly."* Forwarded to `rereview-B1-r1` as the place to push, with the specific warning that a relaxed increment is usually fine while the RELEASE side needs `acq_rel` or a fence, and the instruction that **"correct by argument, untestable here" is a legitimate verdict and a different one from "verified".** Now a constraint: an implementer naming where it reasoned rather than tested is the strongest signal a reviewer gets.
**Ruling R104 delivered: `4919c79` — a `removed` row's `from` must be `core::`-rooted and qualified, refused with the reason in the message.** 102 tests (from 98), 3 failures predicted and exactly 3 observed, ruff and `mkdocs --strict` clean. **A companion case pins the MECHANISM** — bare is a macro lookup, two-component needs a namespace nothing opens — *"so the rule cannot be argued back out later on the grounds that the collision is theoretical."* It is not: `core::tui::runtime` declares all five names until B12.
- **C0's first macro check was wrong, it failed two CORRECT rows, and C0 put that on the record rather than burying it.** It asserted the header *defines* the macro; `rows[376]`/`rows[377]` (`CORE_GENERATOR_FORCE_FALLBACK`, `CORE_RANGES_FORCE_FALLBACK`) immediately failed. Those rows are right: the macro is one a **consumer** defines and core-cpp only TESTS (`#if defined(__cpp_lib_ranges_fold) && !defined(CORE_RANGES_FORCE_FALLBACK)`), which is exactly why they carry no `target.symbol` — **and their `note` field says so in words.** C0 had read `kind`, `to` and `target` while writing the survey and skipped `note`.
- **Its self-assessment is the sharpest of the night and names a NEW member of the family:** *"the answer was inside the row I was reporting on. It is the same failure as the rest of the evening, one field to the right."* The four before it were enumerations — instances that came to mind. This one is **treating part of a record as the whole record**, and the field skipped was the one written by someone who anticipated the reader. Now a constraint.
- Corrected to **consults** rather than **defines**: define it, or test with `defined()`/`#ifdef`/`#ifndef`/`#cmakedefine`, comments blanked first. **Four cases pin the distinction, one asserting the two real rows still pass "so nobody tightens it back"** — a pattern worth reusing: *when you deliberately choose the looser rule, pin the looseness, or the next person tightens it and calls it a fix.*
- **The `to` / `target.symbol` agreement is MERELY OBSERVED**, nothing compares them for any kind but `include`; it holds for 237 of 238 symbol rows because every author wrote the same string twice (the exception is B4's deliberate `EventLoop::submit` watch). **C0 deliberately did not close it, and the reasoning is what earns the call:** closing needs a schema field meaning *these differ on purpose* before any check can exist, and **inventing an opt-out to enforce a rule that has never been broken is the wrong trade.** Same judgment A12 made arguing me out of a gate an hour earlier. Acceptable rather than a deferral only because it is written into the report so C1 does not discover it. Third "true by discipline, not construction" finding tonight.
- **`core-cpp.migrate-renames` is RED on master and it is B4's, not this round's** — proved by running `HEAD`'s copy of the gate against the same table and getting an identical result. `rows[437-439]`: `core::net::EventLoop::submit`, `core::net::PlatformLoop`, `core::net::testing::TestLoop` all now exist, so B4's three `pending` rows must flip to `delivered` in its commit. **C0's framing: the pending arm doing exactly the job R92 kept it alive for** — a ruling about a row checkable in neither direction, now the thing reporting that three symbols landed without their rows. And the blast radius is **one CI job rather than 24**, because the gate carries `tree-level`: **R92 and R96 both earning out on the same red.** C0 did not touch the rows — B4 owns the file and is actively editing it, so flipping them from there is the shared-tree hazard in its original form. No sixth message sent to B4; it has had the item verbatim since B1's report.
- **Verified C0's edit to the session memory rather than taking it**, because that file outlives this session and is mine to be responsible for. It is accurate and better than what it replaced: the fifth facet of *ask for the thing itself, never a shorthand that resembles it* is correctly distinguished from the first four — **those are a DIFFERENT THING standing in for the answer (a status, a base, a marker, a grep); this one is the answer itself with a field unread.** Folding it into the existing memory rather than starting a second was the right call.
Task C0 (new): **the plan has gone stale and lanes read their briefs out of it.** Counted rather than guessed in `docs/superpowers/plans/2026-09-18-core-cpp.md`: `core::coro` 11, `src/core/coro` 6, `EventSource` 10, `DefaultEventSource` 2, `makeDefaultEventSource` 1, `FdInterest` 1. The module has been `core::async` since `12bb267` and `EventSource` became `IoBackend` in B3. **B5 through B13 have not run, and `scripts/task-brief` extracts their text from this file** — so a brief telling a fresh agent to create `src/core/coro/Task.hpp` is wrong about the tree it will land in, and that agent reads one extracted task, not the repository's history.
- **Mechanism: drive the substitution from `renames.json` itself** — the authoritative table, C0's own, and using the rename table to repair the document that predates the renames is the right instrument rather than a hand-written list. The gate falls out of it: after the update, **no term in the plan may match the `from` of any `delivered` row.**
- **The constraint that decides the task: rename only, never re-scope.** The plan is executable — its job is to tell the next task what to build — but it is also a record of what was decided, so this authorises a VOCABULARY correction and not a rewrite. A passage wrong about *what to build* rather than *what to call it* is **reported and left**: that is a ruling per passage, and quietly correcting one inside a mechanical pass is how scope moves without anyone deciding it. Asked for the count including zero.
- **Spec explicitly out of scope, with a reason and a question.** A12 is editing its label list this round and two lanes in one file is the hazard of the evening — but AGENT.md calls it *the design* and B-task dispatches cite it by section (*"the design spec's §2 item 7"*), so C0 is to **report whether it is as stale and by how much.** That is a separate ruling and I would rather make it with C0's numbers than my guess.
- R87 flagged for the read: `renames.json` is under active edit in B4's lane for its three pending rows, so base reads on `git show HEAD:<path>`.
**A12 reversed its own "nothing of mine is implicated" and the reversal is partly my fault.** `clang-tidy` in run `35550061379` failed with **29 errors, all A12's**, every one `readability-enum-initial-value` on `core::tui::KeyCode`. Mechanism, exact: **clang-tidy resolves configuration from the `.clang-tidy` nearest the SOURCE FILE**, and the header-self-check units are generated into the *build tree* — so they were analysed under the **root** config and could never see `src/core/tui/.clang-tidy`, which already turns that check off for `KeyCode` with its reason. **Every tui header failed for a finding its own directory had already settled.** Fixed as `6270a32`: tidy off for the check targets. 29 FAILED before, **0 after**, 159 units, 7.5 s, reproduced and verified on the clang-tidy preset rather than argued; `build.ninja` still carries **191** tidy invocations over the real sources, which is the number proving nothing lost coverage.
- **The fix is right rather than expedient, and both alternatives are worse — one by A12's own rule.** Generating the units beside the headers pollutes the source tree; copying the per-directory configs into the build tree makes **a reason recorded twice, which is a reason that can disagree with itself.** Switching tidy off for a target whose only question is *does this header compile with nothing included before it* is the correct scope for that target.
- **MY FAILURE, and it is the cause of the near-miss: I gave a forecast a scope it had not earned.** I told A12 its run would go red and that it was not its own. True of sixteen jobs, false of the seventeenth, stated as an unqualified claim about the run. A12: *"I nearly stopped at your `NativeHandle` explanation because it accounted for the POSIX legs so cleanly."* Two constraints added, the second in my name: **a correct explanation that covers most of the symptom is the most expensive kind of wrong, because it stops the search**; and **a forecast that does not state its scope will be read as covering everything** — I should have written "this accounts for the POSIX compile legs; check the others yourself", which would have cost three words and pointed at the one leg that mattered.
- **A12's own diagnosis is the sharper half and the most exact recursive catch of the night.** It verified `3012952` on clang-debug and cl-debug and proved it green at pristine `HEAD` — **on clang-debug** — and **the clang-tidy leg is the one leg that adds a TOOL to the compile.** *The row that differs is the row to check*, one hour after it taught me the rule. And the part that bites properly: **"per-leg" is the entire justification for the header check existing rather than being `tree-level`** — it argued that legs are not interchangeable evidence and then treated one leg as evidence for the rest, with the argument sitting in its own commit message.
- Honest split now recorded: the POSIX compile failures are `7e2e3ae`'s, fixed by `8d05bb7`; the `clang-tidy` leg was A12's, fixed by `6270a32`. A12 reported the reversal unprompted rather than letting it ride on a subsequent green.
- **SEVEN consecutive heads at zero jobs, and the defect has now eaten its own fix twice.** Verified rather than assumed: `6270a32` — **the clang-tidy fix** — is run `35551047473`, `jobs=0`; `c5e2db6` the same. So both the POSIX fix (`8d05bb7`) and the tidy fix (`6270a32`) have landed with no run ever executing on their heads. A12's monitor was re-pointed at `6270a32` as deliberate precision and was dead before it landed — **the ancestor problem's cousin: a target whose run cannot ever complete.** Told it to watch a descendant or nothing until R105 lands.
- **R105 promoted to A12's top priority ahead of everything else.** The tally went five → seven while we discussed it, and two of the seven are the fixes for the other five. **A task from a v0.1.0 tag with effectively no validated master.**
- **`3be3243` accepted**: `no-tsan` out of the plan and spec as *lists, not decisions*, applied exactly as ruled; the direction-of-harm now stated in the `build.yml` comment — *it ran more, never less; no green was false* — which is the part a reader in six months needs. `AGENT.md:134` correctly left with B1.
- **Applied my own new rule immediately, on the next forecast I made.** Told A12 I expect `80bbe1c`'s run to fail on `clang-tidy` with the same 29 errors (it contains `8d05bb7` but not `6270a32`) — and stated explicitly that **the expectation covers the clang-tidy leg and nothing else**, that any other failing leg is unaccounted for and must be read from the log, and that a green on clang-tidy means my ancestry is wrong and I want to hear it. That is the three words I failed to write last time, written before the same mistake could repeat.
Task C0 plan-vocabulary survey: **REFUSED, correctly, and the refusal was worth more than the task. My task was wrong in three separate ways and C0 found all three.**
- **I counted and called it a measure.** 43 occurrences, and I concluded staleness without asking what one of them was DOING. C0 mapped every one to its `### Task` section: 20 are the record of a **completed** task, 8 are mapping statements with the old name **left of the arrow**, 12 are a **consumer's own** symbol that keeps its name (endo's `tui/runtime/EventSource`, tuidu's `src/{coro,...}`), 3 are **upstream artefact** names. **In all four classes the old name is the correct name.** Its phrase is the constraint: *the count is not the measure.*
- **The case I would have destroyed: all 18 `coro` occurrences are in A4/A5/A5b, and A5b IS the rename** — its heading reads *"Rename `core::coro` to `core::async`"*. My pass yields *"Rename `core::async` to `core::async`"*: **a mechanical edit erasing the evidence that the thing it enforced had ever been decided.**
- **I asserted a mechanism without reading it.** My whole premise was that B5-B13's briefs would carry A5's text. `brief.sh` extracts `^### Task <id>` plus the `## Phase B` preamble, so **A5's text cannot reach a B or C agent**, and the shared preamble is 13 clean lines. I reasoned about how the tool works instead of opening it — the memory C0 edited an hour ago, applied to a tool rather than a record.
- **Worst: the gate I proposed was itself this evening's defect.** It would have demanded **197 rewrites across 36 delivered rows**, nearly all correct text, **because the table's `from` side is the consumer's spelling and the plan is a document ABOUT consumers** — so a gate forbidding the old names forbids the plan from describing its own subject. I have spent the night refusing gates that fire on true negatives and then designed one that fires on 197 of them. And `rewrite.py` could not have driven it anyway: `(?<![\w:])coro::` does not match inside `core::coro::`, which shows I had not checked the instrument either.
- **Zero re-scope passages**, given as a number because I asked for it including zero. **Spec: same four classes, 12 occurrences, zero `coro` in either spelling** — clean, because it has no A5/A5b to record. No separate ruling needed; C0 did not open it for writing.
- **One real defect, and it was mine, in a dispatch: `task-B6-dispatch.md:15` claimed `core::async::asTask(aw)` EXISTS.** It is nowhere in `src/`; plan `:860` says *"Add"* it. **Fixed** — the dispatch now says B6 adds it, cites the plan, and records that an earlier draft claimed otherwise so nobody re-derives the confusion. **New constraint: a dispatch is the document with no reader who can contradict it** — read once, by a fresh agent, with no way to check its claims against the tree, which makes a false existence claim there far more dangerous than the same error in a plan or rulebook. Third time tonight a lane has caught a stale claim in a document I wrote.
**Ruling R105 DELIVERED as `74308e7`, and observably working: three Build runs alive at once for the first time tonight** — `80bbe1c` queued, `3be3243` pending, `74308e7` in progress. **`3be3243` surviving a later push is what proves it**, not the YAML parsing: under the old scheme it would have been destroyed like the heads before it. The change is self-applying, because a push event reads the workflow file at its own SHA.
- **A12 validated the expression by finding the existing proof rather than producing an argument:** the `A && B || C` ternary is already used twice in that same workflow (lines 278, 654), on those runners, green today — so the idiom is not being introduced here. Then it named the one trap, a falsy middle operand collapsing to `C`, and showed the middle operand is `github.ref`, non-empty for every trigger. Now a constraint.
- **It answered the question I asked rather than the one I wrote.** I said report before landing if it misbehaves; it established that **neither failure mode cancels MORE than today** — a wrong `github.ref` on push reproduces present behaviour, a wrong `github.sha` on a PR cancels less — so there was no path to worse than the defect and landing was correct. The instruction used for its purpose rather than obeyed to its letter.
- **The durable part is the distinction it drew by reading all four workflows rather than only the one it was sent to fix: "a push is a record" is an argument about VERIFICATION runs, not deployment ones.** `docs.yml` has the identical group shape and **should keep it**, because it deploys and the newest push is the one whose site should win; `release.yml` groups by ref on tags, already unique. That turned a change into a principle.
- **[SUPERSEDED - see the entry below: seven is correct and this correction was the error.]** I wrote *"seven consecutive heads at zero jobs."* Re-read in order: **six** cancelled (`7e2e3ae`, `ba48a50`, `5801090`, `8d05bb7`, `c5e2db6`, `6270a32`) and **not consecutive** — `3012952` ran and failed between the second and third. A12 said "a sixth head", which is exact. **Fifth count of mine wrong tonight, and this one came from a command I had run myself**: I read the rows and wrote a summary instead of reading the summary off the rows.
- **A12 declined to let the forecast failure be recorded as mostly mine, and its rule is better.** *Read the failure before agreeing with an explanation of it, however good the explanation* — because it does not depend on the other party phrasing carefully, so it still works when they have not. Recorded as primary; mine kept under my own name, because **the duty of the person making a confident claim is not discharged by the recipient having a way to check it.** Both true; A12's survives a careless colleague, and tonight the careless colleague was me.
**`80bbe1c` completed `failure` — and the scoped forecast held exactly.** Non-success jobs: `clang-tidy` and `ci-ok` (the aggregator, by construction). **`clang-tidy` alone**, precisely the scope claimed and nothing beyond it. I read the job list rather than assume it matched, which is the only honest way to hold myself to A12's rule on my own forecast. **This is the first completed run since `870d12b` to speak to anything**, and it settles two commits that had landed with no run on their own head: **`8d05bb7` is confirmed** (every POSIX leg green on a head containing it) and **`6270a32` is confirmed necessary** (the 29 tidy errors still present on a head without it).
Task (new, to A12): **audit the four pre-written dispatches — B5, B6, B7, B8 — for claims about what EXISTS.** Comes directly from C0 finding `task-B6-dispatch.md` asserting `core::async::asTask(aw)` exists when the plan says *"Add"* it. Report, do not fix; for each claim say whether it exists at `HEAD`, and where a claim concerns something a previous task must deliver, say which task and whether it has run. Rationale given: **a dispatch is the document with no reader who can contradict it** — better to find these now than have four fresh agents each lose an hour to one.
**THE COUNT IS SEVEN, A12 WAS RIGHT, AND MY CORRECTION WAS THE ERROR.** Counted properly with job counts per run: `6270a32`, `c5e2db6`, `8d05bb7`, `5801090`, `ba48a50`, `7e2e3ae`, **`ceb471d`** — all `jobs=0`. **I missed `ceb471d`**, which fell outside the `--limit 10` window I used to "re-check", and **I read the smaller set as the whole set.** So the sequence was: *seven consecutive* (right number, wrong adjacency) → *six, not consecutive* (wrong number, right adjacency) → the truth, **seven, not consecutive** (`3012952` and `80bbe1c` ran in between). The earlier ledger entry is marked superseded rather than deleted.
- **The instructive part is new and it is mine: a correction is a measurement too, and I gave it none of the scrutiny I had just given the thing it corrected.** *Doubting a number is not the same as verifying its replacement* — and the pleasure of catching yourself is exactly when you stop checking. Now a constraint.
- **A12's monitor was not dead and I was wrong to say so**: it matches `git merge-base --is-ancestor 6270a32 <head>`, so any descendant fires it. A12 also corrected its own earlier description — the re-point's *precision* was real but the *mechanism* it gave was not, since the monitor never needed a run on the named commit. **Correcting a description whose conclusion still held** is the discipline that keeps a record trustworthy, and it is the opposite of what I did with the count.
Dispatch sweep (C0, taken up under the standing invitation rather than assigned — **my overlap**, since I told C0 to speak up about existence claims and then sent A12 the same task): **CLEAN.** B4, B5, B7, B8 carry no `core::` name the tree lacks and no unverified existence claim; B6's two remaining hits are benign (`:17` is my fix, `:92` is a literal placeholder in the removed-row instruction). B8's `connect()`/`listen()` claim **verified by hand** — both are member declarations in `src/core/net/Sockets.hpp`.
- **C0's near-miss is a third distinct shape from the same family:** it listed `src/core/net/` for filenames matching `connect|listen`, found none, and that briefly read as *the claim is false*. **An absent filename is not an absent function.** Its diagnosis: *"I asked a question shaped like the one I wanted answered, and the shape was wrong."* Three distinct members from C0 tonight — a **field** of a record left unread, a **filename** standing in for a declaration, and a rename row's **`from` spelling** asked in the wrong namespace.
- **And the line about method is worth more than a clean record would have been:** *"I am three for three this evening on first-cut probes that over- or under-report, and the only reason none of them reached you as a finding is that I ran the control before the conclusion each time."* A lane with no wrong first cuts either got lucky or is not probing hard enough; **what makes C0 trustworthy is not that its probes were right, it is that the control ran first.**
Task (A12, redirected): **the BEHAVIOUR-claim audit of the B5-B8 dispatches**, which is precisely the limit C0 named as out of reach for a grep. Those dispatches assert that a cancel from the flow's own token throws while a cancel from the resource returns a value; that `close()` touches no member after completing; that a watched direction outranks `onError`; that `Interest::None` mutes without detaching; that `selectReadinessCallback` returns exactly one callback; that `ScriptedBackend` consumes a muted step. **Some are true today, some become true only when a prior task lands, and at least one is a rule I wrote before the code existed.** For each: true at `HEAD`, true only after task N, or wrong. Report, do not fix.
- **C0 restructured the session memory again and applied our own rule to it: it folded the two new shapes into a GENERALISATION rather than adding two more bullets**, on the grounds that *"the list was already five long and the enumeration is the weaker half."* The generalisation it landed on — **each is a probe whose shape quietly answers a neighbouring question** — is better than any of the five instances, and the list is now kept as evidence for the rule rather than as the rule. Verified by reading, since that file is durable and mine.
- **It also rewrote "how to apply" around the CONTROL rather than around CARE, "because care is not what saved any of them."** *Make the probe fail on purpose before believing what it says* — delete the row that covers a symbol and require the probe to name exactly that symbol; feed it the case you know is present; say the expected failure count aloud first.
- **Its diagnosis of my own near-miss is sharper than mine was:** *"the second number arrives wearing the authority of a correction."* That is why it was expensive — **I had just performed the ritual of doubting myself, which felt like diligence and substituted for it.** Its control is the one I did not run: *state what the narrower query should return before running it, and notice when the window changed along with the count.* I changed two things at once — the query's limit and the number — and attributed the difference to one.
Task (C0, new): **audit `global-constraints.md` — 356 lines, ~120 carrying specific factual claims, and nobody has ever checked it while every dispatch cites it as binding.** I wrote almost all of tonight's entries from other lanes' reports and my own summaries, having been wrong about a count five times in one evening. Scope: **checkable claims only** — paths and line numbers (`posix/PollBackend.cpp:107`, `platform/Types.hpp:26`/`:48`, `IoBackend.hpp:176-190`), commit SHAs (exist, on master, and are the change described), **tool behaviours I stated as fact rather than quoted** (`ctest` ANDing `-LE`; clang-tidy resolving config from the nearest `.clang-tidy` to the source file; `git log --name-only -- <path>` filtering rather than annotating), and numbers. **Judgments explicitly out of scope** — rulings and principles are mine and are not defects even where C0 would have ruled otherwise; but **a fact whose wrongness would make the RULE wrong is to be flagged loudly rather than quietly fixed.** Pure factual errors fixed in place and listed; anything where correcting the fact might change the meaning reported for me to rule.
- **Prediction stated first so it is a check and not a survey: three to eight wrong claims**, mostly line numbers drifted under other lanes' edits, and at least one number written from a report rather than from the tree. **A zero is to be reported as surprising**, with how the search was conducted.
- Framed explicitly as not-invented-work, because I invented a task for C0 once tonight (the plan vocabulary pass) and it correctly refused: this is the same class of defect C0 has found three times, in the one document I have been writing all evening and no one has read back.
**R105 confirmed working: `74308e7` and `3be3243` are BOTH `in_progress` simultaneously.** Under the old grouping the later push would have destroyed the earlier's queued run, as it did seven times tonight. `74308e7` is `origin/master`'s head and contains `6270a32`, so its run should be the first to cover the clang-tidy fix.
**Decision: stop pre-writing dispatches beyond B8, and it is a correction of my own practice rather than a new rule.** I wrote B5, B6, B7 and B8 ahead of need on the argument that *"a dispatch composed under time pressure is where my completeness claims have failed tonight"* — and then B6's carried a false existence claim (`core::async::asTask` *exists*, when the plan says **Add** it) **for exactly the reason that it was written before the code existed.** The pre-writing did not avoid the failure; it changed its shape from rushed to stale, and stale is worse because a dispatch is *the document with no reader who can contradict it*. It survived until C0 happened to grep for it while refusing a different task.
- So: **write the next dispatch when the previous task reports**, and spend idle time on audits — which is where tonight's real finds have come from anyway (the fourth stale pin, the test-binary count, the `-LE` defect, the inert `removed` row, the `asTask` claim). Every one of those came from reading something already written, not from writing something new.
Dispatch audit (A12, the behaviour/existence half): **six defects, all mine, all now fixed.** Checked against `HEAD` (`74308e7`) and fastcached at `0708dd54`; reported, not fixed, correctly.
- **The one that matters, and it is worse than `asTask`: `task-B7-dispatch.md` claimed `completionPort()` was "the `#if defined(_WIN32)` member the interface already declares".** `IoBackend.hpp` has no `completionPort` and **no `#if defined(_WIN32)` member at all** beyond `DefaultHandleKind`. The interface's own comment is the truthful version — `BackendKind::Iocp` *"arrives in Task B7; `makeBackend` answers null"*. **Worse than `asTask` because it asserts ANOTHER LANE ALREADY DELIVERED something**, so the failure mode is not a fruitless search but **a defect opened against a lane that did nothing wrong.** Now a constraint. Fixed: the dispatch says B7 adds it, quotes the interface, and records the earlier draft's claim.
- Four smaller, all fixed: `Net/IocpListener.*` does not exist (`IocpListener` is `Net/IocpSocket.hpp:129` / `.cpp:687`); `Net/DialOptions.*` does not exist (`DialOptions` is `Net/IConnector.hpp:110`); `ruff format --check` is stale in B5 and B7 (AGENT.md mandates `scripts/python-style.py --all --check`, which refuses a bare run with exit 2); and B5's `clang-format.py --check` carried no `<paths>`, which also refuses. B8 already had both right.
- **One ambiguity fixed: B5's opening "Today both wake on an interval and re-check" meant FASTCACHED's today.** `DeadlineTimer`, `interruptibleSleepUntil`, `addTimer`, `WakeReason` and `wakeBound` are all absent from `src/`; read cold against this tree it is a claim about core-cpp. Now reads *"In fastcached at the pin"* and names the five absent symbols.
- **A12's citation observation is the durable finding and inverts the intuition: a line reference into ANOTHER repository at a PINNED SHA is the safest kind of citation, because the SHA freezes it.** It expected B7's `IocpSocket.cpp:555-561` and `:327-334` to be the most rotted things in the set; both are exact. The citations that rot are the ones into our own moving tree. Now a constraint.
- **It reported what HELD as well as what failed** — ten presets, `core-cpp#6` and `#16` open with matching titles, contour's `connect()`/`listen()` genuinely in `Sockets.hpp`, every fastcached source but `DialOptions`, `portability.yml` really dispatching FreeBSD on demand. **A survey listing only defects leaves me unable to tell a thorough audit from a shallow one.** Now a constraint.
- **Best negative result of the night:** it checked that both formatter scripts refuse a bare run with **exit 2 rather than 0**, *because a refusal exiting 0 would have read as a pass to anything scripting them* — a silent green of exactly the shape we have spent the evening removing. Checked precisely because it would have been a real defect; found absent; said so.
- **Its cross-task table changes my sequencing: `assertTeardownIsSerialisedWithDispatch` is B4's and UNLANDED, while B6 and B7 both require it for G5.** I had that recorded as an obligation on B6 and B7 rather than as a thing they cannot begin without. **B6 does not open until B4 lands it.** Same for `PlatformLoop`/`testing::TestLoop`, which B5 and B7 need and which exist only as untracked files in the worktree.
Task B1, `04472c5`: **it used the wait to turn both of its own "reasoned about rather than tested" flags into evidence.** The gap was real — **every claim in the suite was taken on the thread that started the join**, because a child awaiting `ResumeOn` suspends on its starter's thread, so `claimOn` had never once run concurrently with itself. The new case has sixteen children park **twice**, so the second `claimOn` runs on whichever pool thread resumed it, concurrently with a sibling's `claimOn`, with `Parked::resume()`'s `disarm()`, and with a third claim's `release()`. 21 TSan runs and 21 ASan runs clean, plus four presets; 346 assertions / 84 cases, up from 343/83.
- **And half its prediction was wrong in the interesting direction, which is the real find.** It expected the pre-existing single-park join to show NO race on `_parks`; it showed **fourteen** — `Parked::resume()`'s local `ParkedWork` is destroyed on the pool thread, so **the decrement was already concurrent and already covered; what was never covered is the increment.** Right conclusion, wrong reason, reported in those words. **The consequence is bigger than the bookkeeping: if the decrement was already concurrent under the OLD code, the ordering hazard predates R97 and the refcount merely made it visible.** Asked `rereview-B1-r1` to check that reasoning independently, because it changes what the relaxed `fetch_add` question is about.
- **Ruling: landing it mid-re-review was right, on B1's own reasoning** — the range `ac0ff76..ceb471d` is immutable and the reviewer's worktree is detached at `ceb471d`, so nothing shifted. **Told the reviewer explicitly** rather than let it trip over the commit, and told it that **a case written by the author to cover a hazard the author identified is the weakest kind of evidence against that author's own blind spot** — so its existence changes what a clean result means rather than settling anything. B1 told to hold any future commit that would land INSIDE a live range.
- **Ruling: the Windows `SKIP` finding is approved but held until the re-review reports.** B1 found the Windows async binary reports **81** cases where Linux reports **84**, with three `#ifndef _WIN32` cases (exception propagation through a coroutine frame crashes the Catch2 harness on MSVC — a sound, documented reason that stays). What is missing is that **a compiled-out case reports nothing, where `.agent/rules/testing.md` asks for `SKIP` so a case that could not run says so.** Three lines makes the counts agree. The hold is sequencing only: `04472c5` earned its way in mid-review because it covers what the reviewer is hunting; unrelated housekeeping has no such justification.
Task B3, fix round 2: **staged, not committed — waiting on the background Linux presets (ASan, TSan, clang-tidy) rather than committing on partial evidence.** Already pushed: `7e2e3ae` (F5) and `8d05bb7` (its Linux break). Staged: R101 items 1-4 (the reorder, `Failed` documented best-effort, the "instead of" clause deleted, two new parity cases, and the rules entry including **why `EV_EOF` must not be touched**), F3 (`_wakeup` declared before the kernel descriptor in both backends, so a throwing mem-init list cannot leak the epoll/kqueue fd), F4 (a control block `REQUIRE`ing `dispatched == 2` before the #475 case, turning its premise into an assertion), F6's two comments with `AGENT.md` untouched, F7 (both `DefaultBackend.cpp` files routed through `makeBackend(preferredBackendKind())`), F8 argued with the reason in report §4, R102's G1/G3 paragraph naming `EventLoop::runOnce`, and the precondition rewording now stating **non-consumption** as the property. Verified on four presets; **the R101 RED came in at both levels exactly as predicted** — 2 unit failures and 2 kernel failures on poll and epoll.
Behaviour audit of the B5-B8 dispatches (A12): **the prediction landed on R101 itself — "at least one is a rule I wrote before the code existed."** `selectReadinessCallback` at `HEAD` (`IoBackend.hpp:175`) is still the **pre-R101 order**: `onError` beats a watched direction whenever `Failed` is set. **B3's fix round 2 exists only in the working tree and has never been committed** — `git log -- IoBackend.hpp` shows one commit, `e7963de`. So the ruling is written, the code is written, and nothing has landed.
- **B6 was correctly scoped (*"after B3's fix round 2 lands"*); B7 and B8 asserted it flat.** Same split for *"`onError` fires only when `Failed` arrives with neither direction set"* — false at `HEAD`, true after. **A12's tell is exact and describes my writing process better than I would have:** *"B6 carrying the caveat and B7/B8 not is the giveaway: B6 was written while the fix was pending; the other two inherited the conclusion without the condition."* I wrote B6's section while the ruling was fresh and provisional, then wrote B7's and B8's from memory of **the conclusion** — and **a conclusion does not carry its own preconditions.** Now a constraint: *a caveat is the first thing lost when a claim is restated, and propagating a ruling into another document IS restating it.*
- **Two present-tense claims in B6, both false, both fixed.** *"You are the first code in this repository that will set `ReadinessHandler::onError`"* — **four places already do** (`BackendParity_test.cpp:873`, `:1347`, `IoBackend_test.cpp:41`, and `EventLoop.cpp:120`, which sets it to `nullptr` deliberately with a comment saying why). A12's point is the one that stings: an agent greps, finds four, and **doubts the brief on its first check** — now a constraint, *a brief that loses its reader's trust in the first five minutes is worse than one that says less.* And *"the `core::net::contract::*` slot guards ARE how that is enforced"* — they do not exist; spec §2 item 7 and plan `:161` put them in **B6's own scope**. The same mistake as `asTask`, in the same document, which I had already fixed once.
- **B5's rulebook line fixed to CITE, not ADD** — the cancellation rule is already at `.agent/rules/async-and-net.md:53-57` and only its origin URL is missing; duplicating it would make *a reason recorded twice, which is a reason that can disagree with itself.*
- **The verified-true list is load-bearing, not decoration.** *"kqueue sets `Failed` only on `EV_ERROR`, not on `EV_EOF`"* (`KqueueBackend.cpp:286`) and Wfmo only on `WAIT_FAILED` (`:78`), **read in the code** — which is what makes B8's macOS story a fact rather than a plausible narrative, and that story is the entire justification for requiring `SO_ERROR`. Had it been wrong, B8's central instruction would have rested on nothing. Also confirmed true at `HEAD`: B8's Linux story, "exactly one callback", the `close()` rule at `:306`, and B6's cancellation rules at `:53-57`.
- **New verdict category, now a constraint: "true only after task N lands" is distinct from "true" and from "wrong", and it is the one that matters most in a dispatch** — a document read once by a fresh agent cannot tell a ruling from its implementation unless it says which it is describing.
- **A12 dispatched to audit `task-B3-dispatch.md`**, which fell outside its four; its reason for suspecting it is right — B3 is the lane whose fix round is uncommitted, so its dispatch is the likeliest place for another pending-stated-as-present claim. Sharpened with new information: **B3 has just pushed `48af4e6` for a clang-tidy error on `ScriptedBackend_test.cpp:22` that it had earlier told me did not implicate it** — its third self-correction — so that file has moved twice tonight and the dispatch describing it predates both moves.
**MY INFERENCE THAT THE ORDERING HAZARD PREDATED R97 IS FALSE. B1 refuted it with three checks; I verified all three myself and retracted to the reviewer within the turn.** `git show ac0ff76^:src/core/async/ParkedWork.hpp` matches `_parks|fetch_add|fetch_sub|AbandonState|claim\(\)|release\(\)` **zero** times — before R97 `abandon` was a plain `std::coroutine_handle<>`, so **there was no counter and therefore no ordering question about one**. `ParkedWork.hpp` first appears in `b21229d`, this task. `git tag` is empty and the CHANGELOG has only `[Unreleased]`: nothing has shipped.
- **The word my inference turned on: B1's "existing case" was *"A join whose children finish on a pool"*, written in the same fix round for I2, minutes before the probe** — not a pre-task case. B1 owns that *"existing"* meant twenty minutes old in a report where it otherwise meant the pre-task tree. **I told it that is over-weighting its share:** it gave me the claim with its evidence attached, and I built a conclusion on top without running any of the three commands that would have refuted it. **The word made the error available; not checking is what made it mine.**
- **The worse half of my error, in B1's framing: calling the counter's ordering "pre-existing" MERGES THE FIX WITH THE DEFECT IT REMOVED.** There was a real hazard in the pre-R97 code — N parks each destroying the same root from whichever thread dequeued them — and that is C1, which R97 fixes. I took a fact about the fix and re-attributed it to the code the fix replaced, which makes the fix look like it inherited a problem it eliminated. Now a constraint.
- **B1's reason for flagging it FAST is sharper than the correction and is now a constraint:** a reviewer told to check *"was this live in shipped behaviour?"* searches pre-R97 trees for a counter that does not exist, and **"the premise is false" and "I could not locate it, so: inconclusive" look identical from inside that search.** It would have spent the time and returned the weaker answer. **A false premise handed to a searcher does not come back as a refutation; it comes back as noise** — so retract the moment you doubt, rather than waiting for a negative result you cannot interpret.
- The correct, narrower finding survives intact: **a test-coverage fact, not a shipped-hazard one.** Concurrent `release()` was already reached by the single-park case; concurrent `claim()` was reached by nothing until `04472c5`. The relaxed `fetch_add` question is genuinely new with R97.
**A12's sequencing correction, acted on: an UNCOMMITTED dependency is worse than an unlanded one, not merely earlier.** B3's R101 reorder exists only in the shared working tree; `origin/master` at `48af4e6` still has the pre-R101 order. Its distinction — *an unlanded-but-committed dependency is a scheduling fact; an uncommitted one can be lost to a stray checkout, a clobbered file, or a lane reverting a shared file from the worktree rather than from `HEAD`* — is right, and **every mechanism that has bitten this checkout tonight acts on exactly that state**, including B3 nearly publishing B4's uncommitted edits from the same file. So the reorder goes on the hold list **ahead of** B4's G5 helper: **lateness is recoverable, loss is not.** Told B3, explicitly without pushing it to skip the ASan/TSan/tidy verification it is waiting on, since the shortcut under urgency is what produced both of its self-corrections tonight; offered the split-commit option and said I would back either call.
- **A12's rule from the citation inversion is better than the inversion: *check the claim you are most confident is wrong*** — the same instrument as "commit to the answer first", aimed at your own confidence rather than at the result. It paid twice in opposite directions tonight: the fastcached line numbers it expected to be most rotted held, and the leg it was most confident about was the one that broke.
- B7's stray `**` removed. A12 was right to flag a cosmetic artifact: **a dispatch is read once, cold, by someone with no way to tell a formatting glitch from emphasis that was meant.**
- **R105 unambiguous: four runs alive, three executing**, where the old grouping would have left exactly one — the eighth through eleventh heads tonight that would otherwise have gone unverified. `74308e7` is among them, so the run covering `6270a32` is in flight.
Constraints-file audit (C0): **seven wrong claims — inside my predicted 3-8, and at the top of C0's own 4-7 stated before it looked.** One fixed by C0 in place, six reported; all now repaired.
- **My four dispatch examples were in the WRONG FILE, and that is its own finding.** `PollBackend.cpp:107`, `Types.hpp:26` and `IoBackend.hpp:176-190` live in `progress.md` and the B3 task files; **`Types.hpp:48` is in no file at all**; `global-constraints.md` carries **two** path:line claims, not dozens. **I described the contents of a document from my memory of having written it** — the same act as every other failure of mine tonight, applied to my own output, and answerable by one grep.
- **Line 78 was a TRIPLE recursive instance.** Its justification was false — *"a bare row would tell every consumer to delete a type that is still there"* — when `removed` rows carry `apply: none` and tell a consumer nothing; **the danger is inertness**, which **line 237 of the same file says correctly**. So it demonstrated line 201's own rule (*a reason recorded twice is a reason that can disagree with itself*) on itself, 160 lines apart. And it was an **enumeration naming three of six** (`core::tui::runtime` declares `FdInterest`, `FdToken`, `WaitOutcome`, `FdRegistration`, `FdRegistry` and `EventSource`) **in the file whose longest section forbids enumerations.** Rewritten: inertness as the danger, the convention that teaches the broken spelling, all six names, and `4919c79` as the enforcement.
- **Ruling: lanes now watch their OWN SHA.** C0 was right that line 52 is a ruling and not an edit — R105 (`74308e7`) removed its premise, and `build.yml:24-27` now says a push to master *"groups by SHA and is never superseded"* while line 52 still told every lane its run would usually be cancelled. **The advice survived and its necessity did not**, which is the whole finding: closing against a run on a CONTAINING commit stays valid because `master` is linear, but it is no longer forced — **and a rule presenting optional advice as forced spends authority it has not got.** The zero-job diagnostic stays for reading history and PR runs. Line 332's outstanding item marked shipped.
- **The aged table dated rather than chased, on C0's call.** `.agent/rules/testing.md:13` and `AGENT.md:130` now hold the corrected text, and "13 registrations over 8 modules, net has four" is now 16 and 5. Stamped `2026-09-21` with a note that every pointer has since been repaired — **the rows are correct AS HISTORY, which is what a "what it said / what was true" table is for**, and chasing the numbers would produce a table needing chasing again tomorrow.
- **C0's method note is the most valuable part and is now two constraints: a measured figure wants its date or its tree beside it.** Three numbers have already aged silently — *"3 of 20"* is 4 of 20, the ctest counts are per-preset, *"197 across 36 rows"* moves with the table. None is wrong, and that is why it is dangerous: **a reader who recomputes one and gets a different answer learns to distrust the file rather than the number.**
- **C0's own error was my prediction landing precisely:** *"all 43"* → *"all 41"*, from summing its report's four class counts instead of counting the document — *at least one number written from a report rather than from the tree*. Fixed in place with the derivation shown (11 + 7 + 17 + 1 + 3 + 1 + 1), conclusion unchanged.
- **Everything else held**: all 7 SHAs; `ctest` ANDing repeated `-LE` re-measured (27 total, 17 with one flag, **27** with two); `git log -20 -- CHANGELOG.md` returning 20 of 20 while only 4 of the last 20 commits touch it; `.gitattributes`, CMake `3.25...3.31`, emsdk `3.1.56`, the pinned `22.1.8` tools; every file path; 237 of 238; all 8 `removed` rows `core::`-rooted. **C0 also ran three malformed probes during the audit and caught all three** — report truncated there; asked for the rest, because the method is what I want.
**HAZARD CLOSED: `global-constraints.md` is tracked as of `d44e2d6`, pushed.** C0 found it sat under `.superpowers/sdd/`, whose `.gitignore` is a single `*` — **no index, no diff, no history, no protection against a concurrent overwrite**, while every dispatch cites it as binding and several lanes write to it. It changed on disk between C0's read and its one-character edit, and again while it wrote the report; **the edit applied only because it was a three-character replacement against a unique string.** Nothing would have reported the loss of a paragraph another lane had just written.
- **C0's argument is what decided it, and it is stronger than "this file is important": the file states three rules for shared files — private index, `HEAD`-blob base, snapshot/reset/diff — and NOT ONE of them could be applied to the file itself.** A document that cannot obey its own rules is a special case, and special cases are where rules stop being read. Cost: one `.gitignore` negation (`*`, `!*/`, `!.gitignore`, `!global-constraints.md`), with the reasoning written into the file so the exemption does not read as an accident.
- Done under the full discipline, possible for the first time on this file: snapshot before, explicit two-path `git add`, verified exactly two staged, `git commit --only` with `-m` **before** the `--` separator (the first attempt failed because everything after `--` is a pathspec), snapshots diffed after — only those two paths changed state.
Dispatch audit of `task-B3-dispatch.md` (A12): **the claim I sent it to check is TRUE and survived intact; two defects elsewhere, one of them the exact inverse of its own citation finding.**
- **The `Interest::None` muted-step claim holds in all four backends** — poll's `.fd = -1`, epoll's `EPOLL_CTL_DEL`, kqueue's disarm, Wfmo's commented `continue` — **and the agreement survived the entire `EventSource` → `IoBackend` rewrite.** `IoBackend.hpp:302-305` documents it MORE fully than the old header: *"mute means SILENT on every backend: a muted registration reports nothing, not even the hangup and error conditions a kernel volunteers."* B3 strengthened a guarantee while porting it.
- **Defect 1, the purest form of the shape I keep producing: the dispatch cited `EventSource.hpp` as the authority for idempotent detach at `:102`, while `:45` instructs deleting that file.** One document, forty lines apart, **doomed at the moment of writing.** Fixed — the guarantee is carried into `IoBackend.hpp` and the old citation named as the earlier draft's error.
- **Defect 2 is the exact inverse of A12's citation finding, from the same audit.** `:95` said contour's `EventSourceParity_test` was **1243 lines**: contour's is **1131**, the 1243 is **core-cpp's own copy** at `e7963de^`, and `BackendParity_test.cpp` is **1588** today. The number was ours, the credit contour's, wrong when written and drifted 345 lines since. Against B7's fastcached line numbers at a pinned SHA still being exact, the pair states the rule completely: **a citation into a pinned tree cannot rot; a count of your own tree rots before the document is read.** Both halves from one audit, which is what makes it a rule rather than two anecdotes.
- **Two reporting habits worth repeating.** The `isHostDriven()` note — *true by a default in the interface, not by per-backend overrides, so grepping the four concrete backends finds nothing, which looks like a hole and is not* — a near-miss **disarmed for the next reader** rather than merely avoided. And the `wake()` coalescing claim marked **unverified, not confirmed false**, because *"asserting it from the call chain would be the plausible-narrative move."*
- **The empty category is a real result with a real explanation:** no defect in that dispatch was "true only after B3's uncommitted round lands", because **the dispatch predates R101 entirely** — it describes the interface B3 was told to build, and B3 built it. **A document written before a ruling cannot misstate the ruling; only documents written after it can**, which is exactly where B7's and B8's defects were.
- **C0's three malformed probes, tail received, and the pattern is the finding:** *"the probe answered a neighbouring question and answered it correctly. None of them failed."* Wrong repository (six SHAs checked against core-cpp; two were fastcached's, and **the control — a fake `deadbee` — passed, so the instrument was working perfectly on the wrong question**); a pathspec after `--format` returning 0 and reading as *"the `git log` filtering claim is false"*; an empty directory listing read as an absent function. **In two of three the wrong answer was the more INTERESTING one** — two bad SHAs in my file, a tool claim of mine shown false — which is the direction that travels fastest and is least re-checked. Now a constraint: *be slowest with the finding you most want to be true.*
**Task B3 fix round 2: COMPLETE and pushed** -- `7cd86fc` (R101 items 1-4, plus F4's control and F6's comment) and `c94a0b9` (F3, F7, F6's public-header comment, the precondition rewording). **R101 is out of the shared working tree**, which closes the uncommitted-dependency hazard A12 raised. Gates on the committed tree: Linux `clang-debug` 28/28 with 15 hygiene, `gcc-release` 16/16, **`clang-asan-ubsan` 16/16, `clang-tsan` 16/16**; Windows `cl-debug` 30/30, `clangcl-release` 30/30; `mkdocs --strict` and pinned clang-format clean. The `clang-tidy` preset is still building.
- **B3 narrowed the verification rather than skipping it, and could name why:** ASan and TSan cannot speak to a pure function's branch order, and clang-tidy is the gate that had already caught it once -- so it ran the pinned 22.1.8 directly against the compile database for its three changed TUs. **Now a constraint: skipping a gate is judgment only if you can name what it would have told you.** The same lane's earlier skip under time pressure could not be justified that way and put a defect on master.
- **Its first targeted tidy run was worthless and looked green -- the purest form of the evening's lesson.** `--quiet`, piped to `grep`, reading `EXIT=0` that was **`head`'s** status, while `--quiet` suppressed the only output that could say whether anything had been processed. **A file missing from the compile database produces exactly that: silence and a zero.** Re-run asking for the thing itself: exit captured directly, output to a file, `"warnings generated"` present **three times, one per TU**. Its words: *a gate that does not report reads as passed, and I nearly accepted one.* The count is what most would omit and what proves all three were reached.
- **B3's reworded precondition is STRONGER than the property I gave it, and I had it wrong.** I said *non-consumption*, reasoning about the detector. It found `collectSignalled()` probes **every non-muted registration** with `WaitForSingleObject(h, 0)`, not only the flagged one -- so **a consuming handle registered anywhere is drained by the rescan of a dispatch that has nothing to do with it.** The requirement is *"waiting on it does not change it"*: stronger, and easier to check a new handle class against. **Third time tonight a lane improved a property I proposed by reading the code I was reasoning about.**
- **F8 argued and taken, on one sentence:** `await_resume()` is `void`, so the only available shape conflates a refused registration with `OperationCancelled` -- strictly worse, and the exact defect `149659a` prevents -- and changing that signature is a change to the loop's contract, **B4's to make deliberately.** Recorded as argued rather than deferred, and added to B4's obligations rather than left in a report.
- **B3's fourth self-correction: it read the JOB LIST and inferred ownership from job names.** `clang-tidy` had failed on its own `ScriptedBackend_test.cpp:22` unused `using` alongside the tui lane's `KeyCode.hpp`; only the log said so. Fixed as `48af4e6` before the tui lane could chase it. **Same shape as A12's monitor firing on a run that completed having executed nothing.**
- **I produced the same defect pointed at myself within the hour.** Thirty minutes ago I wrote *"R101 is not true at `HEAD` yet"* into B6, B7 and B8. True when written, false when B3 pushed. **All three rewritten as INSTRUCTIONS rather than states** -- *read `selectReadinessCallback` yourself and say what you found* -- because a dispatch cannot hold a state, only tell you to check one.
- **A12's monitor gave a vacuous answer and it caught it: `cancelled (0 jobs)` on `6270a32` read as a verdict**, because the exit condition asked *"did a run containing my commit complete?"* when the question is *"did the clang-tidy JOB conclude?"* -- **a run can complete without running anything.** Re-armed on the job's terminal state. **And it refuses to claim the fix verified**: `style` is success in three runs containing it, `clang-tidy` still in progress in all three -- *"the one leg that failed before, the only leg that adds a tool, has not answered yet."* Its own rule on its own commit.
- **R105 confirmed in production: six Build runs alive simultaneously** (`3be3243d`, `74308e7`, `04472c5`, `48af4e6`, `7cd86fc`, `c94a0b9`); five would have been cancelled under the old group. A12 located the cutover to the second -- `6270a32` was pushed before `74308e7`, still carried the old group, superseded 43 seconds later.
- **C0 sharpened the control rule past my version: a control is built from the same misconception as the probe.** It shares an author and a premise, so **it only ever tests the half you already doubted.** *The control covers the instrument; the question needs its own check, and the check for a question is naming what a wrong answer would look like.* And why the dangerous ones were the interesting ones: **a boring wrong answer gets re-checked out of dissatisfaction; an interesting one gets reported.**
- **B1 has the Windows `SKIP` written, verified and PARKED, as ruled.** Prediction held exactly: `cl-debug` 84 cases, 81 passed / 3 skipped, exit 0; `clangcl-release` 334 assertions; Linux unchanged at 346/84. Assertion counts differ by design; **the CASE counts now agree, which was the point.** It flagged the failure mode of parking: **the patch is based on `48af4e6` and will rot** -- *"it applied cleanly" is not the same claim as "it still does what I measured"* -- so it will re-run all four toolchains before landing.
**The SDD workspace prose is tracked as of `8c7daa5` (132 files), extending `d44e2d6`.** A12 checked whether the hazard `d44e2d6` closed was closed everywhere and found it was not: `.superpowers/sdd/2026-09-18-core-cpp/` held 164 files, **one tracked**. Its argument is the one that decided it and it used my own comment against me -- *"every dispatch cites it as binding, several lanes write to it, and an untracked file has no index, no diff, no history and no protection against a concurrent overwrite"* -- **nothing in which is specific to `global-constraints.md`.** I wrote a general justification and applied it to one file, which is the narrowest possible form of the defect I have spent the evening fixing in other people's documents.
- **Three refinements to A12's proposal, all sharpening.** The negation is a **pattern** (`!task-*.md`), not the three filename shapes that exist today -- an enumeration would be wrong the first time someone adds a fourth, and I was not going to write a list into the file that closes this hazard. **`progress.md` is included**: A12's case was about shared documents and the concurrency hazard genuinely does not apply to the ledger, since I am its only writer -- but the LOSS argument applies hardest there, because it is the recovery map and the skill's own warning is that `git clean -fdx` takes it. **Briefs are included, refining A12's regenerable/irreplaceable split:** `scripts/task-brief` extracts them from the plan, so they look derivable -- but **the plan moves**, as A12 moved it removing `no-tsan`, so a brief regenerated today is not the text the agent received. **A derivation from a moving source is a record, not an artifact.** `review-*.diff` stays ignored precisely because its source is two immutable commits.
- **A12's bounding statement is the most honest thing in the report and is stronger than the audits it bounds:** *"my checks find claims that disagree with the tree; they cannot find a claim that was deleted."* Every "wrong when written" versus "drifted since" dating it gave me tonight was inference from surrounding evidence, not verification, because the documents had no history. **It could not have detected a silently clobbered dispatch, and said so rather than letting five clean audits imply otherwise.** And on whether anything was already lost: *"permanently unverifiable, which is the argument for closing it now rather than evidence that it bit us"* -- the distinction between a precaution and a claim.
**`6270a32` is VERIFIED on CI: 155 of 155 header self-check objects clean on the clang-tidy leg in two independent runs** (`35551084421`/`3be3243d` and `35551223325`/`74308e7`), matching A12's pristine-`HEAD` count exactly. The 29 failures are gone; the per-target `CXX_CLANG_TIDY ""` was the right fix.
- **A12 checked the check had RUN before reading the absence of errors as a pass.** Its first grep used the wrong target name (`hdrcheck`) and returned zero mentions -- **which reads identically to "the check silently did not run"**. The real name is `*-header-self-check`, 155 distinct objects. **Third time tonight a gate nearly reported green by not reporting at all.**
- The one real failure in both runs is B3's `ScriptedBackend_test.cpp:22` unused `using`, **already fixed in `48af4e6` two commits later**; both failing runs are on SHAs predating it. No action for B3. A12 predicted, before the runs land, that `48af4e6`, `7cd86fc` and `c94a0b9` are all green on clang-tidy.
- **A12's process note, now a constraint: with per-SHA runs a red leg is more often an older SHA than a new defect.** That is R105's cost side and the right trade; `git merge-base --is-ancestor <fix> <run SHA>` settles it in one command before anyone spends an hour on a failure someone already fixed.
- **A12 declined to save memories for the citation rule and the confidence-aiming rule, and the reasoning is correct:** both are being recorded in `global-constraints.md`, which is now tracked, and **duplicating repo content into memory is exactly what the memory rules exclude.** Worth recording that the decision was made deliberately rather than forgotten -- and that tracking the constraints file changed what belongs in memory, which was not a consequence I had anticipated when I ruled on `d44e2d6`.
**Status check sent to B4, with no new work in it.** Everything in Phase B now waits on that lane and it has not reported since its dispatch. Told it the three obstacles that have cleared -- B3's R101 reorder landed (`7cd86fc`) so `IoBackend.hpp` is out of the shared tree; F5 landed (`7e2e3ae`) so mute cases are safe; `origin/master`'s POSIX break is fixed (`8d05bb7`) -- and asked the question I actually care about: **whether the five routings I sent in quick succession crowded out its own work.** Each was justified individually; five inside an hour may not have been. Offered to split them: **the `ParkedWork` claim rule is the only one load-bearing for B4's core task**, and the rest can be a follow-up commit or their own task without anything downstream suffering. Also offered that landing `assertTeardownIsSerialisedWithDispatch` ahead of the rest unblocks the most, since it is the one thing B6 and B7 cannot start without.
- **This is the coordination risk I have been creating all evening and had not priced.** Every routing was a real finding from another lane's audit, and routing them promptly was right -- but **a lane on the critical path is the one place where the cost of an interrupt is not local.** The audits that produced them (A12's two, C0's three, B1's and B3's self-corrections) were cheap precisely because those lanes were idle; B4 was not.
